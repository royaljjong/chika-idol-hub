# Chika Idol Box 감사 및 재설계 계획

기준일: 2026-08-31

## 2026-09-01 Release R-1 — 전체 로컬 구현 커밋·푸시·프로덕션 배포

- 사용자 의도·기대 흐름: 검증 완료된 전체 로컬 작업을 하나의 재현 가능한 릴리스로 고정하고 원격 저장소와 Vercel 프로덕션에 반영한 뒤, 실제 공개 URL 검증 결과와 커밋·배포 식별자를 현행 인수인계에 남긴다.
- 범위: 현재 저장소의 추적·신규 제품 코드, 데이터, 자산, 운영 스크립트, 현행·역사 문서를 커밋한다. `.tmp`, `.next`, `.vercel`, 로컬 환경·로그는 제외한다. 빌드·검증 통과 후 현재 브랜치를 `origin`에 푸시하고 연결된 `chika-idol-hub` 프로젝트에 프로덕션 배포한다.
- 실패·롤백 경계: 검사 실패 시 커밋·푸시·배포를 진행하지 않는다. 푸시 실패는 강제 푸시하지 않고 원격 상태를 재확인한다. 배포 실패 시 기존 프로덕션 별칭을 보존하며, 새 배포가 READY이고 공개 URL 스모크가 통과하기 전 완료로 기록하지 않는다. 비밀·로컬 보고서·실행 로그는 커밋하지 않는다.
- 완료 조건·검증: `.tmp` 제외 확인, lint·typecheck·데이터·날짜·파라미터·프로덕션 빌드·diff 검사, 커밋 SHA, upstream 푸시, Vercel production READY, 프로덕션 핵심 다국어 URL과 잘못된 경로 상태를 확인하고 `WORK_ORDER.md`와 `CODEX_HANDOVER.md`에 결과를 반영한다.

## 2026-08-31 MVP H-5 — 전체 기능·파라미터 계약 정합화

- 사용자 의도·현재 문제: 제품 의도부터 URL·CLI·데이터·UI·실패 상태를 재감사한 결과 지역과 구역의 교차 소속이 검증되지 않고, 잘못된 탭·라이브 필터 URL이 정규화·보존되지 않으며, 근거 없는 랭킹 기본값과 사용되지 않는 지도 입력, 공연 상태 미표시, 비대화형 lint 실패, 파라미터 회귀 검사 부재가 남아 있다.
- 기대 흐름: 모든 공유 URL은 유효한 상태로 정규화되고 새로고침·뒤로가기에서 동일한 화면을 복원한다. 데이터 스키마는 공식 근거 없는 지표를 생성하지 않으며, 지도·라이브 UI는 실제 소비하는 데이터와 상태만 계약한다. 검증 명령은 비대화형으로 재현 가능해야 한다.
- 범위: 지역↔구역 공통 계약, 홈 지도·그룹 탭·라이브 필터 URL, 랭킹 스키마·데이터 검증, 지도 props, 공연 상태 라벨·필터, ESLint CLI, 파라미터 회귀 검사, 관련 문서·명령을 동기화한다.
- 안전 경계·비범위: 공식 출처·공개 승격·이미지 권리·외부 문의·커밋·배포 정책은 바꾸지 않는다. 기존 레거시 지표는 공식 근거가 없으면 삭제하되 검증된 값은 출처·확인일과 함께 보존한다.
- 완료 조건·검증: 잘못된 지역/구역·탭·라이브 필터가 안전하게 정규화되고, 근거 없는 기본 지표가 런타임에 주입되지 않으며, 취소·연기·매진 상태가 구분된다. 파라미터 회귀 검사·데이터 검사·TypeScript·ESLint·774/774 빌드·핵심 HTTP 스모크·diff 검사를 통과한다.
- 구현 결과: 지역↔구역 단일 계약을 스키마·검증기·홈 URL 복원에 연결하고, 그룹 탭과 라이브 필터를 공유 URL·뒤로가기와 동기화했다. 취소 공연의 예정 제외와 상태 라벨을 추가했으며 사용하지 않는 지도 입력을 제거했다. 출처 없는 레거시 랭킹 필드를 삭제하고 스키마의 합성 점수 기본값을 폐지했으며, provenance 없는 재유입을 검증 실패로 막았다. ESLint CLI와 파라미터 회귀 검사를 추가했고 2026-09-01 전체 정적·데이터·빌드·HTTP·브라우저·diff 검사와 바탕화면 수동 갱신기 재실행이 통과했다.

## 2026-08-31 MVP 5-A-1 — 바탕화면 수동 갱신기 신뢰성 복구

- 사용자 의도·현재 문제: 바탕화면 `Chika Idol Box 일일 업데이트.cmd`가 제대로 동작하는지 사용자가 확인하기 어렵다. 직접 오프라인·네트워크 write 실행은 모두 PASS하므로 Node·수집기·데이터 계약보다 진입점의 인코딩·오류 표시·로그·검증 가능성이 취약한 상태다.
- 기대 흐름: 더블클릭 → 저장소·Node·tsx 사전 점검 → 후보 write 실행 → 콘솔과 영구 로그에 단계별 결과 표시 → 최신 JSON/Markdown 보고서 존재·성공 상태 확인 → 성공/부분 실패를 명확한 종료 코드와 안내로 반환한다.
- 범위: 저장소에 검증 가능한 PowerShell 운영 진입점을 추가하고 바탕화면 `.cmd`는 그 진입점만 호출하는 얇은 CRLF 래퍼로 교체한다. `daily-update.ts`의 후보 격리·비자동 공개·이미지 비발송 정책은 유지한다.
- 안전 경계·비범위: 공개 공연·생일·이미지 자동 승격, 외부 문의, 예약 작업, 커밋·배포는 추가하지 않는다. 네트워크 실패 시 기존 후보와 이전 보고서를 파괴하지 않고 새 실행 로그와 실패 상태를 남긴다.
- 완료 조건·검증: `--skip-network`, `--write`, PowerShell 진입점의 `-NoPause`, 바탕화면 `.cmd --no-pause`가 재현 가능하게 종료되며 최신 보고서가 현재 실행 이후 생성되고 `success=true`임을 확인한다. 데이터·타입·빌드·diff 검사와 문서 동기화를 완료한다.
- 재현 후 방향 보완: 첫 PowerShell 진입점 시험에서 Node 수집·검증은 PASS했지만 Windows PowerShell 5.1의 기본 `Get-Content` 인코딩이 BOM 없는 UTF-8 `latest-report.json`을 ANSI로 해석해 한글·일본어 문자열을 손상시키고 JSON 파싱을 실패시켰다. 진입점의 네이티브 출력과 보고서 읽기를 UTF-8로 명시해 실제 실패와 인코딩 오판을 분리한다.
- 구현 결과: `scripts/run-daily-update.ps1`이 Node·tsx·저장소를 사전 확인하고 다국어 네이티브 출력과 BOM 없는 JSON을 UTF-8로 처리하며, 실행별 로그·보고서 최신성·`success=true`를 검증하도록 구현했다. 바탕화면 CMD는 ASCII·CRLF 얇은 래퍼로 교체했다. 오프라인 진입점과 실제 CMD write 실행이 모두 종료 코드 0으로 통과했고 후보 55건·생일 미확인 28명·이미지 권리 검토 2건을 보고했다. 데이터·후보·이미지·타입 검사, 774/774 빌드, diff 검사가 통과했다.

## 2026-08-31 MVP H-4 — 문서 권위·운영 상태·검증 계약 정합화

- 사용자 의도·큰 줄기: 전체 Markdown 감사에서 확인된 현행/역사 문서의 권위 역전, 이미지 문의 상태, MVP H 완료 상태, 기준일, 기본·확장 검증 범위의 모호성을 제거해 새 작업자가 금지된 과거 흐름이나 외부 발송을 재개하지 않도록 한다.
- 범위: `AGENTS.md`의 현행 기준 우선순위와 검증 계층, 이 계획과 `WORK_ORDER.md`의 기준일·H 상태, `IMAGE_PERMISSION_REQUESTS.md`의 발송 중단 상태, `CODEX_HANDOVER.md`의 문서 권위·검증 적용 조건을 동기화한다.
- 안전 경계·비범위: 코드·데이터·UI·외부 문의·커밋·배포는 변경하지 않는다. 날짜가 명시되고 역사 배너가 있는 과거 수치·결정 기록은 삭제하지 않으며 현행 근거로만 사용하지 않도록 분리한다.
- 실패·회귀 위험: 주제 문서의 유용한 기술 이력을 폐기하거나, 기본 검증과 운영·인수인계 검증을 무조건 동일하게 만들어 작은 작업의 범위를 과도하게 넓히거나, 문의 초안을 승인 문서로 오인할 수 있다.
- 완료 조건·검증: 현행 계획→작업지시→인수인계가 역사·주제 문서보다 우선하고, 이미지 문의 2건이 `발송 중단·내부 권리 검토`로 표시되며, H-1~4 상태와 2026-08-31 기준일이 일치한다. 모든 Markdown의 관련 상태·명령·문서 참조를 `rg`와 `git diff --check`로 재검사한다.
- 구현 결과: 현행 문서 순서를 계획→작업지시→인수인계로 교정하고 역사·주제 문서의 보조 지위를 명시했다. 이미지 문의 2건을 발송 중단·내부 권리 검토로 전환했으며 H-1~4, 기준일, 기본/확장/문서 전용 검증 계약을 동기화했다. 관련 상태 전수 검색과 Markdown 참조 확인, `git diff --check`가 통과했고 기존 LF→CRLF 경고만 남았다.

## 2026-08-31 MVP H-3 — 현행 인수인계 재감사·갱신

- 사용자 의도·큰 줄기: 최신 구현·검증·승인 경계를 새 작업자가 한 문서에서 재현할 수 있도록 현행 인수인계의 낡은 참조와 누락된 명령을 정리한다.
- 범위: `CODEX_HANDOVER.md`의 MVP 상태, 잔여 생일 그룹, 공연 후보 분류, 최신 검증 기준선, 재현 명령, 다음 승인 경계를 계획·작업지시·실제 데이터와 대조해 갱신한다.
- 안전 경계·비범위: 코드·데이터·정책·공개 UI·배포 상태는 변경하지 않는다. 과거 `HANDOVER.md`는 역사 자료로 유지하고 현행 문서만 갱신한다.
- 완료 조건·검증: 완료된 エイアイカ가 잔여 목록에서 제거되고, 제외 4건이 사진집/미디어 2·생일 2로 정확히 표현되며, 후보 보고·데이터 검사 명령과 H-2 HTTP 결과·MVP G 승인 대기가 명시된다. 오프라인·데이터·후보 보고와 `git diff --check`로 대조한다.
- 구현 결과: 현행 인수인계의 H 상태를 H-1~3으로 갱신하고, 잔여 생일 그룹에서 완료된 エイアイカ를 제거했다. 재현 명령에 공연 후보·이미지 후보·데이터 검사를 추가하고, 제외 4건을 사진집/미디어 2·생일 2로 명확히 했으며 MVP G의 별도 사용자 승인 대기를 기록했다. 후보·이미지·오프라인·데이터 보고와 diff 검사가 통과했다.

## 2026-08-31 MVP H-2 — 프로덕션 빌드 로컬 HTTP 스모크 검증

- 사용자 의도·큰 줄기: 정적 경로 생성 성공만으로 완료를 단정하지 않고, 실제 Next.js 프로덕션 서버에서 지역·그룹·멤버·공연의 핵심 사용자 흐름이 응답하는지 검증한다.
- 범위: 기존 774/774 빌드를 로컬 3001 포트에서 실행하고 한국어·일본어·영어 홈, 대표 그룹 상세, 대표 멤버 상세, C/E-13에서 승격한 공연 5건의 다국어 상세, 라이브 목록, 잘못된 경로의 404를 HTTP로 확인한다.
- 안전 경계·비범위: 코드·데이터·외부 서비스·프로덕션 배포는 변경하지 않는다. 로컬 서버만 사용하며 응답 본문에는 비밀정보를 기록하지 않는다.
- 완료 조건·검증: 정상 경로는 HTTP 200, 존재하지 않는 경로는 404를 반환하고, 대표 HTML에 페이지 식별 가능한 제목 또는 엔티티명이 포함된다. 결과를 작업지시서와 현행 인수인계에 기록한다.
- 구현 결과: 3개 언어 홈·라이브·タイトル未定 그룹·阿部葉菜 멤버와 C/E-13 공연 5건의 3개 언어 상세를 포함한 정상 27경로가 모두 HTTP 200 및 해당 언어 엔티티명을 반환했다. 존재하지 않는 한국어 공연 경로는 404를 반환해 총 28/28 통과했다. 검증용 로컬 서버 PID 69916을 종료하고 3001 포트에 LISTENING 프로세스가 없음을 확인했다.

## 2026-08-31 MVP C/E-14 — タイトル未定 공식 캘린더 후보 갱신

- 사용자 의도·큰 줄기: 공식 공급원 변경을 격리 후보에 먼저 반영하고, 미완결 5건의 상세·분류가 확정됐는지 재검토한다.
- 범위: 공식 Google Calendar ICS를 다시 수집해 `data/chika-live-candidates.json`만 갱신하고, 변경·신규·이력 보존 항목을 공개 공연과 대조한다.
- 안전 경계·비범위: 수집 결과를 공개 공연으로 자동 승격하지 않는다. 시간·장소·티켓·행사 성격이 공식 원문에서 확정되지 않으면 기존 검토 상태를 유지한다.
- 완료 조건·검증: 후보 갱신 후 공개 연결·상세 부족·분류 대기·제외·검토 가능 집계가 일관되고, 승격 가능한 항목이 있으면 독립 소배치로 처리한다. 후보 보고·오프라인·데이터·타입·빌드·diff 검사를 실행한다.
- 구현 결과: 공식 ICS는 17건(현재 15·이력 2)으로 변경돼 후보 스냅샷을 갱신했다. 기존 미완결 5건은 시간·장소·티켓 또는 분류 근거가 여전히 없어 상태를 유지했고 자동 공개 승격은 0건이다. 공개 연결 45·상세 부족 3·분류 대기 2·제외 4·검토 가능 1 집계와 774/774 빌드가 통과했다.

## 2026-08-31 MVP F-25 — 잔여 공식 생일 근거 재감사

- 사용자 의도·큰 줄기: 잔여 생일 미확인 28명을 계속 줄이되 공식 원문이 없는 값을 채우기 위해 추정하지 않고, 그룹별 차단 사유를 재검증해 다음 실행 단위를 확정한다.
- 범위: MyDearDarlin’ 공식 프로필의 로컬 출처 흔적, ドラマチックレコード 공식 소속사 그룹 프로필, Sweet Alley·Mirror,Mirror·yosugala 공식 프로필 가용성을 재조사한다. 데이터 값과 UI는 공식 생일 원문이 확보될 때만 변경한다.
- 안전 경계·비범위: 생탄제 일정·SNS 핸들·검색 스니펫·2차 기사로 생일을 추정하지 않는다. 공식 멤버 재적 확인은 생일 근거로 대체하지 않으며 이미지·활동 상태·담당색도 변경하지 않는다.
- 완료 조건·검증: 조사 근거와 미확정 사유를 작업지시서·인수인계에 남기고, 반영 가능한 공식 생일이 없으면 28명의 빈 상태를 보존한다. 후보 보고·오프라인 일일 실행·데이터·타입·프로덕션 빌드·diff 검사를 실행한다.
- 구현 결과: MyDearDarlin’ 저장소 내부에는 공식 프로필 인덱스와 현역 7인 근거만 있고 개인 생일 원문은 없음을 확인했다. BIG ISLAND RECORDS 공식 그룹 프로필도 ドラマチックレコード 현역 6인만 확인하며 잔여 4명의 생일은 공개하지 않는다. 다른 잔여 그룹에서도 반영 가능한 1차 생일 근거가 없어 데이터 28명의 빈 상태를 보존했다. 전체 검증과 774/774 빌드가 통과했다.

## 2026-08-31 MVP F-24 — Sweet Alley 笹木里緒菜 공식 소속사 프로필

- 사용자 의도·큰 줄기: 공식 개인 프로필이 확보된 멤버를 독립 소배치로 반영해 생일 미확인 현역을 줄인다.
- 범위: TWIN PLANET ENTERTAINMENT 공식 프로필의 笹木里緒菜 완전 생년월일 `1998-04-16`과 福井県 출신을 반영한다.
- 안전 경계·비범위: 혈액형·신장·취미·특기·언어·사진·SNS·담당색은 추가하지 않는다. 다른 Sweet Alley 멤버는 공식 개인 프로필이 확보될 때까지 변경하지 않는다.
- 완료 조건·검증: 笹木里緒菜가 완전 생년월일·출신지·공식 소속사 URL 근거를 가지며 생일 미확인 현역이 29→28명으로 감소하고 전체 검증이 통과한다.
- 구현 결과: 소속사 공식 완전 생년월일·출신지와 직접 URL을 반영해 미확인 생일이 28명으로 감소했다. 후보·오프라인·데이터·타입 검사와 774/774 프로덕션 빌드가 통과했다.

## 2026-08-31 MVP F-23 — INUWASI 공식 6인 생일 완결

- 사용자 의도·큰 줄기: 그룹 공식 멤버 인덱스와 개인 프로필로 생일 미확인 현역을 안전하게 줄인다.
- 범위: 공식 멤버 인덱스의 6인과 개인 페이지를 대조해 ライカ `02-14`, がるむ `11-29`, はのんまゆ `08-08`, カリヲリ `07-31`, すずめ `12-21`, 六椛 `03-21`을 `birthMonthDay`로 반영한다.
- 안전 경계·비범위: 메인 홈의 5인 노출과 별도 공식 멤버 인덱스·개인 페이지·2026년 행사/발매 자료의 6인 근거가 엇갈리므로 활동 상태와 공식 인원은 변경하지 않는다. 출생연도·출신지·담당색·사진·SNS·지표를 추가하지 않는다.
- 완료 조건·검증: 현행 6인이 모두 개인 공식 월·일 생일 URL을 가지며 생일 미확인 현역이 35→29명으로 감소하고 전체 검증이 통과한다.
- 구현 결과: 6개 개인 공식 페이지의 월·일 생일을 반영해 미확인 생일이 29명으로 감소했다. 후보·오프라인·데이터·타입 검사와 774/774 프로덕션 빌드가 통과했다.

## 2026-08-31 MVP F-22 — エイアイカ 공식 3인 생일·출신지 완결

- 사용자 의도·큰 줄기: 생일 미확인 현역을 그룹 공식 프로필 근거가 완결된 소배치부터 줄인다.
- 범위: 공식 사이트의 현역 3인 명단과 프로필에 따라 怜伽 `05-17`·奈良県, 玲奈 `06-15`·兵庫県, 千聖 `11-02`·岐阜県를 반영한다.
- 안전 경계·비범위: 출생연도·혈액형·담당색·사진·SNS·지표는 추가하지 않는다. 월·일만 공개된 생일은 `birthMonthDay`로 저장한다.
- 완료 조건·검증: エイアイカ 3인이 공식 월·일 생일·출신지·공식 URL 근거를 가지며 생일 미확인 현역이 38→35명으로 감소하고 전체 검증이 통과한다.
- 구현 결과: 공식 사이트의 현역 3인 명단·생일·출신지를 대조해 반영했다. 미확인 생일은 35명으로 감소했고 후보·오프라인·데이터·타입 검사와 774/774 프로덕션 빌드가 통과했다.

## 2026-08-31 MVP C/E-13 — タイトル未定 공식 공연 5건 완결

- 사용자 의도·큰 줄기: 공식 캘린더의 미완결 후보를 행사·공연장·공식 티켓 원문과 대조해 공개 공연 탐색으로 연결한다.
- 범위: 2026-09-06 くさのねアイドルフェスティバル, 09-07 LadySteady, 09-11 shibuya luisant, 09-20~21 フクフェス, 10-12 ぐんまちゃんアイドルフェスティバル 5건의 출연·전체 행사 시간·장소·일반 가격·티켓 근거를 반영한다.
- 안전 경계·비범위: 그룹별 무대 시각을 행사 전체 시각으로 추정하지 않는다. 9/4 삿포로, 9/12 클린오션, 9/24 목요 정기, 10/2~4 삿포로 가제, 10/18 홋카이도는 공식 세부 정보가 완결될 때까지 후보로 유지한다. フクフェス는 タイトル未定가 출연하는 9/20 2부를 대표 시각으로 쓰고 9/21까지의 다일 행사임을 보존한다.
- 완료 조건·검증: 공개 공연 65→70, 공개 연결 40→45, 상세 미완결 6→3, 분류 미완결 4→2 및 후보 보고·오프라인 일일 실행·데이터 검사·타입 검사·프로덕션 빌드·`git diff --check` 통과.
- 구현 결과: 공식 행사·공연장·티켓 원문으로 5건을 공개 연결해 공개 공연 70건·공개 연결 45건·상세 미완결 3건·분류 미완결 2건이 됐다. 후보·오프라인·데이터·타입 검사와 774/774 프로덕션 빌드가 통과했다.

## 2026-08-31 MVP C/E-12 — TGC·IRC 라이브 승격 및 사진집 후보 정리

- 사용자 의도·큰 줄기: 공식 라이브 퍼포먼스가 확인된 결합 이벤트는 공개 탐색에 연결하고, 출판·온라인 특전회는 라이브 후보 큐에서 명시적으로 분리한다.
- 범위: 2026-09-19 TGC와 2026-09-20 IRC를 공식 전체 행사 시간·장소·일반 지정석 가격·티켓 원문과 함께 공개 연결한다. 사진집 서점 이벤트와 온라인 특전회는 `release_or_media`·`rejected`로 분류한다.
- 안전 경계·비범위: 전체 행사 시각을 FRUITS ZIPPER 개별 무대 시각으로 오인시키지 않는다. 사진집 행사를 삭제하지 않고 감사 후보로 보존하며, ASOBIEXPO HAWAII는 시간·일반권 가격 발표 전 승격하지 않는다.
- 완료 조건·검증: 공개 공연 63→65, 공개 연결 38→40, 검토 가능 5→1, 제외 2→4 및 전체 검증 통과.
- 구현 결과: TGC·IRC 2건을 공개 연결하고 사진집 2건을 비라이브로 제외해 공개 공연 65건·공개 연결 40건·검토 가능 1건·제외 4건이 됐다. 후보·오프라인·데이터·타입 검사와 759/759 빌드가 통과했다.

## 2026-08-31 MVP C/E-11 — KAWAII LAB. COLLECTION 공식 이벤트 1건 승격

- 사용자 의도·큰 줄기: 공식 출연·시간·장소·가격·티켓 계약이 완결된 라이브 퍼포먼스 포함 이벤트를 검증 라이브 탐색에 연결한다.
- 범위: 공식 상세 `44011`과 KAWAII LAB 공식 공지 `87217`에서 확인한 2026-10-12 북규슈 공연을 OPEN 14:00 / START 16:00, 지정석 11,000엔, 공식 안내와 함께 공개 연결한다.
- 안전 경계·비범위: 패션 쇼와 라이브가 결합된 공식 공연임을 제목과 원문으로 보존하며 좌표·포스터·판매 상태를 추정하지 않는다. 일반 TGC·IRC, 사진집, 온라인·해외 행사는 포함 정책을 바꾸지 않는다.
- 완료 조건·검증: 공개 공연 62→63, 공개 연결 37→38, 검토 가능 6→5 및 전체 검증 통과.
- 구현 결과: 공식 KAWAII LAB 공지로 1건을 공개 연결해 공개 공연 63건·공개 연결 38건·검토 가능 5건이 됐고 후보·오프라인·데이터·타입 검사와 753/753 빌드가 통과했다.
- 다음 정책 경계: 잔여 5건은 일반 패션 행사(TGC·IRC), 사진집 서점/온라인 특전회, 해외 복합 문화행사(ASOBIEXPO HAWAII)다. 이를 공개 라이브에 포함하려면 제품 범위와 필수 가격·티켓 계약을 명시적으로 결정해야 한다.

## 2026-08-30 MVP C/E-10 — TOMAKOMAI·ROCK IN JAPAN 공식 페스티벌 2건 승격

- 사용자 의도·큰 줄기: 공식 출연 일정과 주최 측 공연·티켓 원문이 함께 완결된 대형 페스티벌을 검증 라이브 탐색에 연결한다.
- 범위: 2026-09-12 TOMAKOMAI MIRAI FEST와 2026-09-13 ROCK IN JAPAN FESTIVAL을 공식 전체 행사 OPEN/START·장소·1일권 가격·티켓 원문과 함께 공개 연결한다.
- 안전 경계·비범위: 전체 행사 시작 시각을 FRUITS ZIPPER 개별 무대 시각으로 표현하지 않는다. 좌표·포스터·판매 가능 여부를 추정하지 않고 공식 페이지의 현재 판매 상태는 고정 데이터로 복제하지 않는다.
- 완료 조건·검증: 공개 공연 60→62, 공개 연결 35→37, 검토 가능 8→6 및 전체 검증 통과.
- 구현 결과: 두 공식 페스티벌을 공개 연결해 공개 공연 62건·공개 연결 37건·검토 가능 6건이 됐고 후보·오프라인·데이터·타입 검사와 750/750 빌드가 통과했다.

## 2026-08-30 MVP C/E-9 — KAWAII MANAFY LAND 공식 이벤트 1건 승격

- 사용자 의도·큰 줄기: 공식 일정에 수록된 유료 아이돌 이벤트 중 시간·장소·가격·신청 원문이 완결된 후보를 공개 공연 흐름에 연결한다.
- 범위: 공식 상세 `44246`과 공식 판매 페이지에서 확인한 2026-09-04 진나카 마나 출연 이벤트를 1부 대표 시각(OPEN 12:30 / START 13:00), KAWAII MONSTER LAND, 각 부 8,000엔, 공식 신청 링크와 함께 공개 연결한다.
- 안전 경계·비범위: 4부 구성 전체를 단일 공연 시각으로 오인시키지 않도록 공식 상세로 연결하며, 좌표·포스터·판매 결과를 추정하지 않는다. 사진집·패션·해외 행사 정책은 변경하지 않는다.
- 완료 조건·검증: 공개 공연 59→60, 공개 연결 34→35, 검토 가능 9→8 및 전체 검증 통과.
- 구현 결과: 1부 대표 시각과 각 부 8,000엔·공식 신청 링크로 1건을 공개 연결해 공개 공연 60건·공개 연결 35건·검토 가능 8건이 됐고 후보·오프라인·데이터·타입 검사와 744/744 빌드가 통과했다.

## 2026-08-30 MVP C/E-8 — 미에 공식 투어 1건 승격

- 사용자 의도·큰 줄기: 남은 공연 후보를 공식 근거 완결 순서로 소진해 `공식 후보 → 사람 검토 → 공개 공연 → 공식 원문·티켓` 흐름을 이어 간다.
- 범위: 공식 일정 후보 `40029`의 2026-09-21 미에 공연을 공식 투어 페이지에서 재검증한 시간·장소·지정석 8,000엔·티켓 안내와 함께 공개 연결한다.
- 안전 경계·비범위: 좌표·포스터·판매 상태를 추정하지 않고, 가격·티켓 근거가 불완전한 다른 이벤트나 사진집 행사를 함께 승격하지 않는다.
- 완료 조건·검증: 공개 공연 58→59, 공개 연결 33→34, 검토 가능 10→9를 확인하고 후보 보고·오프라인 일일 실행·데이터 검사·타입 검사·프로덕션 빌드·`git diff --check`가 통과한다.
- 구현 결과: 미에 1건을 공개 연결해 공개 공연 59건·공개 연결 34건·검토 가능 9건이 됐고 후보·오프라인·데이터·타입 검사와 741/741 빌드가 통과했다.

## 2026-08-30 MVP C/E-7 — 히로시마·삿포로·오키나와 공식 투어 5건 승격

- 범위: 공식 상세 `40043`·`40044`·`40045`·`40048`·`40049`의 시간·장소·지정석 8,000엔·티켓 안내를 공개 연결한다.
- 안전 경계: 좌표·포스터·판매 상태를 추정하지 않으며 기존 공개 도쿄 2건은 중복 생성하지 않는다.
- 완료 조건: 공개 공연 53→58, 공개 연결 28→33, 검토 가능 15→10 및 전체 검증 통과.
- 구현 결과: 5건을 공개 연결해 공개 공연 58건·공개 연결 33건·검토 가능 10건이 됐고 후보·오프라인·데이터·타입 검사와 738/738 빌드가 통과했다.

## 2026-08-30 MVP C/E-6 — 시코쿠·규슈 공식 투어 4건 승격

- 범위: 공식 상세 `40039`~`40042`의 다카마쓰·마쓰야마·후쿠오카·구마모토 공연을 시간·장소·지정석 8,000엔·티켓 안내와 함께 공개 연결한다.
- 안전 경계: 좌표·포스터·판매 상태를 추정하지 않으며 UI·스키마·자동 공개는 변경하지 않는다.
- 완료 조건: 공개 공연 49→53, 공개 연결 24→28, 검토 가능 19→15 및 전체 검증 통과.
- 구현 결과: 4건을 공개 연결해 공개 공연 53건·공개 연결 28건·검토 가능 15건이 됐고 데이터·후보·타입 검사와 723/723 빌드가 통과했다.

## 2026-08-30 MVP C/E-5 — 가나자와 공식 투어 단일 승격

- 범위: 공식 상세 `40038`의 2026-10-11 가나자와 공연을 시간·장소·지정석 8,000엔·티켓 안내와 함께 공개 연결한다.
- 안전 경계: 좌표·포스터·판매 상태를 추정하지 않는다.
- 완료 조건: 공개 공연 48→49, 공개 연결 23→24, 검토 가능 20→19 및 전체 검증 통과.
- 구현 결과: 공개 공연 49건·공개 연결 24건·검토 가능 19건이며 데이터·타입 검사와 711/711 빌드가 통과했다.

## 2026-08-30 MVP C/E-4 — 오사카·우쓰노미야·센다이 공식 투어 5건 승격

- 범위: 공식 상세 `40033`~`40037`의 2026-09-30~10-07 투어 5건을 시간·장소·지정석 8,000엔·티켓 안내와 함께 공개 연결한다.
- 연결: 후보 5건과 공개 공연 5건을 일대일 연결하며 UI·스키마·자동 공개는 변경하지 않는다.
- 안전 경계: 좌표·포스터·판매 상태를 추정하지 않고 지도 핀을 만들지 않는다.
- 완료 조건: 공개 공연 43→48, 공개 연결 18→23, 검토 가능 25→20 및 전체 검증 통과.
- 구현 결과: 공식 상세 5건을 공개 연결해 공개 공연 48건·공개 연결 23건·검토 가능 20건이 됐고 데이터·후보·타입 검사와 708/708 빌드가 통과했다.

## 2026-08-30 MVP C/E-3 — 기후·나고야 공식 투어 공개 승격

- 범위: 공식 상세 재확인이 완료된 2026-09-23 기후 `40030`, 2026-09-25 나고야 `40032`의 시간·공연장·지정석 8,000엔·티켓 안내를 공개한다.
- 보류: 미에 `40029`는 현재 공식 상세 재조회가 실패해 후보 상태를 유지한다.
- 연결: 후보 2건과 공개 공연 2건을 일대일 연결하며 새 UI·스키마·자동 공개는 만들지 않는다.
- 안전 경계: 좌표·포스터·판매 상태는 추정하지 않고 지도 핀을 만들지 않는다.
- 완료 조건: 공개 공연 41→43, 공개 연결 16→18, 검토 가능 27→25 및 전체 검증 통과.
- 구현 결과: 기후·나고야 2건을 공식 상세로 공개 연결해 공개 공연 43건·공개 연결 18건·검토 가능 25건이 됐고 데이터·후보·타입 검사와 693/693 빌드가 통과했다. 미에는 보류했다.

## 2026-08-30 MVP C/E-2 — 공식 투어 공연 후보 2건 공개 승격

- 사용자 의도: 생일 추정을 보류한 상태에서 기존 공식 공연 신뢰 계약으로 안전한 MVP 가지를 계속 진전시킨다.
- 범위: FRUITS ZIPPER 공식 상세의 2026-09-16 사이타마·2026-09-18 마쓰도 투어 2건에 확인된 시간·공연장·지정석 8,000엔·공식 티켓 안내를 반영한다.
- 데이터·UI·명령 연결: 두 후보와 두 공개 공연 ID를 일대일 연결해 그룹 라이브·전체 일정·후보 보고에 노출한다.
- 안전 경계: 좌표·포스터·판매 상태를 추정하지 않으며 지도 핀은 만들지 않는다. 자동 공개 계약은 추가하지 않는다.
- 완료 조건: 공개 공연 39→41, 공개 연결 후보 14→16, 검토 가능 후보 29→27이 되며 전체 검증이 통과한다.
- 구현 결과: 공식 상세 2건을 공개 공연과 연결해 공개 공연 41건·공개 연결 16건·검토 가능 27건이 됐다. 데이터·후보·타입 검사와 깨끗한 캐시 기준 687/687 빌드가 통과했다.

## 2026-08-30 MVP C/E-1 — 공식 공연 후보 단일 공개 승격

- 사용자 결정: 생일은 개인 공식 프로필 정책을 유지하고 잔여 38명은 추정 없이 보류하며, 기존 신뢰 규칙 안의 다른 MVP 가지를 진행한다.
- 범위: FRUITS ZIPPER 공식 상세에서 날짜·시간·공연장·가격·티켓 안내가 확인된 2026-09-03 요코스카 투어 후보 1건을 공개 공연과 연결한다.
- 데이터·UI·명령 연결: 후보의 `publishedEventId`와 공개 공연 ID를 일대일 연결해 그룹 라이브 탭·일정 카드·일일 후보 집계에 반영한다. 새 UI·스키마·자동 공개는 만들지 않는다.
- 안전 경계: 공식 상세에 없는 좌표·포스터·판매 상태는 추정하지 않고, 공연장 지도 핀은 만들지 않는다. 공식 상세/투어 안내 링크만 제공한다.
- 완료 조건: 공개 공연 38→39, 공개 연결 후보 13→14, 검토 대기 40→39가 되며 데이터·후보 보고·타입·빌드·diff 검사가 통과한다.
- 구현 결과: 공식 상세와 투어 안내를 근거로 공개 공연 39건·공개 연결 후보 14건·검토 가능 후보 29건이 됐다. 데이터·후보 보고·타입 검사와 681/681 프로덕션 빌드가 통과했다.

## 2026-08-30 MVP F-21 — CYNHN 소속사 공식 생일 완결 소배치

- 사용자 의도: 개인별 공식 근거 정책으로 확인된 CYNHN 나머지 3명의 생일을 보강해 현역 4인의 생일을 완결한다.
- 범위: 디어스테이지 공식 개인 프로필의 綾瀬志希 `03-14`, 青柳透 `05-26`, 広瀬みのり `09-08`, 개인 URL과 확인일을 반영한다.
- 데이터·UI·명령 연결: 기존 `birthMonthDay` 계약을 통해 생일 캘린더·멤버 상세·누락 집계에 연결한다. 새 UI·스키마·명령은 만들지 않는다.
- 안전 경계: 출생연도·출신지·담당색·사진·SNS·취미·특기는 추가하지 않는다. 공식 소속사 페이지가 없는 정보는 빈 상태로 유지한다.
- 완료 조건: CYNHN 현역 4인이 모두 소속사 공식 월·일 생일 근거를 가지며 생일 미확인 현역이 41명에서 38명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 결과를 작업지시서·인수인계에 기록한다.
- 구현 결과: 세 개인 레코드에 소속사 공식 월·일 생일과 직접 URL을 반영했다. 오프라인 집계는 미확인 38명·향후 30일 17명이며 데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.

## 2026-08-30 MVP F-20 — 月雲ねる 소속사 공식 생일 단일 소배치

- 사용자 의도: 승인된 개인별 공식 근거 정책으로 소속사 프로필이 직접 제공하는 생일을 계속 보강한다.
- 범위: 디어스테이지 공식 아티스트 프로필의 CYNHN 月雲ねる 월·일 생일 `09-15`, 개인 공식 URL과 확인일만 반영한다.
- 데이터·UI·명령 연결: 기존 `birthMonthDay` 계약을 통해 생일 캘린더·멤버 상세·누락 집계에 연결한다. 새 UI·스키마·명령은 만들지 않는다.
- 안전 경계: 출생연도·출신지·담당색·사진·SNS·취미·특기는 추가하지 않고 다른 CYNHN 멤버는 변경하지 않는다.
- 완료 조건: 月雲ねる가 월·일 생일과 소속사 공식 URL 근거를 가지며 생일 미확인 현역이 42명에서 41명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 결과를 작업지시서·인수인계에 기록한다.
- 구현 결과: 月雲ねる에 소속사 공식 월·일 생일과 직접 URL을 반영했고 당시 미확인 현역은 41명으로 감소했다. 데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.

## 2026-08-30 MVP F-19 — 高梨有咲 공식 출판사 프로필 단일 소배치

- 사용자 의도: 승인된 개인별 공식 근거 정책으로 정확한 프로필이 확보된 멤버를 계속 보강한다.
- 범위: 集英社 週プレNEWS 공식 프로필에 명시된 ドラマチックレコード 高梨有咲의 완전 생년월일 `2005-11-21`, 출신지 가나가와현, 담당색 노랑, 공식 출판사 URL과 확인일을 반영한다.
- 데이터·UI·명령 연결: 기존 `birthDate`·출신지·공식 색상명 근사 계약을 통해 생일 캘린더·멤버 상세·누락 집계·카드 색상에 연결한다. 새 UI·스키마·명령은 만들지 않는다.
- 안전 경계: 신장·특기·사진·SNS·그라비아 정보는 범위 밖으로 두고, 다른 ドラマチックレコード 멤버는 변경하지 않는다. 노랑 HEX는 UI 표시 근사값이다.
- 완료 조건: 高梨有咲가 완전 생년월일·출신지·담당색·공식 출판사 URL 근거를 가지며 생일 미확인 현역이 43명에서 42명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 결과를 작업지시서·인수인계에 기록한다.
- 구현 결과: 高梨有咲에 공식 출판사 프로필의 완전 생년월일·출신지·담당색을 반영했고 당시 미확인 현역은 42명으로 감소했다. 데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.

## 2026-08-30 MVP F-18 — 개인별 공식 근거 소배치 정책·新居歩美

- 사용자 승인: 그룹 전체가 한 번에 완결되지 않아도, 멤버 개인의 공식 소속사·공식 프로필에 정확한 정보가 있으면 해당 멤버만 독립 소배치로 반영한다.
- 정책 경계: 멤버별 출처·확인일을 독립 보존하고 미확인 멤버는 빈 상태로 유지한다. 생탄제 개최일·SNS 계정명·2차 기사만으로는 생일을 반영하지 않는다.
- 범위: BIG ISLAND RECORDS 공식 아티스트 프로필에 명시된 ドラマチックレコード 新居歩美의 완전 생년월일 `2003-06-28`, 출신지 가가와현, 개인 공식 URL과 확인일만 반영한다.
- 데이터·UI·명령 연결: 기존 `birthDate`·출신지 계약을 통해 생일 캘린더·멤버 상세·누락 집계에 연결한다. 새 UI·스키마·명령은 만들지 않는다.
- 안전 경계: 신장·혈액형·취미·특기·사진·SNS·지표와 담당색은 이번 범위에서 추가하지 않는다. 다른 ドラマチックレコード 멤버는 변경하지 않는다.
- 완료 조건: 新居歩美가 완전 생년월일·출신지·개인 공식 URL 근거를 가지며 생일 미확인 현역이 44명에서 43명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 실제 결과를 `WORK_ORDER.md`와 현행 인수인계에 기록한다.
- 구현 결과: 新居歩美에 소속사 공식 완전 생년월일·출신지와 직접 URL을 반영했고 당시 미확인 현역은 43명으로 감소했다. 데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.

## 2026-08-30 MVP F-17 — BYBBiT 공식 생일·담당색 완결 소배치

- 사용자 의도: 잔여 생일 미확인 중 소속사 공식 프로필이 현역 전체를 완결하는 그룹을 우선 보강한다.
- 범위: TSUIMA 공식 탤런트 인덱스의 BYBBiT 5인과 공개 데이터 5인을 대조하고, 개인 프로필의 월·일 생일·담당색 명칭·개인 공식 URL·확인일을 반영한다.
- 데이터·UI·명령 연결: 기존 `birthMonthDay`와 공식 색상명 근사 계약을 통해 생일 캘린더·멤버 상세·누락 집계·카드 색상에 연결한다. 새 UI·스키마·명령은 만들지 않는다.
- 안전 경계: 공식 프로필에 없는 출생연도·출신지·애칭을 추정하지 않고, 프로필 사진·SNS·혈액형·신장·취미·특기는 범위 밖으로 둔다. HEX는 공식 색상명이 아니라 UI 표시 근사값이다.
- 실패·빈 상태·회귀 위험: 공식 인덱스와 데이터의 5인 명단이 다르거나 개인 페이지 하나라도 생일이 없으면 부분 반영하지 않는다. 이미지 URL을 추출하거나 다른 BYBBiT 과거 멤버 정보를 섞지 않는다.
- 완료 조건: 현역 5명이 공식 월·일 생일·담당색 명칭·개인 URL 근거를 가지며 생일 미확인 현역이 49명에서 44명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 실제 결과를 `WORK_ORDER.md`와 현행 인수인계에 기록한다.
- 결과: 공식 5인 인덱스와 개인 프로필을 완결 대조해 월·일 생일·담당색 명칭만 반영했고 생일 미확인 현역이 44명으로 감소했다. 오프라인 실행·데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.
- 결과: 공식 5인 인덱스와 개인 프로필을 완결 대조해 월·일 생일·담당색 명칭만 반영했고 생일 미확인 현역이 44명으로 감소했다. 오프라인 실행·데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.

## 2026-08-30 이미지 운영 결정 — 소개 사이트의 최소 권리 사용

- 사용자 의도: Chika Idol Box를 공식 제휴·상품 판매 서비스가 아닌 출처 기반 소개·디렉터리로 유지하며, 광고 가능성과 무관하게 필요한 최소 범위만 사용한다.
- 기대 흐름: 그룹·멤버명과 사실 정보는 출처·확인일·공식 링크로 소개하고, 공식 로고 파일이나 사진을 복제하지 않아도 일반 텍스트·자체 워드마크·플레이스홀더로 탐색을 완결한다.
- 정책·안전 경계: 출처 표시는 이미지 재사용 허가를 대신하지 않는다. 공식 로고·사진은 명시적 허가나 재사용 라이선스가 생길 때만 반영하고, 공식성·제휴를 암시하지 않으며 광고는 콘텐츠와 구분한다.
- 이번 결정: FRUITS ZIPPER·CANDY TUNE 이미지 문의는 필수 MVP에서 제외하고 발송하지 않는다. 두 후보는 비노출 `rights_review` 기록으로만 보존한다.
- 변경하지 않는 범위: 이미지 후보 상태를 승인·거절로 임의 변경하지 않고, 공식 자산을 다운로드·복제·핫링크하지 않는다.
- 완료 조건·검증: 제품 계획·작업지시·인수인계의 다음 순서에서 외부 문의를 제거하고, 데이터 깊이 보강을 재개한다. 문서 정합성은 `git diff --check`로 확인한다.

## 2026-08-30 MVP F-16 — 宮代柚花 공식 프로필 단일 소배치

- 사용자 의도: 공식 근거가 완결되는 잔여 단일 멤버를 우선 처리해 데이터 깊이를 안전하게 높이고, 다음 승인 경계까지 진행한다.
- 범위: 手羽先センセーション 공식 개인 프로필에 명시된 宮代柚花의 월·일 생일 `11-19`, 출신지 오사카부, 개인 공식 URL과 확인일만 반영한다. 그룹 명단·다른 멤버·이미지·SNS·지표는 변경하지 않는다.
- 데이터·UI·명령 연결: 기존 `birthMonthDay` 계약을 통해 생일 캘린더·멤버 상세·누락 집계에 연결하고 기존 출신지 표시를 재사용한다. 새 UI·스키마·명령은 만들지 않는다.
- 안전 경계: 공식 프로필에 없는 출생연도·애칭과 텍스트로 확정되지 않은 담당색 HEX를 추정하지 않는다. 프로필 이미지와 SNS 링크도 이번 소배치에 포함하지 않는다.
- 실패·빈 상태·회귀 위험: 생탄제 개최일을 생일로 오인하거나 가짜 출생연도를 만들거나, 현재 6인 명단을 단일 멤버 레코드와 함께 덮어쓸 수 있다. 공식 개인 프로필의 `誕生日`만 생일 근거로 사용한다.
- 완료 조건: 宮代柚花가 월·일 생일·출신지와 개인 공식 URL 근거를 가지며 생일 미확인 현역이 50명에서 49명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 실제 결과를 `WORK_ORDER.md`와 현행 인수인계에 기록한다.
- 결과: 공식 월·일 생일과 출신지만 보강해 생일 미확인 현역이 49명으로 감소했으며 오프라인 실행·데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.
- 후속 조사: エイアイカ·yosugala 공식 프로필에는 생일이 없고 MyDearDarlin’ 공식 사이트는 세부 프로필 조회가 타임아웃되어 F-17 데이터 반영을 보류했다. SNS 핸들·생탄제·2차 기사로 생일을 추정하지 않는다.

## 2026-08-30 MVP F-15 — 与田理央那 공식 프로필 단일 소배치

- 사용자 의도: 잔여 생일 미확인 중 공식 근거가 완결되는 가장 작은 단위부터 데이터 깊이를 안전하게 높인다.
- 범위: 군마현 공식 행사 게스트 프로필에 명시된 与田理央那의 완전 생년월일, 출신지, 담당색 명칭과 확인일을 반영한다. 그룹 명단·다른 멤버·이미지·SNS·지표는 변경하지 않는다.
- 데이터·UI·명령 연결: 기존 `birthDate`, 출신지, 공식 색상명 근사 계약을 통해 생일 캘린더·멤버 상세·누락 집계에 연결한다. 새 UI나 스키마는 만들지 않는다.
- 안전 경계: 공식 행사 프로필에 없는 애칭·사진·개인 정보는 추정하지 않는다. 노랑 HEX는 표시 근사값이며 `memberColorBasis=official_name_approximation`으로 구분한다.
- 실패·회귀 위험: 행사 개최일을 생일로 오인하거나, 비공식 소개를 공식 근거로 저장하거나, 기존 FES☆TIVE 9인 명단을 함께 덮어쓸 수 있다.
- 완료 조건: 与田理央那가 완전 생년월일·출신지·담당색과 공식 공공기관 URL 근거를 가지며 생일 미확인 현역이 51명에서 50명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 실제 결과를 `WORK_ORDER.md`와 현행 인수인계에 기록한다.
- 결과: 계획 범위대로 1명만 보강해 생일 미확인 현역이 50명으로 감소했으며 오프라인 실행·데이터 검사·타입 검사·678/678 프로덕션 빌드가 통과했다.

## 2026-08-30 MVP F-14 — NEO BREAK 공식 생일 완결 소배치

- 사용자 의도: 다음 권장 데이터 깊이 실행 단위인 NEO BREAK 재확인을 마치고, 공식 근거가 6명 전체에 연결될 때만 알려진 월·일 생일을 공개 데이터에 반영한다.
- 범위: 공식 멤버 인덱스의 현재 6인과 공개 데이터 6인을 대조하고 `birthMonthDay`, 개인 공식 프로필 URL, 확인일을 갱신한다. 개인 페이지가 반복 타임아웃인 西叶華는 공식 일정의 생탄제 날짜를 교차 근거로 사용한다.
- 데이터·UI·명령 연결: 기존 월·일 생일 계약, 홈 생일 캘린더, 멤버 상세, 일일 30일 보고와 누락 집계를 재사용한다. 새 스키마나 UI는 만들지 않는다.
- 안전 경계: 출생연도·나이·출신지·담당색·사진은 추정하거나 다른 비공식 프로필에서 가져오지 않는다. 개인 페이지가 열리지 않는 사실을 숨기지 않고, 공식 일정 외의 2차 출처는 공개 데이터 근거로 사용하지 않는다.
- 실패·빈 상태: 공식 인덱스와 데이터 명단이 다르거나 6명 중 한 명이라도 공식 월·일 근거가 없으면 부분 반영하지 않는다. 개인 페이지 타임아웃이어도 공식 그룹 일정이 같은 멤버의 생일을 명시하면 그 일정 URL을 해당 필드의 근거로 보존한다.
- 회귀 위험: 같은 그룹 안의 서로 다른 페이지 확인일·출처를 일괄 덮어쓰거나, 생탄제 행사일을 출생연도로 오해하거나, 비공식 검색 결과를 공식 근거처럼 저장할 수 있다.
- 완료 조건: 6명 모두 공식 월·일 생일과 공식 URL 근거를 가지며 생일 미확인 현역 수가 57명에서 51명으로 감소한다.
- 검증 방법: 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, 한국어 멤버 상세 HTTP 확인, `git diff --check`를 실행하고 실제 결과를 `WORK_ORDER.md`에 기록한다.

## 2026-08-30 문서 정합성 감사 — MVP A~H 기준선

- 사용자 의도: 제품의 큰 줄기와 데이터·UI·명령·운영 가지를 바꾸지 않고, 현행 MVP A~H 분류와 충돌하는 오래된 현재 상태 표기만 바로잡는다.
- 범위: 이 문서의 MVP E 현재 실행 표기와 MVP H 누락, 지도 기술·적대적 검토 문서의 역사 자료 경계를 정리한다. 코드·데이터·UI·명령·운영 동작은 변경하지 않는다.
- 안전 경계: 과거 의사결정과 검증 증거는 삭제하거나 현재 사실로 다시 쓰지 않는다. `CODEX_HANDOVER.md`의 현행 기준, 승인된 홈 계층 탐색, 그룹 라이브 지도, 후보/공개 분리, 권리·배포 경계를 보존한다.
- 회귀 위험: 역사 기록에 현재 지시처럼 보이는 문구를 남기거나, 반대로 역사적 맥락을 지워 지도·운영 결정의 근거를 잃을 수 있다.
- 완료 조건: 권장 MVP 목록이 A~H를 모두 포함하고 E를 완료로 표시하며, 현재 실행 단위가 이번 문서 감사로 식별되고, 지도·적대적 검토 문서가 현행 요구사항의 출처가 아님을 명시한다.
- 검증 방법: 대상 문구 검색, 변경 문서 diff 검토, `git diff --check`를 실행하고 실제 결과를 `WORK_ORDER.md`에 기록한다. 문서 전용 변경이므로 데이터·타입·빌드는 실행하지 않으며 미실행으로 기록한다.

## 2026-08-30 통합 제품 기준선 — 현재 MVP·줄기·가지

이 절이 현재 구현과 다음 작업의 우선 기준이다. 아래의 2026-08-29 이전 지도 로드맵과 배치별 수치는 의사결정 이력이며, 이 절과 충돌할 때 현재 기준으로 사용하지 않는다.

### 제품의 큰 줄기

- Chika Idol Box는 공식 근거가 있는 일본 라이브아이돌을 `일본 → 도시 → 활동 지역 → 그룹 → 멤버·라이브 → 공식 원문·티켓·공연장` 순서로 발견하는 다국어 디렉터리다.
- 홈의 지도는 도로지도가 아니라 일본 열도의 실제 형상을 기반으로 한 단계형 탐색 UI다. 도시를 고른 뒤 아키하바라·시부야 같은 활동 지역과 대표 비주얼 그룹 카드를 보여준다.
- 실제 도로·공연장 지도는 그룹을 선택한 뒤 `라이브 · 공연장 지도` 탭에서만 사용한다. 홈을 범용 실지도로 되돌리지 않는다.
- 후보와 공개 데이터는 분리한다. 공식 출처·확인일·권리 근거 없는 공연, 생일, 사진, 로고, 그라비아는 자동 게시하지 않는다.
- 이미지 우선순위는 `허가된 공식 사진 → 허가된 공식 로고 → 자체 텍스트 워드마크 → 이름 기반 대체 비주얼`이다. 사용 금지 또는 허가 불명 자산은 공개 임베드하지 않는다.

### 전체 가지와 현재 진행 상태

| 가지 | 현재 상태 | 다음 유지·보완 조건 |
|---|---|---|
| 신뢰·날짜 | 완료 기준선 | 모든 미래 공연·생일·감사 명령은 `Asia/Tokyo`를 공유한다. |
| 출처·후보 분리 | 완료 기준선 | 수집기는 후보만 쓰고 사람이 승인하기 전 공개 데이터로 승격하지 않는다. |
| 홈 지역 탐색 | 사용자 승인 완료 | 일본 SVG → 도시 → 활동 지역 → 그룹 카드 흐름과 URL 복원을 회귀 보호한다. |
| 그룹 상세 | 완료 기준선 | 프로필·멤버가 기본이며 라이브 탭에서만 선택 그룹의 실제 공연장 지도를 지연 로드한다. |
| 라이브·공연장 | Gate 2 통과, 상시 운영 | 일정 경과·취소·변경에 대비해 일일 후보와 검증을 유지한다. |
| 그룹·멤버 | 37그룹·188멤버 계약 완료, 내용 보강 지속 | 활동 상태와 공식 프로필 근거를 보존하며 미확인 생일을 추정하지 않는다. |
| 그룹·멤버 이미지 | MVP 2-B 완료 | 공식 0, 텍스트 워드마크 11, 그룹 대체 26, 멤버 대체 188. 권리 승인 전 원격 이미지 사용 금지. |
| 이미지 후보·권리 | 큐 운영 시작 | 후보 3건 중 권리 검토 2, 승인 0, 거절 1. 외부 문의는 사용자 승인 없이 발송하지 않는다. |
| 그라비아 | 안전한 link-only 기준선 | 공식 원문을 연결하고 권리 확인 전 표지·갤러리를 임베드하지 않는다. |
| 뉴스·공지 | 제한적 공식 공지 | 루머·사생활·미성년자 침해를 배제하고 확장 전 별도 편집 정책을 확정한다. |
| 자동화·운영 | 수동 일일 실행기 복구·검증 완료 | UTF-8 PowerShell 진입점과 ASCII·CRLF 바탕화면 래퍼로 후보 수집·생일·검증·보고·실행 로그를 수행하며 자동 공개하지 않는다. |
| 접근성·실패 상태 | 구현 기준선 | 지도/WebGL/좌표/일정 없음에도 목록과 공식 링크를 유지한다. |
| 배포·브라우저 | 로컬 검증 완료, 배포 미승인 | 사용자 실화면 승인 완료. 커밋·배포는 별도 요청 전 수행하지 않는다. |

### 권장 MVP 순서

1. **MVP A — 신뢰 기반**: 날짜, 스키마, 출처, 후보/공개 분리, 검증 명령. 완료.
2. **MVP B — 핵심 발견 수직 흐름**: 일본 → 도시 → 활동 지역 → 그룹 → 상세. 완료 및 사용자 승인.
3. **MVP C — 그룹별 라이브**: 선택 그룹 일정·검증 공연장·실지도·외부 길찾기. 완료.
4. **MVP D — 안전한 비주얼**: 이미지 계약, 워드마크/대체 상태, 권리 후보 큐. MVP 2-B까지 완료; 승인 공식 자산은 0건.
5. **MVP E — 일일 운영 루프**: 공연 후보·생일·검증 보고에 이미지 권리 검토 큐를 함께 연결한다. 완료.
6. **MVP F — 데이터 깊이**: 미확인 생일·멤버 프로필·공연 후보를 공식 원문 소배치로 보강하고 권리 근거가 생긴 이미지만 반영한다.
7. **MVP G — 확장 가지**: 그라비아 밀도, 공식 공지, 검색·교차 탐색을 각각의 정책·품질 게이트 후 확장한다.
8. **MVP H — 인수인계·문서 정합성**: 현행 기준과 역사 기록을 분리하고 제품·MVP·검증·운영 상태를 한 기준선으로 유지한다. H-1~4 완료; 정합성은 상시 유지한다.

### MVP F-1 — 연도 비공개 공식 생일 계약

- 첫 데이터 깊이 소배치는 공식 개인 프로필이 연결된 selfish 현역 멤버 6명이다.
- 공식 페이지는 생일의 월·일만 명시하므로 출생연도를 추정하거나 가짜 연도를 저장하지 않는다. `birthDate(YYYY-MM-DD)`와 별도로 `birthMonthDay(MM-DD)`를 두고 알려진 정밀도를 그대로 보존한다.
- 홈 생일 캘린더와 일일 30일 보고는 두 정밀도를 모두 사용할 수 있지만 나이 계산은 완전한 `birthDate`에서만 가능하다.
- 이미지가 공식 페이지에 존재해도 이번 배치는 텍스트 프로필 데이터만 다루며 사진 URL을 추출·복제·핫링크하지 않는다.

### MVP F-2 — I’mew 공식 프로필 완결 소배치

- I’mew 공식 프로필의 현재 6인 명단을 공개 데이터와 대조하고, 연도 비공개 생일 4건과 공식 애칭 6건을 함께 보강한다.
- 완전 생년월일이 이미 공식 확인된 2명은 기존 `birthDate`를 유지하며, 나머지는 `birthMonthDay`만 저장한다.
- 애칭의 일본어 원문을 기준으로 한국어·영문에는 읽기용 음역을 제공한다. 본명, 활동 상태, 사진 권리는 이 배치에서 추정하거나 변경하지 않는다.

### MVP F-3 — 可憐なアイボリー 11인 공식 프로필 완결

- 공식 인덱스의 11인 명단과 각 개인 공식 프로필을 대조해 완전 생년월일, 애칭, 출신지를 한 그룹 단위로 완결한다.
- 개인 페이지의 `나이`는 정적 갱신 지연 가능성이 있어 저장하지 않고 안정적인 생년월일만 사용한다.
- 담당색은 공식 프로필에 명시되지 않으므로 미확인을 유지하며 이미지 역시 권리 승인 전 사용하지 않는다.

### MVP F-4 — AsIs 7인 월·일 생일·담당색 완결

- 공식 7인 인덱스와 개인 프로필에서 연도 비공개 생일, 출신지, 담당색 명칭을 완결한다.
- 공식 페이지는 색상명만 제공하고 HEX를 제공하지 않으므로 UI HEX는 접근 가능한 표시 근사값이며 `memberColorBasis=official_name_approximation`으로 구분한다.
- 출생연도·나이·사진은 추정하거나 사용하지 않는다.

### MVP F-5 — UtaGe! 7인 생일 정합성 완결

- 공식 단일 프로필 페이지의 7인 명단·출신지·담당색을 기존 데이터와 대조하고 누락된 월·일 생일 7건을 보강한다.
- 기존 화면 HEX는 공식 색상명 기반 표시 근사값으로 분류하며 공식 HEX라고 주장하지 않는다.
- 공식 페이지에 없는 애칭·출생연도·나이는 null로 유지하고 이미지는 사용하지 않는다.

### MVP F-6 — FRUiTY 9인 생일·담당 과일 의미 교정

- 공식 9인 프로필의 월·일 생일과 담당 과일을 완결한다.
- 기존 데이터는 담당 과일을 `memberColorName`에 저장해 색상처럼 표시하는 의미 오류가 있다. 이를 `memberMotif`로 이동하고 담당색은 미확인으로 되돌린다.
- 멤버 상세와 카드에는 담당 과일을 별도 정체성 정보로 표시하며 임의 색상 HEX를 만들지 않는다.

### MVP F-7 — HelloYouth 9인 공식 프로필 완결

- 공식 멤버 인덱스와 개인 프로필 9개를 대조해 완전 생년월일, 출신지, 멤버 컬러 명칭과 개인 공식 출처를 한 그룹 단위로 완결한다.
- 공식 페이지는 색상명만 제공하므로 UI HEX는 접근 가능한 표시 근사값이며 `memberColorBasis=official_name_approximation`으로 구분한다.
- 취미·특기·신장·사진은 핵심 발견 흐름과 권리 범위 밖이므로 저장하지 않으며, 특히 미성년 멤버의 공개 프로필 밖 정보를 추정하지 않는다.

### MVP F-8 — JamsCollection 8인 공식 프로필 완결

- 공식 8인 인덱스와 개인 프로필을 대조해 연도 비공개 생일, 애칭, 출신지, 담당색 명칭과 개인 공식 출처를 완결한다.
- 공식 색상명만 제공되므로 UI HEX는 표시 근사값으로 분리하고, 출생연도·나이·사진은 추정하거나 사용하지 않는다.
- 기존 월·일 생일·색상 근거 계약을 재사용하며 새 UI나 스키마를 만들지 않는다.

### MVP F-9 — MAGICAL SPEC 8인 공식 프로필 완결

- 공식 8인 인덱스와 개인 프로필에서 월·일 생일, 출신지, 담당색 명칭과 개인 출처를 완결한다.
- 기존 담당색 값은 공식 원문과 대조하고 `official_name_approximation` 근거를 붙인다. 공식 프로필에 애칭은 없으므로 null을 유지한다.
- 출생연도·나이·사진은 추정·사용하지 않으며 기존 생일·색상 UI 계약을 재사용한다.

### MVP F-10 — NANIMONO 7인 생일·담당색 정합성 완결

- 공식 단일 프로필의 현재 7인 명단, 월·일 생일과 담당색을 공개 데이터와 대조한다.
- 기존 담당색 명칭과 UI HEX는 공식 색상명 기반 표시 근사값으로 분류한다. 공식 페이지에 없는 출신지·애칭·연도는 null을 유지한다.
- 공식 페이지의 사진은 재사용 권리 확인 전 사용하지 않으며 기존 생일·색상 UI 계약을 재사용한다.

### MVP F-11 — Palette Parade 7인 공식 생일 소배치

- 공식 단일 프로필의 현재 7인 명단과 월·일 생일을 공개 데이터와 대조한다.
- 공식 페이지에 담당색·출신지·애칭·출생연도가 없으므로 미확인을 유지하며 생일만 알려진 정밀도로 저장한다.
- 사진은 재사용 권리 확인 전 사용하지 않고 기존 월·일 생일 UI·운영 계약을 재사용한다.

### MVP F-12 — AVAM 7인 공식 프로필 완결

- 공식 7인 인덱스와 개인 프로필에서 월·일 생일, 애칭, 멤버 컬러 명칭과 개인 출처를 완결한다.
- 공식 페이지에 출신지·출생연도는 없으므로 null을 유지하고, UI HEX는 공식 색상명 기반 표시 근사값으로 구분한다.
- 프로필 사진은 권리 확인 전 사용하지 않으며 기존 생일·색상·애칭 UI 계약을 재사용한다.

### MVP F-13 — Ringwanderung 5인 공식 프로필 완결

- 공식 5인 인덱스와 개인 프로필에서 월·일 생일, 출신지와 개인 공식 출처를 완결한다.
- 공식 페이지에 담당색·애칭·출생연도는 없으므로 미확인을 유지하고 키·취미·특기는 핵심 계약 밖이라 저장하지 않는다.
- 사진은 재사용 권리 확인 전 사용하지 않으며 NEO BREAK의 타임아웃 개인 페이지 2건은 별도 재확인 대상으로 남긴다.

### MVP H-1 — 현행 인수인계 단일 기준선 완전 갱신

- `CODEX_HANDOVER.md`의 역사 실행 로그와 현행 운영 기준이 섞여 생긴 수치·우선순위·브라우저 상태 모순을 제거한다.
- 제품 줄기, MVP A~H 진행 상태, 데이터·이미지·지도·운영 계약, 최신 검증 수치, 미완료·차단 항목, 다음 권장 순서를 한 문서의 단일 현재 기준선으로 재구성한다.
- 폐기된 `HANDOVER.md`는 역사 자료로 보존하고 현행 문서 링크만 유지한다. 과거 의사결정 상세는 제품 계획과 작업지시서에서 추적한다.

### 이번 실행 단위

- MVP A~H 분류, 완료된 MVP E, H-1 이후의 현행 문서 기준을 서로 일치시킨다.
- 과거 지도 로드맵·기술 스파이크·적대적 검토는 근거 기록으로 보존하되 현재 홈·그룹 라이브 지도 요구사항으로 오인되지 않게 문서 지위를 명시한다.
- 완료 조건은 대상 문서의 현재/역사 경계가 명확하고, 문서 diff 검토와 `git diff --check`가 통과하는 것이다.

## 2026-08-29 감사 후 MVP 수정·보완 로드맵

### 제품의 큰 줄기

- 제품 목적은 공식 근거가 있는 라이브아이돌·멤버·공연·그라비아를 지역·날짜·인물 기준으로 탐색하고 공식 원문·티켓·판매처로 안전하게 연결하는 것이다.
- 대상 사용자는 일본 표준시 기준으로 예정 공연을 판단하는 일본어·한국어·영어 사용자다. 같은 순간에 화면, 그룹 상세, 지도 집계, 운영 감사가 서로 다른 날짜를 사용해서는 안 된다.
- 후보 데이터는 공개 데이터와 분리하고, 공식 출처·확인일·권리 상태가 없는 사실은 공개하지 않는다. 그라비아 `link_only` 이미지 비노출, 미성년자 안전, Gate 2 미달 UI 비노출은 계속 유지한다.
- 지도는 데이터 밀도와 운영 계약을 함께 충족해야 한다. 기술 스파이크용 외부 타일을 공급자 결정 전 프로덕션 기본 지도처럼 사용하지 않는다.

### 전체 기능·데이터·UI·명령·운영 가지

1. 날짜 가지: `Asia/Tokyo` 달력 날짜를 공통 계약으로 만들고 공연 목록·그룹 상세·지도 집계·Gate 보고가 같은 기준을 사용한다.
2. 지도 가지: 전국 State A의 서버 렌더링 SVG·목록을 안전한 기본값으로 유지하고, MapLibre/GSI는 명시적 개발 스파이크 플래그에서만 활성화한다. WebGL·모듈·타일 실패 시 목록과 지역 필터가 계속 동작한다.
3. Gate 가지: 도쿄 23구는 그룹·특별구·공연장·미래 공연 네 조건이 모두 충족될 때까지 공개하지 않는다. 날짜 경과로 변하는 수치는 기준일과 함께 명령에서 산출한다.
4. 라이브 가지: 예정·과거, 지역, 그룹, 공연장, 티켓, 미정 시간·장소·가격과 빈 결과를 같은 공개 공연 계약에서 처리한다.
5. 그라비아 가지: 공개/후보 분리와 `link_only` 루트·갤러리 이미지 비노출을 회귀 검증하며 지도나 라이브 데이터에서 권리를 상속하지 않는다.
6. 그룹·멤버 가지: 종료·former·hiatus 상태와 검증 수록 범위를 보존하고 날짜 수정이 멤버·랭킹·생일 데이터에 영향을 주지 않게 한다.
7. 출처·후보 가지: 수집기 dry-run/write 분리, 실패 시 기존 후보 보존, 공개 UI 비참조를 유지한다.
8. 문서·운영 가지: 현재 기준선과 역사 실행 수치를 분리하고 빌드 페이지 수·미래 공연 수처럼 변하는 값에는 실행일과 명령을 붙인다. 미커밋 배포와 Git HEAD를 계속 구분한다.

### MVP 실행 순서

- MVP 1 — 신뢰 시간축: 일본 날짜 공통 유틸, UI·운영 명령 연결, 날짜 경계 회귀 검사.
- MVP 2 — 안전한 전국 지도: SVG/목록을 프로덕션 기본값으로 복구하고 명시적 개발 플래그에서만 MapLibre/GSI 스파이크를 허용한다.
- MVP 3 — 현행 기준선 복구: 인수인계의 최신 빌드·공연 수·지도 상태를 실제 검증 결과와 맞추고 동적 수치를 고정 사실처럼 쓰지 않는다.
- MVP 4 — Gate 2 데이터 밀도: 공식 좌표 공연장 4곳과 연결 미래 공연 13건의 간격을 공식 원문으로 채운다. UI 출시는 별도 완료 게이트다.
- MVP 5 — 운영 완결: 실제 네트워크 지도 공급자·권리·SLA 결정, 브라우저·모바일·접근성 검증, 재현 가능한 커밋/배포 기준선을 만든다.

### 이번 실행 경계와 완료 조건

- 이번 실행은 MVP 1~3만 구현한다. 새 외부 데이터 수집, Gate 2 수량 확대, 공급자 계약, 배포는 하지 않는다.
- 완료 조건은 날짜 기준 단일화, 기본 프로덕션에서 외부 타일 요청 제거, 명시적 플래그에서만 스파이크 활성화, 현재 문서 기준선 동기화, 데이터 검사·날짜 회귀 검사·타입 검사·프로덕션 빌드·diff 검사 통과다.

### 2026-08-29 MVP 4 Gate 2 밀도 소배치 착수

- 현재 Gate 2는 도쿄 그룹 26/20·특별구 5/5를 통과했고 지도 준비 공연장 11/15·연결 미래 공연 17/30이 부족하다.
- 이번 줄기는 새로운 구를 넓히는 작업이 아니라 `공개 그룹 공식 일정 → 반복 도쿄 공연장 → 공연장 공식 주소·좌표 → GeoArea → 미래 공연 → 공식 티켓/원문`을 완결하는 밀도 작업이다.
- 기존 공연 후보의 도쿄 위치 검토 대기는 0건이므로, 공개 그룹 공식 일정에서 새 후보를 조사한다. 검색 결과·팬 캘린더·SNS 재전재만으로 공개하지 않는다.
- 첫 목표는 신규 공식 좌표 공연장 최대 4곳과 그 공연장 미래 공연 최대 13건이다. 한 번에 공식 근거가 완결되지 않으면 검증된 수량만 작은 배치로 반영하고 Gate를 미달로 유지한다.
- 공연장 좌표는 공연장 공식 페이지가 제공하는 Google Maps 링크·임베드 또는 공식 좌표 표기만 허용한다. 주소만 확인되면 공연장 연결은 가능하지만 `map-ready` 집계에는 포함하지 않는다.
- 공연의 날짜·시간·가격·티켓은 그룹·주최자 공식 원문에 명시된 값만 저장한다. 여러 출연 그룹 중 공개 그룹만 연결하며 누락 그룹을 임의 생성하지 않는다.
- 실패·빈 상태: 공식 일정 접근 실패, 미래 일정 없음, 공연장 명칭 불일치, 좌표 미확정은 정상 보류 사유다. 기존 공개·후보 데이터를 삭제하거나 수량 목표를 위해 추정하지 않는다.
- 완료 조건: 중복 없는 공연장·공연·그룹 공간 근거가 같은 공식 원문 체계로 연결되고, 데이터 검사·Gate 2 감사·날짜 계약·타입 검사·빌드·diff 검사가 통과한다.

### 2026-08-29 MVP 4 Gate 2 밀도 소배치 완료

- AsIs 공식 일정 13건을 Spotify O-Crest, Spotify O-EAST·O-WEST, SHIBUYA PLEASURE PLEASURE, Shinagawa Stellar Ball, SELENE b2에 연결했다. 신규 지도 준비 공연장은 3곳이며, 기존 주소 전용 SELENE b2에 공식 지도 좌표를 보강해 지도 준비 공연장 증가는 합계 4곳이다.
- 공연장 주소·좌표는 각 공연장 공식 접근 페이지가 제공하는 지도 링크·임베드만 사용했고, 공연 날짜·시간·티켓은 AsIs 공식 개별 일정에 명시된 값만 반영했다. 공식 시간·티켓이 없는 필드는 추정하지 않고 기존 TBA/null 계약을 유지했다.
- Gate 2는 도쿄 활동 그룹 26/20, 활동 근거 특별구 5/5, 지도 준비 공연장 15/15, 해당 공연장 연결 미래 공연 31/30으로 통과했다. 13건 신규 공연 외에 기존 SELENE b2 공연 1건도 좌표 보강으로 지도 준비 집계에 새로 포함됐다.
- Gate 통과는 데이터 준비도 판정이며 도쿄 23구 UI의 자동 공개 승인이 아니다. 다음 MVP는 공간 드릴다운 정보 구조·빈 상태·접근성·공급자 운영 경계를 별도 기획·작업지시로 확정한 뒤 구현한다.

### 2026-08-29 MVP 5-A 수동 일일 업데이트 운영기

- 사용자 의도는 매일 한 번 직접 실행해 공식 일정 후보와 생일 현황을 최신화하고, 자동 수집이 불가능한 항목도 누락 목록으로 남기는 것이다.
- 큰 줄기는 `공식 공급원 → 격리 후보 갱신 → 상세 보강 → 생일/누락 보고 → 데이터 검증 → 운영 보고서`다. 후보를 공개 공연으로 자동 승격하거나 공식 근거 없는 생일을 추정하지 않는다.
- 공연 가지는 기존 タイトル未定 ICS, FRUITS ZIPPER 공식 일정 HTML, FRUITS ZIPPER 상세 보강기를 재사용한다. 각 공급원 실패는 개별 실패로 기록하고 다음 단계를 계속한다.
- 생일 가지는 현재 공개·현역·검증 멤버의 `birthDate`만 사용해 오늘과 향후 30일을 계산한다. 생일이 null인 멤버는 자동 추론하지 않고 공식 프로필 보강 대기 목록으로 보고한다.
- 운영·안전 경계: 네트워크 결과는 후보 파일만 갱신하고, 실행 로그에 비밀정보·응답 본문을 저장하지 않으며, 배포·커밋·Windows 예약 작업 등록은 자동 수행하지 않는다.
- 바탕화면 실행기는 저장소의 고정 일일 명령을 호출하는 얇은 진입점이다. 저장소 이동·Node 부재·일부 수집 실패를 사람이 읽을 수 있게 표시하고 종료 전에 결과 위치를 안내한다.
- 완료 조건: dry-run과 write 모드, 공급원 부분 실패, 생일/누락 보고, 후보 격리, 데이터 검사, 타입 검사, 프로덕션 빌드, diff 검사가 확인되고 실제 결과를 작업지시서에 기록한다.
- 첫 write 검증에서 미래 일정만 다시 수집할 때 날짜가 지난 `published` 후보가 교체 대상에서 빠져 공개 공연의 감사 연결이 끊기는 회귀를 확인했다. 새 운영 정책은 공급원에서 사라지거나 기준일이 지난 후보라도 `published`·`rejected` 이력은 영구 보존하고, 재발견되지 않은 `review_pending`만 향후 별도 보존 기간 정책 대상으로 두는 것이다.

### 2026-08-29 Core M1 및 지도 경험 재감사

- 전체 정합성 감사에서 그룹 상세·Gate 2만 `Asia/Tokyo` 날짜를 사용하고 홈·라이브 목록·두 일정 수집기는 UTC 날짜를 사용하는 모순을 확인했다. 생일 위젯도 브라우저 로컬 월, 한국어 단위 고정, 12월 `13월` 표기 위험이 있다.
- Core M1은 홈·라이브·수집기의 기준일을 공통 일본 날짜 계약으로 통일하고, 생일 기준 월을 서버에서 주입하며 ja/ko/en 월·일 표현과 연말 순환을 고친다.
- 지도 재감사 결론: 현재 기본 지도는 근사 SVG 6조각·도시 5핀·하단 그룹 카드이고, 승인된 목표는 실제 일본 지형·도도부현/23구 경계·공연 밀도·공연장 핀·병렬 결과 패널·공유 URL·모바일 bottom sheet다. 현재 구현은 안전한 폴백일 뿐 사용자가 원하는 주 탐색 지도로 간주하지 않는다.
- 기존 SVG의 색·핀·장식을 개선해 완성 지도처럼 보이게 하는 작업은 하지 않는다. SVG는 네트워크/WebGL 실패 시 접근 가능한 전국 목록 보조로만 유지한다.
- 지도 다음 MVP는 `Map M1 정보 구조와 공간 데이터 → Map M2 실제 경계·카메라·URL 프로토타입 → Map M3 공연장·공연·결과 패널 → Map M4 모바일·접근성·공급자 승인` 순서로 분리한다.
- Map M1 착수 전 결정할 항목은 기본 첫 화면(전국/도쿄), 우선 시각 레이어(공연/그룹), 도쿄 23구 전체 경계와 근거 5개 구의 데이터 부족 표현, 타일 공급자 또는 자체 베이스맵 정책이다.
- 이번 실행은 Core M1을 구현하고 지도는 감사·재설계 작업지시까지만 확정한다. 실제 지도 UI를 근거·공급자·브라우저 검증 없이 성급히 교체하지 않는다.

### 2026-08-29 Map M1/M2 실제 지도 MVP 착수

- 국토지리원 공식 타일 안내는 실시간 웹 로드를 출처 표시 조건으로 신청 없이 사용할 수 있다고 명시한다. MVP 공급자는 GSI 담색 지도로 한정하고 `地理院タイル` attribution과 공식 목록·이용규약 링크를 항상 표시한다.
- 이는 영구 SLA 승인이 아니다. 제3자 권리·기본측량성과별 조건, 서비스 무보장, 사용자 IP의 외부 요청을 운영 위험으로 명시하며 `NEXT_PUBLIC_MAP_PROVIDER=fallback`으로 네트워크 지도를 즉시 비활성화할 수 있게 한다.
- Map M1/M2 최소 결과는 기본 실지도, 전국 그룹/공연 밀도 레이어 전환, 선택 지역 카메라 이동, 지도와 결과 패널의 데스크톱 병렬 구조, 모바일 세로 구조, URL의 `mapRegion`·`mapLayer` 상태다.
- 좌표·공식 출처가 있는 공연장만 핀으로 표시한다. 23구 경계는 N03 또는 동등 공식 경계의 라이선스·용량·단순화가 검증되기 전 추정 도형을 만들지 않는다.
- 지도 실패 시 SVG를 다시 주 지도처럼 노출하지 않고, 동일 지역 칩·수치·그룹/공연 링크를 제공하는 명시적 목록 폴백으로 사용한다.

### 2026-08-29 Map M4 실사용 검증 우선 실행

- 다음 실행은 23구 경계를 바로 추가하기 전에 현재 GSI 실지도 수직 조각을 데스크톱·모바일에서 실제로 열어 타일, 카메라, 핀, 결과 패널, URL 복원, 폴백을 검증한다.
- 사용자 피드백의 핵심은 지도의 존재가 아니라 원하는 공간 탐색 느낌이다. 실제 화면에서 지도 비율·정보 밀도·패널 위계·모바일 조작이 부족하면 결함을 먼저 작업지시서에 기록하고 수정한다.
- 23구 경계는 공식 데이터의 권리·정확도·용량 계약을 검증한 뒤 별도 실행 단위로 추가한다. 브라우저 검증 실패를 임의 경계나 장식으로 덮지 않는다.

### 2026-08-30 도쿄 검증 구 드릴다운

- 23개 구 전체 polygon 선택은 승인 가능한 면 데이터가 확보될 때까지 보류하되, 현재 `GeoArea`와 공연장에 공식 근거가 연결된 주오·고토·시부야·지요다·미나토 5개 구는 장소 중심 탐색의 다음 가지로 공개한다.
- 도쿄 → 검증 구 → 공연장 → 공연 흐름을 `mapWard` URL 상태와 결과 패널에 연결한다. 데이터가 없는 나머지 구를 빈 활성 구처럼 표시하거나 기존 활동권역 `district`와 행정구를 혼합하지 않는다.

### 2026-08-30 Map M4 접근성·저사양 완결

- 지도 캔버스 조작에 의존하지 않고 지역·구·레이어·공연장 결과를 표준 버튼과 링크로 모두 사용할 수 있게 한다.
- 선택 상태는 색뿐 아니라 `aria-pressed`와 제목·수치로 전달하고, 모션 감소 환경에서는 지도 카메라와 카드 전환 애니메이션을 즉시 적용한다.
- 모바일 지도 조작 버튼은 최소 44px 터치 영역을 유지하고 키보드 포커스가 배경에 묻히지 않게 한다.
- 데이터 검사기는 공연장 `geoAreaId`가 존재하는지만 보지 않고 ward인지, 도쿄 공연장이 도쿄 ward 아래에 있는지까지 검증한다.

### 2026-08-30 사용자 실화면 반증에 따른 지도 구조 재설계

- 사용자 제공 데스크톱 캡처 4장을 기준으로 현재 구현이 목표를 충족하지 못한다고 판정한다. 전국 제목과 도쿄 카메라가 불일치하고, 실지도가 시간 경과 후 근사 SVG로 교체되며, 지도 아래 대형 그룹 카드가 주 탐색을 다시 디렉터리로 분리한다.
- 제품의 지도 줄기는 `전국 실지도 → 지역 선택 → 카메라 확대 → 선택 지역 아이돌이 지도 안에서 등장 → 공연장·공연 선택`이다. 지도와 그룹 목록을 위아래 독립 섹션으로 유지하지 않는다.
- 전국 상태에서는 아이돌 전체 36개를 즉시 펼치지 않는다. 지역을 선택한 뒤에만 지도 하단 오버레이에 해당 그룹을 compact result로 표시한다.
- 외부 타일의 개별 실패나 느린 응답으로 근사 SVG가 지연 교체되어서는 안 된다. SVG는 명시적 provider fallback 또는 MapLibre/WebGL 초기화 자체 실패에서만 사용한다.

## 2026-08-27 UI 가지 정합성 보완 결정

- 공통 내비게이션은 홈/전국 지도, 검증 라이브, 그라비아 출판 탐색을 동등한 제품 가지로 노출하며 모바일에서 가지를 숨기지 않는다.
- 전국 지도 State A는 검증 그룹 수와 예정 공개 공연 수를 함께 보여 주고 지역 필터가 적용된 라이브 목록으로 연결한다.
- 도쿄 23구 드릴다운은 Gate 2가 `NOT READY`인 동안 노출하지 않는다.
- 그라비아 링크 디렉터리는 독립 핵심 가지지만 `link_only` 항목의 이미지를 복제·임베드하지 않는다.

## 현재 제품 기준선과 정합성 복구 원칙

- 큰 줄기: 공식 원문으로 검증된 일본 라이브아이돌·멤버·공연을 실제 지역 탐색과 연결하고, 불확실한 정보는 공개 사실처럼 보이지 않게 한다.
- 현재 위치: 로컬 공개 기준은 원본 37그룹·활동 36그룹·멤버 188명·공연 5건이며, 실제 전국 지도 Gate 1은 오사카 세 번째 검증 그룹을 확보해 통과했다.
- 이번 가지: 누적 실행 이력은 보존하면서 상위 설계도, 작업지시서, 지도 설계도, 최신 인수인계의 현재 수치와 다음 실행을 같은 기준선으로 맞춘다.
- 실행 단위: 문서의 현재 기준선·완료/미완료 경계·다음 실행을 정리하고 실제 데이터 검사와 빌드 결과로 확인한다. 코드·데이터·UI·배포는 변경하지 않는다.
- 신뢰·안전 경계: `すーぱーぷーばぁー!!`의 멤버 표기와 활동휴지 상태를 공식 원문으로 화해하기 전 공개 승격하거나 Gate 1 통과를 선언하지 않는다. 프로덕션 상태는 외부 재검증 없이 최신이라고 단정하지 않는다.
- 사용자 승인 경계: 지도 시각 디자인, 데스크톱/모바일 정보 밀도, MapLibre·타일 공급자 선택, 뉴스·경계 그룹 범위처럼 제품 경험이나 비용·권리 정책을 바꾸는 결정은 후보와 권고안을 먼저 모아 사용자 승인 후 구현한다.
- 완료 조건: 활성 문서의 현재 수치와 다음 실행이 일치하고, 데이터 검사·출처 보고·타입 검사·프로덕션 빌드·`git diff --check` 결과가 작업지시서에 기록되며, 승인 필요 안건이 별도 목록으로 정리된다.

## 2026-08-27 승인된 통합 제품 줄기

- 사용자는 지도·모바일·지도 기술 스파이크·단계 출시·핵심 그룹 범위·공식 공지 중심 뉴스 방향을 모두 승인했다.
- 제품의 큰 줄기는 `공식 근거가 있는 라이브아이돌과 그라비아 아이돌을 발견 → 신뢰 가능한 인물·그룹·출판/공연 엔터티로 연결 → 지역·날짜·인물 기준으로 탐색 → 공식 티켓·발매·원문으로 이동`이다.
- 라이브 가지는 `지역 → 활동권역/공연장 → 그룹·공연 → 티켓`을 따른다. 그라비아 가지는 `인물 → 공식 출판사/소속사 발매·기사 → 제호·발매일·판매처 → 권리 상태에 맞는 링크`를 따른다.
- 동일 인물이 두 가지 활동을 하더라도 신원 연결만 공유한다. 공연 지역을 그라비아 활동 근거로 추정하거나, 출판물 이미지를 그룹 프로필 이미지로 재사용하지 않는다.
- 그라비아는 부가 장식이 아니라 독립 핵심 탐색 가지다. 초기 기본 정책은 `link_only`이며 공식 임베드 허용 또는 권리 확인 전 이미지를 복제·캐시·재배포하지 않는다.
- 지도 UI는 정보 중심 구조에 팬 친화적 시각 언어를 적용하고, 모바일은 45~55vh 지도와 bottom sheet 및 항상 사용 가능한 목록 대체 UI를 제공한다.
- MapLibre 기술 스파이크는 공급자 계약 없이 진행한다. 전국 Gate 1 MVP를 먼저 출시하고 도쿄 23구는 Gate 2 충족 후 확장한다.
- 공개 그룹은 라이브·지하아이돌 핵심 범위를 유지한다. 메이저/성우 경계 그룹은 별도 범주 설계 전 공개하지 않는다.
- 뉴스 1차 범위는 공식 공지 링크 피드다. 외부 기사·AI 요약은 후속 승인 대상으로 유지하고 가십·루머·사생활 콘텐츠는 금지한다.

## 2026-08-25 공연 탐색 MVP 전체 설계

공연 탐색의 현행 제품 역할과 공개·후보 분리 원칙은 아래 전국 확장 설계와 `SOURCE_RESEARCH_AND_MVP.md`를 따르며, 구현 완료 상태와 다음 보강 순서는 `CODEX_HANDOVER.md`의 공연 탐색 절을 현재 기준으로 사용한다.

## 2026-08-25 전국 아이돌·그라비아 확장 재설계

상세 설계와 조사 근거는 `SOURCE_RESEARCH_AND_MVP.md`를 단일 확장 설계도로 사용한다.

### 재정의한 큰 줄기

- 제품은 `전국 아이돌 백과`를 먼저 주장하지 않는다. `공식 근거가 확인된 그룹·멤버·공연·그라비아를 탐색하고 공식 원문으로 연결하는 검증형 디렉터리`다.
- navi-idol 같은 제3자 디렉터리는 누락 대상을 발견하는 인덱스이지 사실의 최종 근거가 아니다.
- 확장 흐름은 `외부 목록 발견 → 출처 후보 카탈로그 → 공식 홈페이지·사무소·SNS 대조 → 엔터티 후보 검토 → 공개 데이터 승격 → 주기적 변경 확인`으로 통일한다.
- 지하·라이브아이돌과 그라비아 활동은 동일 인물이 겹칠 수 있지만, 그룹 소속·개인 신원·출판물 권리·공연 일정은 별도 가지로 검증한다.

### MVP 가지와 순서

1. P0 신뢰 기반: 발견 출처 레지스트리, 후보 상태, 중복·활동 종료·이미지 권리·접근 실패 기록.
2. P1 그룹 확장: 그룹 후보 → 공식 사이트/사무소 확인 → 최소 공개 그룹 → 공식 현역 명단 보강.
3. P1 공연 확장: 공개 그룹의 공식 일정 어댑터 → 후보 검토 → 공연장·지역·티켓 정규화 → 공개.
4. P1 그라비아 확장: 출판사·공식 판매처 발매 후보 → 인물 연결 → 권리 상태 확인 → 링크 중심 공개.
5. P2 탐색: 지역·날짜·그룹·멤버·그라비아 교차 탐색과 빈 상태의 공식 원문 이동.
6. P2 운영: 공급원 상태 이력, 변경 diff, 재검증 기한, 검토 큐, 중복 병합 기록.
7. P3 개인화·지표: 데이터 품질 게이트 통과 후 즐겨찾기·알림·독립 지표 스냅숏을 도입한다.

### 이번 실행 경계

- 조사 출처와 발견된 엔터티 씨앗을 비공개 카탈로그로 저장하고 검증·보고 명령을 만든다.
- navi-idol의 텍스트·사진을 복제하거나 검색 색인 일부를 전체 목록으로 표방하지 않는다.
- 기존 공개 그룹·멤버·공연·그라비아 데이터와 UI는 자동 변경하지 않는다.

## 2026-08-26 실제 지도 중심 탐색 재설계

상세 정보 구조, 단계별 화면 상태, 데이터 계약, 출시 게이트는 `MAP_DISCOVERY_MVP.md`를 따른다.

### 제품 차별점 재확정

- 핵심 차별점은 카드 목록 위의 장식 지도나 도시 필터가 아니라 `실제 일본 지도 → 실제 도도부현/도시 → 행정구역 → 활동 거점·공연장 → 그룹·공연 → 지도 길찾기·티켓`으로 이어지는 공간 탐색이다.
- 그룹의 이미지상 콘셉트 지역, 사무소 주소, 실제 활동 지역, 최근 공연 장소를 섞지 않는다. 지도 노출은 공식 활동 근거 또는 검증된 공연 데이터에서 파생한다.
- 도쿄의 渋谷区 같은 행정구와 原宿·秋葉原 같은 생활권/공연권역을 서로 다른 레벨로 모델링한다.

### 구현 시점

- 지금은 실제 지도 계약과 데이터 수집 기준을 고정한다.
- 실제 지도 UI 구현은 핵심 범위 공개 그룹과 공연장 좌표가 최소 밀도 게이트를 통과한 뒤 시작한다. 빈 지도에 과장된 핀을 배치하지 않는다.
- 지도 MVP 이전에도 그룹 후보 확장 작업마다 공식 활동 지역·일정 URL·공연장 근거를 함께 수집해 구현 준비도를 높인다.

### 뉴스 후속 상담 게이트

- 연예 뉴스형 피드는 공식 공지 집계, 외부 기사 링크, 자체 편집 기사 중 무엇을 제공할지에 따라 법적·운영 책임이 크게 달라진다.
- 뉴스 MVP 시작 전 사용자와 범위, 문체, 출처 허용 목록, 정정·삭제, 루머·사생활·미성년자 취급, AI 요약 표시, 이미지 권리를 별도 상담하고 결정문을 작성한다.
- 상담 전에는 기존 검증 공지를 확장할 수 있지만 연예부식 자체 기사·가십·루머 콘텐츠는 만들지 않는다.

### 다음 실행: 초기 그룹 후보 공식 근거 완성

- 초기 후보 11개를 그룹 공식 사이트, 소속사 아티스트 페이지, 공식 SNS 순서로 대조한다.
- 공식 URL이 확인되면 URL과 확인일을 기록하고 `official_source_found`로 올리되 현역 명단 검증 전 `ready_for_review` 또는 공개 데이터로 승격하지 않는다.
- 활동 종료·개명·재편은 공식 공지 없이는 추정하지 않는다. 현재 활동 근거와 과거 공식 페이지가 충돌하면 양쪽 URL을 보존하고 상태 판정을 보류한다.
- OCHA NORMA, i☆Ris처럼 지하·라이브아이돌 핵심 범위와 경계에 있는 그룹은 공식성 여부와 별개로 `범위 검토 필요`를 유지한다.

### 다음 실행: 핵심 후보 첫 공개 배치

- 핵심 범위 후보 중 현재 활동, 공식 현역 명단, 활동 지역, 공식 일정 경로가 모두 확인되는 그룹을 첫 공개 배치로 선별한다.
- 한 그룹을 추가할 때 그룹 카드만 만들지 않고 최소 현역 명단, 공식 프로필·일정, 공간 근거를 함께 연결한다.
- 명단 변동·현 체제 종료·공식 페이지 신선도가 불명확한 후보는 공개 수량을 위해 승격하지 않는다.
- 첫 배치 결과로 지도 Gate 1의 공개 그룹 수와 공간 근거 충족률을 다시 계산한다.

첫 배치는 2026-08-26 완료했다. I’mew（あいみゅう）, エイアイカ, 可憐なアイボリー와 공식 현역 멤버 20명을 승격했으며, Gate 1 진척은 활동 그룹 13/30·명시적 공간 근거 3/13(23%)이다. 다음 배치는 잔여 핵심 후보 5개 검증과 기존 활동 그룹 10개의 공간 근거 이관을 병행한다.

### 다음 실행: 핵심 후보 두 번째 공개 배치와 공간 근거 이관

- 잔여 핵심 후보 5개를 2026년 현재 공식 원문으로 다시 판정하고, 현역 명단·일정·활동지역 근거가 모두 충족된 그룹만 공개한다.
- 활동 종료·현 체제 종료·SNS 단독·명단 충돌 후보는 차단 사유를 카탈로그에 남기고 승격하지 않는다.
- 기존 활동 그룹은 저장된 지역 라벨을 근거로 재사용하지 않고 기존 공식 출처 또는 검증 공연 원문에서 증명되는 범위만 `areaEvidence`로 이관한다.
- 공개 그룹 확장과 공간 근거 이관은 같은 Gate 1 지표로 재계산하되, 그룹 수와 근거율 중 하나만 충족해도 지도 UI 구현을 앞당기지 않는다.

두 번째 배치는 2026-08-26 완료했다. `selfish`와 현역 6명을 공개하고 기존 4개 그룹의 공식 공간 근거를 이관했다. Gate 1은 활동 그룹 14/30·공간 근거 8/14(57%)이며, 잔여 핵심 후보 4개는 각각 명단 충돌·현재 명단 부족·현 체제 종료·프로필 미노출 사유로 보류했다.

### 다음 실행: 기존 활동 그룹 공간 근거 완결 배치

- 공간 근거가 없는 FRUITS ZIPPER, CANDY TUNE, iLiFE!, Appare!, 虹のコンキスタドール, FES☆TIVE 6개를 공식 콘셉트·공식 일정·주최/공연장 원문으로 대조한다.
- 브랜드 콘셉트 지역은 실제 거점과 구분하고, 최근 공식 공연은 `verified_event_history`, 공식 거점만 `official_base`로 기록한다.
- 한 번의 공연을 상시 거점처럼 표현하지 않으며, 근거가 약한 그룹은 누락 상태를 유지한다.
- Gate 1 공간 근거율을 재계산하되 그룹 밀도 30개 조건은 별도로 유지한다.

공간 근거 완결 배치는 2026-08-26 완료했다. 기존 누락 6개를 모두 공식 일정·공식 행사 원문과 연결해 활동 그룹 공간 근거율은 14/14(100%)가 됐다. 다만 공개 활동 그룹은 14/30이므로 실제 지도 UI는 아직 시작하지 않고, 다음 그룹 발견·공식 검증 배치에서 최소 16개를 추가 확보한다.

### 다음 실행: 지도 밀도용 신규 핵심 후보 16개 발굴

- 공식 라이브 행사 출연진과 공식 사무소 아티스트 목록에서 기존 공개·후보와 중복되지 않는 라이브아이돌을 발견한다.
- 각 후보는 발견 행사 URL, 그룹/사무소 공식 URL, 공식 일정 URL 후보, 핵심/경계 범위를 가진다.
- 발견 단계는 공개 승격과 분리한다. 현재 활동·전체 현역 명단·공간 근거를 모두 확인하기 전에는 공개 데이터가 읽지 않는다.
- 도쿄에만 과도하게 편중되지 않도록 전국·지역 그룹을 포함하되, 실제 공식 근거가 있는 분포만 기록한다.

후보 발굴 배치는 2026-08-26 완료했다. 공식 행사 2개를 발견 출처로 추가하고 신규 핵심 후보 16개를 모두 그룹 공식 원문과 연결했다. 카탈로그는 출처 10개·후보 27개(`imported` 4개, 공식 소스 확인·미승격 23개)가 됐지만, 현역 명단·2026 일정·공간 근거를 모두 통과한 후보만 다음 공개 배치에서 승격한다. 공개 Gate 1은 활동 그룹 14/30·공간 근거 14/14(100%)로 유지한다.

### 다음 실행: 고준비도 후보 공개 검증 배치

- 신규 16개 중 공식 현역 명단과 일정 경로가 비교적 명확한 INUWASI, UtaGe!, AsIs, JamsCollection, AVAM을 우선 검토한다.
- 각 그룹은 현역 전체 명단, 멤버별 공식 근거, 2026 일정, 행정구/활동권역을 증명하는 공간 원문을 한 묶음으로 완성한다.
- 명단 충돌·졸업 예정·일정 부재가 발견되면 후보 상태와 차단 사유만 갱신하고 공개 수량을 위해 승격하지 않는다.
- 공개 후 Gate 1 그룹 수와 공간 근거율을 재계산하고 도쿄 23구용 데이터 밀도는 별도로 집계한다.

고준비도 공개 검증 배치는 2026-08-26 완료했다. AsIs, UtaGe!, JamsCollection, AVAM 4개와 공식 현역 29명을 공개했고 INUWASI는 공식 페이지 간 5명/6명 충돌로 보류했다. Gate 1은 활동 그룹 18/30·공간 근거 18/18(100%)이며 실제 지도 시작까지 검증 그룹 12개가 더 필요하다.

### 다음 실행: 신규 후보 두 번째 공개 검증 배치

- Devil ANTHEM., NANIMONO, Palette Parade, CYNHN을 우선 대상으로 현재 전체 명단·공식 일정·공간 근거를 대조한다.
- Sweet Alley, Mirror,Mirror, Ringwanderung, situasion, かすみ草とステラ, MyDearDarlin', 衛星とカラテア는 명단 신선도나 공식 원문 부족을 먼저 해소한다.
- INUWASI는 공식 5명/6명 표시 충돌을 설명하는 가입·활동 공지가 확인될 때만 다시 판정한다.
- 승격 결과로 Gate 1을 다시 계산하되 30개 도달 전 실제 지도 UI를 시작하지 않는다.

두 번째 공개 검증 배치는 2026-08-26 완료했다. Devil ANTHEM., NANIMONO, Palette Parade, CYNHN 4개와 공식 현역 24명을 공개했다. Gate 1은 활동 그룹 22/30·공간 근거 22/22(100%)이며 실제 지도 시작까지 검증 그룹 8개가 더 필요하다.

### 다음 실행: 잔여 신규 후보 공식 명단 완결 배치

- Sweet Alley, Mirror,Mirror, Ringwanderung, MyDearDarlin'을 우선 검토해 현재 전체 명단·일정·공간 근거를 완결한다.
- situasion, かすみ草とステラ, 衛星とカラテア는 기존 명단 충돌·세대 변동·공식 명단 원문 부족을 먼저 해소한다.
- INUWASI는 5명/6명 공식 표시 충돌이 해소된 경우에만 재검토한다.
- Gate 1의 30개 충족이 가까워져도 최소 3개 도도부현 분포와 도쿄 23구 세부 밀도는 별도 게이트로 유지한다.

잔여 신규 후보 우선 배치는 2026-08-26 완료했다. Sweet Alley, Mirror,Mirror, Ringwanderung, MyDearDarlin' 4개와 현역 26명을 공개했다. Gate 1은 활동 그룹 26/30·공간 근거 26/26(100%)이며 전국 지도 그룹 수 게이트까지 4개가 남았다.

### 다음 실행: Gate 1 최종 후보 판정과 지도 기술 준비

- situasion, かすみ草とステラ, 衛星とカラテア, INUWASI의 명단 충돌·세대 변동·공식 원문 부족을 최신 가입/졸업 공지로 재검증한다.
- 기존 보류 kimikara, 神聖マキナ, クララ・マグラ, MiDi ON!도 차단 사유가 해소됐는지 함께 확인한다.
- 4개 이상이 안전하게 승격돼 30개를 넘더라도 최소 3개 도도부현 분포, 도쿄 그룹·구·공연장·미래 공연 게이트를 별도 계산한다.
- 실제 지도 기술 스파이크 전 타일 공급자·행정경계 데이터·모바일 성능·목록 대체 UI를 작업지시서로 확정한다.

최종 후보 1차 판정은 2026-08-26 완료했다. INUWASI 1개와 현역 6명을 공개했고 situasion·かすみ草とステラ·衛星とカラテア는 공식 명단 완결 조건 미충족으로 보류했다. Gate 1은 활동 그룹 27/30·공간 근거 27/27(100%)이며 3개가 더 필요하다.

### 다음 실행: Gate 1 부족분 확보와 분포 감사

- 기존 보류 핵심 후보 kimikara, 神聖マキナ, クララ・マグラ, MiDi ON!의 차단 사유가 최신 공식 공지로 해소됐는지 재검증한다.
- 이 배치는 2026-08-26 완료했다. 보류 4개는 차단 사유가 해소되지 않았고, Task have Fun·yosugala·ドラマチックレコード와 현역 13명을 신규 공개했다. 공개 활동 그룹 30/30과 공간 근거 30/30(100%)는 충족했지만, 도쿄 26개 대 비도쿄 각 1개의 편중으로 `3개 도도부현 × 각 3개` 분포 조건은 실패했다.

### 다음 실행: 비도쿄 지역 밀도 확장

- 오사카·홋카이도·후쿠오카·아이치의 공식 지역 행사와 로컬 아이돌 공식 사이트에서 비중복 핵심 후보를 발굴한다.
- 최소 3개 도도부현에서 각각 공개 검증 그룹 3개를 확보할 때까지 현재 명단·공식 일정·공간 근거 게이트를 동일하게 적용한다.
- 도쿄 23구 Gate 2는 그룹 수만 충족했으므로, 지역 분포와 함께 최소 5개 구 근거·공연장 15개·미래 공연 30개를 별도 수집한다.
- 뉴스 가지는 기존 방침대로 구현 전에 사용자와 범위·매체·안전 정책을 상담한다.
- 1차 실행 단위는 오사카·홋카이도·후쿠오카·아이치 공식 후보 조사와, 최소 3개 권역에서 지역당 2개씩 안전한 추가 승격 가능 여부 판정이다. 공식 현역 명단·2026 일정·지역 근거 중 하나라도 비면 다음 소배치로 넘긴다.
- 1차 소배치는 NEO BREAK·BYBBiT·MAGICAL SPEC과 현역 19명을 공개해 오사카·삿포로·후쿠오카를 각각 2개로 늘렸다. Gate 1 분포 완료까지 세 권역에 각 1개가 남았으며 다음 우선 후보는 FRUiTY·HelloYouth와 활동휴지 상태를 화해한 오사카 후보 또는 다른 공식 완결 후보이다.
- 분포 완결 실행 단위는 すーぱーぷーばぁー!!·FRUiTY·HelloYouth 세 후보의 기준일 상태를 공식 원문으로 확정하는 것이다. 통과하면 Gate 1을 완료하고 다음 단계는 실제 지도 구현 자체가 아니라 MapLibre·타일·행정경계 기술 스파이크 및 Gate 2 공연 데이터 준비로 전환한다.
- FRUiTY와 현역 9명은 공개 승격해 홋카이도 3개 조건을 충족했다. 다음 최소 실행은 すーぱーぷーばぁー!!과 HelloYouth를 각각 오사카·후쿠오카의 세 번째 그룹으로 안전하게 연결하는 것이다.
- HelloYouth와 현역 9명을 추가해 후쿠오카도 3개 조건을 충족했다. Gate 1 완결에는 오사카 세 번째 그룹 1개만 남았으며, 다음 실행은 すーぱーぷーばぁー!!의 공식 읽기·휴지 상태 최종 대조다.
- 안전한 승격이 3개 미만이면 공식 행사·사무소 목록에서 신규 비중복 후보를 추가 발굴하되 현역 명단·일정·공간 근거를 동시에 확보한다.
- 공개 30개 도달 시 최소 3개 도도부현별 3개 그룹 분포를 감사하고 도쿄 편중이면 전국 지도 기술 구현을 시작하지 않는다.
- 도쿄 23구 단계의 그룹 20개·5개 구·공연장 15개·미래 공연 30개를 별도로 계산한다.

### 제품의 큰 줄기

- 대상 사용자: 일본 라이브아이돌 공연을 지역·날짜·그룹 기준으로 찾고 공식 티켓 경로까지 이동하려는 일본어·한국어·영어 사용자.
- 해결할 핵심 문제: 그룹 정보는 흩어져 있고 일정은 공식 사이트·캘린더·티켓 플랫폼마다 달라서 사용자가 오늘 무엇을 믿고 어디로 이동해야 하는지 판단하기 어렵다.
- 핵심 가치 흐름: `검증된 공연 발견 → 날짜·지역·그룹 탐색 → 공연 상세 확인 → 공식 일정·티켓 원문 이동`.
- 신뢰 경계: 자동 수집 결과나 제3자 목록은 후보로만 취급한다. 공개 이벤트는 공식 사이트·공식 SNS·공식 공연장·티켓 플랫폼 중 하나의 원문과 확인일을 가져야 한다.
- 제품 표현: 데이터가 적을 때 전국 완성을 주장하지 않고 검증된 범위와 미정 필드를 그대로 표시한다.

### 기능·데이터·UI 가지

1. 공연 데이터: 공연, 출연 그룹, 날짜·시간 확정도, 공연장, 가격, 티켓, 상태, 출처를 별도 엔터티로 관리한다.
2. 탐색 UI: 공연 목록은 날짜 상태·지역·그룹을 필터링하고 결과 수와 빈 상태를 제공한다.
3. 공연 상세: 출연 그룹, 일정, 장소, 가격, 공식 원문, 티켓, 확인일을 한 화면에 연결한다.
4. 그룹 연결: 그룹 상세에서 해당 그룹의 검증된 예정 공연으로 이동한다.
5. 지역 연결: 그룹의 이미지상 거점과 실제 공연 개최 지역을 구분한다.
6. 수집 운영: 후보 수집 → 공식 원문 대조 → 공개 → 변경·취소·만료 재검증 흐름을 둔다.
7. 개인화 후속: 즐겨찾기, 저장 검색, 알림은 공연 데이터 품질과 양이 확보된 뒤 추가한다.
8. 분석 후속: 대반 관계, 공연장 추천, 팔로워 추세, 영상 디렉터리는 MVP 이후 별도 가지다.

### MVP 범위와 출시 게이트

- MVP 포함: `/[locale]/live`, `/[locale]/live/[eventId]`, 내비게이션 진입점, 서버 데이터 조회, 날짜·지역·그룹 필터, 미정 시간·장소·티켓 상태, 공식 출처·확인일 표시.
- MVP 데이터: 공식 공개 캘린더에서 확인한 최소 공연 레코드로 수직 흐름을 검증한다. 확정되지 않은 시간·장소·티켓은 `null`로 유지한다.
- 제외: 로그인, 즐겨찾기, 이메일 알림, 결제, 광고, 자동 게시 수집기, 대반 분석, 영상·Spotify 임베드.
- 출시 게이트: 검증된 미래 공연이 최소 1건 노출되고, 모든 공개 사실 필드가 출처 계약을 통과하며, 빈 결과에서도 공식 일정 링크로 갈 수 있어야 한다.

### 다음 가지: 공식 일정 후보 수집

- 목적: 공식 공개 캘린더의 미래 일정을 반복 가능한 방식으로 발견하되 자동으로 화면에 게시하지 않는다.
- 흐름: `공식 캘린더 수집 → 최소 필드 정규화 → 중복 제거 → review_pending 저장 → 사람이 공식 원문 대조 → chika-live.json 승격`.
- 수집 필드: 원본 이벤트 ID, 제목, 시작·종료일, 원문 장소, 발견된 티켓 URL, 공식 일정 URL, 수집일.
- 저장하지 않는 범위: 불필요한 원문 설명 전체, 개인 정보, 광고·추적 파라미터, 비공식 검색 결과.
- 공식 캘린더에는 공연 외 생일·발매·미디어 일정도 섞이므로 후보를 `live_or_event`, `birthday`, `release_or_media`, `unknown`으로 분류하고 공연으로 단정하지 않는다.
- 안전 경계: 네트워크 실패 시 기존 후보·공개 데이터를 덮어쓰지 않으며, 후보 상태는 공개 UI에서 읽지 않는다.
- 완료 조건: dry-run과 명시적 write 모드가 분리되고, 후보 스키마·중복 검사·미래 일정 필터가 검증된다.

### 다음 가지: 잔여 후보 검토

- 공개 데이터에 이미 존재하는 후보는 `publishedEventId`로 연결해 후보·공개 상태를 일치시킨다.
- 제목에서 공연이 아닌 것이 명백한 생일 항목은 삭제하지 않고 `rejected`로 남겨 재수집 시 다시 검토 대기로 돌아오지 않게 한다.
- 나머지는 공식 원문에서 시간·장소·티켓 중 확인된 사실만 보강하며, 근거가 부족하면 `review_pending`을 유지한다.
- 운영자가 후보별 공개 준비도와 차단 사유를 한 번에 볼 수 있는 읽기 전용 검토 보고 명령을 제공한다.

### 다음 가지: 다중 공식 일정 공급원

- 두 번째 공급원은 FRUITS ZIPPER 공식 사이트의 월별 공연 일정 HTML을 사용한다. 폐기된 `/schedule/`이 아니라 실제 `/live_information/schedule/list` 경로를 기준으로 한다.
- 후보 데이터는 단일 `source`에서 다중 `sources` 레지스트리로 전환하고 각 후보가 `sourceKey`를 가진다.
- 공급원별 마지막 성공 시각, 응답 콘텐츠 해시, 후보 수를 기록해 변경 여부를 감지한다.
- 네트워크·파싱 실패는 기존 후보를 지우지 않고 종료 코드와 오류로 드러낸다.
- FRUITS ZIPPER 수집은 `box_live_*` 공연 항목만 대상으로 하며 TV·라디오·잡지 항목은 후보에서 제외한다.

### 다음 가지: FRUITS ZIPPER 상세 원문 보강

- 월별 목록에서 발견한 후보의 공식 `/live_information/detail/{id}` 원문을 다시 읽어 개최 장소, 시간 표기, 티켓 링크를 보강한다.
- 원문에 명시된 값만 저장하며 `OPEN`·`START`가 모두 없거나 INFO가 비어 있으면 기존 값을 유지하고 검토 대기로 남긴다.
- 가격은 현재 후보 계약에 독립 필드가 없으므로 이번 실행에서는 임의 구조로 저장하거나 공개 데이터에 자동 반영하지 않는다. 향후 공개 승격 작업에서 공식 원문과 함께 수동 대조한다.
- 상세 페이지 하나라도 HTTP 실패하면 write 전에 전체 실행을 실패시켜 부분 갱신을 막는다. 반복 실행은 기존 수동 상태와 공개 연결을 보존한다.
- 상세 원문에서 발견한 외부 링크는 티켓·접수 문맥이 확인되는 HTTP(S) URL만 후보에 포함하고, 공식 상세 페이지 자체는 `sourceUrl`로 유지한다.
- 보강은 후보 품질 개선 단계이며 `approved` 또는 공개 공연으로 자동 승격하지 않는다.

## 2026-08-25 문서 기준선 정리

- 사용자 의도: 저장소의 모든 Markdown과 작업 지침을 한 번에 점검해 다음 작업자가 낡은 경로·수치·정책을 실행하지 않게 한다.
- 현재 문제: 초기 `HANDOVER.md`가 폐기 사실 없이 현행 명세처럼 보이고, `CODEX_HANDOVER.md`의 저장소 경로와 정적 생성 수가 최근 검증 결과와 다르며, 저장소 자체 `AGENTS.md`가 없다.
- 기대 흐름: 루트 `AGENTS.md` 확인 → 제품 기획서 → 작업지시서 → 현행 인수인계 순서로 읽고, 초기 문서는 역사 자료로만 참조한다.
- 정책·안전 경계: 기존 사용자 데이터 변경은 건드리지 않고 문서·작업 규칙만 보완한다. 검증하지 않은 프로덕션 상태는 새로 단정하지 않는다.
- 바꾸지 않는 범위: 멤버 데이터, UI, 배포, 외부 서비스 상태.
- 완료 조건: 현행 경로·검증 수치가 일치하고, 폐기 문서가 명확히 표시되며, 루트 `AGENTS.md`가 문서 우선순위와 검증 절차를 정의한다.

## 2026-08-25 현역 명단 완성 작업

- 사용자 의도: 수집 중으로 비어 있는 그룹 상세에서 현재 공식 명단을 탐색할 수 있게 한다.
- 현재 문제: 虹のコンキスタドール, FES☆TIVE, LinQ, 手羽先センセーション은 공식 인원수만 있고 활성 멤버가 0명이다.
- 기대 흐름: 공식 그룹 프로필 확인 → 최소 멤버 레코드 등록 → 그룹 상세와 검색에서 활성 멤버 노출.
- 정책·안전 경계: 공식 사이트의 현재 명단만 사용하고, 확인되지 않은 생일·담당색·SNS·사진·지표는 추정하지 않는다.
- 단계 완료 조건: 네 그룹에 공식 출처를 가진 활성 멤버 최소 1명이 생기고 `partial`로 전환되며 데이터 검증·타입 검사·프로덕션 빌드가 통과한다. 전체 명단 완성은 다음 가지로 유지한다.

## 0. 확정된 제품 결정

- `liveidol.blog`에서는 공연 필드와 라이브·그룹·멤버·생일·그라비아 탐색 구조만 참고하고 디자인은 복제하지 않는다.
- 그라비아는 출판사·소속사·공식 판매처로 검증된 발매 정보와 외부 링크 중심으로 제공한다.
- 표지 이미지는 출처뿐 아니라 권리 상태가 확인될 때만 직접 노출한다.
- 순위는 X 팔로워, Instagram 팔로워, Google Trends 검색 관심도를 각각 독립 스냅숏으로 보관한다.

## 1. 큰그림

제품 목적은 일본 라이브아이돌을 날짜·지역·공연장 기준으로 찾고, 그룹·멤버의 공식 채널과 티켓으로 연결하는 다국어 공연 탐색 서비스다. 사카미치·48 데이터와 코드를 합치지 않는다.

현재 문서와 커밋 메시지는 완성된 전국 데이터베이스처럼 표현하지만 실제 구현은 프로토타입이다. 당분간 `전국 완성 서비스`가 아니라 `검증 중인 디렉터리`로 정의해야 한다.

## 2. 중간그림

- 지역 탐색의 핵심 단위는 그룹의 이미지상 거점이 아니라 실제 공연장과 공연 일정이다.
- 그룹·멤버·공연·공연장·공지·랭킹을 별도 엔터티로 관리한다.
- 모든 사실 데이터는 공식 사이트 또는 공식 SNS 출처와 확인일을 가진다.
- 로고·단체 사진·멤버 사진은 생성 SVG와 공식 이미지를 명확히 구분한다.

## 3. 세부 감사 결과

- 문서는 18개 그룹을 주장하지만 원본 데이터는 11개 그룹이다.
- 전체 멤버는 15명이며 그룹별 1~3명뿐이다.
- 멤버 이미지 15건은 실제 사진이 아니라 생성 글리프 SVG다.
- 데이터에 없는 그룹용 SVG 에셋이 13개 남아 있다.
- 공지 4건, 그라비아 5건만 존재한다.
- 일부 공지 제목과 연결 URL이 공식 검색 결과와 일치하지 않는다.
- 인기·검색량·팔로워 수는 출처와 수집일 없이 시드에 직접 입력됐다.
- 모든 링크가 근거 없이 `verified: true`로 기록돼 있다.
- 지도 지역·구역과 그룹 연결은 코드에 하드코딩됐고 출처가 없다.
- 공연·공연장·가격·티켓 상태 데이터 모델이 없다.
- 패키지에 등록된 `data:validate` 파일이 원본에는 없었다.
- 그룹 상세는 클라이언트 컴포넌트에서 전체 JSON을 직접 불러 확장성이 낮다.

## 4. 실행 순서

1. 출처 없는 랭킹과 사실 불일치 공지를 숨긴다.
2. 실제 사진과 생성 플레이스홀더를 UI에서 구분한다.
3. 데이터 검증기와 출처 계약을 도입한다.
4. 그룹·멤버 데이터는 공식 프로필 기준으로 처음부터 다시 수집한다.
5. 공연·공연장·티켓 모델과 공식 일정 수집기를 구축한다.
6. 지도는 공연장 데이터가 확보된 도시부터 단계적으로 활성화한다.
7. 서버 페이지와 클라이언트 필터를 분리해 전체 JSON의 브라우저 번들을 막는다.
8. 정보구조가 안정된 뒤 야간 공연 편집지 스타일로 재설계한다.

## 5. 완료 기준

- 문서·UI·데이터의 그룹 및 멤버 수가 일치한다.
- 모든 공개 링크·공지·지표에 출처와 확인일이 있다.
- 지도 핀은 실제 공연 또는 검증된 활동 지역을 근거로 한다.
- 순위에는 플랫폼·집계일·단위·출처가 표시된다.
- 미검증 데이터는 기본적으로 렌더링되지 않는다.
## 2026-08-30 지도 탐색 계층 재정의 — 사용자 의도 확정

- 사용자 의도는 홈에서 실제 도로지도와 공연장 핀을 바로 조작하는 것이 아니다. 홈은 일본 열도의 실제 형상을 닮은 고품질 탐색 일러스트를 사용한다.
- 핵심 흐름은 `일본 전국 → 도시(도쿄·오사카 등) → 활동 지역(아키하바라·시부야·신주쿠 등) → 대표 이미지가 있는 아이돌 그룹 → 그룹 상세 → 라이브 선택 → 실제 공연장 지도`다.
- 도시·활동 지역 카드는 지도 위의 공간적 위치에 놓이며, 선택할 때 같은 무대 안에서 확대·전환되는 감각을 제공한다. 별도 우측 필터 패널이 탐색의 중심이 되어서는 안 된다.
- 아이돌 그룹은 지역을 선택한 뒤에만 사진·로고·단체 비주얼 중심 카드로 노출한다. 이름만 있는 작은 칩이나 전국 그룹 전체 목록은 이 흐름을 대체할 수 없다.
- 실제 도로지도, 공연장 위치, 이벤트 밀도는 라이브 의도가 생긴 뒤의 정보다. 홈 지도에는 표시하지 않고 그룹 상세의 라이브 단계로 이동한다.
- 지도 형상은 손으로 단순화한 임의 다각형 대신 출처·라이선스가 확인된 일본 행정구역 벡터를 로컬 자산으로 사용해 네트워크가 없어도 품질을 유지한다.
- 신뢰 경계: 그룹과 활동 지역 연결은 기존 검증 필드만 사용하며, 사진은 데이터의 `imageUrl`이 있는 경우에만 노출한다. 사진이 없으면 브랜드 색과 명칭을 사용한 명시적 대체 카드를 제공한다.
- 이번 MVP에서 바꾸지 않는 범위: 새로운 그룹 사진의 무단 수집, 검증되지 않은 활동 지역 추정, 외부 지도 서비스에 대한 종속적 홈 경험.

### 다음 연결 가지 — 그룹 상세 라이브 지도

- 홈의 지리 탐색은 그룹을 발견하는 단계에서 끝난다. 실제 지도는 선택한 그룹에 대한 공연 의도가 생긴 뒤에만 표시한다.
- 그룹 상세의 기본 탭은 프로필·멤버이며, 사용자가 `라이브` 탭을 명시적으로 선택하면 그 그룹의 예정 공연과 공연장 좌표만 실제 지도에 표시한다.
- 지도 핀 선택은 공연장 이름·주소·그 공연장의 해당 그룹 예정 공연으로 연결한다. 그룹과 무관한 전국 공연장 핀은 섞지 않는다.
- 좌표와 출처가 검증된 공연장만 지도에 표시한다. 좌표 없는 공연은 일정 목록에 유지하며 `지도 위치 미확인` 상태를 명시한다.
- 지도 자체를 불러오지 못해도 공연 일정과 Google Maps 원문 링크는 계속 사용할 수 있어야 한다.

### 탐색 왕복 가지 — 지역·그룹·라이브 URL 상태

- 발견 흐름은 한 방향 링크로 끝나지 않고 그룹 상세에서 원래 활동 지역으로 돌아갈 수 있어야 한다.
- 그룹 상세의 지역 표시는 홈의 `mapRegion`·`mapDistrict` 상태로 연결한다. 홈으로 돌아가면 일본 전국부터 다시 선택하게 하지 않고 해당 활동 지역의 그룹 카드 단계가 복원된다.
- 라이브 탭은 `tab=live`로 공유·새로고침·뒤로 가기에서 복원한다. 프로필 탭은 기본 상태이므로 불필요한 쿼리를 제거한다.
- 잘못된 탭 값은 프로필로 안전하게 정규화한다. 탭 URL 상태는 그룹·공연 데이터 계약을 변경하지 않는다.

## 2026-08-30 그룹·멤버 대표 이미지 MVP

### 큰 줄기와 현재 기준선

- 지도에서 지역을 선택한 다음 사용자는 텍스트 목록이 아니라 얼굴·단체 사진·공식 로고를 통해 그룹과 멤버를 식별해야 한다. 대표 이미지는 발견→그룹→멤버 흐름의 핵심 가지다.
- 현재 그룹 37개 중 `imageUrl`은 11개뿐이고 26개가 비어 있다. 멤버 188명 중 이미지 경로는 15개, 173개는 비어 있다.
- 기존 그룹 11개와 멤버 15개의 로컬 SVG는 공식 사진·공식 로고가 아니라 생성 플레이스홀더다. 이를 실제 사진처럼 취급하지 않는다.

### MVP 순서

1. 이미지 MVP 0 — 계약·감사: 그룹과 멤버 모두 `공식 사진 / 공식 로고 / 생성 플레이스홀더 / 링크 전용` 종류, 출처, 확인일, 권리 상태를 구분하고 검증기가 모순을 차단한다.
2. 이미지 MVP 1 — 카드 폴백: 공식 사진을 우선하고, 없으면 공식 로고, 그마저 없으면 그룹 색·이름 또는 멤버 담당색·글리프 대체 비주얼을 사용한다. 플레이스홀더에는 명시적 라벨을 둔다.
3. 이미지 MVP 2 — 안전한 소배치: 핵심 그룹부터 공식 사이트·소속사 미디어킷·사용 허가가 명확한 자산을 검토한다. URL을 발견했다는 사실만으로 다운로드·핫링크·재배포하지 않는다.
4. 이미지 MVP 3 — 운영: 일일 업데이트는 이미지 교체 후보와 원문 해시만 검토 큐에 남기며 자동 공개하지 않는다. 변경·삭제 요청과 권리 철회 시 즉시 링크 전용 또는 플레이스홀더로 내린다.

### 정책·안전 경계

- `official_embed`는 공식 원문이 직접 임베드를 허용하거나 제공한 경우, `permission_confirmed`는 별도 사용 허가 근거가 있는 경우만 허용한다. 그 외 공식 페이지 이미지는 `link_only`다.
- SNS 프로필·게시물, 검색 결과 썸네일, 팬 촬영물, 유료 화보, 미성년자 이미지는 공개 페이지에 있다는 이유만으로 복제하지 않는다.
- 출처와 권리 상태가 없는 현재 생성 SVG는 `placeholder`로 강제 분류한다. 실사진처럼 보이는 생성 이미지는 사용하지 않는다.
- 이번 실행은 MVP 0~1을 구현한다. 실제 공식 사진·로고 수집은 권리 근거를 확보한 후 별도 소배치로 진행한다.

### 이미지 MVP 2 권리 판정·문의 운영

- 권리 정책 원문이 무단 사용을 금지하면 해당 프로필 자산 후보는 `rejected`로 닫고, 다른 명시적 라이선스 자산을 별도로 찾는다.
- 사용 허가 창구가 있는 후보는 `rights_review`로 유지하고 문의 URL·정책 URL·요청 초안을 연결한다.
- 문의 초안에는 서비스 목적, 표시 위치와 크기, 상업성 여부, 변형 여부, 저장/핫링크 방식, 출처 표기, 사용 기간, 삭제 요청 대응을 포함한다.
- 실제 문의 제출은 외부 커뮤니케이션이므로 사용자 명시 승인 전 자동 전송하지 않는다. 답변도 허가 범위·기간·표기 조건을 구조화한 뒤에만 `approved`로 승격한다.

### 사진 제한 그룹의 로고·워드마크 정책

- 사진 사용이 금지되거나 허가되지 않은 그룹은 사진 후보를 공개하지 않고 로고 가지로 전환한다.
- 공식 로고도 권리자의 상표·저작권 자산이므로 명시적 사용 허가 또는 공개 사용 가이드가 있을 때만 `official_logo`로 표시한다.
- 공식 로고 허가가 없으면 그룹명 텍스트와 기존 브랜드 색만 사용한 서비스 자체 `text_wordmark`를 표시한다. 공식 로고의 도형·서체·장식을 모사하지 않고 `공식`이라는 표현을 사용하지 않는다.
- 우선순위는 `허가된 공식 사진 → 허가된 공식 로고 → 자체 텍스트 워드마크`다. 사진 제한은 로고의 자동 허가로 해석하지 않는다.
