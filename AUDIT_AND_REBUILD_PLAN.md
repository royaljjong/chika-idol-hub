# Chika Idol Box 감사 및 재설계 계획

기준일: 2026-08-20

## 0. 확정된 제품 결정

- `liveidol.blog`에서는 공연 필드와 라이브·그룹·멤버·생일·그라비아 탐색 구조만 참고하고 디자인은 복제하지 않는다.
- 그라비아는 출판사·소속사·공식 판매처로 검증된 발매 정보와 외부 링크 중심으로 제공한다.
- 표지 이미지는 출처뿐 아니라 권리 상태가 확인될 때만 직접 노출한다.
- 순위는 X 팔로워, Instagram 팔로워, Google Trends 검색 관심도를 각각 독립 스냅숏으로 보관한다.

## 1. 큰그림

제품 목적은 일본 라이브아이돌을 날짜·지역·공연장 기준으로 찾고, 그룹·멤버의 공식 채널과 티켓으로 연결하는 다국어 공연 탐색 서비스다. 사카미치·48 데이터와 코드를 합치지 않는다.

현재 문서와 커밋 메시지는 완성된 전국 데이터베이스처럼 표현하지만 실제 구현은 프로토타입이다. 당분간 `전국 완성 서비스`가 아니라 `검증 중인 디렉터리`로 정의해야 한다.

## 2. 중간그림

- 지역 탐색의 핵심 단위는 그룹의 이미지상 거점이 아니라 실제 공연장과 공연 일정이다.
- 그룹·멤버·공연·공연장·공지·랭킹을 별도 엔터티로 관리한다.
- 모든 사실 데이터는 공식 사이트 또는 공식 SNS 출처와 확인일을 가진다.
- 로고·단체 사진·멤버 사진은 생성 SVG와 공식 이미지를 명확히 구분한다.

## 3. 세부 감사 결과

- 문서는 18개 그룹을 주장하지만 원본 데이터는 11개 그룹이다.
- 전체 멤버는 15명이며 그룹별 1~3명뿐이다.
- 멤버 이미지 15건은 실제 사진이 아니라 생성 글리프 SVG다.
- 데이터에 없는 그룹용 SVG 에셋이 13개 남아 있다.
- 공지 4건, 그라비아 5건만 존재한다.
- 일부 공지 제목과 연결 URL이 공식 검색 결과와 일치하지 않는다.
- 인기·검색량·팔로워 수는 출처와 수집일 없이 시드에 직접 입력됐다.
- 모든 링크가 근거 없이 `verified: true`로 기록돼 있다.
- 지도 지역·구역과 그룹 연결은 코드에 하드코딩됐고 출처가 없다.
- 공연·공연장·가격·티켓 상태 데이터 모델이 없다.
- 패키지에 등록된 `data:validate` 파일이 원본에는 없었다.
- 그룹 상세는 클라이언트 컴포넌트에서 전체 JSON을 직접 불러 확장성이 낮다.

## 4. 실행 순서

1. 출처 없는 랭킹과 사실 불일치 공지를 숨긴다.
2. 실제 사진과 생성 플레이스홀더를 UI에서 구분한다.
3. 데이터 검증기와 출처 계약을 도입한다.
4. 그룹·멤버 데이터는 공식 프로필 기준으로 처음부터 다시 수집한다.
5. 공연·공연장·티켓 모델과 공식 일정 수집기를 구축한다.
6. 지도는 공연장 데이터가 확보된 도시부터 단계적으로 활성화한다.
7. 서버 페이지와 클라이언트 필터를 분리해 전체 JSON의 브라우저 번들을 막는다.
8. 정보구조가 안정된 뒤 야간 공연 편집지 스타일로 재설계한다.

## 5. 완료 기준

- 문서·UI·데이터의 그룹 및 멤버 수가 일치한다.
- 모든 공개 링크·공지·지표에 출처와 확인일이 있다.
- 지도 핀은 실제 공연 또는 검증된 활동 지역을 근거로 한다.
- 순위에는 플랫폼·집계일·단위·출처가 표시된다.
- 미검증 데이터는 기본적으로 렌더링되지 않는다.
