# 2026-08-29 MVP 1–4 적대적 정합성 검토

> **문서 지위 — 시점 고정 감사 기록:** 아래 판정·결함·위험·“다음 MVP 순서”는 2026-08-29 당시 로컬 상태에 대한 증거 기록이며 현재 요구사항이나 작업 우선순위가 아니다. 현행 제품·MVP A~H·지도·운영 기준은 `AUDIT_AND_REBUILD_PLAN.md`와 `CODEX_HANDOVER.md`, 실행 단위는 `WORK_ORDER.md`를 따른다. 특히 당시의 홈 MapLibre 플래그·도쿄 23구 계획은 현재 승인된 홈 계층 탐색과 그룹 라이브 지도 계약을 대체하지 않는다.

## 판정

**현재 로컬 MVP 4 데이터 배치는 조건부 GO다.** Gate 2 데이터 기준과 프로덕션 빌드는 통과했지만, 도쿄 23구 UI·MapLibre 운영 공급자·커밋/배포는 아직 NO-GO다. Gate 2 통과를 UI 출시 승인으로 해석하지 않는다.

## 확인된 결과

- 데이터 검사: 37그룹, 188멤버, 9 GeoArea, 공개 공연 38건, 후보 52건. 오류 0, 검토 경고 39건.
- Gate 2: 도쿄 활동 그룹 26/20, 근거 특별구 5/5, 지도 준비 공연장 15/15, 연결 미래 공연 31/30, 전국 미래 공연 37건으로 PASS.
- 일본 날짜 경계 검사, TypeScript, Next.js 프로덕션 빌드, diff 검사가 통과했다. 빌드는 정적 페이지 678/678을 생성했다.
- 공개 `src`에서 공연·그라비아 후보 및 출처 카탈로그 import를 찾지 못했다.
- 그라비아 홈과 목록은 `rightsStatus !== 'link_only'` 조건으로 대표·갤러리 이미지를 차단한다.
- MapLibre 컴포넌트 렌더링은 `NEXT_PUBLIC_ENABLE_MAPLIBRE_SPIKE === 'true'` 조건 아래에 있고, 기본 UI는 SVG/목록을 유지한다.
- 도쿄 23구 전용 경로·화면은 추가되지 않았다.

## 확인된 결함

### Low — pnpm 기본 스크립트 진입이 로컬 공급망 정책에 막힘

- 영향: 일반적인 `pnpm data:validate`·`pnpm typecheck` 재현 경로가 제품 스크립트 실행 전에 실패할 수 있다.
- 증거 등급: E1. pnpm 11.22.0이 esbuild 0.28.2와 sharp 0.34.5의 무시된 빌드 스크립트를 보고하고 exit 1로 종료했다.
- 반증/완화: 이미 설치된 `node_modules`의 `tsx`·`tsc`를 직접 호출한 제품 검사는 모두 통과했고 Next 빌드도 통과했다. 제품 코드 결함은 아니다.
- 최소 조치: 다음 운영 MVP에서 필요한 네이티브 빌드 스크립트 목록을 검토하고 승인 정책을 문서화한다. 이번 실행에서는 의존성 상태를 바꾸지 않는다.

## 신뢰 가능한 미검증 위험

- 39개 데이터 경고는 비차단 상태지만 출처 최신성과 멤버 필드 완결성을 약화할 수 있다. `scripts/validate.ts` 경고를 유형별로 분류하고 공개 영향 순으로 줄여야 한다.
- 공연 취소·연기·매진은 정적 확인일 이후 자동 갱신되지 않는다. Gate 2의 31건은 날짜 경과 또는 상태 변경으로 30 아래로 내려갈 수 있으므로 UI 출시 직전에 동일 감사 명령을 다시 실행해야 한다.
- 플래그 활성 MapLibre의 실제 네트워크, 모바일 제스처, 키보드 접근성, 공급자 권리/SLA는 검증되지 않았다.
- 큰 미커밋 작업 트리와 배포 기준선이 분리되어 있어 현재 로컬 PASS를 프로덕션 상태로 단정할 수 없다.

## 기각한 가설

- 후보 데이터가 공개 UI에 직접 유입된다는 가설: `src` import 검색에서 근거 없음.
- `link_only` 그라비아 이미지가 렌더링된다는 가설: 대표 이미지와 갤러리 모두 명시적 필터가 있음.
- Gate 2 통과와 동시에 23구 UI가 공개됐다는 가설: 관련 전용 UI/경로가 없고 문서도 별도 승인으로 분리함.
- 기본 환경에서 외부 지도 타일이 마운트된다는 가설: MapLibre 렌더 분기가 명시적 공개 환경 변수의 정확한 문자열 `true`를 요구함.

## 외부 모델 단계와 한계

- agy 중립 브리프: 완료. 제품 줄기, 변경 가지, 신뢰 경계, 검증 증거와 미확인 항목을 보존했다.
- Sonnet 저장소 조사: NOT RUN. 읽기 중 컨텍스트 자동압축이 반복되어 결과 없이 종료됐다. 규칙에 따라 자동 재시도하지 않았다.
- Opus 판정: NOT RUN. 판정할 Sonnet 증거 레코드가 없었다.
- agy 최종 합성: NOT RUN. Opus 결과가 없었다.
- Codex 최종 도전: 완료. 위 고위험 연결을 직접 정적 검색하고 실제 검사 명령을 재실행했다.

## 다음 MVP 순서

1. MVP 5-A 운영 재현성: pnpm 승인 정책, 경고 39건 분류, 깨끗한 커밋 기준선과 재현 명령을 확정한다.
2. MVP 5-B 도쿄 드릴다운 설계: 23구 정보 구조, GeoArea→공연장→공연 연결, 로딩·빈·실패·날짜 경과 상태, 모바일·키보드 접근성을 기획서와 작업지시서에 먼저 작성한다.
3. MVP 5-C 지도 운영 결정: 타일 공급자 권리·비용·SLA·개인정보 경계를 승인하고 플래그 활성 브라우저 검증을 수행한다.
4. MVP 5-D 구현·회귀: 승인된 최소 UI를 구현하고 Gate 2 재감사, 데이터·날짜·타입·빌드·브라우저·접근성 검사를 통과한다.
5. MVP 5-E 인수인계·배포: 실제 검증 결과를 문서에 반영하고 사용자 승인 후에만 커밋·배포한다.
