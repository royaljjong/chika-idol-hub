# Chika Idol Box 작업지시서

기준일: 2026-08-31

## 2026-09-02 Release R-3 — 커밋·배포·인수인계

### 실행 단위

1. 현재 변경 범위와 비밀·로컬 산출물 제외를 확인하고 전체 검증을 재실행한다.
2. 검증된 변경을 현재 `codex/trust-data-phase2` 브랜치에 커밋하고 `origin`에 비강제 푸시한다.
3. 연결된 `chika-idol-hub` Vercel 프로젝트에 프로덕션 배포하고 READY·production alias·공개 핵심 URL을 확인한다.
4. 커밋 SHA·배포 ID/URL·공개 검증 결과를 인수인계에 반영한 문서 후속 커밋을 푸시한다.

### 실패·회귀 위험과 완료 조건

- 검사·푸시·배포 실패는 다음 단계로 진행하지 않고 원인을 확인한다. 기존 프로덕션은 새 배포 검증 전까지 보존한다.
- 공개 검증은 ko/ja/en 홈·그라비아, 대표 라이브·그룹 경로, 의도된 404, `html lang`, 그라비아 후보 비노출을 포함한다.
- 커밋·배포 결과는 실행 후 실제 값만 기록한다.

### 실행 결과

- 배포 전 데이터·일본 날짜·37그룹 파라미터·TypeScript·수정 파일 ESLint·774/774 프로덕션 빌드·`git diff --check`를 통과했다. 저장소 전체 `npx eslint .`은 `.next`까지 장시간 순회해 완료 전 중단했으며, 제품 빌드 내장 lint/type 단계와 변경 파일 직접 ESLint 결과를 릴리스 근거로 사용한다.
- 커밋·푸시·배포 식별자와 공개 HTTP 결과는 실행 후 아래 인수인계와 함께 갱신한다.

## 2026-09-02 UX A-3 — 브라우저 상호작용·그라비아 감사

### 변경 전 조사 범위

- 홈: 데스크톱/모바일 단계 배치, 도시·지역 선택, URL·뒤로가기.
- 그룹 live: 직접 URL, 탭 키보드, 공연장 버튼·선택 패널, 지도 실패 fallback.
- 그라비아: `GRAVURE_DISCOVERY_MVP.md`, 공개/후보 JSON, schema/data loader, 목록·멤버 연결, `link_only` 이미지 비노출, 다국어·외부 공식 링크, 빈 상태.

### 실패·회귀 위험과 완료 조건

- 브라우저 도구가 빈 DOM이나 세션 손실을 반환하면 검증 실패로 기록하고 정적·HTTP 증거와 구분한다.
- 권리 상태가 불명확한 자산 URL·이미지를 공개 UI에 추가하지 않는다. 후보 JSON은 런타임에서 참조하지 않아야 한다.
- 발견 결함은 제품 계획에 원인·범위를 먼저 추가한 뒤 하나씩 수정하고 관련 검사 통과 후 다음 단계로 진행한다.

### 감사 확정 변경 단위

1. 공개 그라비아 5건의 필수 출처·확인일·권리 상태 누락을 검증 실패로 승격한다.
2. 누락 필드를 추정하지 않고 무근거 공개 레코드를 제거하며 후보 2건은 그대로 격리한다.
3. `link_only + imageUrl`과 임베드 권리 없는 갤러리 조합을 스키마·검증에서 차단한다.
4. 데이터 검사로 공개 0건/후보 2건을 확인한 뒤 타입·린트·빌드·접근성·링크 및 빈 상태를 순서대로 검증한다.
5. HTTP 감사에서 확정된 전역 `html lang="ja"` 고정과 Analytics 중복 마운트를 로케일 루트 레이아웃 통합으로 수정한다. ko/ja/en 각각의 SSR `lang`과 기존 774개 경로 빌드를 검증한다.

### 구현·검증 결과

- 공개 그라비아 5건은 출처 URL·확인일·권리 상태가 없는 과거 시드임을 확인해 공개 JSON에서 제거했다. 공개 0건·검토 후보 2건이며 후보 파일은 런타임에서 참조하지 않는다.
- 공개 스키마의 `sourceUrl`, `checkedAt`, `rightsStatus`를 필수화하고 `link_only + imageUrl`을 검증 오류로 만들었다. 갤러리는 `official_embed` 또는 `permission_confirmed`만 허용한다. 시드 재실행도 검증 공개 목록 0건만 생성하도록 분리했다.
- 실제 SSR 감사에서 발견한 전역 `html lang="ja"` 고정과 Analytics 중복을 로케일 루트 레이아웃으로 통합했다. ko/ja/en 그라비아 경로에서 각각 `lang=ko/ja/en`, 현지화 빈 상태, 후보 이름 비노출을 확인했다.
- 데이터 검사 PASS — 37그룹, 188멤버, 공개 그라비아 0건, 후보 2건, 공개 공연 70건, 공연 후보 58건, 검토 경고 19건. 일본 날짜·37그룹 파라미터·TypeScript·관련 ESLint·774/774 프로덕션 빌드·`git diff --check` PASS.
- 브라우저 axe 호출은 성공했지만 `passes=0`, `inapplicable=0`인 빈 판정이라 접근성 통과 근거로 사용하지 않았다. SSR 의미 구조와 언어 속성은 HTTP 결과로 검증했다.

## 2026-09-02 UX A-2 — 홈 ARIA·지도 출처 대비 보완

### 변경 범위·실패 위험

- `InteractiveJapanMap.tsx`: 국가 단계의 빈 breadcrumb에는 role/label을 주지 않고 도시·지역 단계에서만 navigation 의미를 제공한다.
- `globals.css`: 지도 출처 문구의 전경색과 글자 크기를 AA 대비에 맞게 보강한다.
- breadcrumb 버튼·URL 복원·지도 출처 내용과 위치는 유지한다. 불필요한 landmark 또는 저대비 재발이 회귀 위험이다.

### 완료 조건·검증

- 홈 ko axe WCAG 2 A/AA 확정 위반 0, 대표 경로 회귀 0.
- TypeScript, ESLint, 프로덕션 빌드, `git diff --check` PASS.

### 구현·검증 결과

- breadcrumb는 국가 단계에서 ARIA 속성을 제거하고 도시·지역 단계에서만 `role=navigation`과 이름을 제공하도록 수정했다.
- 지도 출처 문구를 8px `#526275`에서 9px `#94a3b8`로 보강했다. axe가 보고한 기존 대비는 3.14:1이었다.
- TypeScript·관련 ESLint·774/774 프로덕션 빌드 PASS. 홈 ko axe WCAG 2 A/AA 확정 위반은 2→0, ja live·en group·ko member·404는 0건을 유지했다.
- 이미지·그라데이션 위 요소는 axe가 배경을 결정하지 못해 color-contrast incomplete로 남았다. 모바일 device 자동 검사는 실제 DOM 없는 `passes=0/inapplicable=0` 결과라 무효 처리했으며 모바일 실화면은 미검증으로 남긴다.

## 2026-09-02 UX A-1 — 지도·탭 접근성 및 인수인계 정합성

### 변경 범위와 연결

- `GroupLiveMap.tsx`: 지도 핀과 동등한 공연장 선택 버튼 목록을 항상 제공하고 선택 상태를 노출한다.
- 그룹 상세 `page.tsx`: WAI-ARIA 탭 키보드 이동, roving `tabIndex`, URL 기반 초기 탭 복원을 연결한다.
- `CODEX_HANDOVER.md`: 일일 갱신 결과인 공연 후보 58건으로 현행 수치를 동기화한다.
- `Navigation.tsx`: 실브라우저 axe 검사에서 확인된 기존 제품 탐색 ARIA 역할 누락을 `nav` 랜드마크로 교정한다.
- 데이터·스키마·공개 승격·지도 공급자·배포는 변경하지 않는다.

### 실패·빈 상태와 회귀 위험

- 좌표가 없으면 기존 빈 상태를 유지한다. 지도가 실패해도 공연장 버튼·공연 목록·공식 링크를 유지한다.
- 탭 키보드 이동은 브라우저 뒤로가기와 `?tab=live`를 깨뜨리지 않아야 한다.
- 서버/클라이언트 초기 상태 차이로 hydration 경고를 만들지 않고, 첫 클라이언트 렌더의 프로필 깜빡임만 제거한다.

### 단계별 완료 조건·검증

1. 공연장 버튼 목록: 키보드로 선택 가능하고 선택된 버튼에 `aria-pressed`가 반영되며 타입·린트 통과.
2. 탭 계약: 좌우/Home/End, roving `tabIndex`, 포커스 이동, URL·뒤로가기 복원이 타입·린트 통과.
3. 초기 복원: `?tab=live`에서 첫 클라이언트 렌더가 live이며 잘못된 값은 profile로 정규화되고 타입·파라미터 검사 통과.
4. 문서 동기화: 현행 후보 58건이 일관되고 데이터 검사·관련 검색 통과.
5. 전체: 데이터·일본 날짜·파라미터·타입·린트·프로덕션 빌드·`git diff --check` 통과. 가능하면 로컬 HTTP와 브라우저 실화면도 확인한다.
6. axe 재검사에서 이번 공연장 목록과 제품 탐색의 ARIA 경고가 제거되고 새 확정 위반이 없어야 한다.

### 구현·검증 결과

- 1단계 공연장 대체 목록: 좌표 검증 공연장을 항상 노출하는 44px 버튼 목록, `aria-pressed`, 선택 공연장의 Google Maps·일정 링크를 추가했다. 지도 fallback에서도 목록이 유지된다. 단계 타입·린트 PASS.
- 2단계 탭 계약: profile/live 탭에 roving `tabIndex`, 좌우·상하·Home·End 자동 활성화와 포커스 이동을 추가하고 기존 URL·popstate 계약을 유지했다. 단계 타입·린트 PASS.
- 3단계 초기 복원: 페이지 `searchParams` Promise로 초기 탭을 정해 `?tab=live`의 서버 HTML부터 live 패널을 렌더한다. 기본 URL은 profile만 렌더하며 잘못된 값 정규화는 기존 effect가 유지한다. 타입·린트·파라미터 PASS; HTTP 본문은 live URL `livePanel=1/profilePanel=0/venueList=1`, 기본 URL `livePanel=0/profilePanel=1`.
- 4단계 현행 수치: 후보 58건, 공개 연결 45·상세 부족 3·분류 대기 2·제외 4·검토 가능 4로 검증기·보고서·인수인계를 동기화했다.
- 실브라우저 axe에서 새 공연장 목록의 역할 누락을 발견해 `role=group`으로 보완하고, 기존 제품 탐색의 역할 없는 `aria-label`도 `nav` 랜드마크로 교정했다. 재검사 결과 WCAG 2 A/AA 확정 위반 0, ARIA incomplete 0이다. 이미지·그라데이션으로 자동 판정할 수 없는 색 대비 42개는 incomplete 1종으로 남아 수동 시각 검토 대상이다.
- 최종 검증: 데이터 37그룹·188명·70공연·58후보, 일본 날짜, 37그룹 파라미터, TypeScript, ESLint 0경고, 774/774 프로덕션 빌드 PASS. 대표 HTTP `/ko` 200, `/ja/live` 200, 의도된 missing live 404. React 품질 점검에서 기존 MapLibre 동적 import, 직접 import, effect 정리, memoized venue 계산을 유지했고 새 waterfall·불필요한 effect·대형 의존성을 추가하지 않았다.

## 2026-09-01 Release R-2 — 현행 인수인계 최종 동기화

- 변경 범위: `CODEX_HANDOVER.md`의 브랜치 HEAD와 배포 기준 SHA를 분리하고, 배포 결과 문서 커밋 `938c995`까지 원격 동기화된 상태를 기록한다. 제품 런타임과 Vercel 배포는 바꾸지 않는다.
- 완료 조건·검증: `codex/trust-data-phase2`와 origin 동기화, Git HEAD `938c995`, 배포 코드 `3cb2234`, production deploy `dpl_6gGN5AxRSeiY6d1QjGvc51EnsGsH`가 모순 없이 표시되고 관련 키워드 검색과 `git diff --check`가 통과한다.
- 구현·검증 결과: `CODEX_HANDOVER.md`에서 Git HEAD `938c995`와 프로덕션 배포 코드 SHA `3cb2234`를 별도 필드로 분리하고, origin 동기화 상태와 문서 전용 차이를 명시했다. 관련 SHA·배포 ID 검색과 `git diff --check`가 통과했다. 이번 요청으로 수정한 계획·작업지시·인수인계 3개 문서는 아직 커밋·푸시하지 않은 로컬 문서 변경으로 남긴다.

## 2026-09-01 Release R-1 — 빌드·커밋·푸시·프로덕션 배포

- 변경 범위: 전체 현행 작업 트리 중 제품 코드·데이터·자산·운영·문서를 릴리스한다. `.tmp`, 빌드 산출물, Vercel 로컬 연결 정보와 환경 파일은 커밋 대상에서 제외하고 `.gitignore`에 `.tmp`를 명시한다.
- 데이터·UI·명령 연결: 검증된 데이터·UI·자동화 → 로컬 production build → Git 커밋·현재 브랜치 upstream push → 연결된 Vercel 프로젝트 production deploy → 공개 URL HTTP·본문 스모크 → 현행 인수인계의 SHA·URL·검증 기준선으로 연결한다.
- 실패·빈 상태: 검사 실패 시 릴리스 중단, 원격 분기 시 강제 푸시 금지, Vercel ERROR/BUILDING이면 완료 처리 금지, 공개 URL 4xx/5xx 또는 핵심 본문 누락 시 배포 로그를 확인하고 기존 프로덕션을 보존한다.
- 회귀 위험: `.tmp` 로그·보고서나 자격정보가 커밋되는 문제, 큰 누적 변경에서 신규 파일 누락, Git 푸시와 Vercel 직접 배포가 다른 커밋을 가리키는 문제, 배포 성공을 실제 공개 동작 성공으로 오인하는 문제가 있다.
- 완료 조건·검증: 커밋 대상 목록과 비밀 패턴을 확인하고 lint·typecheck·데이터·날짜·파라미터·build·diff 검사를 통과한다. 커밋·푸시 후 동일 SHA 기반 production 배포가 READY이며 핵심 공개 URL을 검증하고 결과를 이 항목과 `CODEX_HANDOVER.md`에 기록한다.
- 구현·검증 결과: 릴리스 전 검사 완료. `.tmp`·`.next`·`.vercel`·환경 파일은 제외됐고 신규 파일·10MB 초과 자산·대표 비밀 패턴 검사에 이상이 없다. ESLint 0경고, TypeScript, 데이터 계약(37그룹·188명·70공연·55후보·경고 24), 일본 날짜, 파라미터 계약, 774/774 프로덕션 빌드와 `git diff --check`가 통과했다. 커밋·푸시·배포 식별자와 공개 HTTP 결과는 실행 후 이 항목과 현행 인수인계에 추가한다.
- 배포 중 설계 변경: 커밋 `3bd93ea` 푸시 후 첫 production deploy `dpl_6fV9NMeYDR6njWQhBK694hYQqekq`는 READY·production alias 연결에 성공했지만 Vercel CLI가 391.5MB·2175파일을 업로드했다. Git의 `.tmp` 제외만으로 배포 입력 제외가 보장되지 않으므로 `.vercelignore`를 추가하고, 최종 인수인계 커밋을 푸시한 다음 더 작은 입력으로 production을 재배포·검증한다.
- 최종 구현·배포 결과: 전체 구현 커밋 `3bd93ea`와 Vercel 입력 최소화 커밋 `3cb2234`를 `origin/codex/trust-data-phase2`에 푸시했다. `3cb2234` 기준 최종 production deploy `dpl_6gGN5AxRSeiY6d1QjGvc51EnsGsH`는 READY이며 `https://chika-idol-hub.vercel.app`에 alias됐다. `.vercelignore` 적용 후 입력은 391.5MB·2175파일에서 362.3KB·148파일로 감소했고 원격 빌드도 774/774를 통과했다. 공개 스모크는 한국어 홈·일본어 라이브·영어 그룹 live 탭·한국어 멤버 4경로 HTTP 200, 의도된 미존재 공연 404로 5/5 PASS다. 최근 1시간 error 로그 조회는 0건이었다. pnpm이 `esbuild`·`sharp`·`unrs-resolver` 설치 스크립트를 무시했다는 경고는 있었으나 로컬·Vercel 빌드와 배포 산출물 생성은 모두 성공했다.

## 2026-08-31 MVP H-5 — 전체 기능·파라미터 정합성 수정·보완

- 변경 범위: 공통 navigation/geo 계약, 홈 지도, 그룹 상세 탭, 라이브 탐색 필터·상태, 그룹·멤버 지표 스키마와 데이터 검증, package lint·회귀 검사, 제품·운영 문서를 수정한다.
- 데이터·UI·명령 연결: `RegionId/DistrictId`와 소속 매트릭스 → 그룹 데이터·홈 URL 복원 → 지도 단계, 라이브 search params → 필터 state·공유 URL, 지표 값 → provenance → 랭킹 노출, 공연 status → 필터·상태 라벨, package scripts → 비대화형 CI 검증으로 연결한다.
- 실패·빈 상태: 불일치 지역/구역은 도시 단계로, 잘못된 탭은 profile로, 잘못된 라이브 필터는 `all`로 정규화한다. 취소 공연은 예정 목록에서 제외하고 전체 보기에서 취소로 표시한다. 지표 값 또는 provenance가 불완전하면 랭킹에 포함하지 않는다.
- 회귀 위험: URL 갱신 루프, locale·다른 query 손실, 뒤로가기 불일치, 기존 레거시 지표 삭제 과정의 사용자 데이터 손상, ESLint가 기존 코드 스타일 전체를 차단하는 문제가 있다.
- 완료 조건·검증: 파라미터 단위 검사, 데이터 검사, TypeScript, ESLint CLI, 프로덕션 빌드, locale별 홈·그룹·라이브·잘못된 URL HTTP 확인, `git diff --check`를 수행하고 실제 결과를 기록한다.
- 구현·검증 결과: 지역↔구역 공통 계약과 교차 소속 검증을 추가해 `osaka+shibuya` 같은 조합은 도시 단계로 정규화한다. 홈 지도에서 사용하지 않던 공연·공연장·공간 props를 제거했고, 그룹 탭과 라이브의 mode/date/region/group 상태를 URL·뒤로가기에 연결했다. 취소 공연은 예정 보기에서 제외하며 매진·취소·연기를 라벨로 표시한다. 출처·확인일 없는 그룹 111개 필드와 멤버 752개 레거시 랭킹 필드를 제거하고 스키마 기본 점수 주입을 폐지했으며, 불완전 지표를 데이터 오류로 승격했다. `next lint`를 ESLint 9 비대화형 명령으로 교체하고 `verify:parameters`를 추가했다. 2026-09-01 기준 ESLint 0경고, TypeScript, 데이터 검사(37그룹·188명·70공연·55후보·경고 24), 일본 날짜·파라미터 계약, 774/774 프로덕션 빌드, 홈·라이브·그룹 HTTP 200, 브라우저의 잘못된 지도 URL 정규화, `git diff --check`가 통과했다. Windows PowerShell 5.1 오프라인 진입점과 실제 바탕화면 CMD write도 종료 코드 0·`success=true`로 재검증했고 로그 `20260901-000333.log`, `20260901-000401.log`를 남겼다. 커밋·배포·외부 문의·자동 공개는 수행하지 않았다.

## 2026-08-31 MVP 5-A-1 — 바탕화면 수동 갱신기 진단·복구

- 변경 범위: `scripts/run-daily-update.ps1`, 바탕화면 `Chika Idol Box 일일 업데이트.cmd`, `DAILY_UPDATE.md`, 제품 계획·작업지시·현행 인수인계를 수정한다. 후보 수집기와 공개 데이터 계약은 바꾸지 않는다.
- 진단 근거: 기존 `.cmd`는 UTF-8 BOM 없이 LF 줄바꿈이며 직접 `daily-update.ts --skip-network`와 `--write`는 모두 PASS했다. 따라서 핵심 수집 파이프라인이 아니라 더블클릭 진입점의 Windows 호환성, 영구 로그 부재, 보고서 성공 확인 부재, 테스트 가능한 무정지 모드 부재를 주원인으로 본다.
- 데이터·UI·명령 연결: 바탕화면 CMD → 저장소 PowerShell 진입점 → Node/tsx → `daily-update.ts --write` → 후보 JSON·날짜별 Markdown·`latest-report.json` → 실행 로그와 사용자 성공/실패 안내로 연결한다.
- 실패·빈 상태: 저장소·Node·tsx 누락, 자식 프로세스 비정상 종료, 최신 보고서 미생성·파싱 실패·`success=false`를 각각 비정상 종료로 처리한다. 실패 시 기존 후보·보고서를 삭제하지 않는다.
- 회귀 위험: 배치 인코딩/줄바꿈으로 한글 경로가 깨지거나, 오래된 `latest-report.json`을 새 성공으로 오인하거나, `pause` 때문에 자동 검증이 멈추거나, 네트워크 부분 실패를 성공으로 표시할 수 있다.
- 완료 조건·검증: 직접 오프라인/write, PowerShell `-NoPause`, CMD `--no-pause`, 최신 보고서 타임스탬프·성공 판정, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 확인하고 실제 결과를 기록한다.
- 재현 결과·설계 변경: 새 PowerShell 진입점의 첫 오프라인 실행에서 자식 Node 단계와 보고서 생성은 PASS했으나 `Get-Content`가 BOM 없는 UTF-8 JSON을 Windows 기본 ANSI로 읽어 다국어 문자열을 깨뜨렸고 `ConvertFrom-Json`이 실패했다. 콘솔 네이티브 출력 인코딩과 JSON 읽기 인코딩을 UTF-8로 고정하고 동일 경로를 다시 검증한다.
- 구현·검증 결과: 저장소에 `scripts/run-daily-update.ps1`을 추가하고 바탕화면 CMD를 ASCII·CRLF 얇은 래퍼로 교체했다. 첫 오프라인 시험에서 Windows PowerShell 5.1이 BOM 없는 UTF-8 JSON을 ANSI로 읽어 정상 보고서를 파싱 실패하는 원인을 재현했고, 네이티브 입출력 및 `Get-Content -Encoding utf8` 계약으로 수정했다. 수정 후 PowerShell `-Offline -NoPause`와 실제 바탕화면 CMD `--no-pause` write 실행이 각각 종료 코드 0으로 통과했다. write 실행은 タイトル未定 17건과 FRUITS ZIPPER 38건을 갱신·보존해 전체 후보 55건을 유지했고, 네 단계와 최신 보고서 `success=true`가 모두 PASS했다. 실행 로그는 `.tmp/daily-update/launcher/20260831-225914.log`, `20260831-225929.log`에 남았다. 후보 보고 `45/3/2/4/1`, 이미지 후보 `3/2/0/1`, 데이터 검사 `37그룹 / 188명 / 70공연 / 55후보 / 경고 39`, TypeScript, 774/774 프로덕션 빌드, `git diff --check`가 통과했다. 기존 LF→CRLF 경고만 남았으며 커밋·배포·외부 문의·자동 공개는 수행하지 않았다.

## 2026-08-31 MVP H-4 — 전체 Markdown 정합성 수정·보완

- 변경 범위: 제품 계획과 작업지시서에 실행 설계를 먼저 기록한 뒤 `AGENTS.md`, `IMAGE_PERMISSION_REQUESTS.md`, `AUDIT_AND_REBUILD_PLAN.md`, `CODEX_HANDOVER.md`의 문서 권위·상태·검증 표현을 동기화한다.
- 데이터·UI·명령 연결: 데이터와 UI는 바꾸지 않는다. 문서 진입 순서를 현행 계획→실행→인수인계→조건부 주제 이력으로 고정하고, 기본 정적 검증과 후보·운영·HTTP 확장 검증의 적용 조건을 연결한다.
- 실패·빈 상태: 역사 문서는 삭제하지 않고 명시적 배너와 현행 문서 링크를 유지한다. 문의 후보는 권리 승인도 발송 대기도 아닌 `내부 권리 검토` 상태로 보존하며, 별도 사용자 승인 없이는 외부 전송하지 않는다.
- 회귀 위험: 과거 기록의 수치를 현행으로 다시 승격하거나, `문의 준비`를 발송 승인으로 해석하거나, 기본 4개 검사만으로 운영·인수인계 완료를 주장할 수 있다.
- 완료 조건·검증: 기준일과 H 상태, 문서 우선순위, 이미지 문의 상태, 기본/확장 검증 계약이 서로 모순되지 않는다. 관련 키워드·문서 참조 전수 검색과 `git diff --check` 결과를 아래 구현·검증 결과에 기록한다.
- 구현·검증 결과: `AGENTS.md`의 권위 순서를 현행 계획→최신 작업지시→현행 인수인계→조건부 주제 이력으로 교정하고, 역사 배너 문서가 현행 기준을 덮지 못하도록 명시했다. `IMAGE_PERMISSION_REQUESTS.md`의 두 `문의 준비` 상태를 `발송 중단 · 내부 권리 검토`로 바꾸고 발송 승인 문서가 아님을 상단에 고정했다. 제품 계획·작업지시의 기준일을 2026-08-31로, MVP H를 H-1~4 완료로 동기화했으며 기본 4개 검사와 후보·운영·HTTP 확장 검사, 문서 전용 검증의 적용 조건을 분리했다. 관련 상태 키워드 전수 검색에서 남은 `H-1 완료`는 날짜가 고정된 과거 작업 결과 1건뿐이며, 참조된 실제 Markdown 문서가 모두 존재함을 확인했다. `git diff --check`는 오류 없이 통과했고 기존 LF→CRLF 경고만 남았다. 코드·데이터 변경이 없는 문서 작업이므로 타입 검사와 빌드는 재실행하지 않았다.

## 2026-08-31 MVP H-3 — 현행 인수인계 정합성 갱신

- 변경 범위: `CODEX_HANDOVER.md`만 현행 상태로 수정하고, `AUDIT_AND_REBUILD_PLAN.md`·`WORK_ORDER.md`에는 이번 갱신 의도와 검증 결과를 기록한다.
- 데이터·UI·명령 연결: 현재 데이터 집계와 후보 버킷, 이미지 권리 큐, H-2 HTTP 스모크, package 스크립트를 인수인계의 기준선·재현 명령·다음 순서에 연결한다.
- 실패·회귀 위험: 완료된 그룹을 다시 미완료로 표시하거나, 제외 후보 종류를 잘못 단일화하거나, 로컬 검증을 프로덕션 배포 검증으로 오인할 수 있다.
- 완료 조건·검증: 현행 수치 37그룹·188명·70공연·55후보·생일 미확인 28명과 후보 `45/3/2/4/1`, 이미지 `0/11/26`, H-2 `28/28`을 보존한다. 후보 보고·오프라인 일일 실행·데이터 검사·`git diff --check`를 통과한다.
- 구현·검증 결과: `CODEX_HANDOVER.md`의 낡은 エイアイカ 잔여 참조를 수정하고 완료 상태 H-1~3, 재현 가능한 후보·이미지·데이터 검사 명령, 제외 분류, H-2 28/28, MVP G 승인 대기를 동기화했다. 공연 후보 보고 `45/3/2/4/1`, 이미지 후보 `전체 3 / 권리 검토 2 / 승인 0 / 거절 1`, 오프라인·데이터 검사 `37그룹 / 188명 / 70공연 / 55후보 / 생일 미확인 28명 / 경고 39`와 `git diff --check`가 통과했다. 기존 LF→CRLF 경고만 남았다.

## 2026-08-31 MVP H-2 — 로컬 프로덕션 HTTP 스모크 검증

- 변경 범위: `next start --port 3001`로 현재 프로덕션 빌드를 실행하고 핵심 다국어 URL을 읽기 전용 GET으로 확인한다. 검증 결과 외 코드·데이터 변경은 없다.
- 데이터·UI·명령 연결: 홈→그룹→멤버, 라이브 목록→C/E-13 공연 상세 경로가 실제 서버 라우팅·정적 산출물·데이터 조회와 연결되는지 대조한다.
- 실패·빈 상태: 포트 점유 시 소유 프로세스를 확인하고 이번 검증에서 시작한 서버만 종료한다. 지도나 외부 링크의 네트워크 상태는 범위 밖이며 404 경로는 의도된 실패 상태로 확인한다.
- 회귀 위험: 빌드 성공과 런타임 성공을 혼동하거나, 한 언어·한 엔티티만 확인해 다국어 경로 전체를 과대 주장할 수 있다.
- 완료 조건·검증: 3개 언어 홈·라이브, 대표 그룹·멤버, C/E-13 공연 5건의 3개 언어 경로가 200을 반환하고 존재하지 않는 경로가 404를 반환한다. 응답 본문 식별자와 서버 종료까지 확인한다.
- 구현·검증 결과: 정상 27경로는 HTTP 200과 언어별 그룹·멤버·공연 명칭을 모두 포함했고 의도된 잘못된 경로 1건은 404로 총 28/28 통과했다. 첫 실행의 12건 마커 실패는 한국어·영어 HTML에서 일본어 `タイトル未定`를 찾은 검증 가정 오류였으며 상태 코드는 모두 200이었다. 데이터의 언어별 명칭으로 마커를 교정해 재검증했다. PID 69916만 종료했고 3001 포트 LISTENING 해제를 확인했다.

## 2026-08-31 MVP C/E-14 — タイトル未定 공식 ICS 후보 갱신

- 변경 범위: `npm run data:collect:title-mitei:write`로 공식 ICS의 변경분을 격리 후보 파일에 반영하고 기존 공개 공연 링크·이력 보존 상태를 대조한다.
- 데이터·UI·명령 연결: 수집기는 후보만 갱신하며 공개 UI는 `data/chika-live.json`만 읽는다. 이후 보고 명령으로 다섯 미완결 후보의 상세·분류 버킷 변화를 확인한다.
- 실패·빈 상태: 네트워크 실패 시 기존 후보 파일을 보존한다. 장소·티켓·시간이 비어 있거나 제목이 임시값이면 공개 승격하지 않는다.
- 회귀 위험: 재수집이 수동 검토 상태·공개 연결 ID·제외 이력을 덮어쓸 수 있다. 후보 수와 각 기존 `publishedEventId`, `reviewStatus`를 검증한다.
- 완료 조건·검증: 후보 갱신과 상태 대조 후 후보 보고, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 빌드, `git diff --check`를 통과한다.
- 구현·검증 결과: 공식 ICS 17건(현재 15·이력 2)을 `data/chika-live-candidates.json`에 갱신했다. 미완결 5건의 내용과 상태는 그대로이며 공개 승격 0건, 전체 후보 55건, `published 45 / needs_details 3 / needs_classification 2 / excluded 4 / reviewable 1`이다. 오프라인 일일 실행·데이터·타입 검사와 774/774 프로덕션 빌드가 통과했다. 최초 결합 검증에서 타입 검사 프로세스가 무출력 정지해 세션만 중단한 뒤 독립 명령으로 재실행하여 exit 0을 확인했다.

## 2026-08-31 MVP F-25 — 잔여 공식 생일 근거 재감사

- 변경 범위: 공개 데이터는 공식 생일 원문이 확보된 경우에만 수정한다. 이번 실행은 `AUDIT_AND_REBUILD_PLAN.md`, `WORK_ORDER.md`, `CODEX_HANDOVER.md`의 조사 상태와 차단 사유를 동기화한다.
- 데이터·UI·명령 연결: MyDearDarlin’ 7명, Mirror,Mirror 6명, Sweet Alley 7명, ドラマチックレコード 4명, yosugala 4명의 `birthDate`·`birthMonthDay` 빈 상태와 공식 출처 계약을 대조한다. 스키마·UI·자동화 명령은 변경하지 않는다.
- 실패·빈 상태: 공식 페이지가 로딩 실패하거나 그룹 재적만 제공하면 생일은 계속 빈 상태다. 검색 결과·행사 제목·SNS 사용자명·2차 매체의 날짜는 반영하지 않는다.
- 회귀 위험: 조사 완료를 데이터 완결로 오인하거나, 공식 그룹 페이지의 멤버 명단을 개인 생일 근거로 과대 해석할 수 있다. 생일 미확인 수 28명과 기존 provenance를 보존한다.
- 완료 조건·검증: 공식 1차 근거 가용성 결론을 기록하고 후보 보고, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 통과한다.
- 구현·검증 결과: MyDearDarlin’ 로컬 공식 출처에는 생일 증거가 없고, BIG ISLAND RECORDS 공식 그룹 프로필은 ドラマチックレコード의 현역 명단만 제공함을 확인했다. Sweet Alley·Mirror,Mirror·yosugala도 반영 가능한 개인 공식 생일 원문이 없어 공개 데이터를 변경하지 않았다. 후보 보고 `published 45 / needs_details 3 / needs_classification 2 / excluded 4 / reviewable 1`, 오프라인 일일 실행(37그룹·188명·70공연·55후보·미확인 생일 28명), 데이터·타입 검사, 774/774 프로덕션 빌드, `git diff --check`가 통과했다. 기존 LF→CRLF 경고만 남았다.

## 2026-08-31 MVP F-24 — Sweet Alley 笹木里緒菜 단일 소배치

- 변경 범위: `data/chika-groups.json`의 `sweet-sasaki-riona`에 소속사 공식 완전 생년월일·출신지와 직접 provenance를 반영한다.
- 데이터·UI 연결: 기존 `birthDate`·`birthplace` 계약으로 생일 캘린더·멤버 상세·누락 집계에 연결하며 새 UI·스키마·명령은 만들지 않는다.
- 실패·회귀 위험: 공식 프로필의 부가 정보와 이미지는 범위 밖이다. 검색 결과·생탄제·팬 데이터만 확인되는 다른 7명은 함께 반영하지 않는다.
- 완료 조건·검증: 생일 미확인 28명 및 후보 보고, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 통과.
- 구현·검증 결과: 笹木里緒菜에 소속사 공식 완전 생년월일·출신지를 반영해 미확인 생일 28명·향후 30일 18명을 확인했다. 후보·오프라인·데이터·타입 검사와 프로덕션 빌드 774/774가 통과했다.

## 2026-08-31 MVP F-23 — INUWASI 공식 개인 프로필 6인 소배치

- 변경 범위: `data/chika-groups.json`의 INUWASI 6인에 개인 공식 페이지의 월·일 생일과 직접 provenance를 반영한다.
- 데이터·UI 연결: 기존 `birthMonthDay` 계약으로 생일 캘린더·멤버 상세·누락 집계에 연결하며 스키마와 UI는 변경하지 않는다.
- 실패·회귀 위험: 메인 홈은 현재 5인만 노출하지만 `/member/` 인덱스, 六椛 개인 페이지, 2026년 5~7월 공식 행사 자료는 6인을 확인한다. 이 소배치에서는 상충하는 활동 상태를 추정하지 않고 기존 6인 현역 명단을 보존한다.
- 완료 조건·검증: 생일 미확인 29명 및 후보 보고, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 통과.
- 구현·검증 결과: 6인의 개인 공식 생일 URL과 월·일을 반영해 미확인 생일 29명·향후 30일 18명을 확인했다. 후보·오프라인·데이터·타입 검사 및 프로덕션 빌드 774/774가 통과했다.

## 2026-08-31 MVP F-22 — エイアイカ 공식 프로필 3인 소배치

- 변경 범위: `data/chika-groups.json`의 エイアイカ 현역 3인에 공식 월·일 생일과 출신지를 반영하고 그룹·개인 provenance를 현행 공식 사이트와 2026-08-31 확인일로 갱신한다.
- 데이터·UI 연결: 기존 `birthMonthDay`·`birthplace` 계약을 통해 생일 캘린더·멤버 상세·누락 집계에 연결한다. 새 UI·스키마·명령은 만들지 않는다.
- 실패·회귀 위험: 이전 팬클럽 프로필과 현행 공식 사이트의 동일 3인 명단을 대조한다. 출생연도·혈액형·색상·이미지는 범위 밖이며 월·일을 완전 생년월일로 확장하지 않는다.
- 완료 조건·검증: 생일 미확인 35명 및 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 통과.
- 구현·검증 결과: 현역 3인의 공식 월·일 생일과 출신지를 반영해 미확인 생일 35명·향후 30일 18명을 확인했다. 후보·오프라인·데이터·타입 검사와 프로덕션 빌드 774/774가 통과했다.

## 2026-08-31 MVP C/E-13 — タイトル未定 공식 공연 5건 공개 승격

- 변경 범위: 후보 5건에 공식 행사명·시간·장소·가격·티켓 URL을 기록하고, 각각 고유 공개 공연 ID에 연결한다. `data/chika-live-candidates.json`, `data/chika-live.json`만 구현 대상으로 삼는다.
- 데이터·UI·명령 연결: 기존 후보 `publishedEventId` 계약과 다국어 공연 상세·필터를 재사용한다. Spotify O-EAST는 기존 공연장 ID를 재사용하고 나머지 장소는 좌표를 추정하지 않은 지역 라벨로 표시한다.
- 실패·빈 상태: 행사 전체 OPEN/START와 그룹별 출연 시각을 구분한다. フクフェス는 그룹이 출연하는 9/20 2부의 17:30/18:00을 대표 시각으로 기록하고 `endsOn` 9/21 및 양일 일반 가격을 함께 명시한다. 무료 행사도 유료 우선 구역과 일반 무료를 함께 표시한다.
- 회귀 위험: 같은 이름의 9/25 LadySteady를 9/7 행사와 혼동하지 않고, 공식 근거가 불완전한 나머지 5건은 변경하지 않는다. 동적 판매 상태·포스터·미확인 좌표는 저장하지 않는다.
- 완료 조건·검증: 공개 공연 70건·공개 연결 45건·상세 미완결 3건·분류 미완결 2건을 확인하고 후보 보고, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행한다.
- 구현·검증 결과: 공식 근거가 완결된 5건을 공개 연결했다. 후보 보고는 공개 연결 45·상세 미완결 3·분류 미완결 2·검토 가능 1·제외 4, 오프라인·데이터·타입 검사 PASS, 프로덕션 빌드 774/774 PASS다. HTTP 실실행은 하지 않았고 각 공연의 3개 언어 정적 상세 경로 생성을 확인했다.

## 2026-08-31 MVP C/E-12 — TGC·IRC 공개 승격 및 사진집 2건 제외

- 후보 `fruits-zipper-4044f50e80314991`는 FRUITS ZIPPER 공식 상세과 TGC 공식 티켓 페이지에서 FRUITS ZIPPER 무대, OPEN 12:00 / START 14:00, 일반 지정석 12,500엔을 대조한다.
- 후보 `fruits-zipper-a08ec28244cc3f3b`는 공식 상세과 IRC 공식 페이지에서 `LIVE Performance STAGE`, OPEN 11:00 / START 12:00, 일반 지정석 8,500엔을 대조한다.
- 사진집 후보 `fruits-zipper-f11c6c8d7b0d4091`·`fruits-zipper-3228d3ddfb00c4f3`는 원문과 날짜를 보존한 채 `release_or_media`·`rejected`로 전환한다.
- 실패·회귀 위험: 행사 전체 시각과 그룹 개별 무대 시각을 구분하고, 현재 SOLD OUT 같은 동적 판매 상태는 저장하지 않는다. 해외 ASOBIEXPO는 `review_pending`으로 유지한다.
- 완료 조건·검증: 공개 공연 65건·공개 연결 40건·검토 가능 1건·제외 4건 및 후보·오프라인·데이터·타입·빌드·diff PASS.
- 구현·검증 결과: TGC·IRC에 행사 전체 시간·장소·일반 지정석 가격·공식 티켓 링크를 연결하고 사진집 2건은 `release_or_media`·`rejected`로 보존했다. 공개 공연 65건·공개 연결 40건·검토 가능 1건·제외 4건, 후보·오프라인·데이터·타입 PASS, 빌드 759/759 PASS. HTTP는 미실행이고 3개 언어 정적 경로를 확인했다.

## 2026-08-31 MVP C/E-11 — KAWAII LAB. COLLECTION 1건 공개 승격

- 후보 `fruits-zipper-8bef148efc9a7bb6`를 FRUITS ZIPPER 공식 상세 `44011`과 KAWAII LAB 공식 공지 `87217`의 날짜·시간·출연·장소·지정석 11,000엔·티켓 안내에 대조한다.
- `fukuoka` 지역과 좌표 없는 공연장 계약을 재사용한다. 포스터·판매 상태·업그레이드 좌석 구매 가능 여부는 추가하지 않는다.
- 실패·회귀 위험: 라이브 퍼포먼스 포함 결합 이벤트라는 공식 성격을 유지하고 일반 패션 행사로 확대 해석하지 않는다.
- 완료 조건·검증: 공개 공연 63건·공개 연결 38건·검토 가능 5건 및 후보·오프라인·데이터·타입·빌드·diff PASS.
- 구현·검증 결과: 후보와 KAWAII LAB 공식 공지를 공개 연결했다. 공개 공연 63건·공개 연결 38건·검토 가능 5건, 후보·오프라인·데이터·타입 PASS, 빌드 753/753 PASS. 부분 적용으로 후보만 먼저 변경된 상태를 감지해 공개 레코드를 보완한 뒤 전체 계약을 재검증했다. HTTP는 미실행이고 3개 언어 정적 경로를 확인했다.

## 2026-08-30 MVP C/E-10 — 공식 페스티벌 2건 공개 승격

- 후보 `fruits-zipper-39e61ded59b9529b`와 `fruits-zipper-a1f6c313259b334c`를 출연 공식 상세 및 행사 공식 공연·티켓 페이지에 대조한다.
- TOMAKOMAI는 전체 행사 OPEN 09:30 / START 11:00과 9/12 일반 1일권 10,900엔, ROCK IN JAPAN은 전체 행사 OPEN 08:30 / START 11:45과 9/13 1일권 16,000엔을 사용한다.
- 실패·회귀 위험: 그룹 개별 무대 시각은 확인되지 않았으므로 행사 전체 시각임을 가격 문구와 공식 원문 연결로 보존한다. 판매 상태·좌표·포스터는 추가하지 않는다.
- 완료 조건·검증: 공개 공연 62건·공개 연결 37건·검토 가능 6건을 확인하고 후보·오프라인·데이터·타입·빌드·diff 검사를 실행한다.
- 구현·검증 결과: TOMAKOMAI와 ROCK IN JAPAN 2건을 공식 행사·티켓 원문으로 공개 연결했다. 공개 공연 62건·공개 연결 37건·검토 가능 6건, 후보·오프라인·데이터·타입 PASS, 빌드 750/750 PASS. HTTP는 미실행이고 3개 언어 정적 경로를 확인했다.

## 2026-08-30 MVP C/E-9 — KAWAII MANAFY LAND 1건 공개 승격

- 후보 `fruits-zipper-769e3b2ec19cd4ca`를 공식 상세 `44246`과 공식 판매 페이지의 4부 시간·장소·각 8,000엔·신청 정보에 대조한다.
- 공개 레코드는 후보가 제공한 1부 OPEN/START를 대표 시각으로 사용하고, 전체 회차는 공식 원문 링크에서 확인하게 한다. 좌표·포스터·판매 상태는 추가하지 않는다.
- 실패·회귀 위험: 개인 멤버 출연 이벤트임을 그룹 전체 단독 공연처럼 확장하지 않으며, 공식 상세 제목의 `(真中)` 표기를 보존한다. 사진집·온라인 특전회 등 다른 유형은 묶어 승격하지 않는다.
- 완료 조건·검증: 공개 공연 60건·공개 연결 35건·검토 가능 8건을 확인하고 후보 보고·오프라인 일일 실행·데이터 검사·타입 검사·프로덕션 빌드·`git diff --check`를 실행한다.
- 구현·검증 결과: 후보와 공식 판매 페이지를 연결해 공개 공연 60건·공개 연결 35건·검토 가능 8건을 확인했다. 후보·오프라인·데이터·타입 PASS, 빌드 744/744 PASS. HTTP 실행은 생략했고 3개 언어 정적 경로 생성을 확인했다.

## 2026-08-30 MVP C/E-8 — 미에 공식 투어 1건 공개 승격

### 범위·연결

- 후보 `fruits-zipper-3d2d029cab6c7e19`를 공식 일정 상세 `40029`와 공식 투어 특설 페이지의 날짜·시간·장소·가격·티켓 정보에 대조해 고유 공개 ID에 연결한다.
- 기존 다국어 라이브 상세 계약을 재사용하고 후보의 공식 원문 감사 연결을 보존한다.

### 실패·빈 상태·회귀 위험

- 검증 좌표가 없으므로 `venueId: null`과 기존 `other` 지역을 사용한다. 포스터·판매 상태는 비워 두며 공식 페이지 조회 실패를 임의 데이터로 메우지 않는다.
- 사진집·패션·해외 행사 등 현재 공개 라이브 포함 정책과의 경계가 필요한 다른 후보는 이번 실행 범위에서 변경하지 않는다.

### 완료 조건·검증

- 공개 공연 59건·공개 연결 34건·검토 가능 9건을 확인한다.
- `data:review:live-candidates`, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행하고 실제 결과를 기록한다.
- 구현·검증 결과: 공식 투어 페이지에서 미에 후보의 날짜·시간·장소·지정석 8,000엔·티켓 안내를 재확인해 공개 연결했다. 후보 34건 공개·9건 검토 가능, 공개 공연 59건, 오프라인 일일 실행·데이터·타입 PASS, 빌드 741/741 PASS. HTTP 실행은 생략했고 빌드에서 각 언어 정적 경로 생성을 확인했다.

## 2026-08-30 MVP C/E-7 — 히로시마·삿포로·오키나와 5건 공개 승격

- 후보 5건을 공식 상세와 대조해 고유 공개 ID에 연결하고 기존 라이브 UI·후보 보고 계약을 재사용한다.
- 좌표·포스터·판매 상태는 비워 두며 도쿄 `40046`·`40047`은 이미 공개 연결돼 변경하지 않는다.
- 공개 공연 58건·공개 연결 33건·검토 가능 10건을 확인하고 후보·오프라인·데이터·타입·빌드·diff 검사를 실행한다.
- 구현·검증 결과: 5건을 공식 상세와 공개 연결했다. 공개 공연 58건·공개 연결 33건·검토 가능 10건, 후보·오프라인·데이터·타입 PASS, 빌드 738/738 PASS. HTTP 실행은 생략했고 빌드에서 각 언어 정적 경로 생성을 확인했다.

## 2026-08-30 MVP C/E-6 — 다카마쓰·마쓰야마·후쿠오카·구마모토 공개 승격

- 공식 상세 `40039`~`40042`와 후보 4건의 날짜·시간·공연장을 대조해 고유 공개 ID에 연결한다.
- 지정석 8,000엔과 공식 투어 안내만 반영하고 좌표·포스터·판매 상태는 비워 둔다.
- 공개 공연 53건·공개 연결 28건·검토 가능 15건을 확인하고 후보·오프라인·데이터·타입·빌드·diff 검사를 실행한다.
- 구현·검증 결과: 4건을 공식 상세와 공개 연결했다. 공개 공연 53건·공개 연결 28건·검토 가능 15건, 데이터·후보·타입 PASS, 빌드 723/723 PASS.

## 2026-08-30 MVP C/E-5 — 가나자와 공식 투어 공개 승격

- 후보 `fruits-zipper-f38006882dad9ce3`를 공식 상세 `40038`과 대조해 고유 공개 ID에 연결한다.
- 공식 시간·공연장·지정석 8,000엔·투어 안내 링크만 반영하고 좌표·포스터·판매 상태는 비워 둔다.
- 공개 공연 49건·공개 연결 24건·검토 가능 19건을 확인하고 후보·오프라인·데이터·타입·빌드·diff 검사를 실행한다.
- 구현·검증 결과: 가나자와 후보를 공개 연결했다. 공개 공연 49건·공개 연결 24건·검토 가능 19건, 데이터·타입 PASS, 빌드 711/711 PASS.

## 2026-08-30 MVP C/E-4 — 공식 투어 5건 공개 승격

### 범위·연결

- 오사카 `40033`·`40034`, 우쓰노미야 `40035`, 센다이 `40036`·`40037` 후보를 공식 상세와 대조해 고유 공개 ID에 연결한다.
- 공식 시간·공연장·지정석 8,000엔·투어 안내 링크를 기존 라이브 UI에 전달한다.

### 실패·회귀 위험과 검증

- 좌표 근거가 없어 `venueId: null`로 두며 지역 enum만 기존 `osaka`/`other`를 사용한다. 포스터·판매 상태는 추정하지 않는다.
- 공개 공연 48건·공개 연결 23건·검토 가능 20건을 확인하고 후보 보고·오프라인·데이터·타입·빌드·diff 검사를 실행한다.

### 구현·검증 결과

- 오사카 2건·우쓰노미야 1건·센다이 2건을 공식 상세와 일대일 공개 연결했다.
- 후보 보고는 공개 연결 23건·검토 가능 20건, 오프라인·데이터 검사는 공개 공연 48건으로 PASS했다.
- 타입 검사와 프로덕션 빌드는 PASS이며 정적 페이지 708/708을 생성했다.
- `git diff --check` PASS. 기존 LF→CRLF 경고만 확인됐다.

## 2026-08-30 MVP C/E-3 — 기후·나고야 투어 2건 공개 승격

### 의도·범위와 연결

- 공식 상세 `40030`, `40032`와 후보 레코드의 날짜·시간·공연장을 대조하고 가격·티켓 안내를 보강한다.
- 후보 `fruits-zipper-a174c10b792b6717`, `fruits-zipper-c1c0a1e784a5d3e3`를 고유 공개 ID와 연결한다.

### 실패·빈 상태·회귀 위험

- 재조회 실패한 미에 `40029`는 변경하지 않는다. 두 공개 공연도 좌표 근거가 없어 `venueId: null`, `region: other`로 둔다.
- 판매 상태·포스터·좌표를 추정하지 않으며 공식 투어 안내만 티켓 정보 링크로 사용한다.

### 완료 조건·검증 방법

- 공개 공연 43건, 공개 연결 후보 18건, 검토 가능 후보 25건을 확인한다.
- 후보 보고, 오프라인 일일 실행, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 실행한다.

### 구현·검증 결과

- 기후·나고야 후보를 고유 공개 ID에 연결하고 공식 시간·장소·가격·티켓 안내를 반영했다. 미에 후보는 변경하지 않았다.
- 후보 보고는 공개 연결 18건·검토 가능 25건, 오프라인 실행·데이터 검사는 공개 공연 43건으로 PASS했다.
- TypeScript `--noEmit`과 프로덕션 빌드는 PASS이며 정적 페이지 693/693을 생성했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 확인됐다.

## 2026-08-30 MVP C/E-2 — 사이타마·마쓰도 공식 투어 공개 승격

### 의도·범위와 연결

- 공식 상세 `40027`, `40028`의 날짜·시간·공연장·가격을 후보와 공개 공연에 반영한다.
- 후보 `fruits-zipper-175796ce712b739f`, `fruits-zipper-14255cd2f71f5ac0`를 각각 고유 공개 ID와 연결한다.

### 실패·빈 상태·회귀 위험

- 공연장 좌표 근거를 이번 배치에서 확인하지 않으므로 `venueId: null`, `region: other`를 유지하고 지도 대신 일정 정보만 제공한다.
- 티켓 잔여·매진 여부, 포스터, 좌표를 추정하지 않는다. 공식 투어 안내를 티켓 정보 링크로 사용한다.

### 완료 조건·검증 방법

- 공개 공연 41건, 공개 연결 후보 16건, 검토 가능 후보 27건이 된다.
- 후보 보고, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, 프로덕션 빌드, `git diff --check`를 실행한다.

### 구현·검증 결과

- 사이타마·마쓰도 후보를 고유 공개 ID에 연결하고 공식 시간·공연장·가격·티켓 안내를 반영했다.
- 후보 보고는 공개 연결 16건·검토 가능 27건, 오프라인 실행·데이터 검사는 공개 공연 41건으로 PASS했다.
- TypeScript `--noEmit` PASS. 기존 `.next`에서는 최종 생성 단계가 무음 종료되어 `.tmp/next-before-ce2`로 보존 이동했고, 깨끗한 캐시 재빌드는 687/687 PASS했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 확인됐다.

## 2026-08-30 MVP C/E-1 — FRUITS ZIPPER 요코스카 공연 공개 승격

### 의도·범위와 연결

- 공식 상세 `40026`의 2026-09-03 공연을 후보 큐에서 공개 공연으로 단일 승격한다.
- OPEN 17:30 / START 18:30, 요코스카 예술극장 대극장, 지정석 8,000엔과 공식 투어 티켓 안내를 반영한다.
- 후보 `fruits-zipper-957a87a2b608c43b`와 공개 ID를 일대일 연결한다.

### 실패·빈 상태·회귀 위험

- 좌표가 검증되지 않았으므로 `venueId: null`, `region: other`로 두고 일정 목록은 유지하되 지도 핀은 표시하지 않는다.
- 포스터·판매 상태·좌석 잔여 여부는 추정하지 않는다. 수집기 재실행이 수동 `published` 연결을 보존해야 한다.

### 완료 조건·검증 방법

- 공개 공연 39건, 공개 연결 후보 14건, 검토 대기 후보 39건이 된다.
- 후보 보고, 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check`를 실행한다.

### 구현·검증 결과

- 후보를 공개 ID `fruits-zipper-tour-yokosuka-2026-09-03`과 연결하고 공식 시간·장소·가격·티켓 안내를 공개 공연에 반영했다.
- 후보 보고는 공개 연결 14건·검토 가능 29건, 오프라인 실행·데이터 검사는 공개 공연 39건으로 PASS했다.
- TypeScript `--noEmit`과 Next.js 프로덕션 빌드는 PASS이며 정적 페이지 681/681을 생성했다.
- `git diff --check`는 PASS이며 기존 작업 트리의 LF→CRLF 경고만 확인됐다.

## 2026-08-30 MVP F-21 — CYNHN 소속사 공식 생일 완결 소배치

### 의도·범위와 연결

- 디어스테이지 공식 개인 프로필의 綾瀬志希, 青柳透, 広瀬みのり 정보를 기존 공개 레코드와 대조한다.
- 공식 원문에 명시된 월·일 생일 `03-14`, `05-26`, `09-08`, 각 개인 공식 URL과 확인일만 반영한다.
- 기존 생일 캘린더·멤버 상세·누락 집계와 `birthMonthDay` 계약을 재사용한다.

### 실패·빈 상태·회귀 위험

- 출생연도·출신지·담당색·사진·SNS·취미·특기는 추가하지 않는다.
- 생탄제 개최일이나 2차 기사에서 생일을 추론하지 않으며, 기존 月雲ねる 레코드는 변경하지 않는다.

### 완료 조건·검증 방법

- CYNHN 현역 4인이 모두 소속사 공식 월·일 생일 근거를 가진다.
- 생일 미확인 현역이 41명에서 38명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- 綾瀬志希 `03-14`, 青柳透 `05-26`, 広瀬みのり `09-08`과 각 디어스테이지 개인 URL을 반영했다.
- 오프라인 일일 실행·데이터 검사는 PASS이며 미확인 생일 38명, 향후 30일 생일 17명이다.
- TypeScript `--noEmit`과 Next.js 프로덕션 빌드는 PASS이며 정적 페이지 678/678을 생성했다.
- `git diff --check`는 PASS이며 기존 작업 트리의 LF→CRLF 경고만 확인됐다.

## 2026-08-30 MVP F-20 — 月雲ねる 소속사 공식 생일 단일 소배치

### 의도·범위와 연결

- 디어스테이지 공식 아티스트 프로필의 月雲ねる 정보를 기존 공개 레코드와 대조한다.
- 공식 원문에 명시된 월·일 생일 `09-15`, 개인 공식 URL과 확인일만 반영한다.
- 기존 생일 캘린더·멤버 상세·누락 집계와 `birthMonthDay` 계약을 재사용한다.

### 실패·빈 상태·회귀 위험

- 출생연도·출신지·담당색·사진·SNS·취미·특기는 추가하지 않는다.
- 다른 CYNHN 멤버는 미확인 상태로 유지하고 생탄제 개최일을 생일로 변환하지 않는다.

### 완료 조건·검증 방법

- 月雲ねる가 월·일 생일과 소속사 공식 URL 근거를 가진다.
- 생일 미확인 현역이 42명에서 41명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- 月雲ねる `09-15`와 디어스테이지 개인 URL을 반영해 당시 미확인 생일이 41명으로 감소했다.
- 오프라인 일일 실행·데이터 검사·TypeScript `--noEmit`·Next.js 프로덕션 빌드가 PASS했고 정적 페이지 678/678을 생성했다.

## 2026-08-30 MVP F-19 — 高梨有咲 공식 출판사 프로필 단일 소배치

### 의도·범위와 연결

- 集英社 週プレNEWS 공식 프로필의 高梨有咲 정보를 기존 공개 레코드와 대조한다.
- 완전 생년월일 `2005-11-21`, 출신지 가나가와현, 담당색 노랑, 공식 출판사 URL과 확인일만 반영한다.
- 기존 생일 캘린더·멤버 상세·누락 집계와 공식 색상명 근사 계약을 재사용한다.

### 실패·빈 상태·회귀 위험

- 신장·특기·사진·SNS·그라비아 정보는 추가하지 않고 다른 ドラマチックレコード 멤버는 변경하지 않는다.
- 담당색 HEX는 `official_name_approximation`으로 명시한다.
- 동명이인 검색 결과나 생탄제 날짜를 근거로 사용하지 않는다.

### 완료 조건·검증 방법

- 高梨有咲가 완전 생년월일·출신지·담당색과 공식 출판사 URL 근거를 가진다.
- 생일 미확인 현역이 43명에서 42명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- 高梨有咲의 `2005-11-21`, 가나가와현, 노랑 담당색과 공식 출판사 URL을 반영해 당시 미확인 생일이 42명으로 감소했다.
- 오프라인 일일 실행·데이터 검사·TypeScript `--noEmit`·Next.js 프로덕션 빌드가 PASS했고 정적 페이지 678/678을 생성했다.

## 2026-08-30 MVP F-18 — 개인별 공식 근거 소배치·新居歩美

### 승인 정책·범위와 연결

- 사용자 승인에 따라 그룹 전체 완결을 기다리지 않고 개인별 공식 소속사·프로필 근거가 확보된 멤버를 독립 소배치로 반영한다.
- BIG ISLAND RECORDS 공식 프로필의 新居歩美 완전 생년월일 `2003-06-28`, 출신지 가가와현, 개인 공식 URL과 확인일만 반영한다.
- 기존 생일 캘린더·멤버 상세·누락 집계 계약을 재사용하고 UI·스키마·명령 구조는 변경하지 않는다.

### 실패·빈 상태·회귀 위험

- 생탄제 날짜·SNS 계정명·2차 기사로 생일을 추정하지 않으며, 공식 개인 프로필이 없는 다른 멤버는 미확인 상태로 유지한다.
- 담당색·사진·SNS·지표·신장·혈액형·취미·특기는 추가하지 않는다.
- 단일 레코드 보강 과정에서 다른 ドラマチックレコード 멤버나 명단을 덮어쓰지 않는다.

### 완료 조건·검증 방법

- 新居歩美가 완전 생년월일·출신지와 개인 공식 URL 근거를 가진다.
- 생일 미확인 현역이 44명에서 43명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- 新居歩美의 `2003-06-28`, 가가와현과 소속사 개인 URL을 반영해 당시 미확인 생일이 43명으로 감소했다.
- 오프라인 일일 실행·데이터 검사·TypeScript `--noEmit`·Next.js 프로덕션 빌드가 PASS했고 정적 페이지 678/678을 생성했다.

## 2026-08-30 MVP F-17 — BYBBiT 공식 생일·담당색 완결 소배치

### 의도·범위와 연결

- TSUIMA 공식 탤런트 인덱스와 개인 프로필의 BYBBiT 현역 5인을 기존 공개 레코드와 대조한다.
- 공식 원문에 명시된 월·일 생일, 담당색 명칭, 개인 공식 URL과 확인일만 반영한다.
- 기존 생일 캘린더·멤버 상세·누락 집계와 공식 색상명 근사 계약을 재사용하고 UI·스키마·명령 구조는 변경하지 않는다.

### 실패·빈 상태·회귀 위험

- 개인 페이지 5개 중 하나라도 생일 근거가 없거나 공식 인덱스와 데이터 명단이 다르면 부분 반영하지 않는다.
- 출생연도·출신지·애칭·사진·SNS·혈액형·신장·취미·특기는 추가하지 않는다.
- 공식 페이지는 색상명만 제공하므로 HEX는 `official_name_approximation`으로 명시한다.

### 완료 조건·검증 방법

- BYBBiT 현역 5명이 월·일 생일·담당색 명칭·개인 공식 URL 근거를 가진다.
- 생일 미확인 현역이 49명에서 44명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- TSUIMA 공식 5인 인덱스와 개인 프로필을 대조해 佐藤ゆめか `12-27` 핑크, 朝比奈ろこ `11-23` 퍼플, 美月りの `02-08` 블루, 藤井あいら `06-24` 화이트, 竹内りおん `10-14` 라이트그린을 반영했다.
- 담당색 HEX는 `official_name_approximation`이며 출생연도·출신지·애칭·사진·SNS·혈액형·신장·취미·특기는 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 37그룹·188명, 향후 30일 생일 15명, 생일 미확인 현역 44명, 공연 후보 55건, 경고 39건, 이미지 후보 3 / 권리 검토 2.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 197kB, 멤버 상세 137kB.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 변환 경고만 있으며 공백 오류는 없다.

### 구현·검증 결과

- TSUIMA 공식 5인 인덱스와 개인 프로필을 대조해 佐藤ゆめか `12-27` 핑크, 朝比奈ろこ `11-23` 퍼플, 美月りの `02-08` 블루, 藤井あいら `06-24` 화이트, 竹内りおん `10-14` 라이트그린을 반영했다.
- 담당색 HEX는 `official_name_approximation`이며 출생연도·출신지·애칭·사진·SNS·혈액형·신장·취미·특기는 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 37그룹·188명, 향후 30일 생일 15명, 생일 미확인 현역 44명, 공연 후보 55건, 경고 39건, 이미지 후보 3 / 권리 검토 2.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 197kB, 멤버 상세 137kB.
- `git diff --check`는 최종 정합성 갱신 후 실행해 결과를 확정한다.

## 2026-08-30 이미지 운영 결정 — 문의 비필수화

### 의도·범위와 연결

- 소개·디렉터리의 핵심은 그룹명·사실 정보·공식 링크이며 공식 로고·사진 확보를 MVP 완료 조건으로 두지 않는다.
- 일반 텍스트, 자체 워드마크, 플레이스홀더를 유지하고 FRUITS ZIPPER·CANDY TUNE 이미지 사용 허가 문의는 발송하지 않는다.
- 이미지 후보 2건은 공개 데이터와 분리된 비노출 `rights_review` 기록으로 보존한다.

### 안전 경계·완료 조건

- 출처 표기를 재사용 허가로 간주하지 않으며 공식 이미지 파일을 다운로드·복제·핫링크하지 않는다.
- 공식 제휴·승인을 암시하지 않고 향후 광고는 편집 콘텐츠와 구분한다.
- 제품 계획·현행 인수인계의 다음 순서를 동기화하고 `git diff --check`로 문서 정합성을 확인한다.

## 2026-08-30 MVP F-16 — 宮代柚花 공식 프로필 단일 소배치

### 의도·범위와 연결

- 手羽先センセーション 공식 개인 프로필의 宮代柚花 정보를 기존 공개 멤버 레코드와 대조한다.
- 공식 원문에 명시된 월·일 생일 `11-19`, 출신지 오사카부, 개인 공식 URL과 확인일만 반영한다.
- 기존 생일 캘린더·멤버 상세·누락 집계와 `birthMonthDay` 계약을 재사용하고 UI·스키마·명령 구조는 변경하지 않는다.

### 실패·빈 상태·회귀 위험

- 생탄제 날짜를 생일로 변환하지 않고 개인 프로필의 `誕生日` 값만 사용한다.
- 출생연도·애칭·사진·SNS·지표는 추가하지 않으며 다른 手羽先センセーション 멤버와 그룹 명단은 변경하지 않는다.
- 담당색은 공식 페이지의 텍스트 근거로 이번 범위에서 확정하지 않고 기존 미확인 상태를 유지한다.

### 완료 조건·검증 방법

- 宮代柚花가 월·일 생일·출신지와 개인 공식 URL 근거를 가진다.
- 생일 미확인 현역이 50명에서 49명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- `tebasen-miyashiro-yuzuka`에 월·일 생일 `11-19`, 출신지 오사카부와 공식 개인 프로필 URL을 반영했다.
- 출생연도·담당색·애칭·사진·SNS·지표와 다른 手羽先センセーション 멤버·그룹 명단은 변경하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 37그룹·188명, 향후 30일 생일 15명, 생일 미확인 현역 49명, 공연 후보 55건, 경고 39건, 이미지 후보 3 / 권리 검토 2.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 197kB, 멤버 상세 137kB.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 변환 경고만 있으며 공백 오류는 없다.
- F-17 후보 사전 조사 결과, エイアイカ·yosugala 공식 프로필은 생일 미기재, MyDearDarlin’ 공식 세부 프로필은 타임아웃이었다. 2차 기사·SNS 핸들·생탄제 날짜는 공개 데이터 근거로 사용하지 않아 추가 변경 없이 보류했다.

## 2026-08-30 MVP F-15 — 与田理央那 공식 프로필 단일 소배치

### 의도·범위와 연결

- 군마현 공식 행사 게스트 프로필의 与田理央那 정보를 기존 공개 멤버 레코드와 대조한다.
- 공식 원문에 명시된 완전 생년월일 `2002-03-21`, 출신지 후쿠오카현, 담당색 노랑과 확인일만 반영한다.
- 기존 생일 캘린더·멤버 상세·누락 집계와 공식 색상명 근사 계약을 재사용하고 UI·스키마·명령 구조는 변경하지 않는다.

### 실패·빈 상태·회귀 위험

- 행사 날짜를 생일로 변환하지 않고 프로필의 `生年月日` 값만 사용한다.
- 애칭·사진·SNS·지표는 추가하지 않으며 다른 FES☆TIVE 멤버와 그룹 명단은 변경하지 않는다.
- 공식 색상명만 근거로 사용하고 노랑 HEX는 `official_name_approximation` 표시값으로 명시한다.

### 완료 조건·검증 방법

- 与田理央那가 완전 생년월일·출신지·담당색과 공식 공공기관 URL 근거를 가진다.
- 생일 미확인 현역이 51명에서 50명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- `festive-yoda-riona`에 완전 생년월일 `2002-03-21`, 출신지 후쿠오카현, 담당색 노랑과 군마현 공식 행사 게스트 프로필 URL을 반영했다. 노랑 HEX `#FACC15`는 `official_name_approximation`으로 표시했다.
- 애칭·사진·SNS·지표와 다른 FES☆TIVE 멤버·그룹 명단은 변경하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 37그룹·188명, 향후 30일 생일 15명, 생일 미확인 현역 50명, 공연 후보 55건, 경고 39건, 이미지 후보 3 / 권리 검토 2.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 197kB, 멤버 상세 137kB.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 변환 경고만 있으며 공백 오류는 없다.

## 2026-08-30 MVP F-14 — NEO BREAK 공식 생일 완결 소배치

### 의도·범위와 연결

- 현행 인수인계의 다음 권장 실행 단위인 NEO BREAK 재확인을 수행한다.
- 공식 멤버 인덱스의 현재 6인과 데이터 6인을 1:1 대조하고 공식 월·일 생일, 개인 공식 URL과 확인일만 반영한다.
- 개인 페이지가 반복 타임아웃인 西叶華는 NEO BREAK 공식 일정의 생탄제 날짜로 월·일을 교차 확인한다.
- 기존 `birthMonthDay` 스키마, 홈 생일 캘린더, 멤버 상세, 일일 30일 보고와 미확인 집계를 재사용한다. UI·스키마·명령 구조는 변경하지 않는다.

### 실패·빈 상태·회귀 위험

- 공식 명단과 데이터 인원수가 다르거나 6명 전체의 공식 생일 근거가 완성되지 않으면 부분 반영하지 않는다.
- 출생연도·나이·출신지·담당색·사진은 추정하지 않으며 비공식 프로필 검색 결과를 데이터 근거로 저장하지 않는다.
- 생탄제 날짜는 월·일 근거로만 사용하고 행사 연도를 출생연도로 변환하지 않는다.
- 서로 다른 개인 페이지와 일정 URL을 그룹 인덱스 하나로 일괄 덮어쓰지 않는다.

### 완료 조건·검증 방법

- 6명 모두 공식 월·일 생일과 공식 URL 근거를 가진다.
- 생일 미확인 현역 수가 57명에서 51명으로 감소한다.
- 오프라인 일일 실행, 데이터 검사, TypeScript `--noEmit`, Next.js 프로덕션 빌드, 한국어 멤버 상세 HTTP 확인, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- 공식 현재 6인과 공개 데이터 6인을 대조하고 월·일 생일 6건과 개인 공식 URL·확인일을 반영했다. 西叶華는 개인 페이지 타임아웃을 숨기지 않고 공식 일정의 생탄제 근거만 사용했다.
- 사용자 승인에 따라 계획 범위를 벗어나 임시 포함됐던 애칭 5건은 제거했다. 출생연도·출신지·담당색·사진은 추가하지 않았다.
- 오프라인 일일 실행 PASS: 생일 미확인 현역 51명, 향후 30일 생일 15명, 이미지 후보 3 / 권리 검토 2.
- 데이터 검사 PASS: 37그룹, 188멤버, 9개 공간 영역, 공개 공연 38, 공연 후보 55, 검토 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 197kB, 멤버 상세 137kB.
- 한국어 멤버 상세 HTTP 확인은 실행하지 않았다. 빌드의 정적 경로 생성은 통과했지만 HTTP 확인을 통과로 기록하지 않는다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 안내만 있으며 공백 오류는 없다. 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 문서 정합성 감사 — MVP E·H·역사 지도 기준

### 사용자 의도와 변경 범위

- 제품 줄기와 연결된 데이터·UI·명령·운영 계약은 그대로 보존하고 문서 간 현재 상태만 일치시킨다.
- `AUDIT_AND_REBUILD_PLAN.md`의 stale MVP E 현재 실행 표기를 완료 상태로 고치고, 정식 MVP A~H 분류에 H를 포함한다.
- `MAP_DISCOVERY_MVP.md`, `MAP_TECH_SPIKE.md`, `ADVERSARIAL_REVIEW_2026-08-29.md`의 과거 지도·적대적 검토 결론을 역사 자료로 명시해 현행 요구사항으로 오인되지 않게 한다.
- 코드·데이터·UI·명령·운영 설정, `HANDOVER.md` 역사 원문, 외부 시스템은 변경하지 않는다.

### 실패·빈 상태·회귀 위험

- 과거 수치와 판정을 삭제하면 의사결정 근거가 사라질 수 있으므로 본문은 보존하고 문서 지위와 대체 기준만 명시한다.
- 역사 문서의 지도 구상이 현행 홈 요구사항을 덮어쓰거나, MVP H가 별도 비정규 작업처럼 남는 모순을 막는다.
- 사용자 미커밋 변경과 다른 문서 내용은 수정하거나 정리하지 않는다.

### 완료 조건과 검증 방법

- 제품 계획과 현행 인수인계가 동일한 MVP A~H 상태를 사용하고, E는 완료, H-1은 완료로 읽힌다.
- 현재 실행 단위는 이 문서 정합성 감사로 식별된다.
- 세 역사 문서는 현행 기준 문서와 현재 홈·그룹 지도 경계를 문서 첫머리에서 안내한다.
- 대상 문구를 `rg`로 재검색하고 변경 diff를 검토한 뒤 `git diff --check` 결과를 아래에 기록한다. 문서 전용 변경이므로 데이터 검사·타입 검사·빌드는 실행하지 않고 미실행 사유를 기록한다.

### 실행 결과

- `AUDIT_AND_REBUILD_PLAN.md`의 권장 순서를 MVP A~H로 완결하고, MVP E를 완료로, MVP H를 H-1 완료·상시 정합성 유지로 표시했다. H-1 설명의 범위도 A~F에서 A~H로 교정했다.
- 오래된 일일 이미지 권리 큐 작업을 가리키던 `이번 실행 단위`를 이번 문서 정합성 감사로 교체했다. 제품 줄기와 데이터·UI·명령·운영 계약은 변경하지 않았다.
- `MAP_DISCOVERY_MVP.md`, `MAP_TECH_SPIKE.md`, `ADVERSARIAL_REVIEW_2026-08-29.md` 첫머리에 역사 문서 지위, 현행 기준 문서, 현재 홈 계층 탐색과 그룹 라이브 지도 경계를 명시했다. 기존 수치·판정·실행 순서 본문은 삭제하지 않았다.
- `rg` 재검색 PASS: 제품 계획의 E는 완료, A~H와 H-1은 일치하며 세 역사 문서 모두 현재 요구사항·다음 작업 지시가 아님을 첫머리에서 안내한다.
- 변경 diff 수동 검토 PASS: 이번 작업의 변경은 위 다섯 문서에 한정되며 `CODEX_HANDOVER.md`와 다른 사용자 변경은 수정하지 않았다.
- `git diff --check` PASS: 공백 오류는 없고 기존 작업 트리 파일의 LF→CRLF 안내만 출력됐다.
- 데이터 검사·TypeScript·프로덕션 빌드는 실행하지 않았다. 문서 전용 정합성 변경으로 런타임 산출물에 영향이 없으며, 미실행을 통과로 기록하지 않는다.
- 커밋·배포·삭제·외부 통신은 수행하지 않았다.

## 2026-08-30 통합 기준선 갱신 및 MVP E — 일일 이미지 권리 큐 연결

### 사용자 의도와 큰 줄기

- 승인된 핵심 흐름은 `일본 형상 탐색 → 도시 → 활동 지역 → 비주얼 그룹 카드 → 그룹 상세 → 라이브 탭의 실제 공연장 지도`다.
- 전체 MVP와 연결 가지를 현재 구현에 맞춰 다시 고정하고, 매일 실행하는 운영 보고서에서 이미지 권리 검토 대기를 빠뜨리지 않게 한다.
- 사용 금지 또는 재사용 허가를 확인하지 못한 사진은 계속 비노출하며, 허가된 공식 로고가 있을 때만 로고를 사용한다. 자체 워드마크는 공식 로고로 표기하지 않는다.

### 변경 범위와 연결

- 문서: 현재 기준과 다음 우선순위를 제품 계획·작업지시·지도 설계·인수인계·운영 안내에 맞춘다.
- 데이터: `data/chika-image-candidates.json`을 읽기만 하며 후보 상태를 수정하지 않는다.
- 명령: `scripts/daily-update.ts`가 기존 공연 후보·생일·검증 결과와 함께 이미지 후보 상태와 권리 검토 목록을 JSON/Markdown 보고서에 기록한다.
- UI: 이번 실행은 공개 화면을 바꾸지 않는다. 승인 전 이미지가 카드에 노출되는 경로를 만들지 않는다.
- 운영: 바탕화면의 기존 일일 실행기를 그대로 사용하고 외부 문의·다운로드·자동 승격을 추가하지 않는다.
- 실행 안정성: 호출한 터미널의 현재 폴더에 의존하지 않고 `daily-update.ts`의 위치에서 저장소 루트를 계산한다. 바탕화면·저장소·다른 폴더에서 호출해도 동일 데이터와 보고서 경로를 사용한다.

### 실패·빈 상태·회귀 위험

- 이미지 후보 파일이 스키마와 맞지 않으면 잘못된 권리 상태를 조용히 누락하지 않고 실행을 실패시킨다.
- 후보가 0건이면 정상 빈 상태로 보고한다. `rights_review`는 승인과 명확히 구분한다.
- 보고서에는 blocker와 공식 페이지 링크만 표시하고 원격 이미지 URL을 생성하지 않는다.
- 기존 네트워크 단계가 실패해도 이미지 큐·생일 보고는 생성되며 최종 판정은 기존처럼 `PARTIAL/FAIL`이다.
- 잘못된 현재 작업 폴더 때문에 `data/`를 찾지 못하는 회귀를 막는다.

### 완료 조건과 검증 방법

- 오프라인 일일 실행 결과에 이미지 후보 총계, 권리 검토, 승인, 거절과 검토 대상이 포함된다.
- 공개 그룹 데이터와 이미지 후보 데이터의 분리가 유지된다.
- `pnpm data:update:daily:offline`, `pnpm data:validate`, TypeScript 검사, 프로덕션 빌드, `git diff --check` 결과를 이 절에 기록한다.

### 구현·검증 결과

- `daily-update.ts`가 스크립트 위치에서 저장소 루트를 계산하며 이미지 후보 파일을 스키마 검증 후 읽기 전용 집계한다.
- JSON/Markdown 보고서에 이미지 후보 전체·권리 검토·승인·거절 수와 권리 검토 대상의 공식 페이지·확인일·차단 사유를 추가했다. 다운로드·문의·승격 동작은 추가하지 않았다.
- 오프라인 일일 실행 PASS: 공연 후보 55, 향후 30일 생일 1, 생일 미확인 152, 이미지 후보 3 / 권리 검토 2 / 승인 0 / 거절 1.
- 데이터 검사 PASS: 37그룹, 188멤버, 9개 공간 영역, 공개 공연 38, 라이브 후보 55, 이미지 경고 포함 검토 항목 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 8.2kB·첫 로드 144kB.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 사용자 승인 없이 커밋·배포·이미지 문의 발송은 하지 않았다.

## 2026-08-30 MVP F-1 — selfish 연도 비공개 공식 생일 소배치

### 의도·범위

- 공식 개인 프로필이 이미 연결된 selfish 현역 멤버 6명의 생일 월·일을 보강한다.
- 공식 원문은 연도를 공개하지 않으므로 `birthDate`를 조작하지 않고 `birthMonthDay`에 `MM-DD` 정밀도로 저장한다.
- 데이터 계약, 홈 생일 캘린더, 멤버 상세, 일일 30일 보고, 누락 큐, 검증 명령을 한 가지로 함께 연결한다.
- 공식 프로필의 사진은 재사용 권리가 확인되지 않았으므로 이미지 데이터와 공개 카드를 변경하지 않는다.

### 데이터·UI·명령 연결

- 스키마: `birthMonthDay`는 유효한 `MM-DD` 또는 null이며 `birthDate`가 함께 있으면 월·일이 일치해야 한다.
- 데이터: 華月菜々子 01-11, 清水玲衣 03-02, 瀬川ましろ 11-20, 珠白瑠花 07-30, 楠りん 12-18, 水野詩音 07-11을 각 공식 개인 페이지와 2026-08-30 확인일로 기록한다.
- UI: 생일 캘린더는 연도 유무와 무관하게 월·일을 표시한다. 멤버 상세는 완전 날짜와 월·일만 알려진 값을 구분해 표시한다.
- 운영: 일일 보고의 생일 계산과 미확인 집계는 `birthDate ?? birthMonthDay`를 사용하되 출생연도·나이를 추정하지 않는다.

### 실패·빈 상태·회귀 위험

- 02-30 같은 불가능한 월·일, 형식 오류, 완전 날짜와 월·일 불일치는 검증 실패다.
- 연도 없는 값을 임의의 2000년/2026년 출생으로 화면이나 보고서에 표현하지 않는다.
- 기존 완전 생일 36건의 정렬·표시와 윤년 02-29 다음 생일 계산을 깨뜨리지 않는다.
- 생일이 둘 다 null인 멤버만 미확인 큐에 남긴다.

### 완료 조건·검증

- selfish 6명 모두 공식 월·일이 표시되고 생일 미확인 현역 수가 152명에서 146명으로 감소한다.
- 데이터 검사, 일일 오프라인 실행, 타입 검사, 프로덕션 빌드, `git diff --check`를 수행해 결과를 기록한다.

### 구현·검증 결과

- selfish 공식 개인 프로필 6곳을 2026-08-30 재확인하고 월·일만 공개된 사실을 그대로 `birthMonthDay`에 반영했다. 출생연도와 나이는 추정하지 않았다.
- 스키마에 선택적 nullable `birthMonthDay(MM-DD)`를 추가하고 불가능한 날짜 및 `birthDate`와의 월·일 불일치를 데이터 검사 오류로 처리했다. 레거시 seed 객체는 새 필드를 생략할 수 있다.
- 홈 생일 캘린더, 멤버 상세 다국어 라벨, 일일 30일 계산과 미확인 큐를 연결했다. 공식 페이지의 사진은 추출·저장·표시하지 않았다.
- 오프라인 일일 실행 PASS: 향후 30일 생일 1명, 생일 미확인 현역 146명(기존 152명에서 6명 감소), 이미지 권리 큐 상태 불변.
- 데이터 검사 PASS: 37그룹, 188멤버, 공개 공연 38건, 라이브 후보 55건, 경고 39건.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 8.2kB·첫 로드 144kB.
- `git diff --check`는 공백 오류 없이 통과했고 기존 LF→CRLF 경고만 남았다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-2 — I’mew 공식 프로필 완결 소배치

### 의도·변경 범위

- I’mew 공식 프로필 한 페이지에서 현재 6인 명단, 생일, 애칭, 출신지, 담당색을 공개 데이터와 재대조한다.
- 기존 출신지·담당색·완전 생년월일 2건은 공식 원문과 일치하므로 유지한다. 누락된 월·일 생일 4건과 애칭 6건만 추가하고 확인일을 갱신한다.
- 프로필 이미지가 존재해도 재사용 권리가 확인되지 않았으므로 이미지 후보·카드·파일은 변경하지 않는다.

### 데이터·UI·명령 연결

- `birthMonthDay`: 中澤莉佳子 11-28, 桜羽らん 06-12, 旭萌花 01-30, 櫻井あかね 11-14.
- `nickname`: あいにゃ, りかこ, キラシオ, らんち, もえぴ, あかねちゃん을 일본어 원문과 읽기용 한·영 음역으로 저장한다.
- 기존 생일 캘린더·멤버 상세·일일 보고 계약을 그대로 사용하며 별도 UI 분기를 추가하지 않는다.

### 실패·회귀 위험

- 애칭 음역을 공식 영문 표기로 주장하지 않고 읽기 보조 데이터로만 사용한다.
- 공식 프로필 명단과 데이터 멤버 수가 다르면 활동 상태를 임의 수정하지 않고 배치를 중단한다. 이번 대조는 양쪽 모두 6명으로 일치했다.
- 생일 월·일이 기존 완전 날짜와 충돌하면 검증 실패해야 한다.

### 완료 조건·검증

- I’mew 6명 모두 생일과 애칭이 알려진 범위에서 표시되고 생일 미확인 현역이 146명에서 142명으로 감소한다.
- 데이터 검사, 오프라인 일일 실행, 타입 검사, 프로덕션 빌드, `git diff --check`, 로컬 상세 HTTP를 검증한다.

### 구현·검증 결과

- I’mew 공식 프로필을 2026-08-30 재확인해 공개 데이터의 현역 6명과 공식 6인 명단이 일치함을 확인하고 그룹·멤버 확인일을 갱신했다.
- 연도 비공개 생일 4건(`11-28`, `06-12`, `01-30`, `11-14`)과 공식 애칭 6건을 반영했다. 기존 완전 생년월일 2건, 출신지, 담당색은 원문과 일치해 유지했다.
- 사진 URL·이미지 후보·권리 상태는 변경하지 않았다. 한·영 애칭은 일본어 공식 애칭의 읽기 보조 음역이며 별도 공식 영문명으로 주장하지 않는다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 142명(146명에서 4명 감소), 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 8.2kB·첫 로드 144kB.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-3 — 可憐なアイボリー 11인 공식 프로필 완결

### 의도·범위

- 공식 프로필 인덱스의 현역 11명과 공개 데이터 11명을 1:1 대조한다.
- 각 개인 공식 프로필에서 완전 생년월일·애칭·출신지를 반영하고 확인일과 개인 출처 URL을 갱신한다.
- 나이, 혈액형, 신장, 취미·특기는 현행 제품 핵심 흐름과 스키마 범위 밖이므로 이번에는 저장하지 않는다. 그중 나이는 공식 페이지 자체가 현재 날짜와 어긋날 수 있어 특히 파생하지 않는다.
- 담당색·사진은 공식 근거 또는 재사용 권리가 없으므로 미확인/플레이스홀더를 유지한다.

### 실패·회귀 위험

- 공식 인덱스와 데이터 명단이 다르면 중단한다. 현재 양쪽 모두 11명으로 일치한다.
- 미성년 멤버를 포함하므로 공개된 기본 프로필을 넘어 사생활·학교·추정 나이 정보를 추가하지 않는다.
- 생년월일 형식과 실제 달력 유효성, 멤버 ID·그룹 ID·출처 URL을 데이터 검사로 확인한다.
- 기존 `birthDate`가 단순 문자열만 허용하던 계약을 보완해 `YYYY-MM-DD` 형식과 실제 달력 날짜를 함께 검사한다.

### 완료 조건·검증

- 11명 모두 `birthDate`, `nickname`, `birthplace`, 개인 공식 프로필 출처를 가진다.
- 생일 미확인 현역이 142명에서 131명으로 감소한다.
- 데이터 검사, 오프라인 일일 실행, 타입 검사, 프로덕션 빌드, diff, 로컬 상세 응답을 검증한다.

### 구현·검증 결과

- 공식 인덱스 11명과 공개 데이터 11명을 1:1 대조하고 그룹 명단 확인일을 2026-08-30으로 갱신했다.
- 11명 전원에게 개인 공식 프로필 URL, 완전 생년월일, 출신지, 공식 애칭과 확인일을 반영했다. 담당색·이미지는 미확인/플레이스홀더를 유지했다.
- `birthDate`의 `YYYY-MM-DD` 형식과 실제 달력 유효성 검사를 추가했다. 공식 페이지의 정적 나이 값은 저장하거나 파생하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 131명(142명에서 11명 감소), 향후 30일 생일 4명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 8.2kB·첫 로드 144kB, 검색 첫 로드 191kB.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-4 — AsIs 7인 공식 프로필 완결

### 의도·범위

- AsIs 공식 인덱스 7명과 데이터 7명을 대조하고, 각 개인 프로필의 월·일 생일·출신지·담당색 명칭·개인 URL을 반영한다.
- 공식 HEX는 제공되지 않으므로 화면용 색상값은 공식 명칭에 대응한 근사값으로 기록하고 그 근거 유형을 별도 필드로 명시한다.
- 혈액형·신장·특기, 출생연도·나이, 프로필 이미지는 이번 제품 계약과 권리 범위 밖이라 저장하지 않는다.

### 데이터 계약과 실패 상태

- `memberColorBasis`: `official_hex`, `official_name_approximation`, `unverified` 중 선택적으로 기록한다.
- 공식 색상명 기반 근사값은 정확한 브랜드 HEX로 표현하지 않는다. 색상명이 미확인이면 근사 근거를 선언할 수 없다.
- 7명 모두 `birthMonthDay`, `birthplace`, 공식 담당색명, 개인 공식 출처를 가져야 하며 명단 불일치 시 중단한다.

### 완료 조건·검증

- 생일 미확인 현역이 131명에서 124명으로 감소한다.
- 홈·멤버 상세·일일 보고가 기존 월·일 계약으로 동작한다.
- 데이터 검사, 일일 오프라인 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증한다.

### 구현·검증 결과

- AsIs 공식 인덱스 7명과 공개 데이터 7명을 대조하고 그룹 명단 확인일을 2026-08-30으로 갱신했다.
- 7명 전원에게 연도 비공개 생일, 출신지, 공식 담당색명, 개인 공식 프로필 URL을 반영했다. 출생연도·나이·사진은 추가하지 않았다.
- 공식 HEX가 없는 색은 접근 가능한 UI 근사값과 `memberColorBasis=official_name_approximation`을 함께 저장했다. 이를 공식 브랜드 HEX로 표현하지 않는다.
- 색상 근거 계약과 검증을 추가해 미확인 색상명에 근사 근거를 붙이거나 공식 HEX를 미확인 회색으로 선언하는 모순을 차단했다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 124명(131명에서 7명 감소), 향후 30일 생일 4명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 첫 로드 196kB.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-5 — UtaGe! 7인 생일 정합성 완결

### 의도·범위

- UtaGe! 공식 프로필의 현재 7인과 공개 데이터 7인을 대조한다.
- 기존 출신지와 담당색 명칭이 원문과 일치하는지 확인하고 누락 월·일 생일, 명단 확인일, 색상 표시 근거만 추가한다.
- 단일 공식 페이지가 모든 멤버의 근거이므로 개인 URL을 임의 생성하지 않는다.

### 데이터·실패 상태

- 생일: 01-17, 11-21, 03-28, 10-17, 11-11, 02-11, 03-06.
- 색상은 공식 명칭 기반 근사값으로 분류한다. 출신지·색상명 불일치나 명단 수 불일치가 있으면 배치를 중단한다.
- 애칭·출생연도·나이·사진은 원문/권리 근거가 없으므로 추가하지 않는다.

### 완료 조건·검증

- 7명 모두 월·일 생일과 담당색 근거를 갖고 생일 미확인 현역이 124명에서 117명으로 감소한다.
- 데이터 검사, 일일 오프라인 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증한다.

### 구현·검증 결과

- UtaGe! 공식 단일 프로필의 7인 명단과 데이터 7명을 대조하고 명단·그룹·멤버 확인일을 2026-08-30으로 갱신했다.
- 출신지와 담당색 명칭은 기존 값이 공식 원문과 일치해 유지하고 월·일 생일 7건을 추가했다.
- 기존 UI HEX를 공식 색상명 기반 표시 근사값으로 분류했다. 애칭·출생연도·나이·사진은 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 117명(124명에서 7명 감소), 향후 30일 생일 4명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 첫 로드 196kB.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-6 — FRUiTY 9인 생일·담당 과일 의미 교정

### 의도·현재 문제

- FRUiTY 공식 프로필 9명과 데이터 9명은 일치하며 월·일 생일 9건이 누락돼 있다.
- 공식 원문의 `担当フルーツ`가 현재 `memberColorName`에 들어 있어 수박·코코넛 등이 멤버 색상처럼 표현되는 의미 오류가 있다.
- `memberMotif` 계약으로 담당 과일을 분리하고 담당색은 미확인으로 교정한다.

### 데이터·UI 연결

- `memberMotif`는 선택적 다국어 텍스트이며 그룹 고유의 공식 담당 요소를 저장한다.
- 멤버 카드와 상세에서 담당 과일을 색상 배지와 분리해 표시한다. 색상은 공식 근거가 없으므로 회색·미확인을 유지한다.
- 생일 9건: 04-07, 06-21, 12-21, 09-12, 12-05, 09-02, 07-26, 02-15, 04-06.

### 실패·회귀 위험

- 담당 과일을 담당색이나 공식 HEX로 변환하지 않는다.
- 프로필에 없는 출신지·애칭·출생연도·나이는 추가하지 않는다.
- `memberMotif`가 없는 다른 그룹의 카드·상세 레이아웃은 변하지 않아야 한다.

### 완료 조건·검증

- 9명 모두 월·일 생일과 담당 과일을 갖고 생일 미확인 현역이 117명에서 108명으로 감소한다.
- 색상 배지는 미확인, 담당 과일은 별도 표시로 출력된다.
- 데이터 검사, 일일 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증한다.

### 구현·검증 결과

- FRUiTY 공식 프로필의 9인 명단과 데이터 9명을 대조하고 명단·그룹·멤버 확인일을 2026-08-30으로 갱신했다.
- 월·일 생일 9건과 공식 담당 과일 9건을 반영했다. 출신지·애칭·출생연도·나이·사진은 추가하지 않았다.
- `memberMotif` 다국어 계약을 추가하고 카드·상세에 별도 표시했다. 기존에 담당 과일을 색상명으로 저장하던 의미 오류를 제거해 색상은 회색·미확인으로 복구했다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 108명(117명에서 9명 감소), 향후 30일 생일 6명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/196kB, 멤버 상세 5.11kB/137kB.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-7 — HelloYouth 9인 공식 프로필 완결

### 의도·범위

- 공식 멤버 인덱스의 현재 9명과 공개 데이터 9명을 1:1 대조한다.
- 개인 공식 프로필에서 완전 생년월일, 출신지, 멤버 컬러 명칭, 개인 출처 URL과 확인일을 반영한다.
- 취미·특기·신장과 프로필 사진은 이번 핵심 흐름 및 이미지 권리 범위 밖이므로 저장하지 않는다.

### 데이터·UI·명령 연결

- 생년월일 9건: `2000-01-31`, `2004-12-03`, `2004-01-15`, `2006-05-20`, `2007-12-26`, `2006-07-03`, `1999-12-01`, `2011-08-25`, `2003-09-20`.
- 출신지는 공식 원문을 보존한다. `長崎県出身福岡育ち`처럼 출생·성장지가 함께 적힌 값도 임의로 하나만 선택하지 않는다.
- 공식 색상명 9건은 표시용 근사 HEX와 `memberColorBasis=official_name_approximation`을 함께 저장하며 공식 HEX라고 주장하지 않는다.
- 기존 생일 캘린더, 멤버 카드·상세, 일일 보고 계약을 재사용하고 별도 UI 분기를 추가하지 않는다.

### 실패·빈 상태·회귀 위험

- 공식 인덱스와 데이터 명단 또는 인원수가 다르면 활동 상태를 추정하지 않고 중단한다. 현재 양쪽 모두 9명으로 일치한다.
- 완전 생년월일은 실제 달력 검사를 통과해야 하며, 색상명과 근사 HEX의 의미를 혼동하지 않는다.
- 미성년 멤버를 포함하므로 공식 기본 프로필 밖 학교·주소·나이 추정 정보를 추가하지 않는다.
- 사진은 페이지에 존재해도 재사용 권리가 확인되지 않았으므로 플레이스홀더를 유지한다.

### 완료 조건·검증

- 9명 모두 완전 생년월일, 출신지, 공식 색상명·표시 근거, 개인 공식 프로필 출처를 가진다.
- 생일 미확인 현역이 108명에서 99명으로 감소한다.
- 데이터 검사, 오프라인 일일 실행, 타입 검사, 프로덕션 빌드, `git diff --check`, 로컬 상세 응답을 검증하고 실제 결과를 이 절에 기록한다.

### 구현·검증 결과

- 공식 인덱스 9명과 공개 데이터 9명을 1:1 대조하고 명단 확인일을 2026-08-30으로 갱신했다.
- 9명 전원에게 개인 공식 프로필 URL, 완전 생년월일, 출신지, 공식 멤버 컬러 명칭과 표시 근거를 반영했다. 취미·특기·신장·사진은 추가하지 않았다.
- 색상명만 제공된 9색은 접근 가능한 UI 근사값과 `memberColorBasis=official_name_approximation`을 함께 저장했다. 공식 HEX로 주장하지 않는다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 99명(108명에서 9명 감소), 향후 30일 생일 7명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/196kB, 멤버 상세 5.11kB/137kB.
- 로컬 프로덕션 서버의 한국어 大島向葵 상세 경로가 HTTP 200으로 응답함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-8 — JamsCollection 8인 공식 프로필 완결

### 의도·범위

- 공식 프로필 인덱스의 현재 8명과 공개 데이터 8명을 1:1 대조한다.
- 개인 공식 프로필에서 월·일 생일, 애칭, 출신지, 담당색 명칭, 개인 출처 URL과 확인일을 반영한다.
- 취미·특기와 프로필 사진은 핵심 흐름 및 이미지 권리 범위 밖이므로 저장하지 않는다.

### 데이터·UI·명령 연결

- 생일 8건: `10-16`, `08-13`, `07-29`, `03-09`, `02-09`, `03-09`, `04-16`, `12-06`.
- 애칭·출신지는 공식 원문과 읽기용 다국어 값을 저장하고, 색상은 공식 명칭과 `official_name_approximation` 표시 근거를 함께 둔다.
- 기존 생일 캘린더, 멤버 카드·상세, 일일 보고를 재사용한다. 출생연도와 나이를 파생하지 않는다.

### 실패·빈 상태·회귀 위험

- 명단 또는 인원수가 다르면 활동 상태를 임의로 수정하지 않고 중단한다. 현재 양쪽 모두 8명이다.
- 월·일 달력 검사와 색상 근거 검사를 통과해야 한다. 기존 保科凜의 출신지·색상명은 공식 원문과 일치하므로 보존하면서 근거만 명시한다.
- 사진이 개인 페이지에 존재해도 재사용 권리가 없으므로 플레이스홀더를 유지한다.

### 완료 조건·검증

- 8명 모두 월·일 생일, 애칭, 출신지, 공식 색상명·표시 근거, 개인 공식 출처를 가진다.
- 생일 미확인 현역이 99명에서 91명으로 감소한다.
- 데이터 검사, 오프라인 일일 실행, 타입 검사, 프로덕션 빌드, `git diff --check`, 로컬 상세 응답을 검증하고 결과를 기록한다.

### 구현·검증 결과

- 공식 인덱스 8명과 데이터 8명을 1:1 대조하고 그룹·명단 확인일을 2026-08-30으로 갱신했다.
- 8명 전원에게 월·일 생일, 공식 애칭, 출신지, 담당색 명칭, 표시 근거, 개인 공식 프로필 URL을 반영했다. 출생연도·나이·사진은 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 91명(99명에서 8명 감소), 향후 30일 생일 7명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/197kB, 멤버 상세 5.11kB/137kB.
- 로컬 프로덕션 서버의 한국어 津代美月 상세 경로가 HTTP 200으로 응답함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-9 — MAGICAL SPEC 8인 공식 프로필 완결

### 의도·범위

- 공식 인덱스와 데이터의 현재 8명을 1:1 대조한다.
- 개인 프로필의 월·일 생일, 출신지, 담당색, 개인 출처 URL과 확인일을 반영한다.
- 공식 페이지에 없는 애칭·출생연도·나이와 권리 미확인 사진은 추가하지 않는다.

### 데이터·UI·명령 연결

- 생일 8건: `06-03`, `11-01`, `12-29`, `01-30`, `08-02`, `01-09`, `01-05`, `01-09`.
- 출신지는 KOTONE·RINKA·ANNA·MEI·AIKA·SHIKI가 후쿠오카현, AYA가 야마구치현, YUYU가 사가현이다.
- 기존 색상명과 UI HEX는 공식 원문과 일치하는 표시 근사값으로 분류하고 `memberColorBasis=official_name_approximation`을 명시한다.

### 실패·빈 상태·회귀 위험

- 공식 인덱스와 데이터 명단·인원수가 다르면 중단한다. 현재 양쪽 모두 8명이다.
- 동일한 01-09 생일 두 명을 중복 데이터로 오판하지 않으며, 월·일 달력 검사와 색상 근거 검사를 통과해야 한다.
- Sweet Alley는 공식 화면이 색상·생일 값을 텍스트로 제공하지 않아 이번 배치에 섞지 않고 별도 근거 확보 대상으로 남긴다.

### 완료 조건·검증

- 8명 모두 월·일 생일, 출신지, 공식 색상명·표시 근거, 개인 공식 출처를 가진다.
- 생일 미확인 현역이 91명에서 83명으로 감소한다.
- 데이터 검사, 오프라인 일일 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증하고 결과를 기록한다.

### 구현·검증 결과

- 공식 인덱스 8명과 데이터 8명을 대조하고 그룹·명단 확인일을 2026-08-30으로 갱신했다.
- 8명 전원에게 월·일 생일, 출신지, 공식 담당색 표시 근거, 개인 공식 프로필 URL을 반영했다. 애칭·출생연도·나이·사진은 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 83명(91명에서 8명 감소), 향후 30일 생일 7명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/197kB, 멤버 상세 5.11kB/137kB.
- 로컬 프로덕션 서버의 한국어 KOTONE 상세 경로가 HTTP 200으로 응답함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-10 — NANIMONO 7인 생일·담당색 정합성 완결

### 의도·범위

- 공식 단일 프로필의 현재 7명과 공개 데이터 7명을 1:1 대조한다.
- 전원의 월·일 생일과 담당색 표시 근거, 그룹·멤버 확인일을 반영한다.
- 출신지·애칭·출생연도·나이·사진은 공식 근거 또는 권리 범위가 없으므로 추가하지 않는다.

### 데이터·UI·명령 연결

- 생일 7건: `09-30`, `02-08`, `01-16`, `04-24`, `03-29`, `09-04`, `02-20`.
- 기존 담당색 오렌지·파랑·노랑·핑크·흰색·보라·하늘색을 공식 원문과 대조하고 `memberColorBasis=official_name_approximation`을 명시한다.
- 기존 월·일 생일 캘린더, 멤버 카드·상세, 일일 보고를 그대로 사용한다.

### 실패·빈 상태·회귀 위험

- 명단·인원수 또는 기존 담당색명이 공식 원문과 다르면 중단한다. 현재 7명과 색상명 모두 일치한다.
- 숫자 활동명 `47774`를 생일이나 내부 ID로 오인하지 않는다.
- 이미지 URL은 공식 페이지에 있어도 저장·핫링크하지 않는다.

### 완료 조건·검증

- 7명 모두 월·일 생일과 공식 색상명·표시 근거를 가진다.
- 생일 미확인 현역이 83명에서 76명으로 감소한다.
- 데이터 검사, 일일 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증하고 결과를 기록한다.

### 구현·검증 결과

- 공식 단일 프로필 7명과 데이터 7명을 대조하고 그룹·명단·멤버 확인일을 2026-08-30으로 갱신했다.
- 7명 전원에게 월·일 생일과 공식 담당색 표시 근거를 반영했다. 출신지·애칭·출생연도·나이·사진은 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 76명(83명에서 7명 감소), 향후 30일 생일 8명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/197kB, 멤버 상세 5.11kB/137kB.
- 로컬 프로덕션 서버의 한국어 ひなたゆま 상세 경로가 HTTP 200으로 응답함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-11 — Palette Parade 7인 공식 생일 소배치

### 의도·범위

- 공식 프로필의 현재 7명과 공개 데이터 7명을 대조하고 월·일 생일과 확인일을 반영한다.
- 담당색·출신지·애칭·출생연도·나이·사진은 공식 근거 또는 권리 범위가 없으므로 변경하지 않는다.

### 데이터·UI·명령 연결

- 생일 7건: `11-08`, `09-11`, `01-08`, `03-30`, `05-01`, `01-10`, `11-12`.
- 기존 월·일 달력, 멤버 상세, 일일 30일 보고와 미확인 큐 계약을 그대로 사용한다.
- 단일 공식 페이지를 공통 출처로 유지하고 존재하지 않는 개인 URL을 만들지 않는다.

### 실패·빈 상태·회귀 위험

- 명단·인원수가 다르면 활동 상태를 추정하지 않고 중단한다. 현재 양쪽 모두 7명이다.
- 공식 페이지의 성격·특기·좋아하는 것은 현재 핵심 데이터 계약 밖이므로 추가하지 않는다.
- 담당색을 이미지 색상에서 역추출하거나 사진을 핫링크하지 않는다.

### 완료 조건·검증

- 7명 모두 월·일 생일과 최신 공식 출처 확인일을 가진다.
- 생일 미확인 현역이 76명에서 69명으로 감소한다.
- 데이터 검사, 일일 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증하고 결과를 기록한다.

### 구현·검증 결과

- 공식 단일 프로필 7명과 데이터 7명을 대조하고 그룹·명단·멤버 확인일을 2026-08-30으로 갱신했다.
- 7명 전원에게 공식 월·일 생일을 반영했다. 담당색·출신지·애칭·출생연도·나이·사진은 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 69명(76명에서 7명 감소), 향후 30일 생일 9명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/197kB, 멤버 상세 5.11kB/137kB.
- 로컬 프로덕션 서버의 한국어 葵うた 상세 경로가 HTTP 200으로 응답함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-12 — AVAM 7인 공식 프로필 완결

### 의도·범위

- 공식 인덱스와 공개 데이터의 현재 7명을 1:1 대조한다.
- 개인 공식 프로필의 월·일 생일, 애칭, 멤버 컬러, 개인 URL과 확인일을 반영한다.
- 출신지·출생연도·나이·사진은 공식 근거 또는 권리 범위가 없으므로 추가하지 않는다.

### 데이터·UI·명령 연결

- 생일 7건: `09-01`, `09-06`, `09-01`, `10-12`, `03-11`, `03-05`, `04-11`.
- 애칭 7건: `おとは`, `はなくん`, `ななみ`, `そらち`, `あみ`, `のあ`, `ゆかぴ`.
- 담당색 빨강·오렌지·흰색·파랑·초록·보라·핑크를 공식 명칭과 `official_name_approximation` 표시 근거로 저장한다.

### 실패·빈 상태·회귀 위험

- 동일한 09-01 생일 두 명을 중복 오류로 처리하지 않으며 명단·인원수 불일치 시 중단한다.
- 공식 페이지에 없는 출신지를 다른 출처에서 섞거나 나이를 추정하지 않는다.
- 사진 URL은 저장·핫링크하지 않고 개인 공식 페이지 링크만 출처로 사용한다.

### 완료 조건·검증

- 7명 모두 월·일 생일, 애칭, 공식 색상명·표시 근거, 개인 공식 출처를 가진다.
- 생일 미확인 현역이 69명에서 62명으로 감소한다.
- 데이터 검사, 일일 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증하고 결과를 기록한다.

### 구현·검증 결과

- 공식 인덱스 7명과 개인 프로필 7개를 대조하고 그룹·명단·멤버 확인일을 2026-08-30으로 갱신했다.
- 7명 전원에게 월·일 생일, 공식 애칭, 담당색 명칭·표시 근거, 개인 공식 프로필 URL을 반영했다. 출신지·출생연도·나이·사진은 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 62명(69명에서 7명 감소), 향후 30일 생일 12명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/197kB, 멤버 상세 5.11kB/137kB.
- 로컬 프로덕션 서버의 한국어 大原乙葉 상세 경로가 HTTP 200으로 응답함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP F-13 — Ringwanderung 5인 공식 프로필 완결

### 의도·범위

- 공식 인덱스의 현재 5명과 공개 데이터 5명을 1:1 대조한다.
- 개인 공식 프로필의 월·일 생일, 출신지, 개인 URL과 확인일을 반영한다.
- 담당색·애칭·출생연도·나이·사진은 공식 근거 또는 권리 범위가 없으므로 추가하지 않는다.

### 데이터·UI·명령 연결

- 생일 5건: `10-28`, `01-13`, `03-27`, `10-13`, `09-07`.
- 출신지 5건: 구마모토현, 도치기현, 도쿄도, 가가와현, 가나가와현.
- 기존 월·일 생일·멤버 상세·일일 보고 계약을 재사용하고 개인 공식 URL을 출처로 연결한다.

### 실패·빈 상태·회귀 위험

- 명단·인원수가 다르면 중단한다. 현재 공식 인덱스와 데이터는 모두 5명이다.
- 개인 페이지의 키·취미·특기를 현재 스키마에 억지로 넣거나 출생연도를 추정하지 않는다.
- NEO BREAK는 6명 중 2개 개인 페이지가 웹과 로컬 네트워크에서 확인되지 않아 이번 배치에 부분 반영하지 않는다.

### 완료 조건·검증

- 5명 모두 월·일 생일, 출신지, 개인 공식 출처를 가진다.
- 생일 미확인 현역이 62명에서 57명으로 감소한다.
- 데이터 검사, 일일 실행, 타입 검사, 빌드, diff, 로컬 상세 응답을 검증하고 결과를 기록한다.

### 구현·검증 결과

- 공식 인덱스 5명과 개인 프로필 5개를 대조하고 그룹·명단·멤버 확인일을 2026-08-30으로 갱신했다.
- 5명 전원에게 월·일 생일, 출신지, 개인 공식 프로필 URL을 반영했다. 담당색·애칭·출생연도·나이·사진은 추가하지 않았다.
- 오프라인 일일 실행 및 데이터 검사 PASS: 생일 미확인 현역 57명(62명에서 5명 감소), 향후 30일 생일 13명, 37그룹·188멤버·공개 공연 38·후보 55, 경고 39.
- TypeScript `--noEmit` PASS.
- Next.js 프로덕션 빌드 PASS: 정적 페이지 678/678, 홈 첫 로드 144kB, 그룹 상세 7.89kB/197kB, 멤버 상세 5.11kB/137kB.
- 로컬 프로덕션 서버의 한국어 佐藤倫子 상세 경로가 HTTP 200으로 응답함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 커밋·배포·이미지 다운로드·외부 문의는 수행하지 않았다.

## 2026-08-30 MVP H-1 — 전체 인수인계 완전 갱신

### 의도·범위

- `CODEX_HANDOVER.md` 전체의 오래된 동적 수치, 완료 전 지도 설명, 브라우저 미검증 문구, 이미 완료된 다음 작업을 제거한다.
- 현재 제품 흐름, MVP 진행, 데이터 기준선, 이미지 권리, 일일 실행, 검증 명령, 로컬 서버, 배포·Git 경계, 잔여 작업을 단일 최신 기준으로 다시 작성한다.
- `HANDOVER.md`의 초기 프로토타입 본문은 역사 자료로 보존하고 현행 기준이 아님을 명확히 유지한다.

### 실패·회귀 위험

- 과거 수치를 현재 수치처럼 남기거나 프로덕션 배포와 미커밋 로컬 작업 트리를 동일시하지 않는다.
- 브라우저 사용자 승인 완료와 개별 최신 HTTP 검증을 과거의 브라우저 미실행 문구로 덮지 않는다.
- 커밋·배포·외부 문의를 수행하지 않으며, 변경된 사용자 작업 트리를 정리·되돌리지 않는다.

### 완료 조건·검증

- 현행 인수인계에 생일 미확인 57명, 향후 30일 13명, 37그룹·188멤버, 공개 공연 38·후보 55, 이미지 후보 3/검토 2/승인 0/거절 1, 678/678 빌드가 한 기준선으로 기록된다.
- 완료된 작업이 다음 우선순위로 남지 않고 NEO BREAK 재확인과 잔여 공식 프로필 소배치가 다음 순서로 명시된다.
- 오래된 현재형 수치 검색, 문서 링크·핵심 명령 확인, `git diff --check` 결과를 기록한다.

### 구현·검증 결과

- `CODEX_HANDOVER.md`를 역사 실행 로그 누적형 문서에서 현행 단일 기준선 문서로 전면 재작성했다.
- 제품 고정 흐름, 신뢰·권리 경계, MVP A~H, 최신 데이터 수치, F-1~F-13 결과, 생일 미확인 잔여 57명의 그룹별 분포, 지도·일일 운영, 검증 명령, 다음 순서, Git·배포 경계를 한 구조로 통합했다.
- 완료된 MVP E와 지도 작업이 다음 우선순위로 남던 문구, 생일 1명/미확인 152명, 브라우저 미검증 등 오래된 현재형 문구가 남지 않음을 검색으로 확인했다.
- `HANDOVER.md`는 초기 역사 자료로 보존하고 상단의 현행 문서 연결이 정확함을 확인했다.
- 인수인계에서 참조하는 핵심 문서·데이터·스크립트·지도 컴포넌트 경로가 모두 존재함을 확인했다.
- `git diff --check` PASS. 기존 작업 트리의 LF→CRLF 경고만 있으며 공백 오류는 없다.
- 문서 정합성 작업이므로 데이터·코드 동작은 변경하지 않았고 F-13의 데이터·타입·678/678 빌드·HTTP 검증 기준선을 그대로 인계했다.
- 커밋·배포·외부 문의는 수행하지 않았다. 검증 중 파일 잠금 방지를 위해 로컬 3001 서버는 종료했다.

## 2026-08-29 감사 후 MVP 1~3 정합성 복구

### 사용자 의도와 큰 줄기

- 작은 수정도 제품 큰 줄기 → 전체 연결 가지 → MVP → 실행 단위 순서로 먼저 설계하고 검증한다.
- 공식 공연 탐색의 화면·지도·운영 판단을 일본 표준시로 일치시키고, 미결정 지도 공급자를 프로덕션 기본 동작에서 분리하며, 현행 인수인계가 실제 상태를 재현하도록 한다.

### MVP 순서와 이번 변경 범위

1. MVP 1 날짜: 공통 `Asia/Tokyo` 날짜 유틸을 추가하고 그룹 예정 공연과 Gate 2 보고를 연결한다. 날짜 경계 검사 명령을 추가한다.
2. MVP 2 지도: 전국 State A SVG·목록을 기본 렌더링으로 하고 `NEXT_PUBLIC_ENABLE_MAPLIBRE_SPIKE=true`일 때만 MapLibre/GSI 스파이크를 지연 로드한다. 플래그가 없거나 실패하면 외부 타일 없이 동일 지역 선택·목록·라이브 링크를 제공한다.
3. MVP 3 문서: 최신 검증 수치, 지도 런타임 경계, 날짜 의존 수치 표현을 제품 계획·기술 스파이크·인수인계에 동기화한다.
4. MVP 4 이후: Gate 2 데이터 확대, 운영 타일 공급자 승인, 실제 네트워크·모바일 브라우저 검증, 커밋·배포는 이번 실행에서 변경하지 않는다.

### 데이터·UI·명령·자동화 연결

- 데이터: 공개 공연의 `startsOn/endsOn/status` 계약은 유지하며 후보·그라비아·그룹 원본을 변경하지 않는다.
- UI: 그룹 상세 예정 공연과 홈 지역별 예정 공연 수가 같은 일본 날짜를 사용한다. 지도 플래그와 무관하게 지역 칩, 그룹 카드, 빈 상태, `/live?region=` 이동을 유지한다.
- 명령: Gate 2 보고도 같은 날짜 유틸을 사용한다. 별도 날짜 계약 검사 명령으로 UTC/도쿄 날짜가 갈리는 순간과 명시 날짜 주입을 확인한다.
- 권리·안전: 후보 공개 분리, 그라비아 `link_only`, 미성년자, Gate 2 비노출 정책은 비변경 회귀 범위다.
- 운영: `.env`나 배포 설정을 만들지 않는다. 플래그 기본값은 비활성이고 개발자가 명시적으로 켠 환경에서만 외부 GSI 타일을 요청한다.

### 실패·로딩·빈 상태와 회귀 위험

- 잘못된 날짜/시간대 입력은 조용히 UTC로 대체하지 않고 유틸 계약에서 실패시킨다.
- MapLibre 비활성·WebGL 부재·모듈 실패·10초 초기화 실패에도 SVG와 목록이 즉시 또는 계속 사용 가능해야 한다.
- 지도 비활성화를 Gate 2 UI 활성화나 공급자 승인으로 표현하지 않는다.
- 정적 생성 시각에 따른 페이지 수와 미래 공연 수는 실행일 없이 영구 기준선으로 고정하지 않는다.
- 동적 import 제거·조건화가 클라이언트 번들, hydration, 접근 가능한 지역 버튼을 깨뜨릴 수 있으므로 타입·빌드와 정적 코드 검사를 수행한다.

### 완료 조건과 검증 방법

- 일본 날짜 공통 유틸과 날짜 경계 검사가 통과한다.
- 기본 빌드의 지도는 외부 타일을 마운트하지 않으며 플래그 활성 경로는 코드상 보존된다.
- 데이터 검사, Gate 2 감사, 타입 검사, Next 프로덕션 빌드, `git diff --check`를 순차 실행한다.
- 인수인계와 기술 문서에 실제 결과·미검증 항목·잔여 MVP 4~5를 기록한다.

### 실행 결과

- MVP 1: `src/lib/japan-date.ts`에 공통 `Asia/Tokyo` 날짜 계약을 추가하고 그룹 예정 공연 조회와 Gate 2 보고를 연결했다. `scripts/verify-japan-date.ts`는 일본 자정 직전·직후, 연도 경계, 잘못된 날짜 실패를 검증해 통과했다.
- MVP 2: 전국 지도는 플래그가 없는 기본 환경에서 SVG·지역 칩·그룹 목록·라이브 링크를 즉시 사용한다. MapLibre/GSI는 `NEXT_PUBLIC_ENABLE_MAPLIBRE_SPIKE=true`일 때만 마운트하며 기존 WebGL·모듈·10초 초기화 실패 폴백을 유지했다.
- MVP 3: `MAP_TECH_SPIKE.md`와 `CODEX_HANDOVER.md`에 지도 운영 경계, 639페이지 최신 빌드, 공개 공연 원본 25건과 2026-08-29 미래 공연 24건의 구분을 반영했다.
- 데이터 검사 통과 — 37그룹, 188멤버, 9 GeoArea, 공지 4건, 공개 그라비아 5건, 그라비아 후보 2건, 공개 공연 원본 25건, 공연 후보 52건, 출처 10개, 그룹 후보 36개, 경고 39건.
- 날짜 계약 검사와 TypeScript `--noEmit` 통과.
- Gate 2 감사 통과 실행 — 도쿄 그룹 26/20, 특별구 5/5, 지도 준비 공연장 11/15, 연결 미래 공연 17/30, 전국 미래 공연 24건. 판정은 의도대로 `NOT READY`다.
- Next.js 15.5.23 프로덕션 빌드 통과 — 639/639 정적 페이지, 홈 First Load JS 185kB.
- `git diff --check` 통과. 기존 작업 트리의 LF→CRLF 안내만 출력됐다.
- 전체 가지 정적 회귀 확인: 공개 UI의 공연·그라비아 후보/출처 카탈로그 import 없음, 그라비아 루트·갤러리 `link_only` 이미지 차단 유지, 도쿄 23구 UI 문자열·경로 노출 없음, 지도 플래그의 코드·기술 문서·인수인계 연결 일치.
- 미검증 잔여: 플래그 활성 상태의 제한 없는 실제 네트워크 MapLibre 브라우저 검증, 공급자 운영 승인, 39개 데이터 경고 해소, Git 커밋·프로덕션 배포. 모두 MVP 4~5 또는 별도 실행 단위로 유지한다.

## 2026-08-29 MVP 4 도쿄 공연장·미래 공연 밀도 소배치

### 사용자 의도와 큰 줄기

- Gate 2의 남은 병목을 공식 원문으로만 줄이고, 수량 목표보다 공연장·공연·그룹·좌표의 재검증 가능한 연결을 우선한다.
- 전국 State A, 그라비아, 후보 격리, 날짜 계약, 지도 공급자 플래그는 변경하지 않는다.

### 변경 범위와 조사 순서

1. 현재 11개 지도 준비 공연장과 17개 연결 미래 공연을 중복 기준선으로 고정한다.
2. 공개 도쿄 그룹의 공식 일정에서 2026-08-29 이후 반복 공연과 도쿄 공연장을 찾는다.
3. 그룹/주최자 공식 공연 원문과 공연장 공식 주소·지도 좌표를 교차 확인한다.
4. 공식 근거가 완결된 공연장 최대 4곳·미래 공연 최대 13건만 `chika-live.json`과 그룹 `areaEvidence`에 연결한다.
5. 각 변경 뒤 Gate 2를 재계산하고 실패하면 해당 소배치 전체를 쓰지 않는다.

### 데이터·UI·명령·자동화 연결

- 데이터: 기존 ID·공연장 정규화·GeoArea·공개 그룹만 사용하고 후보 파일은 자동 승격하지 않는다.
- UI: 공개 데이터에 연결된 공연만 목록·상세·그룹 상세·지도 지역 집계에 나타난다. 미정 필드는 기존 null/TBA 표현을 유지한다.
- 명령: 데이터 검사, `data:report:map-gate2`, 일본 날짜 계약 검사가 동일 공개 계약을 검증한다.
- 운영: 외부 조사는 공식 그룹·주최자·공연장 페이지만 사용하고 확인일을 기록한다. 배포·커밋은 이번 실행 범위가 아니다.

### 실패·빈 상태·회귀 위험

- 도쿄 후보 0건은 정상 기준선이며 기존 비도쿄 후보를 도쿄 Gate에 포함하지 않는다.
- 주소만 있는 공연장은 특별구 근거에는 사용할 수 있지만 좌표 출처 없이는 지도 준비 수치에 포함하지 않는다.
- 같은 건물의 별도 홀, 유사 공연장명, 행사 전체 시간과 그룹 출연 시간, 선행 공지와 판매 페이지를 혼동하지 않는다.
- 공식 페이지가 바뀌거나 접근 불가하면 URL과 보류 사유만 기록하고 공개 데이터에 쓰지 않는다.
- 날짜 경과로 미래 공연 수가 감소할 수 있으므로 기준일과 원본 공연 수를 분리한다.

### 완료 조건과 검증 방법

- 공식 근거가 완결된 새 공연장·공연·공간 근거만 중복 없이 연결된다.
- Gate 2 변화가 실제 추가 데이터와 일치하고 미달이면 `NOT READY`를 유지한다.
- 데이터·날짜·타입·빌드·diff·후보/권리/Gate UI 회귀 검사를 통과하고 실제 결과를 아래 기록한다.

### 실행 결과

- AsIs 공식 개별 일정 13건을 추가했다. 공식 시간 또는 티켓 URL이 없는 일정은 추정하지 않고 TBA/null로 유지했다.
- 신규 지도 준비 공연장 Spotify O-Crest, SHIBUYA PLEASURE PLEASURE, Shinagawa Stellar Ball 3곳을 추가하고, 기존 SELENE b2에 공식 접근 페이지의 지도 좌표를 보강했다. 결과적으로 지도 준비 공연장은 11개에서 15개로 증가했다.
- AsIs 활동 근거에 시부야구·미나토구 공식 공연 원문을 연결했다. 기존 GeoArea와 공개 그룹만 사용했고 후보 데이터는 승격하지 않았다.
- 데이터 검사 통과 — 37그룹, 188멤버, 9 GeoArea, 공지 4건, 공개 그라비아 5건, 그라비아 후보 2건, 공개 공연 원본 38건, 공연 후보 52건, 출처 10개, 그룹 후보 36개, 경고 39건.
- Gate 2 감사 통과 — 도쿄 활동 그룹 26/20, 활동 근거 특별구 5/5, 지도 준비 공연장 15/15, 해당 공연장 연결 미래 공연 31/30, 전국 미래 공연 37건. 판정은 `PASS`다. 신규 일정은 13건이지만 기존 SELENE b2 공연 1건도 좌표 보강으로 지도 준비 집계에 포함되어 연결 미래 공연은 14건 증가했다.
- 일본 날짜 계약 검사와 TypeScript `--noEmit`을 통과했다.
- Next.js 15.5.23 프로덕션 빌드 통과 — 678/678 정적 페이지, 홈 First Load JS 185kB.
- `git diff --check` 통과. 기존 작업 트리의 LF→CRLF 안내만 출력됐다.
- 최종 재검증에서 `pnpm`은 제품 스크립트 진입 전 공급망 정책 검사에서 esbuild·sharp의 미승인 빌드 스크립트를 감지해 종료했다. 의존성 승인이나 재설치는 하지 않았고, 이미 설치된 `tsx`·`tsc` 로컬 실행 파일을 직접 호출한 데이터·Gate 2·날짜·타입 검사는 모두 통과했다. 이는 제품 검증 실패와 분리된 운영 잔여 항목이다.
- Gate 2 통과는 UI 자동 공개 승인이 아니다. 도쿄 23구 드릴다운은 별도 MVP 작업지시에서 데이터·UI·명령·빈 상태·접근성·운영 공급자 가지를 설계하고 검증한 뒤 공개한다.
- 적대적 검토는 agy 중립 브리프까지 완료했다. Sonnet 저장소 조사는 큰 파일 읽기 중 자체 컨텍스트 자동압축 반복으로 결과 없이 종료되어 자동 재시도하지 않았고, 입력 증거가 없어 Opus 판정·agy 최종 합성도 실행하지 않았다. Codex 직접 반증 검사에서는 공개 `src`의 후보 카탈로그 import 없음, `link_only` 이미지 필터, 지도 플래그 조건, Gate 2 UI 비노출을 다시 확인했다.
- 미검증 잔여: 39개 데이터 경고 해소, pnpm 빌드 스크립트 승인 정책 결정, MapLibre 실제 네트워크·모바일 브라우저 검증, 타일 공급자 운영 승인, 도쿄 23구 UI 구현, Git 커밋·프로덕션 배포.

## 2026-08-29 MVP 5-A 수동 일일 업데이트 운영기

### 사용자 의도와 제품 줄기

- 사용자가 바탕화면에서 하루 한 번 실행해 공식 공연 후보를 갱신하고 생일자·생일 누락 현황을 확인할 수 있게 한다.
- 자동화는 공개 품질 경계를 낮추지 않는다. 신규 네트워크 데이터는 후보 큐에만 쓰고 공개 승격은 공식 원문 검토 후 별도 작업으로 유지한다.

### 변경 범위와 연결 가지

- 명령: 기존 세 공연 수집·보강 명령과 데이터 검증을 순차 조정하는 `scripts/daily-update.ts`를 추가한다.
- 데이터: write 모드에서 `data/chika-live-candidates.json`만 기존 수집기 계약에 따라 갱신한다. 공개 그룹·공연·그라비아 데이터는 쓰지 않는다.
- 생일: 공개 그룹의 현역·검증 멤버 중 공식 `birthDate`가 있는 멤버만 오늘/향후 30일 보고에 포함하고 null은 누락 큐로 분리한다.
- 운영: `.tmp/daily-update/latest-report.json`과 날짜별 Markdown 보고서를 남긴다. 바탕화면 `.cmd`는 Node와 저장소 존재를 검사한 뒤 write 모드를 실행한다.
- UI: 이번 실행은 UI를 바꾸지 않는다. 후보와 운영 보고서는 공개 번들에서 계속 분리한다.

### 실패·빈 상태·회귀 위험

- 한 공급원의 HTTP·파싱 실패가 전체 실행을 막지 않도록 단계별 실패를 기록하고 나머지 수집·보고·검증을 계속한다.
- 수집 결과 0건과 내용 변경 없음은 정상 상태다. 기존 후보의 검토 상태와 공개 연결 ID를 보존한다.
- 상세 보강은 해당 공급원의 후보가 없으면 정상 건너뛰기로 처리한다.
- 생년이 없는 월일만 추정하거나 SNS 검색 결과로 생일을 채우지 않는다. 공식 생일이 0건이어도 빈 보고서를 정상 생성한다.
- write 실행 후 데이터 검증 실패 시 종료 코드는 실패로 반환하되 변경 파일을 자동 되돌리지 않고 보고서에 남긴다.

### 완료 조건과 검증 방법

- 기본 dry-run은 후보 파일을 바꾸지 않고 단계·생일·누락 요약을 출력한다.
- `--write`는 공식 공급원 결과를 후보 파일에만 저장하고 보고서를 만든다.
- `--skip-network`로 네트워크 없이 생일·검증·보고 흐름을 재현할 수 있다.
- 데이터 검사, 후보 보고, 타입 검사, 프로덕션 빌드, 공개 후보 import 검색, diff 검사를 통과하고 실제 결과를 아래 기록한다.

### 실행 결과

- 오프라인 실행은 통과해 향후 30일 생일 1명과 생일 미확인 현역 멤버 152명을 보고했다.
- 공식 공급원 dry-run은 タイトル未定 후보 16건, FRUITS ZIPPER 후보 38건과 상세 시간 26건·장소 38건을 확인했다.
- 첫 write 실행 후 데이터 검사가 `miteititle-a-villa-eve-2026: public source event missing published candidate link`로 실패했다. 원인은 미래 기준 수집 결과가 날짜 지난 `published` 후보를 제거한 것이다.
- 방향 변경: 두 일정 수집기는 재발견되지 않아도 `published`·`rejected` 후보와 공개 연결 ID를 보존한다. 누락된 공식 캘린더 이력은 넓은 과거 기준일로 한 번 재수집해 복구하고, 수정 후 write·검증을 다시 실행한다.
- 두 수집기에 이력 보존 병합을 적용하고 A Villa 2026 전야제 후보를 공식 ICS에서 복구해 공개 공연 ID와 다시 연결했다. 재실행은 タイトル未定 현재 16건+보존 이력 1건, FRUITS ZIPPER 현재 38건으로 통과했다.
- 최종 후보는 55건이며 `published` 13건, `review_pending` 40건, `rejected` 2건이다. 후보 보고 분류는 상세 부족 6건, 분류 필요 4건, 검토 가능 30건이다.
- `scripts/daily-update.ts`, `DAILY_UPDATE.md`, 바탕화면 `Chika Idol Box 일일 업데이트.cmd`를 추가했다. 실행기는 공개 데이터를 수정하지 않고 후보 write 모드와 보고서 생성을 호출한다.
- 오프라인·네트워크 dry-run·write 모드, 데이터 검사, 후보 감사, TypeScript를 통과했다. Next.js 15.5.23 프로덕션 빌드는 678/678 정적 페이지를 생성했고 `git diff --check`는 기존 LF→CRLF 안내 외 통과했다.
- 미확인 잔여: 공급원 고의 실패를 주입한 런타임 시험, 152명 생일의 공식 프로필 보강, 신규 후보의 공개 승격 검토, Windows 작업 스케줄러 자동 등록. 사용자는 하루 한 번 수동 실행하므로 예약 작업은 만들지 않았다.

## 2026-08-29 Core M1 날짜·생일 정합성 및 Map M1 재설계 감사

### 사용자 의도와 큰 줄기

- 권장 MVP 순서로 진행하되 현재 지도를 원하는 지도 경험으로 오인해 확장하지 않는다.
- 먼저 동일 공연·생일이 화면과 운영 명령에서 같은 일본 날짜로 판정되게 하고, 지도는 장소부터 공연·그룹을 발견하는 주 내비게이션으로 다시 설계한다.

### 변경 범위와 가지

- 날짜: 홈 예정 공연, 라이브 예정 필터, タイトル未定·FRUITS ZIPPER 수집 기준일을 `getJapanCalendarDate()`로 통일한다.
- 생일: 서버가 일본 기준 현재 월을 전달하고, 다음 달을 12→1로 순환하며 월·일 배지를 세 언어로 렌더링한다.
- 지도: `MAP_DISCOVERY_MVP.md`에 현 구현과 승인 목표의 구성요소별 차이, 폐기/보존 경계, Map M1~M4를 기록한다. 이번 실행에서 지도 코드는 바꾸지 않는다.
- 문서: 현재 수치와 역사 수치 충돌은 Operations O1 후속으로 남기되 발견된 구체 항목을 기록한다.

### 실패·빈 상태·회귀 위험

- 클라이언트 브라우저 시간대를 사용하지 않고 서버에서 일본 기준일을 전달해 hydration 경계 차이를 피한다.
- 수집기의 `collectedAt`은 감사용 UTC timestamp를 유지하고 `--from` 기본 달력 날짜만 일본 기준으로 바꾼다.
- 생일이 없는 152명은 빈 데이터로 유지하고 추정하지 않는다.
- 실제 지도 공급자와 경계가 없는 상태에서 레거시 SVG를 제거하지 않는다. 폴백을 완성 지도처럼 홍보하지 않는다.

### 완료 조건과 검증 방법

- 일본 자정 경계에서 홈·라이브·그룹 상세·Gate 2·일일 수집 기준이 같은 날짜 계약을 사용한다.
- ja/ko/en 생일 제목·월·일이 각 언어로 표시되고 12월 다음 달은 1월이다.
- 지도 설계 문서가 현재 DOM과 목표 UX 차이, MVP 순서, 공급자·접근성 게이트를 빠짐없이 포함한다.
- 날짜 검사, 일일 오프라인 검사, 데이터 검사, 타입 검사, 프로덕션 빌드, diff 검사를 실행하고 결과를 기록한다.

### 실행 결과

- 홈 예정 공연과 라이브 목록은 서버에서 `getJapanCalendarDate()`로 계산한 같은 기준일을 사용한다. 그룹 상세는 다일 공연의 `endsOn`까지 고려하도록 정정했다.
- タイトル未定·FRUITS ZIPPER 수집기의 기본 `--from` 날짜도 일본 달력 날짜로 통일했다. 수집 timestamp는 감사 계약대로 UTC ISO를 유지했다.
- 생일 위젯은 일본 기준 현재 월을 서버에서 받고 12→1월 순환을 적용했다. 제목·기간 배지·개별 날짜를 ja/ko/en으로 분기해 영어·일본어 화면의 한국어 단위 노출을 제거했다.
- 지도 재감사에서 근사 SVG·5개 도시 핀·하단 카드가 실제 지형·23구·공연 밀도·공연장·병렬 패널·URL 상태 목표를 충족하지 못함을 구성요소별로 확정했다. 레거시 SVG는 폴백 전용이며 Map M1~M4 재설계 전까지 확장하지 않는다.
- Open Design 실행 도구는 현재 세션에 연결되어 있지 않아 외부 디자인 생성은 실행하지 않았다. 로컬 브라우저도 런타임 내부 자산 경로 오류로 시각 확인하지 못했으며, 확인한 것으로 기록하지 않는다. 이번 지도 판정은 문서·DOM·CSS·상태 흐름 직접 대조 근거다.
- 일본 날짜 검사, 일일 오프라인 검사, 데이터 검사(37그룹·188멤버·38공연·55후보, 경고 39건), TypeScript를 통과했다. Next.js 15.5.23 빌드는 678/678 정적 페이지를 생성했다.
- 잔여: 실제 화면 캡처와 모바일 상호작용 감사, Operations O1 문서 기준선 정리, 영어 UI 전수 수정, Map M1 사용자 선택 확정, 공급자·경계 데이터 승인.

## 2026-08-29 Map M1/M2 실제 지도 최소 구현

### 의도·범위

- 근사 SVG를 기본 지도에서 내리고 공식 실지도 위에서 지역과 공연장을 탐색하는 첫 사용 가능 수직 조각을 만든다.
- 전국 첫 화면, 공연 밀도 기본 레이어, 그룹 레이어 전환, 도쿄 선택 시 카메라 이동을 기본값으로 한다.

### 데이터·UI·URL·운영 연결

- 데이터: 활동 그룹의 지역 중심점, 지도 준비 공연장 좌표, 해당 공연장의 예정 공연 수만 사용한다.
- UI: 데스크톱은 지도/결과 패널 2열, 모바일은 지도 위·결과 아래다. 지역 선택, 레이어 전환, 공연장 핀 선택을 키보드 가능한 컨트롤과 동기화한다.
- URL: `mapRegion`과 `mapLayer=events|groups`를 query에 저장하고 새로고침·뒤로가기에서 복원한다.
- 운영: GSI 담색 타일과 attribution을 기본 MVP로 사용하고 `NEXT_PUBLIC_MAP_PROVIDER=fallback`에서 외부 요청 없는 목록 폴백을 강제한다.

### 실패·빈 상태·회귀 위험

- WebGL·모듈·10초 초기 로드 실패는 목록 폴백으로 전환한다. 개별 타일 오류는 즉시 전체 지도를 제거하지 않는다.
- 좌표 없는 공연장과 장소 미정 공연은 핀으로 만들지 않는다.
- 23구 경계를 흉내 내는 임의 SVG를 만들지 않는다. 경계 미구현 상태를 명확히 표시한다.
- URL이 잘못되면 도쿄가 아니라 전국 기본 선택과 공연 레이어로 안전 복원한다.
- 외부 지도는 사용자의 IP가 GSI에 전달될 수 있음을 운영 문서에 기록한다.

### 완료 조건·검증

- 지도 기본값, 레이어·지역 선택, 카메라 이동, 공연장 핀, 결과 패널, URL 동기화, 목록 폴백을 코드와 정적 검사로 확인한다.
- 데이터·날짜·타입·프로덕션 빌드·diff 검사를 통과한다.
- 로컬 브라우저가 복구되지 않으면 시각·모바일 검증을 미통과로 남기고 배포하지 않는다.

### 실행 결과

- GSI 담색 실지도를 로컬 MVP 기본값으로 전환하고 `NEXT_PUBLIC_MAP_PROVIDER=fallback` kill switch와 기존 SVG·목록 폴백을 유지했다.
- 전국/5개 지역 선택, 공연·그룹 밀도 전환, 지역별 카메라, 좌표가 검증된 공연장 핀, 지도 옆 결과 패널과 Google Maps 연결을 추가했다. 도쿄 23구 경계는 공식 경계 데이터 검증 전까지 미구현 안내를 표시한다.
- `mapRegion`·`mapLayer`를 URL에 기록하고 사용자 선택은 브라우저 이력에 추가한다. 새로고침과 `popstate` 복원은 별도 이력을 만들지 않는다.
- 데스크톱은 지도/결과 2열, 800px 이하에서는 지도 위·결과 아래로 전환한다. 완성 목표의 모바일 bottom sheet는 Map M4 잔여다.
- 일본 날짜 계약, 데이터 검사(37그룹·188멤버·38공연·55후보, 경고 39건), 일일 오프라인 실행, Gate 2(26/20·5/5·15/15·31/30), TypeScript와 Next.js 15.5.23 프로덕션 빌드를 통과했고 678/678 정적 페이지를 생성했다. `git diff --check`는 기존 LF→CRLF 안내 외 오류 없이 통과했다.
- 브라우저 런타임 자산 오류로 실제 타일 렌더·핀 클릭·모바일 레이아웃은 검증하지 못했다. 따라서 커밋·배포하지 않았고 시각 품질과 외부 네트워크 동작은 미통과 상태다.

## 2026-08-29 Map M4 브라우저·모바일 검증 및 결함 보완

### 의도와 범위

- 현재 실지도가 사용자가 원한 장소 중심 탐색으로 실제 동작하는지 확인하고, 정적 검사로 발견할 수 없는 시각·상호작용 결함을 수정한다.
- 이번 실행은 데스크톱과 모바일 지도, 지역·레이어·공연장 선택, URL 뒤로가기, 외부 타일 실패 폴백까지다. 23구 GeoJSON 도입은 공식 경계 계약 검증 전까지 변경하지 않는다.

### 검증 시나리오와 완료 조건

- 한국어 홈에서 GSI 타일과 attribution, 전국 공연 밀도, 지도/결과 2열이 보인다.
- 도쿄 선택 시 카메라가 이동하고 공연장 핀을 선택하면 장소명·주소·Google Maps 링크가 결과 패널에 나타난다.
- 공연/그룹 레이어 전환과 지역 선택이 URL에 기록되며 브라우저 뒤로가기로 직전 상태가 복원된다.
- 모바일 폭에서 지도는 45~55vh를 유지하고 결과가 탐색을 가리지 않으며 지역·레이어 컨트롤을 터치할 수 있다.
- 타일 또는 WebGL 실패 시 그룹·공연 링크가 남는 목록 폴백이 제공된다.
- 발견한 결함은 구현 전에 이 절에 원인·영향을 추가하고, 수정 후 타입·빌드·브라우저 재검증 결과를 기록한다.

### 실행 결과

- 정적 상태 흐름 재감사에서 지역·레이어 변경마다 MapLibre 인스턴스가 제거·재생성되어 카메라 이동이 끊기고 타일을 다시 요청하는 결함을 확인했다. 지도 인스턴스는 유지하고 source·paint·camera만 갱신하도록 수정한다.
- 모바일 결과 패널은 단순 세로 블록이라 승인된 bottom sheet 구조가 아니었다. 지도 하단에 붙는 제한 높이 패널, 시각적 손잡이, 내부 스크롤을 추가하되 문서 흐름과 링크 접근성은 유지한다.
- 공연장 선택 결과가 장소명·주소만 보여 공연 발견 줄기가 끊겼다. 선택 공연장의 예정 공연 목록과 상세 링크를 같은 패널에 연결한다.
- 브라우저 연결은 로컬 실행 자산 경로 오류로 초기화되지 않았고, 개발 서버도 응답이 멈춰 프로덕션 빌드 서버로 재검증 경로를 전환한다. 실제 화면 검증이 성공하기 전 완료로 처리하지 않는다.
- 지도 인스턴스를 최초 한 번만 만들고 이후 지역·레이어·언어·공연장 데이터 변경은 GeoJSON source, paint property, `easeTo`만 갱신하도록 보완했다. 지역 전환 시 타일·컨트롤 전체가 깜빡이는 경로를 제거했다.
- 모바일은 52vh 지도 아래에 최대 46vh의 sticky 결과 sheet, 손잡이, 내부 스크롤을 적용했다. 공연장 선택 시 최대 5개의 연결 공연 날짜·제목·상세 링크가 주소·Google Maps와 함께 나타난다.
- 프로덕션 서버 `/ko` HTTP 200을 확인했다. 브라우저 제어 연결은 재시도에도 동일한 실행 자산 경로 오류가 발생해 실제 타일 픽셀·클릭·모바일 viewport 검증은 여전히 미통과다.
- 일본 날짜, 데이터 검사(경고 39건), Gate 2, TypeScript, `git diff --check`를 통과했다. Next.js 15.5.23 빌드는 678/678 정적 페이지를 생성했다.

## 2026-08-29 Map M2 도쿄 공식 경계 상세 레이어

### 사용자 의도와 데이터 경계

- 도쿄를 선택했을 때 근사 도형이 아니라 실제 행정 경계를 배경 지도 위에서 확인할 수 있게 한다.
- N03 2025 원본은 603.03MB이며 개별 페이지에 2차 이용 신청 필요 가능성이 명시돼 있어 다운로드·가공·저장소 포함을 보류한다.
- 대신 공식 GSI 실험 벡터 타일을 실시간 로드하고 시구정촌계 `boundary / ftCode=1212`만 강조한다. 원본 파일을 캐시·재배포하지 않는다.

### 구현 범위와 실패 상태

- 도쿄 선택 카메라를 경계가 제공되는 상세 축척으로 이동하고, 밝은 행정경계 선을 공연장 핀 아래·raster 지도 위에 표시한다.
- 전국·다른 지역에서는 상세 경계를 강조하지 않는다. 경계 타일 실패는 지도 전체 실패로 승격하지 않고 공연장·그룹·목록을 유지한다.
- 이는 23개 polygon 선택 완성이 아니다. 모든 구 면 선택은 승인 가능한 polygon 공급원을 확보한 후속 실행으로 남긴다.

### 완료 조건과 검증

- 공식 vector source URL, attribution, source-layer, feature code가 문서와 코드에서 일치한다.
- TypeScript, 데이터, Gate 2, 빌드, diff 검사를 통과한다.
- 제한 없는 브라우저에서 실제 경계 표시를 확인하지 못하면 시각 검증은 미통과로 기록한다.

### 실행 결과

- GSI `experimental_bvmap` vector source와 `boundary` source-layer의 `ftCode=1212` 시구정촌계 line layer를 추가했다. 도쿄 선택에서만 노출하고 vector zoom 11.05로 이동한다.
- attribution은 공식 벡터 타일 제공 실험 안내로 연결했다. 벡터 source는 raster·지역·공연장 source와 분리돼 경계 타일 실패가 핵심 탐색을 제거하지 않는다.
- N03 원본 다운로드·단순화·저장소 포함은 하지 않았다. 현재 구현은 실제 경계선 상세 표시이며 23개 면 클릭·전체 조망은 후속이다.
- TypeScript, 데이터 검사(37그룹·188멤버·38공연·55후보, 경고 39건), Gate 2와 `git diff --check`를 통과했다. Next.js 15.5.23 빌드는 678/678 정적 페이지를 생성했다.
- 브라우저 제어 자산 오류가 계속되어 경계 타일의 실제 픽셀 표시·팬·줌은 확인하지 못했다. 커밋·배포하지 않는다.

## 2026-08-30 Map M3 도쿄 검증 구·공연장 드릴다운

### 의도와 변경 범위

- 도쿄 상세 지도에서 공식 근거가 있는 행정구를 선택하고 그 구의 공연장·공연·관련 그룹으로 좁혀 가는 수직 흐름을 완성한다.
- `GeoAreaDataset`의 ward와 `ChikaVenue.geoAreaId`, 공연 `venueId`, 그룹 `areaEvidence.areaId`만 연결 근거로 사용한다.

### 데이터·UI·URL 연결

- `getGeoAreas()` 읽기 경계를 추가해 홈 서버 컴포넌트가 검증된 ward 메타데이터를 지도에 전달한다.
- 도쿄를 선택했을 때 실제 공연장 데이터가 있는 구만 칩으로 표시한다. 구 선택은 공연장 핀·공연 수·그룹 카드·결과 제목을 동시에 필터링한다.
- URL은 `mapRegion=tokyo&mapWard={geoAreaId}&mapLayer=...`를 사용한다. 도쿄 밖 지역 또는 잘못된 ward 값에서는 `mapWard`를 제거한다.
- 구 중심 좌표를 임의로 하드코딩하지 않고 해당 구의 검증 공연장 좌표 평균으로 카메라를 이동한다. 좌표가 없으면 도쿄 카메라를 유지한다.

### 실패·빈 상태·회귀 위험

- `district` 활동권역 필터와 ward 행정구 필터를 같은 상태로 취급하지 않는다. ward 선택 시 활동권역은 전체로 초기화한다.
- 공연장 데이터가 사라진 구는 활성 칩에서 자동 제외되며, 그룹 근거만 있는 구는 지도 공연장 드릴다운으로 위장하지 않는다.
- 외부 경계 타일 실패와 무관하게 구 칩·공연장·공연 링크는 로컬 데이터로 계속 동작한다.

### 완료 조건·검증

- 5개 현재 검증 구가 데이터에서 파생되고 구 선택이 URL·핀·공연·그룹을 같은 기준으로 필터링한다.
- 날짜·데이터·Gate 2·타입·빌드·diff 검사를 통과하고 결과를 기록한다.
- 브라우저 실화면 검증 실패는 미통과로 남긴다.

### 실행 결과

- `getGeoAreas()`를 추가해 검증된 행정구 메타데이터를 서버에서 지도에 전달했다. 현재 공연장 데이터에서 파생된 활성 구는 주오·고토·시부야·지요다·미나토 5개다.
- 도쿄 결과 패널에 구 칩과 구별 공연장 수를 추가했다. 구 선택은 `mapWard`, 공연장 핀, 공연 수, 정확한 `areaEvidence.areaId`가 있는 그룹 카드, 공연장 좌표 평균 카메라를 함께 갱신한다.
- 도쿄 밖 지역 선택, 잘못된 URL ward, 전국 복귀에서는 ward를 `all`로 복원하고 `mapWard`를 제거한다. 활동권역 `district` 상태는 별도로 유지하되 ward 변경 시 전체로 초기화한다.
- 일본 날짜, 데이터 검사(37그룹·188멤버·9 geo areas·38공연·55후보, 경고 39건), Gate 2(2026-08-30 기준 26/20·5/5·15/15·31/30), TypeScript와 `git diff --check`를 통과했다.
- Next.js 15.5.23 프로덕션 빌드는 678/678 정적 페이지를 생성했다. 브라우저 제어 환경 오류 때문에 실제 구 칩·카메라·뒤로가기 시각 상호작용은 미검증이며 커밋·배포하지 않는다.

## 2026-08-30 Map M4 접근성·공간 계약 회귀 보완

### 변경 범위

- 지역·레이어·구·활동권역 버튼에 프로그램적으로 읽히는 선택 상태를 제공한다.
- 결과 패널과 공연 목록의 제목 관계를 명시하고 키보드 포커스·모바일 터치 크기를 보강한다.
- `prefers-reduced-motion`에서 지도 `easeTo`와 그룹 목록 전환 애니메이션을 제거한다.
- 데이터 검사에서 공연장 행정구의 level·parent 계약을 검증한다.

### 실패·회귀 위험과 완료 조건

- canvas 자체에 복잡한 application role을 부여하지 않고 모든 기능의 HTML 버튼·링크 대체 경로를 유지한다.
- `aria-pressed`는 실제 React 상태와 항상 같아야 하며 화면에 없는 구를 접근성 트리에 추가하지 않는다.
- 공간 계약 강화가 현재 15개 지도 준비 공연장과 Gate 2 데이터를 모두 통과해야 한다.
- 날짜·데이터·Gate 2·타입·빌드·diff를 재검증하고 브라우저 미검증 항목은 그대로 남긴다.

### 실행 결과

- 공연·그룹 레이어, 전국·지역, 검증 구, 활동권역 버튼에 실제 상태와 동기화된 `aria-pressed`를 추가했다. 결과 패널은 제목과 `aria-labelledby`로 연결했다.
- 지도 영역의 접근 가능한 설명에 동일 기능의 HTML 지역·구 버튼이 뒤따른다는 점을 명시해 canvas 조작이 불가능해도 탐색 경로가 남는다.
- `prefers-reduced-motion`에서 지도 카메라 이동 시간을 0으로 만들고 그룹 결과의 진입·퇴장 애니메이션과 지도 UI transition을 제거했다.
- 지도 필터 버튼은 모바일에서 최소 44px, 데스크톱에서 40px 높이를 보장하고 키보드 focus-visible 외곽선을 추가했다.
- 데이터 검사기는 공연장 `geoAreaId`가 실제 ward인지, 도쿄 공연장의 ward가 `tokyo-to` 아래인지 검증한다. 현재 데이터는 새 공간 계약을 통과했다.
- 일본 날짜, 데이터 검사(경고 39건), Gate 2(26/20·5/5·15/15·31/30), TypeScript와 `git diff --check`를 통과했다. Next.js 15.5.23 빌드는 678/678 정적 페이지를 생성했다.
- 실제 키보드 탭 순서·스크린리더 발화·모바일 터치 검증은 브라우저 제어 환경 오류로 미통과이며 커밋·배포하지 않는다.

## 2026-08-30 사용자 캡처 기반 지도 탐색 구조 교정

### 증거와 원인

- `002010`: 전국 제목·36그룹 상태인데 지도 카메라는 도쿄 도심이다. URL 복원이 map load 전에 끝나면 후속 source/camera 동기화 effect가 다시 실행되지 않는 것이 원인이다.
- `002054`: 초기 실지도 이후 10초 timeout이 전체 폴백을 작동시켜 근사 SVG가 나타난다. 외부 source가 느린 상황과 MapLibre 초기화 실패를 같은 실패로 취급한 것이 원인이다.
- `002033`, `002038`: 지도 아래에 36개 대형 `GroupCard`가 계속 렌더링되어 선택 결과가 지도 공간과 분리된다.

### 변경 범위

- map style 준비 시점에 최신 region·ward·layer 상태를 반드시 재적용한다.
- style 초기화 성공을 지도 준비 완료로 판단하고 개별 raster/vector 응답 지연으로 SVG 전환하지 않는다. 명시적 `fallback` provider와 모듈/WebGL/style 초기화 실패만 폴백한다.
- 지도 아래 `GroupCard` grid, 중복 지역 요약·필터 영역을 제거한다.
- 지역 선택 전에는 지도 안에 탐색 안내만 표시하고, 지역 선택 후 해당 그룹을 지도 하단 compact overlay로 표시한다. ward와 활동권역 선택도 같은 overlay를 갱신한다.
- 공연장 선택과 공연 링크는 우측 결과 패널에서 유지하며 데스크톱 전체 지도 높이를 확대한다.

### 완료 조건·검증

- 전국 최초 상태의 실제 지도 카메라와 제목이 일치한다.
- 도쿄 등 지역 선택 후 카메라 확대와 그룹 overlay가 같은 상태 변경에서 나타난다.
- 지도 아래 대형 그룹 카드 목록이 존재하지 않는다.
- 10초 후에도 초기화에 성공한 실지도가 SVG로 교체되지 않는다.
- 날짜·데이터·Gate 2·타입·빌드·diff를 통과하고 새 캡처 재검증 전에는 시각 완료로 단정하지 않는다.

### 실행 결과

- map style 준비 이벤트에서 `readyVersion`을 갱신해 URL 복원이 먼저 끝난 경우에도 최신 region·ward·layer source와 카메라를 다시 적용하도록 수정했다.
- 지도 준비 판정을 전체 source `load`가 아니라 style 초기화 성공으로 바꿨다. 개별 raster/vector 응답 지연은 10초 SVG 전환을 일으키지 않고, WebGL·모듈·style 초기화 자체 실패만 폴백한다.
- 지도 아래 중복 요약, 지역·활동권역 바, 36개 대형 `GroupCard` grid를 제거했다. 지역·구·활동권역 컨트롤은 결과 패널 안으로 통합했다.
- 전국 상태에서는 지도 위 선택 안내만 표시한다. 지역 선택 후 현재 필터의 그룹 이름·멤버 수·소속사와 상세 링크가 지도 하단 compact horizontal overlay로 나타난다.
- 데스크톱 지도는 72vh·최대 760px로 확대했고 모바일은 58vh 지도 안에 같은 그룹 overlay를 유지한다.
- TypeScript, 일본 날짜, 데이터 검사(경고 39건), Gate 2, `git diff --check`를 통과했다. Next.js 15.5.23 빌드는 678/678이며 홈 first-load 경로 크기는 대형 카드 의존 제거로 187kB에서 146kB로 감소했다.
- 새 구조의 실제 타일·확대·overlay는 사용자의 후속 캡처로 재검증해야 하며 아직 시각 완료로 판정하지 않는다. 커밋·배포하지 않는다.

## 2026-08-28 프로덕션 배포

### 사용자 의도와 큰 줄기

- 로컬에서 정합성과 빌드가 확인된 전국 지도·라이브·그라비아 독립 가지 및 Gate 2 데이터 진척을 현재 Vercel 프로덕션에 반영한다.
- 커밋 여부와 배포 산출물 상태를 구분하며, 기존 연결 프로젝트 `chika-idol-hub` 외 다른 프로젝트를 변경하지 않는다.

### 변경 범위와 연결

- `.vercel/project.json`의 프로젝트·조직 연결을 확인하고 현재 작업 트리를 프로덕션 대상으로 배포한다.
- 배포 URL과 프로덕션 별칭, READY 상태, 실제 한국어 홈·라이브 응답을 확인한다.
- 배포 결과를 작업지시서와 인수인계에 기록한다.

### 실패·빈 상태·회귀 위험

- 로컬 작업 트리는 HEAD `94ff544` 이후 미커밋 변경을 포함하므로 배포 성공을 Git 커밋 완료로 표현하지 않는다.
- 빌드 실패 또는 READY 미도달 시 프로덕션 성공으로 기록하지 않고 로그를 확인한다.
- 배포 후 핵심 경로가 비정상 응답이면 새 배포 상태를 보고하고 임의 롤백하지 않는다.

### 완료 조건과 검증 방법

- Vercel 프로덕션 배포가 READY이며 프로덕션 별칭이 새 배포를 가리킨다.
- `/ko`와 `/ko/live`가 HTTP 200으로 응답한다.
- 배포 URL·상태·기준 HEAD·검증 결과를 문서에 남긴다.

### 실행 결과

- `.vercel/project.json`의 `chika-idol-hub` 연결과 CLI 인증을 확인한 뒤 현재 작업 트리를 프로덕션에 직접 배포했다.
- 배포 ID `dpl_5rjHkeVX9XdvkzByw3mUHTyK9mvY`, 고유 URL `https://chika-idol-kr78ywq70-royaljjongs-projects.vercel.app`, 상태 `READY`를 확인했다.
- 프로덕션 별칭 `https://chika-idol-hub.vercel.app`이 새 배포를 가리키며 `/ko`, `/ko/live` 모두 HTTP 200으로 응답했다.
- Vercel 원격 빌드는 Next.js 15.5.23으로 639개 정적 페이지 생성을 완료했다. 최근 1시간 error 레벨 로그 조회 결과 수집된 오류 로그가 없었다.
- 배포 기준 Git HEAD는 `94ff544`이며 이번 산출물은 이후 미커밋 작업 트리를 포함한다. 배포 성공과 Git 커밋 완료를 구분한다.

## 2026-08-28 시부야 반복 공연장 밀도 4차 소배치

### 사용자 의도와 큰 줄기

- 특별구 5/5 이후 남은 Gate 2 병목인 공식 좌표 공연장과 연결 미래 공연 밀도를 함께 높인다.
- 단발 검색 결과보다 그룹 공식 장기 일정에 여러 번 등장하는 공연장을 우선해 운영 가치가 높은 데이터를 만든다.

### 변경 범위와 연결

- yosugala 공식 투어의 2026-09-20 Spotify O-EAST 공연 1건을 추가한다.
- Palette Parade 공식 2026-09~2027-05 생탄 일정 7건을 Spotify O-WEST, Veats Shibuya, Shibuya WWW에 연결한다.
- 네 공연장은 각 공식 페이지의 주소와 공식 Google Maps 링크·임베드 좌표를 사용하고 `tokyo-shibuya-ku`에 연결한다.

### 실패·빈 상태·회귀 위험

- 생탄 공지는 날짜·장소 선공개 단계이므로 시간·가격·티켓을 추정하지 않는다.
- O-EAST와 O-WEST는 별도 공연장 ID로 유지하고 같은 건물 계열이라는 이유로 합치지 않는다.
- WWW와 WWW X를 혼동하지 않고 공식 공지에 적힌 `渋谷WWW`만 WWW에 연결한다.
- 공식 장기 일정이 변경될 수 있으므로 출처·확인일을 공연마다 유지한다.

### 완료 조건과 검증 방법

- 공식 좌표 공연장 4곳과 미래 공연 8건이 중복 없이 연결된다.
- 데이터 검사, Gate 2 감사, 타입 검사, 프로덕션 빌드, diff 검사를 통과하고 문서에 결과를 기록한다.

### 실행 결과

- Spotify O-EAST·O-WEST, Veats Shibuya, Shibuya WWW 4곳을 공식 주소와 공식 Google Maps 링크·임베드 좌표로 추가하고 시부야구에 연결했다.
- yosugala tour 2026 도쿄 공연 1건은 공식 개장 17:00·시작 18:00과 공식 티켓 경로를 반영했다. yosugala의 행정구 근거도 시부야구 GeoArea로 정규화했다.
- Palette Parade 공식 장기 생탄 일정 7건을 O-WEST 3건, Veats 2건, WWW 2건으로 연결했다. 선공개 공지에 없는 시간·가격·티켓은 null로 유지했다.
- 멤버 원장의 공식 읽기와 대조해 생탄제 한국어·영문 인명 표기를 일치시켰다.
- Gate 2 감사: 도쿄 활동 그룹 26/20, 활동 근거 특별구 5/5, 지도 준비 공연장 11/15, 해당 공연장 연결 미래 공연 17/30, 전국 미래 공연 25건. Gate 2는 `NOT READY`다.
- 데이터 검사 통과(37그룹·188멤버·9 GeoArea·공개 공연 25건), TypeScript·diff 검사 통과, Next 프로덕션 빌드 통과(639/639).

## 2026-08-28 미나토구 특별구 Gate 완결 소배치

### 사용자 의도와 큰 줄기

- 미나토구의 미래 공식 공연으로 Gate 2 특별구 가지를 5/5까지 닫는다.
- 특별구 충족과 지도 준비 공연장 충족을 분리해, 공식 좌표가 없는 공연장을 지도 준비로 과장하지 않는다.

### 변경 범위와 연결

- SELENE b2 공식 공연장 일정의 2026-09-05 `IDOLEAD`와 공개 출연 그룹 selfish를 연결한다.
- 공연장 공식 접근 페이지의 주소를 `tokyo-minato-ku`에 연결한다.
- 공식 접근 페이지에 좌표·외부 지도 링크가 없으므로 좌표와 좌표 출처는 null로 유지한다.
- 검증 중 확인된 UTC 날짜 오프셋을 함께 수정해 Gate 2 감사 기준일을 공연 운영 기준인 `Asia/Tokyo` 날짜로 계산한다.

### 실패·빈 상태·회귀 위험

- 공연장 공식 일정에 개장·시작·티켓 상세가 노출되지 않으면 추정하지 않는다.
- 공식 출연진 중 공개 데이터에 없는 그룹을 임의 생성하지 않는다.
- 주소만 검증된 공연장은 특별구 근거에는 사용하지만 `map-ready venue`에는 포함하지 않는다.
- 자정 직후 UTC 날짜로 전날 공연을 미래로 잘못 집계하지 않도록 감사 표기의 날짜와 필터 날짜를 동일한 도쿄 날짜로 사용한다.

### 완료 조건과 검증 방법

- 미나토구 활동 근거가 1개 이상 추가되어 특별구 Gate가 5/5가 된다.
- 공연장·공연·그룹 근거가 동일한 공식 시설 일정과 연결된다.
- 데이터 검사, Gate 2 감사, 타입 검사, 프로덕션 빌드, diff 검사를 실행한다.

### 실행 결과

- SELENE b2 공식 일정의 2026-09-05 `IDOLEAD`와 공개 출연 그룹 selfish를 연결했다. 상세 페이지에서 확인되지 않은 개장·시작·가격·티켓은 null로 유지했다.
- 공식 접근 페이지의 미나토구 주소를 공연장에 연결했다. 공식 좌표 또는 외부 지도 링크는 확인되지 않아 좌표·좌표 출처를 null로 유지했고 지도 준비 공연장 수에는 포함하지 않았다.
- selfish의 미나토구 활동 근거를 공식 공연장 일정에 연결해 특별구 Gate가 5/5를 충족했다.
- 감사 명령의 기준일을 UTC가 아닌 `Asia/Tokyo` 달력 날짜로 고정했다. 2026-08-28 재실행 결과 표기와 미래 공연 필터가 같은 날짜를 사용한다.
- Gate 2 감사: 도쿄 활동 그룹 26/20, 활동 근거 특별구 5/5, 지도 준비 공연장 7/15, 해당 공연장 연결 미래 공연 9/30, 전국 미래 공연 17건. Gate 2 전체는 `NOT READY`다.
- 데이터 검사 통과(37그룹·188멤버·9 GeoArea·공개 공연 17건), TypeScript·diff 검사 통과, Next 프로덕션 빌드 통과(615/615).

## 2026-08-27 지요다구 공식 합동 공연 3차 소배치

### 사용자 의도와 큰 줄기

- Gate 2에서 아직 근거가 없는 특별구를 기존 공개 그룹의 미래 공식 공연으로 채운다.
- 행사 공식·출연 그룹 공식·공연장 공식 근거를 교차해 공연, 그룹, 특별구, 좌표를 한 실행 단위로 연결한다.

### 변경 범위와 연결

- 2026-09-13 `アイドル甲子園 in KANDA SQUARE HALL`을 공개 공연으로 추가하고 공개 그룹 UtaGe!, selfish, Devil ANTHEM., Palette Parade를 연결한다.
- KANDA SQUARE HALL을 지요다구 GeoArea와 공식 관광 페이지의 Google Maps 좌표에 연결한다.
- 네 그룹에 지요다구 활동 근거를 같은 공식 공연 원문으로 추가한다.

### 실패·빈 상태·회귀 위험

- 공식 출연진 중 공개 데이터에 없는 그룹은 `groupIds`에 임의 생성하지 않는다.
- 행사 전체 개장·시작 시각을 개별 그룹 출연 시각으로 오인하지 않는다.
- 가격은 좌석·당일 조건이 복합적이므로 단일 문자열로 축약하지 않고, 공식 티켓 URL만 연결한다.

### 완료 조건과 검증 방법

- 공연 1건·공연장 1곳·공개 그룹 4개의 지요다구 근거가 동일한 공식 원문과 연결된다.
- 데이터 검사, Gate 2 감사, 타입 검사, 프로덕션 빌드, diff 검사를 통과하고 결과를 기록한다.

### 실행 결과

- KAMAITACI 공식 일정에 게시된 2026-09-13 합동 공연을 공개 공연으로 추가하고, 공식 출연진 중 공개 데이터에 존재하는 UtaGe!·selfish·Devil ANTHEM.·Palette Parade만 연결했다.
- KANDA SQUARE HALL의 공식 주소와 지요다구 관광협회 공식 페이지가 제공한 Google Maps 좌표를 저장하고 `tokyo-chiyoda-ku`에 연결했다.
- 네 그룹의 지요다구 활동 근거를 같은 공식 공연 상세로 추가했다. 행사 개장 09:15·시작 09:30과 공식 티켓 URL은 반영했고 복합 가격은 null로 유지했다.
- Gate 2 감사: 도쿄 활동 그룹 26/20, 활동 근거 특별구 4/5, 지도 준비 공연장 7/15, 해당 공연장 연결 미래 공연 9/30, 전국 미래 공연 16건. Gate 2는 `NOT READY`다.
- 데이터 검사 통과(37그룹·188멤버·9 GeoArea·공개 공연 16건), TypeScript·diff 검사 통과, Next 프로덕션 빌드 통과(612/612).

## 2026-08-27 도쿄 공식 일정 공급원 확대 2차 소배치

### 사용자 의도와 큰 줄기

- 월세 관련 오입력은 제품 작업과 완전히 분리하고 어떤 파일·판단에도 반영하지 않는다.
- Gate 2의 남은 특별구·공연장·미래 공연 간격을 기존 공개 그룹의 공식 일정에서 작은 단위로 줄인다.
- 그라비아 독립 가지와 전국 State A UI는 변경하지 않는다.

### 변경 범위와 연결

- AsIs 공식 일정의 2026-09-09 Zepp DiverCity(TOKYO) 도쿄 파이널과 Palette Parade 공식 공지의 2026-09-08 LINE CUBE SHIBUYA 5주년 공연을 공개 공연으로 추가한다.
- 각 공연장은 공식 시설 페이지의 주소·Google Maps 임베드/링크 좌표만 사용하고 고토구·시부야구 GeoArea에 연결한다.
- 두 그룹의 특별구 활동 근거를 상세 공식 일정 원문에 연결한다.

### 실패·빈 상태·회귀 위험

- 상세 미발표인 개장·시작 시각과 가격·티켓은 추정하지 않는다.
- 공식 지도 임베드의 장소 좌표와 단순 화면 중심을 구분한다. 장소 링크가 별도로 제공되면 장소 좌표를 우선한다.
- 기존 고토구 공연장과 중복 ID를 만들지 않고 공연장별 고유 주소를 유지한다.

### 완료 조건과 검증 방법

- 공식 공연장 2곳과 미래 공연 2건, 그룹 특별구 근거 2건이 연결된다.
- Gate 2 감사 수치가 실제 연결만큼 증가한다.
- 데이터 검사, Gate 2 감사, 타입 검사, 프로덕션 빌드, `git diff --check`를 실행하고 결과를 기록한다.

### 실행 결과

- AsIs 도쿄 파이널과 Palette Parade 5주년 라이브를 각각 공식 일정·공식 공지에서 확인해 공개 공연으로 추가했다. 발표되지 않은 시간·가격·티켓은 null로 유지했다.
- Zepp DiverCity(TOKYO)와 LINE CUBE SHIBUYA를 공식 시설 주소 및 공식 페이지 Google Maps 임베드·장소 링크 좌표와 함께 추가하고 고토구·시부야구에 연결했다.
- AsIs의 고토구, Palette Parade의 시부야구 활동 근거를 상세 공식 원문으로 추가했다.
- Gate 2 감사: 도쿄 활동 그룹 26/20, 활동 근거 특별구 3/5, 지도 준비 공연장 6/15, 해당 공연장 연결 미래 공연 8/30, 전국 미래 공연 15건. Gate 2는 `NOT READY`다.
- 데이터 검사 통과(37그룹·188멤버·9 GeoArea·공개 공연 15건), TypeScript 통과, Next 프로덕션 빌드 통과(609/609).

## 2026-08-27 도쿄 공식 공연장·미래 공연 Gate 2 1차 배치

### 사용자 의도와 큰 줄기

- 전국 지도 State A 다음의 핵심 데이터 가지로, 도쿄 공연 후보를 공식 공연장·특별구·좌표·공연 엔터티에 연결한다.
- 공연 후보를 수량 때문에 자동 승격하지 않고 그룹 공식 일정과 공연장 공식 원문을 모두 대조한다.
- 그라비아 독립 가지와 기존 전국 지도 UI는 변경하지 않는다.

### 변경 범위와 연결

- FRUITS ZIPPER 도쿄 미래 공연 후보 8건을 첫 검토 배치로 삼는다.
- 공연장 5곳: ベルサール汐留, 東京ガーデンシアター, SGC HALL ARIAKE, TOYOTA ARENA TOKYO, 有明アリーナ.
- 공식 주소로 중앙구·고토구 GeoArea를 추가하고 공연장 `geoAreaId`를 연결한다.
- 공식 공연장 페이지가 제공하는 Google Maps 링크·임베드에서 좌표가 확인된 4곳만 좌표와 `coordinateProvenance`를 저장한다. 有明アリーナ는 공식 주소만 저장하고 좌표는 null로 유지한다.
- 8개 후보는 공개 공연과 1:1 연결하고 후보 상태를 `published`로 바꾼다. 시간·티켓이 미발표인 필드는 추정하지 않는다.

### 실패·빈 상태·회귀 위험

- 같은 東京ガーデンシアター의 날짜별 공연을 중복 공연장으로 만들지 않는다.
- 공연장 주소와 그룹 일정의 명칭이 일치하지 않으면 승격하지 않는다.
- Google 검색·비공식 지오코딩 좌표는 사용하지 않는다. 공식 페이지의 지도 링크·임베드 좌표만 허용한다.
- 공개 공연의 `timeStatus`는 공식 시간이 없으면 `tba`, `ticketUrl`은 공식 티켓 원문이 없으면 null이다.
- 특별구 활동 근거 수 증가는 공연 근거일 뿐 행정 경계 `map_ready`를 의미하지 않는다.

### 완료 조건과 검증 방법

- 5개 공연장 ID·주소·특별구가 공식 원문과 일치하고 좌표 4개만 출처와 함께 존재한다.
- 후보 8건과 공개 공연 8건이 중복 없이 1:1 연결된다.
- Gate 2 감사에서 특별구·지도 준비 공연장·연결 미래 공연 수가 실제 데이터만큼 증가한다.
- 데이터 검사, 후보 보고, Gate 2 감사, 타입 검사, 프로덕션 빌드, `git diff --check`를 실행한다.

### 실행 결과

- 중앙구·고토구 GeoArea 2개와 공식 공연장 5개를 추가했다. 벨사르 시오도메·도쿄 가든 시어터·SGC HALL ARIAKE·TOYOTA ARENA TOKYO 4곳은 공식 페이지의 지도 링크·임베드 좌표와 좌표 출처를 저장했고, 아리아케 아레나는 공식 주소만 확인되어 좌표를 null로 유지했다.
- FRUITS ZIPPER 공식 일정 후보 8건을 공개 공연 8건과 1:1 연결했다. 후보 원문에 시간이 명시된 11월 14·15일 공연만 개장/시작 시각을 확정했고, 나머지 시간과 공식 티켓 URL은 추정하지 않았다.
- 그룹 활동 근거를 중앙구·고토구에 연결하고 Gate 2 후보 보고는 `review_pending`만 세도록 정정했다. 처리한 도쿄 위치 후보 8건은 모두 `published`이며 잔여 도쿄 위치 검토 후보는 0건이다.
- Gate 2 감사: 도쿄 활동 그룹 26/20, 활동 근거 특별구 2/5, 지도 준비 공연장 4/15, 해당 공연장 연결 미래 공연 6/30, 전국 미래 공연 13건. Gate 2는 여전히 `NOT READY`다.
- 직접 데이터 검사 통과: 37그룹·188멤버·9 GeoArea·공개 공연 13건·공연 후보 52건, 경고 39건. `pnpm data:validate`는 pnpm의 기존 ignored-builds 승인 정책으로 설치 선행 단계에서 중단됐으나 동일 `scripts/validate.ts` 직접 실행은 통과했다.
- TypeScript 검사, Next 프로덕션 빌드(603/603), `git diff --check` 통과. diff 검사는 기존 LF→CRLF 경고만 출력했다.

## 2026-08-27 통합 가지 내비게이션·전국 지도 State A UI 정합성

### 사용자 의도와 큰 줄기

- `$agy-lean` 읽기 전용 스카우트로 넓은 UI 흐름을 압축 조사하고, 직접 코드 검증을 거쳐 전국 검증 아이돌 탐색이라는 큰 줄기와 지도·라이브·그라비아 독립 가지의 진입·이동을 일치시킨다.
- Gate 1에서 허용된 전국 State A만 강화하고, 데이터가 부족한 도쿄 23구 Gate 2 UI는 구현하지 않는다.
- 그라비아는 핵심 가지로 항상 접근 가능하게 하되 `link_only` 권리 경계를 카드 시각물에도 적용한다.

### 변경 범위와 연결

- 공통 내비게이션: 모바일에서도 홈/지도·라이브·그라비아가 보이고 현재 가지가 표시되도록 한다. 검색·언어 전환은 유지한다.
- 홈/지도: 검증 공개 공연을 서버에서 지도에 전달하고 지역별 `그룹 수 / 예정 공연 수`를 표시한다. 지역 라이브 링크는 `/live?region=`으로 연결한다.
- 라이브: URL의 `region`을 초기 필터로 받아 지도에서 이동한 사용자의 맥락을 보존한다. 유효하지 않은 값은 전체로 안전하게 되돌린다.
- 그라비아: 루트 `rightsStatus === link_only`이면 `imageUrl`을 임베드하지 않는다. 독립 페이지 재사용 시 중복 섹션 제목을 숨길 수 있게 한다.

### 실패·빈 상태·회귀 위험

- 지역별 예정 공연 0건도 정상 수치로 표시하고 라이브 페이지의 빈 상태·필터 초기화를 유지한다.
- 취소·연기 공연과 과거 공연은 지도 예정 수에서 제외한다.
- 모바일 내비게이션은 언어 전환과 충돌하지 않아야 하며 키보드 포커스·현재 페이지 표기를 제공한다.
- `link_only`는 외부 공식 링크를 막는 뜻이 아니라 이미지 복제·임베드만 막는 경계다.

### 완료 조건과 검증 방법

- 모바일·데스크톱에서 세 제품 가지와 현재 위치가 명확하다.
- 지도 수치와 `/live?region=` 초기 필터 결과가 같은 공개 공연 데이터에서 계산된다.
- `link_only` 표지는 placeholder로 렌더되고 중복 그라비아 제목이 제거된다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, 브라우저 확인, `git diff --check`를 실행하고 결과를 기록한다.

### 실행 결과

- 1차 모바일 브라우저 확인에서 공통 세 가지 내비게이션은 정상 노출됐으나, 좁은 MapLibre 화면의 도시 텍스트가 겹치고 홈의 `3-Level Drilldown` 문구가 아직 미충족인 Gate 2를 과장하는 것을 확인했다.
- 새 사실에 따라 모바일 지도 내부 심볼 텍스트는 숨기고 외부 지역 칩·선택 요약을 읽기 경로로 유지하며, 홈 배지는 전국 State A 표현으로 수정한다. 데스크톱 지도 라벨은 유지한다.
- 지도→라이브 `sapporo` 초기 필터는 작동했지만 모바일 네이티브 select의 흰 배경/흰 글자 대비와, 지역 옵션이 개별 공연의 `areaLabel`을 상속해 `삿포로·공연장 미정`으로 보이는 문제를 확인했다. 지역 필터는 안정된 지역명 사전을 사용하고 dark color-scheme을 명시한다.
- 최종 구현: 모바일·데스크톱 공통 가지 탭과 현재 위치 표시, 전국 지도 지역별 그룹/예정 라이브 수치, `/live?region=` 초기 필터, 안정된 지역 라벨, `link_only` 표지 placeholder, 그라비아 독립 페이지 중복 제목 제거를 완료했다.
- 모바일 재검증에서 지도 내부 라벨 겹침이 제거되고, 라이브 탭 현재 상태·삿포로 초기 선택·dark select 대비가 정상 표시됐다. 홈 첫 로드와 의미 있는 본문도 확인했다.
- `agent-browser`는 홈 첫 로드와 network idle까지 성공했으나 후속 CDP 명령에서 연결이 종료됐다. 재시도 한도 내에서 Playwright 모바일 스크린샷으로 전환했다.
- 데이터 검사 통과(37그룹·188멤버·공연 5건·그라비아 후보 2건), TypeScript 통과, Next 프로덕션 빌드 통과(579/579, 홈 First Load JS 185kB).

## 2026-08-27 Gate 2 공간 데이터 기준선 감사

### 사용자 의도와 큰 줄기

- Gate 1과 MapLibre 1차 스파이크 이후의 다음 가지를 도쿄 23구 UI가 아니라 검증 가능한 공간 데이터 기반으로 이어 간다.
- 전국 지도 → 도쿄 23구 → 활동권역 → 공연장 → 공연·티켓의 줄기를 유지하고, 현재 데이터가 Gate 2에 얼마나 부족한지 반복 가능한 명령으로 계측한다.
- 그라비아 독립 가지는 인물 ID만 공유하며 공연장·행정구역 모델에 섞지 않는다.

### 변경 범위와 연결

- `GeoArea`와 공연장 좌표·행정구역 출처 계약을 스키마와 별도 데이터 파일로 준비한다.
- 현재 공개 공연·후보·그룹 공간 근거를 읽어 도쿄 그룹 수, 근거가 있는 구 수, 검증 공연장 수, 미래 공연 수와 Gate 2 차이를 보고하는 명령을 추가한다.
- 첫 배치는 공개 데이터의 사실을 추정 보강하지 않고, 공식 확인된 도쿄 핵심 구 기준 엔터티와 라이선스·출처 메타데이터만 기록한다.
- 23구 경계 GeoJSON, 공연장 좌표, 공개 UI는 이번 실행에서 추가하지 않는다.

### 실패·빈 상태·회귀 위험

- 기존 `district`는 활동권역과 행정구를 혼용하므로 이를 곧바로 도쿄 특별구 ID로 승격하지 않는다.
- 장소 미정 공연은 공연장 또는 구 근거로 집계하지 않는다.
- 미래 공연은 기준일 이후 `scheduled`인 공개 이벤트만 집계하고 후보는 별도 준비도 수치로 표시한다.
- 좌표·주소·경계 출처가 없으면 `verified`로 간주하지 않는다.

### 완료 조건과 검증 방법

- 데이터 계약과 감사 명령이 같은 Gate 2 정의를 사용한다.
- 현재 수치와 부족분이 보고되고 빈 상태에서도 명령이 실패하지 않는다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check` 결과를 실행 결과에 기록한다.

### 실행 결과

- `GeoArea`/`GeoAreaDataset` 계약과 공연장의 `geoAreaId`, 좌표, 좌표 출처 필드를 추가했다. 기존 공연장에는 nullable 기본값을 적용해 호환성을 유지했다.
- 일본 → 도쿄도 → 지요다·미나토·신주쿠·시부야·도시마 5개 특별구의 신원 엔터티를 공식 총무성 통계국·도쿄도 원문과 함께 추가했다. 아직 중심점·경계가 없으므로 모두 `identity_verified`이며 `map_ready`로 과장하지 않는다.
- `data:report:map-gate2` 감사 명령을 추가했다. 결과는 도쿄 활동 그룹 26/20 통과, 검증 활동 특별구 0/5, 지도 준비 공연장 0/15, 해당 공연장 연결 미래 공연 0/30으로 `NOT READY`다. 전국 공개 미래 공연은 5건, 도쿄 위치 문구가 있는 검토 후보는 8건이다.
- 데이터 검사 통과: 37그룹, 188멤버, 공간 엔터티 7개, 공개 공연 5건, 공연 후보 52건, 경고 39건.
- TypeScript 검사와 Next 프로덕션 빌드(579/579 페이지), `git diff --check`를 실행한다. 빌드 결과는 통과했으며 최종 diff 검사는 아래 후속 문서 반영 뒤 재실행한다.

## 2026-08-27 지도 실사용 검증·공급 경계·그라비아 수집기 실행

### 사용자 의도와 실행 순서

- 승인된 전체 로드맵을 중단하지 않고 `지도 실사용 검증 → 공급·행정경계 판단 자료 → 그라비아 공식 출판사 후보 수집기` 순으로 진행한다.
- 지도와 그라비아를 같은 화면 기능으로 섞지 않고, 공통 신뢰 정책과 인물 연결만 공유한다.

### 변경 범위와 연결

- 지도: 로컬 프로덕션 서버에서 데스크톱·모바일 렌더링, MapLibre canvas, 지역 선택, SVG/목록 대체 UI, 콘솔 오류를 확인한다.
- 공급: 국토지리원 타일·국토수치정보 N03·OSM 타일 정책과 MapLibre 기술 제약을 구현 문서에 비교 기록한다.
- 그라비아: 공식 출판사 원문을 읽는 기본 dry-run 수집기를 만들고, 명시적 `--write`에서만 후보 파일을 갱신한다.
- 공개 `chika-gravure.json`과 이미지 파일은 자동 변경하지 않는다.

### 실패·빈 상태·회귀 위험

- 자동 브라우저가 다시 실패하면 최대 2회 후 Playwright 기반 확인으로 전환하고 미검증 항목을 통과로 기록하지 않는다.
- 지도 타일 네트워크 실패는 전체 페이지 실패가 아니라 SVG/목록 대체 상태여야 한다.
- 출판사 마크업·날짜·인물 연결이 불명확하면 후보를 만들지 않거나 `review_pending`으로 유지한다.
- 수집기 실패 시 기존 후보 파일을 보존하고 부분 결과를 쓰지 않는다.

### 완료 조건과 검증 방법

- 지도 canvas 또는 의도된 fallback, 지역 선택, 비어 있지 않은 본문, 오류 overlay 부재가 브라우저 증거로 확인된다.
- 공급자·경계 데이터의 허용 용도와 미결정 사항이 기술 문서에 기록된다.
- 그라비아 dry-run/write/반복 write가 안전하고 후보 검증·보고가 통과한다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check` 결과를 실행 결과에 기록한다.

### 실행 결과

- MapLibre의 복구 가능한 타일 오류까지 전체 폴백으로 처리하던 이벤트 경계를 제거했다. 폴백은 WebGL·모듈 초기화 실패 또는 10초 초기 로드 타임아웃에만 발생한다.
- Playwright 진단에서 WebGL 및 MapLibre 생성은 확인됐으나, 격리 브라우저가 GSI 타일 요청을 `ERR_NETWORK_ACCESS_DENIED`로 차단해 타임아웃 뒤 SVG 폴백이 표시됐다. 제한 없는 네트워크 환경의 실타일 확인은 잔여 검증이다.
- `scripts/collect-wpb-gravure-candidates.ts`와 dry-run/write 명령을 추가했다. 공식 주プレ 목록에서 현재 명단과 일치하는 인물만 후보화하며 항상 `review_pending`·`link_only`로 시작하고 미성년자는 별도 안전 차단 사유를 둔다.
- 2026-08-27 dry-run 결과는 일치 후보 0건이었다. 이는 정상 빈 상태이며 공개 그라비아 데이터는 변경하지 않았다.
- 직접 TypeScript 검사와 Next 프로덕션 빌드(579/579 페이지)는 통과했다. `pnpm typecheck` 래퍼는 기존 `ERR_PNPM_IGNORED_BUILDS` 정책으로 차단되어 직접 명령으로 대체했다.

## 2026-08-27 승인 로드맵 착수와 Gate 1 완결 실행

### 사용자 의도와 큰 줄기

- 승인된 지도·모바일·기술·출시·그룹 범위·뉴스 방향을 구현 가능한 실행 순서로 전환한다.
- 그라비아 아이돌을 제품의 독립 핵심 가지로 포함한다. 라이브 공간 탐색과 그라비아 출판 탐색은 인물 신원만 연결하고 출처·권리·노출 계약은 분리한다.
- 첫 실행 단위는 실제 지도 착수 전 마지막 Gate 1 차단점인 오사카 `すーぱーぷーばぁー!!`의 공식 명단·활동 상태·공간 근거를 재검증하는 것이다.

### 변경 범위와 연결

- 기획·인수인계: 승인 상태, 통합 제품 줄기, 그라비아 권리 경계와 단계 실행 순서를 기록한다.
- 조사·데이터: 후보의 공식 읽기·로마자 표기, 星宮ゆりか의 기준일 활동휴지 상태, 현재 공식 명단, 오사카 활동 근거를 대조한다.
- 통과할 때만 `data/chika-groups.json`과 `data/chika-source-catalog.json`을 공개 ID로 연결하고 지역 분포를 재계산한다.
- Gate 1 통과 후 MapLibre·타일·행정경계 기술 스파이크를 별도 실행 단위로 설계한다. 이번 후보 검증과 지도 UI 구현을 한 변경에 섞지 않는다.
- 그라비아 구현은 별도 수직 실행으로 출판사 공식 후보 어댑터 → `link_only` 공개 계약 → 인물 연결 → 탐색 UI 순서로 진행한다.

### 실패·빈 상태·회귀 위험

- 공식 프로필과 최신 활동 공지가 충돌하면 기준일 현재 활동 인원을 추정하지 않고 후보를 보류한다.
- 읽기·로마자 표기를 검색 결과나 팬 사이트로 보완하지 않는다. 공식 표기가 없으면 원문 일본어만 저장하거나 공개 승격을 보류한다.
- 오사카 단발 공연만으로 상시 지역 정체성을 만들지 않는다. 공식 거점 또는 반복 활동 근거를 사용한다.
- Gate 1 통과가 지도 공급자 계약이나 UI 출시 승인을 뜻하지 않는다.
- 그라비아 이미지 URL을 발견해도 권리 상태가 `link_only`이면 이미지 자체를 저장하거나 화면에 임베드하지 않는다.

### 완료 조건과 검증 방법

- 후보의 공식 현재 명단, 활동휴지 분리, 공식 표기, 오사카 공간 근거가 모두 출처·확인일과 함께 설명된다.
- 통과 또는 보류 판정이 후보 카탈로그·공개 데이터·Gate 1·인수인계에서 일치한다.
- 데이터 변경이 있으면 데이터 검사, 출처 보고, 타입 검사, 프로덕션 빌드, `git diff --check`를 순차 실행한다.
- 승인된 지도·그라비아 후속 실행은 각각 별도 작업지시 단위로 유지한다.

### 실행 결과

- 승인된 지도·모바일·기술·출시·그룹·뉴스 방향과 그라비아 독립 핵심 가지를 제품 기획서와 인수인계에 확정했다.
- 공식 그룹 사이트의 8개 멤버 프로필에서 이름·읽기·생일·담당색을 대조했고, TuneCore의 Nord Tune Records 아티스트 원문에서 関西 거점 8인 구성을 확인했다.
- 공식 공지에 따라 星宮ゆりか는 2026-08-14부터 `hiatus`로 기록했다. 2026년 10월 중순 복귀 예정은 구체 날짜가 다시 공지될 때까지 현역 상태로 앞당기지 않는다.
- 공식 이벤트 목록의 오사카 정기공연과 오사카성 음악당 원맨을 반복 활동 근거로 연결해 `すーぱーぷーばぁー!!`과 8명을 공개 데이터에 추가했다. 일반 현역 노출은 휴지 멤버를 제외한 7명이다.
- 멤버 상태 계약에 `hiatus`를 추가하고, 완결 명단 검증은 `active + hiatus`가 공식 인원수와 일치하는지 확인하도록 수정했다. 일반 UI 데이터는 기존대로 `active`만 노출한다.
- 후보 카탈로그에 공식 후보를 추가하고 즉시 검증된 공개 ID와 연결했다. 최신 기준선은 출처 10개·후보 36개(`imported` 26개, `official_source_found` 10개), 원본 37그룹·188멤버다.
- 지역 분포는 도쿄 26·삿포로 3·후쿠오카 3·오사카 3·나고야 1, 공간 근거 36/36(100%)가 되어 전국 지도 Gate 1을 통과했다.
- 1차 데이터 검사 통과 — 37그룹, 188멤버, 공개 공연 5건, 공연 후보 52건, 출처 10개, 후보 36개, 경고 39건. 출처 보고와 TypeScript 검사도 통과했다.
- 프로덕션 빌드와 최종 `git diff --check`는 후속 실행 후 기록한다.

## 2026-08-27 MapLibre 기술 스파이크 1차

### 변경 범위와 연결

- `MAP_TECH_SPIKE.md`에 승인된 UX, 타일·경계 후보, 실패 대체 UI, 완료 조건을 기록한다.
- `maplibre-gl`을 지연 로드하고 검증 지역 5개의 실제 중심 좌표와 그룹 수를 GeoJSON으로 전달한다.
- 국토지리원 담색 타일을 스파이크 베이스맵으로 사용하고 지도 안에 attribution을 상시 표시한다.
- 기존 SVG 지도는 MapLibre 로딩 중과 WebGL·타일 실패 시 대체 UI로 유지한다.

### 실패·빈 상태·회귀 위험

- 지도 모듈이 초기 서버 렌더링과 기존 목록을 차단하지 않는다.
- 공개 OSM 타일 서버를 프로덕션 백엔드로 사용하지 않는다.
- 좌표가 없는 그룹을 지역 중심점에 개별 핀처럼 위장하지 않는다. 현재 점은 지역 집계 허브다.
- 모바일에서도 지도와 별개인 지역 버튼·그룹 목록을 사용할 수 있어야 한다.

### 실행 결과와 검증

- `maplibre-gl` 6.6.0을 설치했다. pnpm은 설치를 완료했지만 기존 정책과 동일하게 `esbuild`·`sharp` 빌드 스크립트 미승인 경고로 종료 코드 1을 반환했다.
- MapLibre를 클라이언트 지연 청크로 추가하고 삿포로·도쿄·나고야·오사카·후쿠오카 실제 중심 좌표를 GeoJSON source로 연결했다.
- 지도 선택과 기존 지역·세부 구역 필터를 동기화하고 키보드용 지역 버튼을 별도로 유지했다.
- 국토지리원 담색 raster tile과 출처 링크를 사용했다. 타일 공급자 계약은 하지 않았다.
- TypeScript 검사와 Next.js 프로덕션 빌드 통과. 579/579 정적 페이지를 생성했고 홈 First Load JS는 184kB로 MapLibre 본체가 초기 공유 번들에 합쳐지지 않았다.
- 자동 브라우저 첫 페이지 열기는 성공했지만 설치 Chrome이 후속 명령에서 `DevToolsActivePort` 생성 전에 종료됐다. 기본 실행과 `--no-sandbox` 재시도 후 중단했으며 지도 캔버스·클릭·모바일 시각 검증은 잔여 위험이다.

## 2026-08-27 그라비아 후보 계약 1차

### 변경 범위와 연결

- `GRAVURE_DISCOVERY_MVP.md`에 그라비아를 독립 핵심 가지로 정의하고 `link_only`·미성년자·이미지 권리 경계를 기록한다.
- 공개 `chika-gravure.json`과 분리된 후보 스키마·파일·검토 보고 명령을 추가한다.
- 첫 후보는 공식 그룹 원문에서 확인된 近代麻雀水着祭2026의 妃奈乃愛莉·星宮ゆりか 2명이며 기존 검증 멤버 ID와 연결한다.

### 실패·빈 상태·회귀 위험

- 후보는 공개 UI에서 읽지 않으며 수집 성공을 이미지 권리나 공개 승인으로 해석하지 않는다.
- 모든 후보 권리는 `link_only`로 고정하고 이미지 URL을 저장하지 않는다.
- 星宮ゆりか 후보는 미성년자 안전 문구와 주최 측 공식 원문을 추가 대조하기 전 공개하지 않는다.

### 실행 결과와 검증

- `GravureCandidateDataset`, 후보 파일, `data:review:gravure-candidates` 보고 명령과 검증 규칙을 추가했다.
- 후보 2건 모두 `review_pending`, `link_only`이며 공개 그라비아 5건은 자동 변경하지 않았다.
- 데이터 검사 통과 — 37그룹, 188멤버, 공개 그라비아 5건, 그라비아 후보 2건, 공개 공연 5건, 공연 후보 52건, 출처 10개, 그룹 후보 36개, 경고 39건.
- 그라비아 후보 보고, TypeScript 검사, Next.js 빌드(579페이지) 통과.

## 2026-08-27 활성 문서 정합성 복구와 승인 게이트 정리

### 사용자 의도와 변경 범위

- 작은 문서 정리도 제품의 큰 줄기 → 데이터·UI·명령 가지 → 이번 실행 단위 순서로 설계하고 검증한다.
- `AUDIT_AND_REBUILD_PLAN.md`, `SOURCE_RESEARCH_AND_MVP.md`, `MAP_DISCOVERY_MVP.md`, `CODEX_HANDOVER.md`의 현재 기준선과 다음 실행을 최신 로컬 데이터에 맞춘다.
- 누적 실행 이력과 폐기된 `HANDOVER.md`는 삭제하지 않는다. 코드·데이터·UI·배포도 이번 실행에서 변경하지 않는다.

### 데이터·UI·명령 연결

- 최신 기준선은 데이터 검사와 출처 보고의 그룹·멤버·공연·후보 수, 지도 설계의 지역 분포와 Gate 판정, 인수인계의 배포/로컬 분리를 함께 대조한다.
- UI는 현행 수제 SVG 지도와 공연 탐색을 그대로 유지한다. 실제 지도·디자인 변경은 승인 안건을 먼저 정리한 뒤 별도 작업지시서로 진행한다.
- 검증 명령은 데이터 검사 → 출처 보고 → 타입 검사 → 프로덕션 빌드 → `git diff --check` 순으로 실행한다.

### 실패·빈 상태·회귀 위험

- 중간 배치 수치를 현재 상태처럼 덮어쓰지 않고 역사 기록으로 보존한다.
- 로컬 35개 활동 그룹과 프로덕션 10개 활동 그룹 기준선을 섞지 않는다.
- 현재 작업 트리는 대규모 미커밋 상태이므로 기존 변경을 되돌리거나 자동 커밋·배포하지 않는다.
- `すーぱーぷーばぁー!!`의 공식 표기·휴지 상태가 미완료인 동안 Gate 1 미달을 유지한다.

### 완료 조건과 검증 방법

- 활성 문서에서 현재 후보 수는 35개, 공개 승격 후보는 25개, 활동 그룹은 35개, 멤버 원본은 180명으로 일치한다.
- 지도 Gate 1은 삿포로·후쿠오카 충족, 오사카 1개 부족으로 일치하며 다음 실행은 오사카 후보 공식 검증이다.
- 사용자 판단이 필요한 UI·디자인·공급자·제품 범위 결정을 승인 안건으로 모은다.
- 데이터 검사, 출처 보고, 타입 검사, 프로덕션 빌드, `git diff --check`의 실제 결과를 아래 실행 결과에 기록한다.

### 실행 결과

- `AUDIT_AND_REBUILD_PLAN.md`의 기준일을 2026-08-27로 갱신하고 현재 제품 기준선, 이번 정합성 가지, 신뢰·안전 경계, 사용자 승인 경계를 문서 맨 앞에 추가했다. 비어 있던 공연 탐색 설계 제목에는 현행 기준 문서 연결을 명시했다.
- `SOURCE_RESEARCH_AND_MVP.md`의 현재 기준선을 출처 10개·후보 35개(`imported` 25개, `official_source_found` 10개), 원본 36그룹·180멤버, 오사카 1개 부족으로 갱신했다. 완료된 FRUiTY·HelloYouth를 다음 작업으로 남기던 오래된 항목은 실행 이력으로 정리했다.
- `CODEX_HANDOVER.md`에 최신 확장이 검증 완료됐지만 `94ff544` 이후 미커밋 작업 트리라는 운영 경계를 추가하고, UI·모바일·지도 공급자·출시 범위·경계 그룹·뉴스 범위를 사용자 승인 안건으로 분리했다.
- 코드·데이터·UI·배포는 변경하지 않았고 기존 더티 작업 트리를 보존했다.
- 데이터 검사: 통과 — 36그룹, 180멤버, 공지 4건, 그라비아 5건, 공개 공연 5건, 공연 후보 52건, 발견 출처 10개, 발견 후보 35개, 지표 스냅숏 0건, 검토 경고 39건.
- 출처 보고: 통과 — 10 sources, 35 candidates.
- TypeScript `--noEmit`: 통과. 프로덕션 빌드 완료 후 순차 재실행도 종료 코드 0이었다.
- Next.js 프로덕션 빌드: 통과 — 558/558 정적 페이지 생성.
- 활성 문서 현재 수치 검색: 통과 — 후보 35개와 오사카 1개 부족 판정이 출처 설계·지도 설계·작업지시·인수인계에서 일치한다.
- `git diff --check`: 통과. 기존 작업 트리의 LF→CRLF 안내만 출력됐다.

## 2026-08-27 인수인계 기준선 동기화

### 변경 범위와 연결

- 최신 공개 데이터 수량, 지역 분포, 지도 Gate 판정, 검증 결과를 `CODEX_HANDOVER.md`의 현재 상태와 일치시킨다.
- 완료된 FRUiTY·HelloYouth 배치와 남은 오사카 후보 `すーぱーぷーばぁー!!` 검증을 다음 실행 경계로 명확히 구분한다.
- Gate 1 이후 기술 스파이크, Gate 2 데이터 준비, 뉴스 상담 게이트의 실행 순서를 최신 상태로 정리한다.
- 코드·데이터·배포는 변경하지 않고 문서와 인수인계만 갱신한다.

### 실패·빈 상태·회귀 위험

- 프로덕션 기준선과 최신 로컬 작업 트리 수치를 섞어 현재 배포 상태처럼 표현하지 않는다.
- 과거 배치의 실행 기록은 보존하되, 현재 상태를 설명하는 요약과 우선순위에서는 오래된 수치를 제거한다.
- `すーぱーぷーばぁー!!`의 활동휴지 멤버를 현역으로 확정하거나 Gate 1을 이미 통과한 것으로 쓰지 않는다.
- 기존 사용자 변경이 포함된 더티 작업 트리를 정리·되돌리거나 커밋·배포하지 않는다.

### 완료 조건과 검증

- 인수인계의 그룹·멤버·후보·지역 수치가 최신 데이터 검사 결과와 일치한다.
- Gate 1 미달 사유가 오사카 1개 부족으로 단일하게 설명되고 다음 실행이 공식 표기·상태 검증부터 시작한다.
- 최신 검증 결과와 미실행 배포 상태가 분리되어 기록된다.
- 변경 문서에 대해 `git diff --check`를 실행하고 결과를 이 절에 기록한다.

### 실행 결과

- `CODEX_HANDOVER.md`의 원본/활동 그룹, 멤버, 후보, 지역 분포를 최신 로컬 검증 기준선으로 동기화했다.
- Gate 1은 삿포로·후쿠오카 조건 충족, 오사카 1개 부족으로 정리했고 `すーぱーぷーばぁー!!`의 공식 표기·활동휴지 상태 화해를 다음 실행으로 고정했다.
- Gate 1 통과 뒤 기술 스파이크와 Gate 2 데이터 집계, 이후 뉴스 상담 게이트 순으로 우선순위를 재정렬했다.
- 코드·데이터·배포는 변경하지 않았다. `git diff --check -- CODEX_HANDOVER.md WORK_ORDER.md`는 통과했으며 기존 LF→CRLF 작업 트리 경고만 있다.

## 2026-08-26 비도쿄 Gate 1 분포 완결 배치

### 변경 범위와 연결

- 오사카 `すーぱーぷーばぁー!!`, 홋카이도 `FRUiTY`, 후쿠오카 `HelloYouth`의 최신 공식 명단·2026 일정·지역 근거를 검증한다.
- 활동휴지 멤버는 그룹 공식 구성원으로 보존하되 현역 노출 수에서 분리하고, 프로필과 공지의 기준일 상태를 함께 기록한다.
- 통과 후보를 공개 데이터와 후보 카탈로그에 연결한 뒤 오사카·홋카이도·후쿠오카 각 3개 및 전체 Gate 1을 재계산한다.
- Gate 1 통과 시에도 실제 지도 구현 전에 타일·경계 데이터 기술 스파이크와 Gate 2 공연장·미래 공연 자료 준비를 다음 실행으로 설계한다.

### 실패·빈 상태·회귀 위험

- 공식 프로필에 남은 활동휴지자를 현재 공연 참여 멤버로 표현하지 않는다.
- 오래된 일정이나 과거 명단을 최신 상태로 간주하지 않고 2026년 갱신 페이지·공지와 대조한다.
- 지역 본거지와 원정 공연을 구분하며 해당 지역 공식 정체성 또는 반복 활동 근거가 없는 후보는 분포 수치에 포함하지 않는다.
- Gate 1 통과만으로 공연장 좌표·티켓 상태가 완성됐다고 표현하거나 실제 지도 UI를 출시하지 않는다.

### 완료 조건과 검증

- 세 후보마다 기준일 현역/휴지 명단, 공식 일정, 지역 공간 근거가 기록된다.
- 오사카·홋카이도·후쿠오카가 각각 공개 검증 그룹 3개 이상인지 계산된다.
- 지도 Gate 1 최종 판정과 다음 기술·데이터 실행이 계획·지도 설계·인수인계에 반영된다.
- 소스 보고, 데이터 검사, TypeScript 검사, 프로덕션 빌드, `git diff --check`를 통과한다.

### 실행 결과

- FRUiTY 9명에 이어 HelloYouth의 공식 최신 9인 명단, 규슈 지역 정체성, 현재 공식 일정 링크를 확인해 그룹 2개와 현역 멤버 18명을 공개 승격했다.
- すーぱーぷーばぁー!!은 8인 공식 프로필과 오사카 공연은 확인했으나 일부 멤버 표기와 星宮ゆりか 휴지 상태의 최종 대조를 다음 실행으로 넘겼다.
- 후보 카탈로그는 출처 10개·후보 35개(imported 25개, official_source_found 10개)가 됐다. 공개 원본은 그룹 36개·멤버 180명이며 종료 그룹 1개를 제외한 활동 노출은 그룹 35개·현역 검증 멤버 175명이다.
- 분포는 도쿄 26, 삿포로 3, 후쿠오카 3, 오사카 2, 나고야 1이다. 홋카이도·후쿠오카 조건은 충족했고 Gate 1 완결까지 오사카 1개가 남았다.
- 1차 데이터 검사에서 FRUiTY의 `areaEvidence.relation`에 그룹 위치 기준값을 잘못 사용해 실패했다. 관계값을 스키마의 `official_base`로 보정한 뒤 검증을 재실행한다.
- 최종 검증 결과: 소스 보고 통과(10 sources, 35 candidates), 데이터 검사 통과(36 groups, 180 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(558페이지), `git diff --check` 통과(LF→CRLF 작업 트리 경고만 존재).

## 2026-08-26 비도쿄 지역 밀도 1차 확장 배치

### 변경 범위와 연결

- 오사카·홋카이도·후쿠오카·아이치의 공식 지역 행사·사무소·그룹 사이트에서 비중복 핵심 후보를 조사한다.
- 현재 각 1개인 지역 중 최소 3개 권역에 공식 현역 명단·2026 일정·해당 지역 공간 근거를 모두 갖춘 그룹을 우선 추가한다.
- 후보 카탈로그와 공개 그룹·멤버 데이터를 연결하고 `3개 도도부현 × 각 3개` Gate 1 분포를 재계산한다.
- Gate 1을 통과하더라도 Gate 2의 5개 구·공연장·미래 공연 조건과 타일 공급자 결정 전에는 실제 지도 UI를 구현하지 않는다.

### 실패·빈 상태·회귀 위험

- 그룹명이나 멤버 출신지가 지역을 암시해도 공식 거점·공연 원문 없이는 지역 그룹으로 집계하지 않는다.
- 로컬 행사 출연진은 발견 근거로만 사용하며, 현재 전체 명단은 그룹·사무소의 최신 공식 원문으로 별도 확인한다.
- 멤버 변동 공지와 프로필이 충돌하거나 2026 활동 상태가 불명확하면 수량을 위해 승격하지 않는다.
- 기존 도쿄 그룹의 지역값을 출신지나 단발 원정 공연으로 재분류하지 않는다.
- 한 번에 안전하게 검증할 수 있는 후보가 부족하면 권역별 소배치로 나누고 분포 게이트를 미달 상태로 유지한다.

### 완료 조건과 검증

- 조사 후보마다 공식 명단·현재 일정·지역 공간 근거와 통과 또는 보류 사유가 기록된다.
- 승격 후보의 그룹·멤버 ID가 중복되지 않고 후보 카탈로그의 공개 ID와 연결된다.
- 지역별 공개 그룹 수와 Gate 1 전체 판정이 문서에 반영된다.
- 소스 보고, 데이터 검사, TypeScript 검사, 프로덕션 빌드, `git diff --check`를 통과한다.

### 실행 결과

- 1차 소배치에서 NEO BREAK 6명, BYBBiT 5명, MAGICAL SPEC 8명의 공식 현재 명단·2026 일정·오사카/삿포로/후쿠오카 공간 근거를 확인해 그룹 3개와 현역 멤버 19명을 공개 승격했다.
- 九州女子翼는 2026년 8월 말 활동 종료 공식 공지가 있어 신규 활동 그룹에서 제외했다. すーぱーぷーばぁー!!는 공식 프로필에 활동휴지 멤버가 함께 표시되어 현역 노출 범위를 다음 배치에서 화해하며, FRUiTY·HelloYouth는 최신 명단과 2026 일정은 확인했지만 이번 소배치의 검증량을 제한해 다음 우선 후보로 넘겼다.
- 후보 카탈로그는 출처 10개·후보 33개(`imported` 23개, `official_source_found` 10개)가 됐다. 공개 원본은 그룹 34개·멤버 162명이며 종료 그룹 1개를 제외한 활동 노출은 그룹 33개·현역 검증 멤버 157명이다.
- 분포는 도쿄 26, 오사카 2, 삿포로 2, 후쿠오카 2, 나고야 1이 됐다. 그룹 수와 공간 근거율은 충족하지만 `3개 도도부현 × 각 3개` 조건은 아직 미달이므로 실제 지도 UI는 시작하지 않는다.
- 당시 검증 결과: 소스 보고 통과(10 sources, 33 candidates), 데이터 검사 통과(34 groups, 162 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(504페이지). 최종 문서 기준 `git diff --check`는 후속 인수인계 동기화에서 통과했다(LF→CRLF 작업 트리 경고만 존재).

## 2026-08-26 Gate 1 부족분 확보와 분포 감사 배치

### 변경 범위와 연결

- 기존 보류 핵심 후보 kimikara, 神聖マキナ, クララ・マグラ, MiDi ON!의 현재 명단·체제·일정 차단 사유를 최신 공식 원문으로 재검증한다.
- 안전하게 승격 가능한 후보가 3개 미만이면 공식 행사 출연진에서 신규 비중복 핵심 후보를 발굴해 동일한 명단·일정·공간 게이트를 적용한다.
- 공개 30개 도달 여부와 최소 3개 도도부현 분포, 도쿄 편중 상태를 별도로 감사한다.
- 결과에 따라 실제 지도 기술 스파이크 시작 여부와 다음 데이터 우선순위를 문서화한다.

### 실패·빈 상태·회귀 위험

- 소개문 인원수와 프로필 카드 수가 충돌하면 최신 변동 공지 없이 임의로 한쪽을 선택하지 않는다.
- 현 체제 종료·활동 종료 신호가 있으면 후속 체제 공식 발표 전 공개하지 않는다.
- SNS 단독 단편 정보나 제3자 명단으로 전체 현역을 구성하지 않는다.
- Gate 1 숫자를 맞추기 위해 메이저·성우 경계 후보를 핵심 후보로 전환하지 않는다.
- 공개 30개를 달성해도 지역 분포와 공연장·미래 공연 게이트가 미달이면 실제 지도 구현을 시작하지 않는다.

### 완료 조건과 검증

- 기존 보류 4개 각각의 최신 판정과 공식 근거·재검증 조건이 기록된다.
- 필요 시 신규 후보가 공식 전체 명단·일정·공간 근거와 함께 카탈로그 및 공개 데이터에 연결된다.
- 지역별 활동 그룹 분포와 Gate 1 충족 여부가 계산된다.
- 소스 보고, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 통과한다.

### 실행 결과

- 기존 보류 4개를 최신 공식 원문으로 재검증했다. kimikara는 7인조 소개와 6개 프로필 카드 충돌, 神聖マキナ는 2026년 현재 전체 명단·독립 일정 근거 부족, クララ・マグラ는 현 체제 종료 이후 후속 체제 미확정, MiDi ON!은 공식 현재 전체 명단 미노출 사유가 해소되지 않아 모두 공개 보류했다.
- 신규 안전 후보 Task have Fun 3명, yosugala 4명, ドラマチックレコード 6명의 현재 공식 명단·2026 활동·도쿄 공연 공간 근거를 대조해 그룹 3개와 현역 멤버 13명을 공개 승격했다.
- 후보 카탈로그는 출처 10개·후보 30개(`imported` 20개, `official_source_found` 10개)가 됐다. 공개 원본은 그룹 31개·멤버 143명이며 종료 그룹 1개를 제외한 활동 노출은 그룹 30개·현역 검증 멤버 138명이다.
- Gate 1의 그룹 수와 공간 근거는 30/30·30/30(100%)를 충족했다. 다만 지역 분포는 도쿄 26개, 오사카·삿포로·후쿠오카·나고야 각 1개로, 최소 3개 도도부현에서 각각 3개 이상 조건을 충족하지 못했다. 실제 지도 구현은 시작하지 않고 비도쿄 공식 후보 확장을 다음 우선순위로 둔다.
- 1차 프로덕션 빌드는 447페이지 생성 진입 후 `/ja` 홈에서 Task have Fun의 빈 `district`가 지도 라벨 조회에 전달되어 실패했다. 미확인 세부 지역은 기존 중립값 `general`로 보정하고 동일 검증을 재실행한다.
- 최종 검증 결과: 소스 보고 통과(10 sources, 30 candidates), 데이터 검사 통과(31 groups, 143 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(447페이지), `git diff --check` 통과(LF→CRLF 작업 트리 경고만 존재). 빌드와 동시에 실행한 첫 타입 검사는 `.next/types` 재생성 경합으로 실패했으나 빌드 완료 후 순차 재실행해 통과했다.

## 2026-08-26 Gate 1 최종 후보 판정 배치

### 변경 범위와 연결

- situasion, かすみ草とステラ, 衛星とカラテア, INUWASI 4개 후보의 명단 충돌·세대 변동·공식 원문 부족을 최신 공식 공지로 재판정한다.
- 그룹마다 현재 전체 명단, 가입·졸업 효력일, 2026 일정, 실제 활동 지역 근거를 함께 확인한다.
- 모든 신뢰 게이트를 통과한 그룹만 공개 승격하며, 수량 목표를 위해 충돌 후보를 포함하지 않는다.
- 결과에 따라 전국 지도 Gate 1 그룹 수와 다음 지도 기술 준비 여부를 갱신한다.

### 실패·빈 상태·회귀 위험

- 서로 다른 공식 페이지의 명단이 불일치하고 변동 효력일을 설명하는 공식 공지가 없으면 공개하지 않는다.
- 세대별 활동이 병행되는 그룹은 그룹 전체 현역 범위를 공식적으로 확정할 수 있을 때만 통합한다.
- 링크 허브·스토어·공연장 원문만으로 전체 명단을 만들지 않는다.
- 졸업 예정자는 기준일 현재 활동 여부와 효력일을 구분하며, 미래 상태를 현재 상태로 앞당기지 않는다.
- 기존 공개 데이터와 사용자 변경은 수정하지 않고 신규 ID·이름 중복을 검사한다.

### 완료 조건과 검증

- 후보 4개마다 통과 또는 차단 판정, 공식 근거와 재검증 조건이 기록된다.
- 승격 그룹은 현역 전체 명단·공식 일정·공간 근거·확인일을 가진다.
- 소스 보고, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 통과한다.
- Gate 1 충족 여부와 남은 분포·공연장·미래 공연 게이트를 문서화한다.

### 실행 결과

- INUWASI는 공식 2026년 시부야 이벤트와 투어 티켓이 동일한 현역 6명(ライカ, がるむ, はのんまゆ, カリヲリ, すずめ, 六椛)을 명시해 기존 5명/6명 페이지 충돌을 화해하고 공개 승격했다.
- situasion은 공식 경로 간 인원 차이를 설명할 변동 공지 부족, かすみ草とステラ는 세대별 현역·졸업 효력일 완결 부족, 衛星とカラテア는 공식 스토어의 7인 활동 신호에도 전체 공식 이름 프로필 부재로 공개 보류했다.
- 후보 카탈로그는 27개 중 `imported` 17개, `official_source_found` 10개가 됐다. 공개 원본은 그룹 28개·멤버 130명이며 활동 노출은 그룹 27개·현역 검증 멤버 125명이다.
- 지도 Gate 1은 활동 그룹 27/30, 공간 근거 27/27(100%)로 상승했으며 전국 지도 그룹 수 게이트까지 3개가 남았다.
- 검증 결과: 소스 보고 통과(10 sources, 27 candidates), 데이터 검사 통과(28 groups, 130 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(408페이지), `git diff --check` 통과(LF→CRLF 작업 트리 경고만 존재).

## 2026-08-26 잔여 신규 후보 공식 명단 완결 배치

### 변경 범위와 연결

- Sweet Alley, Mirror,Mirror, Ringwanderung, MyDearDarlin' 4개 후보를 2026년 현재 공식 원문으로 재검증한다.
- 현재 활동, 현역 전체 명단, 공식 일정, 도쿄 또는 실제 활동 지역 근거를 그룹별 한 묶음으로 확인한다.
- 모든 게이트를 통과한 그룹만 공개 데이터와 후보 카탈로그에 연결하고 지도 Gate 1을 재계산한다.
- 결과와 차단 사유를 제품 계획·출처 설계·지도 설계·인수인계에 반영한다.

### 실패·빈 상태·회귀 위험

- 공식 홈페이지의 오래된 프로필과 최신 가입·졸업 공지가 충돌하면 현재 명단을 화해하기 전 공개하지 않는다.
- 공식 SNS만 있고 전체 명단 또는 일정 원문을 확인할 수 없으면 공개 보류한다.
- 행사 출연은 단발 공연 근거로만 사용하고 상시 거점으로 표현하지 않는다.
- 선택 프로필은 추정하지 않으며 공식 한자·로마자 표기가 불명확하면 검증된 표기만 수록한다.
- 기존 데이터와 신규 그룹·멤버 ID 중복을 검사하고 사용자 변경은 수정하지 않는다.

### 완료 조건과 검증

- 후보 4개마다 공개 통과 또는 차단 판정과 공식 URL·확인일이 기록된다.
- 승격 그룹은 공식 전체 명단, 일정 URL, `areaEvidence`, 공개 ID를 가진다.
- 소스 보고, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 통과한다.
- Gate 1 잔여 그룹 수와 다음 실행 대상을 문서에 기록한다.

### 실행 결과

- Sweet Alley 8명, Mirror,Mirror 6명, Ringwanderung 5명, MyDearDarlin' 7명의 공식 현재 명단·2026 일정 또는 공식 행사·도쿄 공간 근거를 확인해 그룹 4개와 현역 멤버 26명을 공개 승격했다.
- 후보 카탈로그는 27개 중 `imported` 16개, `official_source_found` 11개가 됐다. 공개 원본은 그룹 27개·멤버 124명이며 활동 노출은 그룹 26개·현역 검증 멤버 119명이다.
- 지도 Gate 1은 활동 그룹 26/30, 공간 근거 26/26(100%)로 상승했다. Sweet Alley·Mirror,Mirror는 공식 행사 공연, Ringwanderung·MyDearDarlin'은 공식 사이트의 도쿄 공연 이력으로 기록했다.
- Sweet Alley는 독립 일정 페이지가 없어 공식 RIF 출연 근거와 현재 공식 8인 명단을 조합했으며 `scheduleUrl`은 추정하지 않고 비워 뒀다.
- 검증 결과: 소스 보고 통과(10 sources, 27 candidates), 데이터 검사 통과(27 groups, 124 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(390페이지), `git diff --check` 통과(LF→CRLF 작업 트리 경고만 존재).

## 2026-08-26 신규 후보 두 번째 공개 검증 배치

### 변경 범위와 연결

- Devil ANTHEM., NANIMONO, Palette Parade, CYNHN 4개 후보를 2026년 현재 공식 원문으로 재검증한다.
- 그룹별 현재 활동, 현역 전체 명단, 공식 일정, 도쿄 또는 실제 활동 지역 근거를 함께 확인한다.
- 모든 게이트를 통과한 그룹만 공개 데이터에 그룹·현역 멤버를 추가하고 후보 카탈로그의 공개 ID를 연결한다.
- 결과를 지도 Gate 1, 출처 조사 설계도, 재설계 계획과 인수인계에 반영한다.

### 실패·빈 상태·회귀 위험

- 레이블의 과거 프로필과 그룹 공식 최신 프로필이 충돌하면 최신 변동 공지로 화해되지 않는 한 공개하지 않는다.
- 공식 일정이 있어도 현역 전체 명단을 확정할 수 없거나 졸업·가입 예정 상태가 불명확하면 보류한다.
- 행사 출연만으로 상시 거점을 만들지 않고 단발 공연은 `verified_event_history`로 기록한다.
- 담당색·생일·출신지·SNS 등 선택 필드는 공식 근거가 확인된 값만 쓰며 나머지는 미확인으로 둔다.
- 기존 공개 데이터와 사용자 변경은 수정하지 않고 신규 ID·이름 중복을 검사한다.

### 완료 조건과 검증

- 후보 4개마다 통과 또는 차단 판정과 공식 근거 URL이 기록된다.
- 승격 그룹은 공식 전체 명단, 일정 URL, 공간 근거, 확인일과 공개 ID를 가진다.
- 소스 보고, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 통과한다.
- 공개 그룹 수와 지도 Gate 1 잔여 부족분을 문서에 기록한다.

### 실행 결과

- Devil ANTHEM. 6명, NANIMONO 7명, Palette Parade 7명, CYNHN 4명의 공식 현재 명단·2026 일정·도쿄 활동 근거를 확인해 그룹 4개와 현역 멤버 24명을 공개 승격했다.
- 후보 카탈로그는 27개 중 `imported` 12개, `official_source_found` 15개가 됐다. 공개 원본은 그룹 23개·멤버 98명이며 활동 노출은 그룹 22개·현역 검증 멤버 93명이다.
- 지도 Gate 1은 활동 그룹 22/30, 공간 근거 22/22(100%)로 상승했다. Devil ANTHEM.·NANIMONO·Palette Parade는 검증 공연 이력, CYNHN은 공식 정기공연 활동으로 기록했다.
- 선택 프로필은 공식 원문에 명시된 항목만 수록하고 미확인 담당색·출신지는 그대로 비웠다.
- 검증 결과: 소스 보고 통과(10 sources, 27 candidates), 데이터 검사 통과(23 groups, 98 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(312페이지), `git diff --check` 통과(LF→CRLF 작업 트리 경고만 존재).

## 2026-08-26 고준비도 신규 후보 공개 검증 배치

### 변경 범위와 연결

- INUWASI, UtaGe!, AsIs, JamsCollection, AVAM 5개 후보를 공식 최신 원문으로 재검증한다.
- 그룹별 현재 활동, 현역 전체 명단, 멤버 이름, 2026 공식 일정, 실제 활동 지역 또는 검증 공연 지역을 한 묶음으로 확인한다.
- 모든 게이트를 통과한 그룹만 `chika-groups.json`에 공개 승격하고 후보의 `importedEntityId`를 연결한다.
- 공개 결과에 따라 지도 Gate 1 그룹 밀도와 공간 근거율, 출처 조사 문서와 인수인계를 갱신한다.

### 실패·빈 상태·회귀 위험

- 행사 출연진·검색 결과·제3자 프로필만으로 현역 명단을 확정하지 않는다.
- 프로필과 공지의 인원수가 충돌하거나 졸업·가입 예정이 있으면 기준일 현재 상태를 확정할 수 있는 공식 원문이 없을 때 공개하지 않는다.
- 단발 공연은 `verified_event_history`로만 기록하고 상시 거점으로 표현하지 않는다.
- 생일·담당색·SNS처럼 공식 근거가 없는 선택 필드는 추정하지 않고 비워 둔다.
- 기존 사용자 변경과 기존 공개 그룹은 수정하지 않으며 신규 그룹 ID·멤버 ID 중복을 검사한다.
- 공식 사무소 운영 프로필을 출처로 보존할 수 있도록 공개 데이터의 `SourceRecord.sourceKind`에 `agency`를 추가하며 기존 값의 의미는 바꾸지 않는다.

### 완료 조건과 검증

- 각 후보의 통과 또는 차단 판정과 근거 URL이 기록된다.
- 승격 그룹은 현역 전체 명단, 공식 출처, 일정 경로, `areaEvidence`를 가진다.
- 후보 카탈로그와 공개 데이터의 연결 상태가 일치한다.
- 소스 보고, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 통과한다.

### 실행 결과

- AsIs 7명, UtaGe! 7명, JamsCollection 8명, AVAM 7명의 공식 현역 명단·2026 일정·도쿄 공연 공간 근거를 확인해 그룹 4개와 멤버 29명을 공개 승격했다.
- INUWASI는 공식 홈페이지 본문이 5명, 공식 `/member/` 목록이 六椛 포함 6명을 표시해 현재 전체 명단 충돌이 해소될 때까지 공개 보류했다.
- 후보 카탈로그는 27개 중 `imported` 8개, `official_source_found` 19개가 됐다. 공개 원본은 그룹 19개·멤버 74명이며 활동 노출은 그룹 18개·현역 검증 멤버 69명이다.
- 지도 Gate 1은 활동 그룹 18/30, 공간 근거 18/18(100%)로 상승했다. 신규 4개의 근거는 단발 또는 투어 공연이므로 모두 `verified_event_history`로 기록했다.
- 공식 사무소 운영 프로필을 정확히 구분하도록 `SourceRecord.sourceKind`에 하위 호환적인 `agency` 값을 추가했다.
- 검증 결과: 소스 보고 통과(10 sources, 27 candidates), 데이터 검사 통과(19 groups, 74 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(240페이지), `git diff --check` 통과(LF→CRLF 작업 트리 경고만 존재).

## 2026-08-26 지도 밀도용 신규 핵심 후보 발굴 배치

### 변경 범위와 연결

- 공식 행사 출연진에서 기존 공개 15개·기존 발견 후보 11개와 중복되지 않는 신규 후보를 최소 16개 발굴한다.
- 후보마다 공식 그룹/사무소 원문을 최소 1개 확보하고 가능하면 공식 일정 경로를 함께 기록한다.
- 공개 데이터와 분리된 `chika-source-catalog.json`에 발견·검토 상태로 적재한다.
- 다음 공개 배치가 현역 명단·일정·공간 근거를 바로 채울 수 있도록 차단 사유와 우선순위를 기록한다.

### 실패·빈 상태·회귀 위험

- 행사 출연만으로 현재 전체 명단이나 거점을 추정하지 않는다.
- 동명 그룹, 개명, 해산, 기간 한정 유닛과 기존 데이터 중복을 확인한다.
- 공식 URL을 찾지 못한 그룹은 수량을 맞추기 위해 공식 확인 상태로 올리지 않는다.
- 메이저·성우·학교·가상 아이돌은 핵심 범위와 분리해 경계 후보로 둔다.
- 카탈로그 변경은 홈·검색·지도·정적 페이지에 자동 노출되지 않는다.

### 완료 조건과 검증

- 신규 고유 후보가 16개 이상이며 각각 발견 원문과 공식 URL 상태를 가진다.
- 기존 공개·후보 ID/이름 중복 검사가 통과한다.
- 핵심 후보별 다음 검증 항목이 메모에 기록된다.
- 데이터 검사, 소스 보고, 타입 검사, 프로덕션 빌드, `git diff --check`를 실행한다.

### 실행 결과

- TV Asahi 공식 `六本木アイドルフェスティバル2026`과 `IDORISE!! FESTIVAL 2026`을 1차 발견 원문으로 등록했다.
- AsIs, INUWASI, Devil ANTHEM., UtaGe!, JamsCollection, Sweet Alley, Mirror,Mirror, Ringwanderung, situasion, NANIMONO, AVAM, かすみ草とステラ, MyDearDarlin', Palette Parade, 衛星とカラテア, CYNHN 16개를 신규 핵심 후보로 적재했다.
- 신규 후보마다 공식 사이트·사무소·공식 일정·공식 스토어 중 최소 1개를 검증 상태로 연결했다. 전체 카탈로그는 출처 10개, 후보 27개, `imported` 4개, `official_source_found` 23개가 됐다.
- 모든 신규 후보는 `importedEntityId: null`로 유지했다. 따라서 공개 데이터는 그룹 15개·멤버 45명, 지도 Gate 1은 활동 그룹 14/30·공간 근거 14/14(100%)로 변하지 않았다.
- situasion은 공식 경로 간 인원 표시 충돌, かすみ草とステラ는 세대·졸업 변동, 衛星とカラテア는 공식 현역 명단·자체 일정 원문 부족을 명시했다. 나머지도 현역 명단·2026 일정·공간 근거가 모두 완결되기 전 공개하지 않는다.
- 검증 결과: 소스 보고 통과(10 sources, 27 candidates), 데이터 검사 통과(15 groups, 45 members, 경고 39건), TypeScript `--noEmit` 통과, Next.js 프로덕션 빌드 통과(153페이지), `git diff --check` 통과(LF→CRLF 작업 트리 경고만 존재).

## 2026-08-26 기존 활동 그룹 공간 근거 완결 배치

### 변경 범위와 연결

- `areaEvidence`가 없는 활동 그룹 6개의 공식 지역·공연 근거를 조사한다.
- 그룹별 근거 관계, 공식 URL, 확인일을 공개 데이터에 추가하고 지도 Gate 1 집계를 갱신한다.
- 콘셉트 지역과 공연 이력은 서로 다른 관계로 기록하며 기존 `region`·`district` 표현은 변경하지 않는다.
- 데이터 변경 결과를 지도 설계도와 인수인계에 반영한다.

### 실패·빈 상태·회귀 위험

- 공식 출처가 아닌 검색 결과·팬 데이터·제3자 일정만으로 공간 근거를 만들지 않는다.
- 전국 투어의 단일 공연장을 그룹 거점으로 단정하지 않는다.
- 공식 콘셉트가 지역명을 사용해도 실제 활동 근거와 혼합하지 않는다.
- URL에서 출연 그룹·장소·개최 사실이 함께 확인되지 않으면 누락 상태를 유지한다.

### 완료 조건과 검증

- 추가한 모든 공간 근거에 관계·공식 URL·확인일이 존재한다.
- 근거가 없는 그룹과 차단 사유가 작업 결과에 남는다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`가 통과한다.
- Gate 1 공간 근거율과 남은 그룹 밀도 부족분을 기록한다.

### 실행 결과

- FRUITS ZIPPER, CANDY TUNE, iLiFE!, Appare!의 2026 공식 일정에서 도쿄 공연과 장소를 확인해 `verified_event_history`로 기록했다.
- 虹のコンキスタドール, FES☆TIVE는 TV아사히 공식 `六本木アイドルフェスティバル2026` 원문에서 아리아케 개최 장소와 출연을 함께 확인해 공연 이력으로 기록했다.
- 6개 모두 단일 공연을 거점으로 단정하지 않았으며 기존 콘셉트·지역 라벨은 변경하지 않았다.
- 지도 Gate 1은 활동 그룹 `14/30`, 공간 근거 `14/14(100%)`다. 공간 근거 조건은 충족했지만 그룹 밀도 16개가 부족하므로 실제 지도 UI 구현은 아직 시작하지 않는다.
- 데이터 검사: 통과 — 15그룹·45멤버, 검토 경고 39건.
- 타입 검사: `node node_modules\\typescript\\bin\\tsc --noEmit` 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 153개 생성.
- `git diff --check`: 통과. 기존 작업 파일의 LF→CRLF 안내만 출력됐다.

## 2026-08-26 핵심 그룹 두 번째 공개 배치·공간 근거 이관

### 변경 범위와 연결

- `kimikara`, `神聖マキナ`, `selfish`, `クララ・マグラ`, `MiDi ON!`의 현재 활동·공식 현역 명단·일정 경로·활동지역 근거를 공식 원문으로 대조한다.
- 네 조건을 모두 충족한 후보만 공개 그룹·멤버로 승격하고 후보의 공개 ID를 연결한다.
- 기존 활동 그룹 10개는 공식 콘셉트·거점·검증 공연 이력 중 실제 확인되는 관계만 공간 근거로 이관한다.
- 데이터 → 홈·검색·상세 → 지도 Gate 1 집계 → 후보 카탈로그·인수인계를 한 실행 단위로 맞춘다.

### 실패·빈 상태·회귀 위험

- SNS 계정 존재나 과거 데뷔 기사만으로 2026년 활동·현역 명단을 추정하지 않는다.
- 현 체제 종료 공지가 있거나 명단이 충돌하면 공개하지 않고 차단 상태와 원문을 보존한다.
- `region`·`district` 기존 값은 지도 근거가 아니므로 그대로 `areaEvidence`로 복사하지 않는다.
- 담당색·생일·로마자·이미지·지표는 공식 근거가 없으면 미확인·`null`·0을 유지한다.
- 기존 공개 그룹·former 멤버·공연 데이터와 사용자의 다른 변경은 되돌리지 않는다.

### 완료 조건과 검증

- 승격 후보마다 공식 현역 명단, 일정 경로, 공간 근거, 확인일과 공개 ID가 있다.
- 미승격 후보마다 활동 종료·명단 부족·일정 부족·공간 근거 부족 중 실제 차단 사유가 기록된다.
- 공간 근거가 없는 기존 그룹을 억지로 채우지 않고 Gate 1 분자·분모를 재계산한다.
- 데이터 검사, 소스 보고, 타입 검사, 프로덕션 빌드, `git diff --check`를 실행해 결과를 기록한다.

### 실행 결과

- 5개 후보를 공식 원문으로 재판정해 `selfish` 1개 그룹과 공식 현역 멤버 6명을 공개 승격했다. 2026-07 갱신 공식 사이트의 명단·일정과 IDORISE 공식 시부야 공연 원문을 공간 근거로 연결했다.
- `kimikara`는 공식 소개의 7인조 표기와 프로필의 6명 표시가 충돌해 보류했다. `神聖マキナ`는 2025 데뷔 명단 이후 2026 전체 명단·일정 완결 근거 부족, `クララ・マグラ`는 현 체제 종료 신호, `MiDi ON!`은 공식 페이지의 현재 전체 명단 미노출로 보류했다.
- 기존 그룹 중 공식 거점·지역 정체성이 이미 원문으로 확인된 カラフルスクリーム, タイトル未定, LinQ, 手羽先センセーション 4개에만 `areaEvidence`를 이관했다. 나머지 6개 기존 활동 그룹은 근거를 추정하지 않았다.
- 후보 현황은 `imported` 4개, `official_source_found` 7개다. 핵심 범위 잔여 후보는 4개이며 경계 후보 3개는 정책 결정 전 승격하지 않는다.
- 로컬 공개 기준은 원본 15그룹, 활동 그룹 14개, 원본 멤버 45명, 현역·검증 수록 40명이다. 지도 Gate 1은 그룹 수 `14/30`, 공간 근거 `8/14(57%)`로 갱신됐다.
- 데이터 검사: 통과 — 15그룹·45멤버·5공연·52공연 후보·8출처·11발견 후보, 검토 경고 45건.
- 소스 보고: 통과 — 공개 연결 4개와 잔여 공식 소스 확인 후보 7개를 확인했다.
- 타입 검사: `node node_modules\\typescript\\bin\\tsc --noEmit` 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 153개 생성.
- `git diff --check`: 통과. 기존 작업 파일의 LF→CRLF 안내만 출력됐다.

## 2026-08-26 핵심 그룹 첫 공개 확장 배치

### 변경 범위와 선별 기준

- 핵심 범위 후보 8개의 공식 사이트에서 현재 활동·현역 명단·공식 일정·활동 지역 근거를 대조한다.
- 네 조건이 충족되는 그룹을 우선해 `chika-groups.json`에 그룹과 공식 확인 가능한 현역 멤버를 추가한다.
- 비공개 후보는 공개 ID와 연결하고 `imported` 상태로 전환하되, 실제 공개 데이터 검증 통과 후에만 완료로 기록한다.
- 지도 준비를 위해 공개 그룹마다 `locationBasis`, 공식 출처와 일정 URL을 반드시 기록한다.

### 실패·빈 상태·회귀 위험

- 공식 명단이 페이지·최근 공지 사이에서 충돌하면 후보 상태를 유지한다.
- 담당색·생일·SNS·이미지 권리가 확인되지 않은 필드는 추정하지 않는다.
- 이름·로마자·한국어 표기가 불확실하면 일본어 공식 표기를 우선하고 번역은 보수적으로 기록한다.
- 기존 11개 그룹과 former/ended 이력을 삭제하거나 재정렬하지 않는다.
- 그룹 추가가 홈·검색·지도 핀 수·정적 상세 경로에 미치는 영향을 검증한다.

### 완료 조건과 검증

- 승격 그룹은 공식 활동 상태, 최소 1명 이상의 공식 현역 멤버, 공식 일정 URL, 공간 근거를 가진다.
- 후보 `imported` 연결과 공개 그룹 ID가 1:1로 검증된다.
- 지도 Gate 1 준비 수치와 잔여 차단 후보가 문서·인수인계에 기록된다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`가 통과한다.

### 실행 결과

- 공식 현역 명단·일정 경로·활동지역 근거를 함께 확보한 `I’mew（あいみゅう）`, `エイアイカ`, `可憐なアイボリー` 3개 그룹과 현역 멤버 20명을 공개 데이터에 추가했다.
- 담당색·출생연도·SNS가 공식 페이지에서 확인되지 않은 값은 `미확인` 또는 `null`로 유지했다. 지역은 거점으로 단정하지 않고 공식 프로필·공연 이력을 `areaEvidence`로 기록했다.
- 세 비공개 후보를 공개 ID와 연결해 `imported`로 전환했다. 후보 현황은 `imported` 3개, `official_source_found` 8개이며 공개 데이터와 1:1 연결 검사를 추가했다.
- 로컬 공개 기준은 원본 14그룹, 활동 그룹 13개, 원본 멤버 39명, 현역·검증 수록 34명이다. 지도 Gate 1은 그룹 수 `13/30`, 공간 근거 `3/13(23%)`이며 기존 10개 활동 그룹의 근거 이관이 남았다.
- 데이터 검사: 통과 — 14그룹·39멤버·5공연·52공연 후보·8출처·11발견 후보, 검토 경고 49건.
- 타입 검사: `node node_modules\\typescript\\bin\\tsc --noEmit` 통과. `pnpm typecheck`는 코드 오류가 아니라 로컬 pnpm의 `ERR_PNPM_IGNORED_BUILDS` 사전 설치 정책으로 실행 차단되어 직접 TypeScript 명령으로 대체했다.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 135개 생성.
- `git diff --check`: 통과.

## 2026-08-26 실제 지도·뉴스·그룹 확장 MVP 설계

### 변경 범위

- 현재 지도와 배포·로컬 그룹 수를 대조하고 사용자 의도와의 차이를 기록한다.
- 일본 → 도도부현/도시 → 행정구역 → 활동권역/공연장 → 그룹·공연 → 길찾기·티켓의 지도 설계도를 작성한다.
- 실제 지도 구현에 필요한 공간 데이터 계약, UX 상태, 모바일·접근성, 성능·지도 라이선스, 출시 밀도 게이트를 정의한다.
- 뉴스는 구현 전 사용자 상담이 필요한 후속 가지로 설계하고, 그룹 대량 확장과 지도 준비 데이터의 연결을 인수인계에 반영한다.

### 현재 문제와 기대 흐름

- 현재 `InteractiveJapanMap`은 여섯 개 수제 SVG path와 비율 좌표 핀으로 일본을 근사하며 실제 확대·행정구 경계·공연장 좌표가 없다.
- 현재 `district`는 행정구와 생활권을 혼합하고 그룹당 하나만 저장해 실제 활동 분포를 표현하지 못한다.
- 기대 흐름은 실제 지형 위에서 지역을 클릭하고 단계적으로 확대하면서 해당 영역의 검증 그룹·오늘/예정 공연·공연장을 동시에 탐색하는 것이다.

### 실패·빈 상태·회귀 위험

- 그룹 수가 적은 지역은 핀 수를 부풀리지 않고 수집 중 상태와 인접 지역을 제공한다.
- 지도 로딩 실패·WebGL 미지원·키/타일 장애 시 동일한 계층형 목록 탐색을 제공한다.
- 행정구와 활동권역을 혼동하거나 그룹의 콘셉트 지역을 실제 활동 근거처럼 표시하지 않는다.
- 지도 타일·경계 데이터의 라이선스와 attribution을 UI에 유지한다.
- 뉴스는 사용자 상담 전 구현하지 않으며 루머·사생활·권리 미확인 이미지를 취급하지 않는다.

### 완료 조건과 검증

- 독립 지도 MVP 문서에 화면 단계, URL 상태, 데이터 모델, 밀도 게이트, 공연·Google Maps URL·티켓 연결이 정의된다.
- 제품 계획·출처 설계·인수인계가 지도 중심 차별점과 그룹 확장 우선순위에서 모순되지 않는다.
- 문서 변경에 대해 `git diff --check`를 실행한다.

### 실행 결과

- 현재 지도는 실제 좌표계가 아닌 수제 SVG path 6개와 백분율 도시 핀 5개이며, 확대·23구 경계·공연장 핀이 없음을 코드에서 확인했다.
- 2026-08-26 배포 `/ko` HTTP 200과 렌더 HTML을 확인했다. 배포 노출은 활동 그룹 10개(도쿄 6, 기타 4)이며 로컬 공개 노출도 원본 11개 중 종료 그룹 1개를 제외한 10개로 동일하다. 비공개 후보 11개는 아직 공개 코드에 적용되지 않았다.
- `MAP_DISCOVERY_MVP.md`에 일본 → 도도부현/도시 → 행정구역/도쿄 23구 → 활동권역 → 공연장 → 그룹·공연 → Google Maps 길찾기·공식 티켓의 수직 흐름을 설계했다.
- `GeoArea`, `GroupAreaEvidence`, 공연장 좌표·Place ID, 이벤트 공간 상속 계약과 행정구/생활권 분리 원칙을 정의했다.
- MapLibre 기반 실제 지도 후보, 타일 공급자 결정 기준, OSM 타일 정책·attribution, 지도 실패 시 계층형 목록 대체, 선택 지역 GeoJSON 지연 로드를 기록했다.
- 공개 그룹 30개, 도쿄 그룹 20개, 도쿄 5개 구, 공연장 15개, 미래 공연 30개 등 단계별 구현 권고 게이트를 정했다.
- 뉴스는 공식 공지/외부 기사/자체 편집의 세 제품으로 나누고 범위·문체·출처·정정·사생활·미성년자·AI 표시·이미지 권리를 사용자와 상담한 뒤 시작하도록 차단 게이트를 넣었다.
- 코드·공개 데이터·UI는 변경하지 않았다.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 초기 그룹 후보 공식 원문 검증

### 변경 범위

- 비공개 그룹 후보 11개의 공식 사이트·소속사·공식 SNS와 현재 활동 근거를 조사한다.
- 공식 원문이 확인된 후보의 `officialUrls`, 상태, 메모만 갱신한다.
- 출처 카탈로그 보고에 공식 원문 확보율을 반영하고 인수인계의 다음 우선순위를 갱신한다.

### 실패·빈 상태·범위 경계

- 검색 결과나 navi-idol만으로 공식 URL 또는 현역 상태를 확정하지 않는다.
- 공식 사이트가 폐쇄됐거나 검색되지 않으면 `discovered`를 유지한다.
- 메이저·성우 유닛 등 제품 범위가 애매한 후보는 자동 제외·공개하지 않고 범위 검토로 남긴다.
- 공개 그룹·멤버·공연·그라비아 및 UI는 변경하지 않는다.

### 완료 조건과 검증

- 후보 11개 각각에 공식 근거 확보 여부와 남은 차단 사유가 기록된다.
- 검증된 공식 URL이 있는 `official_source_found` 후보는 최소 1개의 `verified` URL을 가진다.
- 카탈로그 보고, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`가 통과한다.

### 실행 결과

- 후보 11개 모두 그룹 공식 사이트, 소속사 공식 페이지 또는 공식 SNS를 최소 1개 확보해 `official_source_found`로 갱신했다.
- 핵심 범위 8개와 범위 경계 3개(OCHA NORMA, ピュアリーモンスター, i☆Ris)를 기계 판독 가능한 `scopeFit`으로 분리했다.
- 2026년 공식 활동 원문이 확인된 그룹과 과거 공식 페이지만 확인된 그룹을 메모에서 구분했다. 현역 명단 검증 전 `ready_for_review`로 올리지 않았다.
- クララ・マグラ는 2026년 공식 활동과 현 체제 종료 안내가 함께 확인되어 후속 체제·활동 상태 판정을 보류했다.
- 카탈로그 보고: 통과 — 공식 원문 확보 11/11, 핵심 범위 8, 경계 범위 3.
- 데이터 검사: 통과 — 공개 데이터는 11그룹·19멤버·5공연, 비공개 발견 후보 11개, 경고 39건 유지.
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 첫 시도는 장시간 무응답으로 중단했고 재시도는 컴파일 2.7분 후 정적 페이지 75개를 생성했다.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 navi-idol 조사와 전체 MVP 재설계

### 사용자 의도와 변경 범위

- navi-idol의 대규모 그룹·멤버·공연 정보 구조와 공식 자료 확보 경로를 조사한다.
- 지하·라이브아이돌, 공연, 그라비아를 하나의 큰 줄기와 단계별 가지로 재설계한다.
- 추후 발견 목록과 공식 URL을 즉시 추가할 수 있는 비공개 출처·엔터티 후보 카탈로그, 스키마, 검증·보고 명령을 추가한다.
- 제품 기획서, 상세 설계도, 작업지시서, 인수인계를 같은 기준선으로 맞춘다.

### 데이터·UI·명령 연결

- 제3자 디렉터리 레코드는 `discovery_only` 출처와 `discovered` 후보로만 저장한다.
- 공식 그룹·사무소·출판사·티켓·공연장 원문이 확인되면 후보의 공식 URL 상태를 단계적으로 올린다.
- 공개 데이터로의 승격은 기존 `chika-groups.json`, `chika-live.json`, `chika-gravure.json`에서 별도 수동 변경으로만 수행한다.
- 이번 실행에서는 공개 UI가 새 카탈로그를 읽지 않는다.

### 실패·빈 상태·회귀 위험

- navi-idol은 현재 Vercel 보안 체크포인트로 직접 HTTP·브라우저 접근이 제한된다. 검색 색인에서 확인한 범위와 접근 상태를 명시하고 전체성 수치를 추정하지 않는다.
- 공식 URL이 없거나 활동 상태가 불명확한 후보는 삭제하거나 공개하지 않고 `discovered`로 남긴다.
- 이름이 같은 그룹·인물, 개명·재편·졸업은 자동 병합하지 않는다.
- 사진은 URL을 발견해도 권리 확인 전 저장·노출하지 않는다.

### 완료 조건과 검증

- 출처 역할·신뢰 등급·접근 상태·대상 엔터티·사용 정책이 기계 검증된다.
- 발견 후보가 고유 ID, 발견 URL, 공식 근거 상태, 검토 상태를 가진다.
- 조사 설계도에 navi-idol 필드 맵, 공식 자료 경로, 단계별 MVP 게이트와 향후 실행 순서가 기록된다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`가 통과한다.

### 실행 결과

- `agy-lean` 읽기 전용 탐색으로 현재 데이터 계약, 후보 격리, 검증·공개 게이트와 필수 문서 범위를 확인하고 원문을 직접 대조했다.
- navi-idol 운영사 설명과 검색 색인에서 그룹·멤버·공연 엔터티 및 필드·관계 구조를 확인했다. 직접 HTTP는 Vercel Security Checkpoint 403, 브라우저 자동화는 Chrome DevTools 기동 실패로 전체 URL 수를 산정하지 않았다.
- `SOURCE_RESEARCH_AND_MVP.md`에 발견 인덱스 → 공식 검증 → 후보 검토 → 공개 승격의 큰 줄기, 그룹·공연·그라비아·운영 가지와 단계별 MVP 게이트를 작성했다.
- 비공개 출처 카탈로그 계약과 `data/chika-source-catalog.json`을 추가했다. 출처 8종과 검색 색인에서 확인된 그룹 씨앗 11개를 저장했다.
- 공식 원문이 확인된 kimikara, I’mew, エイアイカ 3개는 `official_source_found`, 나머지 8개는 `discovered`로 유지했다. 공개 그룹·UI에는 추가하지 않았다.
- `data:report:sources` 명령과 고유 출처·후보 ID, 출처 참조, 공식 URL 상태 검증을 추가했다.
- 데이터 검사: 통과 — 기존 공개 11그룹·19멤버·5공연·52공연 후보와 별도로 발견 출처 8개·후보 11개, 경고 39건.
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 75개 생성.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 FRUITS ZIPPER 상세 후보 보강

### 변경 범위와 연결

- 공식 상세 페이지 35건을 읽는 dry-run/write 보강 명령을 추가한다.
- 후보의 `timeText`, `locationText`, `ticketUrls`만 공식 원문으로 보강하고 검토 상태·공개 연결은 보존한다.
- 검토 보고에서 공급원별 보강 결과와 미확정 필드를 확인할 수 있게 한다.

### 실패·빈 상태와 회귀 위험

- 상세 페이지 하나라도 HTTP 실패하면 파일을 쓰지 않는다.
- INFO 또는 티켓 링크가 없는 공식 상세는 정상 빈 상태로 취급한다.
- 시간·장소 파싱 실패 시 추정하지 않고 기존 값 또는 `null`을 유지한다.
- 반복 write가 수동 `reviewStatus`, `candidateKind`, `publishedEventId`를 초기화하지 않아야 한다.
- 이번 실행은 공개 `data/chika-live.json`을 자동 변경하지 않는다.

### 완료 조건과 검증

- 35개 상세 페이지를 모두 처리하고 시간·장소·티켓 보강 수를 출력한다.
- dry-run과 명시적 write를 분리하며 반복 write 결과가 안정적이다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`가 통과한다.

### 실행 결과

- 공식 상세 페이지 35건을 모두 처리했다. 장소 35건과 `OPEN`/`START` 시간 표기 25건을 후보에 보강했다.
- 공식 상세 본문에서 티켓·접수 문맥으로 안전하게 식별할 수 있는 외부 링크는 0건이었다. 링크를 추정하거나 일반 행사 링크를 티켓으로 저장하지 않았다.
- 시간 미정 10건은 원문에 확정 시간이 없어 `null`을 유지했으며 35건 모두 `review_pending` 상태다.
- dry-run과 첫 write는 `changed`, 동일 write 반복은 `unchanged`였다.
- 월별 목록 수집 해시를 보강값과 분리하고 기존 상세값 우선 병합으로 수정했다. 목록 write를 반복한 뒤 상세 dry-run도 `unchanged`여서 시간·장소 보존을 확인했다.
- 공개 공연 데이터는 5건으로 유지했다.
- 데이터 검사: 통과 — 11그룹, 19멤버, 공개 공연 5건, 후보 52건, 경고 39건.
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 75개 생성.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 두 번째 공식 일정 공급원

### 변경 범위

- 후보 스키마를 다중 공급원 레지스트리와 후보별 `sourceKey` 구조로 마이그레이션한다.
- タイトル未定 수집기·검증기·보고기를 새 구조에 맞춘다.
- FRUITS ZIPPER 공식 월별 HTML 수집기와 dry-run/write 명령을 추가한다.
- 그룹 데이터의 낡은 FRUITS ZIPPER 일정 URL을 실제 공식 경로로 수정한다.

### 실패·변경·빈 상태

- 월별 페이지 중 하나라도 HTTP 또는 파싱 실패하면 후보 파일을 쓰지 않는다.
- 성공 시 공급원 응답의 정규화 해시와 후보 수를 저장하고 이전 해시와 비교해 `changed/unchanged`를 출력한다.
- 해당 기간 공연이 0건이어도 정상 빈 결과로 처리하되 기존 다른 공급원 후보는 유지한다.
- 반복 write는 해당 공급원의 후보만 교체하고 다른 공급원의 후보 및 모든 수동 검토 상태를 보존한다.

### 완료 조건과 검증

- 두 공급원이 하나의 후보 파일에서 충돌 없이 검증된다.
- FRUITS ZIPPER dry-run·write·반복 write가 성공하고 변경 상태가 출력된다.
- 공개 공연 데이터는 자동 변경되지 않는다.
- 데이터 검사, 타입 검사, 빌드, `git diff --check`가 통과한다.

### 실행 결과

- 후보 계약을 다중 `sources` 레지스트리와 후보별 `sourceKey` 구조로 마이그레이션했다.
- FRUITS ZIPPER 공식 일정의 실제 경로를 확인하고 기존 404 `/schedule/` 링크를 `/live_information/schedule/list`로 수정했다.
- 2026-08-25부터 6개월 범위의 공식 `box_live_*` 공연 후보 35건을 수집했으며 제목에서 장소 힌트 20건을 추출했다.
- 첫 write는 `changed`, 동일 조건 반복 write는 `unchanged`로 감지됐다.
- タイトル未定 수집기도 다중 공급원 구조에서 반복 실행해 기존 검토 상태와 FRUITS ZIPPER 후보가 보존되고 두 번째 실행이 `unchanged`임을 확인했다.
- 후보 총계: 52건 — `published` 5건, `review_pending` 45건, `rejected` 2건. 공개 공연은 5건으로 유지했다.
- 데이터 검사: 통과 — 2개 공급원, 후보 52건, 공개 공연 5건, 경고 39건.
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 75개 생성.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 잔여 공연 후보 검토

### 변경 범위와 정책

- 후보 17건과 공개 공연 5건의 연결 상태를 대조한다.
- 기존 공개 공연에 대응하는 후보는 `published`, 명백한 비공연 생일 항목은 `rejected`, 그 외는 `review_pending`으로 유지한다.
- 근거가 부족한 후보를 수량 채우기 위해 공개하지 않는다.
- 후보별 종류·날짜·누락 필드·검토 상태를 출력하는 읽기 전용 보고 명령을 추가한다.

### 완료 조건과 검증

- 공개 공연 5건 모두 정확히 하나의 published 후보와 연결된다.
- 비공연 제외 결정이 재수집 후 보존된다.
- 보고 명령이 공개 가능·차단·제외 상태를 구분한다.
- 데이터 검사, 타입 검사, 빌드, `git diff --check`가 통과한다.

### 실행 결과

- 공개 공연 5건 모두 공식 캘린더 후보와 `publishedEventId`로 1:1 연결했다.
- 생일 일정 2건은 비공연으로 `rejected` 처리했고 삭제하지 않았다.
- 잔여 10건은 공식 검색에서 추가 확정 근거를 찾지 못해 공개하지 않았다: 세부정보 부족 6건, 공연 여부 분류 필요 4건.
- `data:review:live-candidates` 보고 명령을 추가해 날짜·상태·분류·근거 필드·공개 연결을 표로 출력한다.
- 공식 캘린더 write 재실행 후에도 `published` 5건과 `rejected` 2건이 유지됐다.
- 데이터 검사: 통과 — 공개 공연 5건, 후보 17건, 경고 39건.
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 75개 생성.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 공식 캘린더 후보 수집 어댑터

### 변경 범위와 연결

- 대상: 후보 스키마, `data/chika-live-candidates.json`, 공식 Google Calendar ICS 수집 스크립트, 검증기, 패키지 명령.
- 연결: 공식 일정 원문 → 후보 데이터 → 검토 대기. 공개 `chika-live.json`과 UI는 자동 변경하지 않는다.
- 첫 공급원: タイトル未定 공식 사이트가 임베드한 공개 Google Calendar.

### 실패·빈 상태와 회귀 위험

- 네트워크·ICS 형식 오류 시 종료 코드 1로 실패하고 기존 후보 파일을 보존한다.
- 미래 일정이 없으면 빈 후보 배열을 허용하되 공개 데이터에는 영향을 주지 않는다.
- 반복 수집은 원본 UID 기반 안정 ID로 중복을 만들지 않는다.
- 재수집 시 기존 검토 상태와 공개 이벤트 연결 ID를 보존해 사람의 결정을 되돌리지 않는다.
- 공식 캘린더의 비공연 항목은 삭제하지 않고 후보 종류를 분류해 잘못된 공연 승격을 막는다.
- 설명 텍스트 전체를 복제하지 않고 티켓 URL·시간 후보 등 검토에 필요한 최소 사실만 추출한다.
- 기본 명령은 dry-run이며 `--write`를 명시해야만 후보 파일을 갱신한다.

### 완료 조건과 검증 방법

- dry-run이 후보 수, 날짜 범위, 미정 필드 수를 출력한다.
- write 모드 결과가 후보 스키마와 데이터 검사에 통과한다.
- 수집기 자체는 공개 공연과 UI를 변경하지 않는다. 사람이 검토해 승격한 후보만 별도 변경으로 공개할 수 있다.
- 타입 검사, 프로덕션 빌드, `git diff --check`가 통과한다.

### 실행 결과

- dry-run 검토 중 2026-08-28 공연의 개장 15:00·시작 16:00·TicketDive URL과 2026-08-31 홀 투어의 カナモトホール 장소가 공식 캘린더에서 추가 확인됐다.
- dry-run: 2026-08-25 이후 후보 17건, `live_or_event` 10건, 장소 미정 16건, 티켓 미정 16건.
- write 및 반복 실행: 후보 17건 저장, 수동 대조한 2건은 `published`, 나머지 15건은 `review_pending`; 재수집 후에도 공개 상태와 공개 이벤트 ID가 유지됐다.
- 공개 승격: 2026-08-28 `A Villa 2026 前夜祭`와 2026-08-31 `Hall Tour 2026 KAKAR`를 추가해 공개 공연 5건, 공연장 1건이 됐다.
- 데이터 검사: 통과 — 11그룹, 19멤버, 공개 공연 5건, 후보 17건, 경고 39건.
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 75개 생성.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 공연 탐색 MVP 수직 구현

### 변경 범위

- 데이터·스키마: `src/lib/schema.ts`, `data/chika-live.json`, `src/lib/data.ts`, `scripts/validate.ts`.
- UI·라우팅: `src/app/[locale]/live/page.tsx`, `src/app/[locale]/live/[eventId]/page.tsx`, 공연 필터 컴포넌트, `Navigation.tsx`, 그룹 상세 연결.
- 문서: 제품 기획서, 작업지시서, `CODEX_HANDOVER.md`.

### 데이터·UI·명령 연결

- 공식 캘린더의 날짜만 확인된 공연은 날짜는 공개하되 시간·장소·가격·티켓을 미정으로 표시한다.
- 목록 서버 컴포넌트가 검증 데이터만 읽고, 직렬화된 초기 데이터를 클라이언트 필터에 전달한다.
- 상세 라우트는 이벤트 ID로 조회하고 알 수 없는 ID는 404 처리한다.
- 그룹 상세는 해당 그룹의 미래 공연이 있을 때 공연 목록과 상세로 연결한다.
- 검증기는 이벤트 ID, 그룹 참조, 공연장 참조, 날짜 순서, 출처, 최소 공식 링크를 검사한다.

### 실패·로딩·빈 상태

- 필터 결과가 없으면 조건 초기화와 공식 그룹 일정 링크를 제공한다.
- 시간·공연장·가격·티켓이 미정이면 추정값 대신 명시적인 미정 문구를 표시한다.
- 티켓이 없으면 구매 버튼을 만들지 않고 공식 일정 원문만 제공한다.
- 알 수 없는 공연 ID는 현재 전역 404로 처리한다.

### 회귀 위험과 호환성

- 기존 `startsAt` 필수 계약을 완화하면서 검증기와 정렬이 null을 안전하게 처리해야 한다.
- 동적 공연 상세가 정적 생성 페이지 수와 locale 라우팅에 영향을 준다.
- 기존 미커밋 멤버 데이터와 문서 변경은 유지한다.

### 완료 조건과 검증 방법

- 세 언어에서 공연 목록과 상세 경로가 빌드된다.
- 날짜·지역·그룹 필터와 빈 상태가 동작한다.
- 미정 필드는 사실처럼 보이지 않고 공식 출처와 확인일이 노출된다.
- 데이터 검사, TypeScript 검사, Next.js production build, `git diff --check`가 통과한다.

### 실행 결과

- 공연 탐색 MVP 구현: 세 언어 목록 `/[locale]/live`, 공연 상세 `/[locale]/live/[eventId]`, 내비게이션과 그룹 상세 연결 완료.
- 공식 캘린더 확인 공연 3건 등록: 2026-08-29~30, 2026-09-22, 2026-10-25. 발표되지 않은 시간·공연장·가격·티켓은 `null`과 미정 UI로 유지했다.
- React 검토: 서버에서 이벤트를 조회하고 필터에 필요한 축약 데이터만 클라이언트 컴포넌트로 전달해 전체 그룹·멤버 JSON 직렬화를 피했다.
- 데이터 검사: 통과 — 11그룹, 19멤버, 공연 3건, 경고 39건.
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 69개 생성.
- 로컬 프로덕션 HTTP 확인: `/ko/live`, 공연 상세, `/ko/g/title-mitei` 모두 200이며 예상 라이브·출처 문구를 확인했다.
- 자동 브라우저 확인: 미완료 — 설치된 Chrome이 `DevToolsActivePort` 생성 전에 종료되어 기본 실행과 `--no-sandbox` 재시도가 실패했다. HTTP 렌더링 확인으로 대체했으며 실제 클릭·시각 검토는 잔여 위험이다.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-25 문서 기준선 정리 작업

### 변경 범위와 연결

- 대상: `AGENTS.md`, `AUDIT_AND_REBUILD_PLAN.md`, `WORK_ORDER.md`, `CODEX_HANDOVER.md`, `HANDOVER.md`.
- 연결: 저장소 진입 지침 → 제품 의도 → 실행 단위 → 최신 운영 상태 → 역사 자료의 순서를 명확히 한다.
- 코드·데이터·UI·자동화 동작은 변경하지 않는다.

### 실패·빈 상태와 회귀 위험

- 저장소에 `AGENTS.md`가 없던 빈 상태를 보완한다.
- 위험: 기존 미커밋 데이터 작업을 문서 정리 과정에서 덮어쓰기, 실제 검증 결과와 문서 수치 불일치, 폐기 명령의 재실행.
- 대응: 기존 변경을 보존하고, 현재 작업 트리와 실제 검증 명령 결과만 기록하며, 초기 인수인계에는 폐기 배너를 둔다.

### 완료 조건과 검증 방법

- 모든 Markdown 및 `AGENTS.md` 파일 목록을 다시 검색해 누락을 확인한다.
- 낡은 절대 경로와 서로 다른 정적 페이지 수를 검색해 제거한다.
- 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 실행한다.
- 실제 결과는 이 절의 실행 결과에 기록한다.

### 실행 결과

- Markdown 및 작업 지침 재검색: 통과 — `AGENTS.md` 포함 5개 확인.
- 낡은 절대 경로와 `45페이지` 수치 검색: 통과 — 현행 문서에서 제거.
- 데이터 검사: 통과 — 11그룹, 19멤버, 경고 39건.
- 타입 검사: 통과. 빌드와 동시에 실행한 최초 검사는 `.next/types` 재생성 충돌로 실패했으나 빌드 완료 후 순차 재실행은 종료 코드 0이었다.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 57개 생성.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.
- 외부 Vercel 상태와 프로덕션 응답은 이번 문서 정리 범위에서 재검증하지 않았으며 인수인계에 그 경계를 표시했다.

## 큰 줄기와 이번 가지

- 큰 줄기: 공식 출처에 기반한 라이브아이돌 탐색 디렉터리의 신뢰도를 높인다.
- 이번 가지: 활성 멤버가 비어 있는 네 그룹에 공식 명단의 활성 멤버 최소 1명씩을 데이터 → 검색 → 그룹 상세 흐름에 연결한다.
- 실행 단위: `data/chika-groups.json`의 최소 멤버 레코드와 상태만 변경한다. UI 코드는 변경하지 않는다.

## 데이터·UI·명령 연결

- 멤버는 `activityStatus: active`, 그룹별 고유 ID, 공식 프로필 `provenance`를 가진다.
- 권리 확인된 사진이 없으므로 `imageUrl: null`, `imageKind: placeholder`를 사용한다.
- 공식 근거가 없는 생일·출신지·닉네임·SNS·지표는 비워 둔다.
- 이번 단계에서는 각 그룹을 `partial`로 전환한다. 전체 명단은 개인 정보와 표기를 추가 검증한 뒤 별도 단계에서 완성한다.
- 기존 데이터 조회 경로가 홈 검색과 그룹 상세에 새 멤버를 노출한다.

## 실패·빈 상태와 회귀 위험

- 개인 상세 정보가 없어도 이름은 노출하고 미확인 필드는 표시하지 않는다.
- 기존 former 레코드는 이력 보존을 위해 삭제하지 않는다.
- 위험: 중복 ID, 그룹 ID 불일치, 활성 인원수 불일치, 번역 표기 오류, 정적 생성 실패.

## 완료 조건과 검증

- 네 그룹에 공식 출처를 가진 활성 멤버가 최소 1명씩 존재하며 `collecting` 상태가 남지 않는다.
- `scripts/validate.ts`, `tsc --noEmit`, Next.js production build, `git diff --check`가 통과한다.
- 검증 결과와 잔여 경고 수를 이 문서 및 인수인계에 기록한다.

## 실행 결과

- 공식 현역 최소 레코드 4명 추가: 隈本茉莉奈, 与田理央那, 海月らな, 宮代柚花.
- 네 그룹 모두 `collecting`에서 `partial`로 전환했다.
- 미확인 담당색은 중립값, 이미지는 placeholder, 미확인 지표는 0으로 명시했다.
- 데이터 검사: 통과 — 11그룹, 19멤버, 경고 39건(기존 검토 항목 수 유지).
- 타입 검사: 통과.
- Next.js 프로덕션 빌드: 통과 — 정적 페이지 57개 생성.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.
- 의존성 설치 시 pnpm이 `esbuild`, `sharp` 빌드 스크립트를 정책상 차단해 종료 코드 1을 냈으나, 직접 실행한 데이터 검사·타입 검사·Next 빌드는 모두 정상 동작했다.
## 2026-08-30 홈 지도 정보구조 전면 교정

### 변경 범위와 연결

- 홈 `InteractiveJapanMap`을 실제 지도 중심 구조에서 단계형 디스커버리 무대로 교체한다.
- 전국 단계에는 정확한 일본 열도 벡터와 도시 카드만 표시한다.
- 도시 단계에는 해당 도시의 검증된 활동 지역 카드를 공간적으로 표시하고 뒤로 가기를 제공한다.
- 활동 지역 단계에는 `imageUrl` 기반 그룹 대표 카드 그리드를 표시하며 그룹 상세로 연결한다.
- 홈에서는 공연장·공연 밀도·실제 지도·우측 필터 패널을 제거한다. 그룹 상세의 예정 공연은 유지하고, 실제 공연장 지도는 후속 실행 단위에서 라이브 탭과 연결한다.
- 영향 파일: `src/components/map/InteractiveJapanMap.tsx`, `src/app/globals.css`, 로컬 일본 지도 벡터 자산 및 라이선스 문서, 제품 기획·작업지시·인수인계.

### 실패·빈 상태·회귀 위험

- 도시 또는 활동 지역에 검증 그룹이 없으면 빈 지도를 만들지 않고 수집 중 문구와 상위 단계 복귀를 제공한다.
- 그룹 사진 로딩 실패와 `imageUrl: null`은 브랜드 색·그룹명 대체 비주얼로 처리한다.
- URL의 `mapRegion`·`mapDistrict`로 현재 단계를 복원하고 브라우저 뒤로 가기를 지원한다.
- 위험: 정확한 지도 벡터의 라이선스 누락, 모바일 카드 겹침, 기존 `mapWard`/밀도 쿼리 호환성, 이미지 비율 깨짐, 전체 홈 번들 증가.

### MVP 실행 순서와 완료 조건

1. 정확한 로컬 일본 지도 벡터와 라이선스를 포함한다.
2. 전국 → 도시 → 활동 지역 → 사진 기반 그룹 카드 전환을 구현한다.
3. 키보드 포커스, 44px 터치 표적, 뒤로 가기, 빈 상태, 이미지 실패 상태를 확인한다.
4. 타입 검사, 데이터 검사, 프로덕션 빌드, `git diff --check`를 통과한다.
5. 실제 브라우저에서 전국 화면, 도쿄 화면, 아키하바라 그룹 화면을 캡처해 사용자 의도와 대조한다.

### 실행 결과

- 홈에서 실제 도로지도·밀도 토글·공연장 UI를 제거하고 `전국 → 도시 → 활동 지역 → 대표 이미지 그룹 카드`의 단일 탐색 무대로 교체했다.
- 전국 단계는 Geolonia의 47도도부현 SVG를 로컬 자산으로 포함해 네트워크 없이 실제 일본 형상을 유지한다. 화면과 `public/maps/README.md`에 GFDL 출처를 기록했다.
- 도시 선택 시 같은 지도 무대가 선택 지점을 중심으로 확대되고, 기존 데이터에 존재하는 활동 지역만 공간 카드로 표시한다. 지역 선택 시 지도는 배경으로 물러나고 그룹 `imageUrl` 기반 카드가 나타난다.
- URL 복원과 뒤로 가기: `mapRegion`, `mapDistrict`를 지원하고 기존 `mapLayer`, `mapWard`는 새 탐색 진입 시 제거한다.
- 이미지 없음·로드 실패: 그룹 브랜드 색과 이름을 사용한 대체 비주얼을 표시한다.
- TypeScript 검사: 통과.
- 데이터 검사 직접 실행: 통과 — 37그룹, 188멤버, 9개 지리 영역, 공연 38건, 후보 55건, 경고 39건. `pnpm data:validate` 래퍼는 기존 pnpm 빌드 스크립트 승인 정책으로 중단되어 실제 `tsx scripts/validate.ts`를 직접 실행했다.
- Next.js 프로덕션 빌드: 통과 — 678/678 정적 페이지, 홈 7.84 kB / 첫 로드 143 kB.
- 실제 시각 검증: 사용자가 새 홈 탐색 결과를 직접 확인하고 만족한다고 승인했다. 추가 캡처는 사용자 요청으로 생략했다.

## 2026-08-30 그룹 상세 라이브 탭·공연장 지도 연결

### 변경 범위와 연결

- 그룹 상세에 `프로필 / 라이브` 탭을 추가하고 기본값은 프로필로 유지한다.
- 라이브 탭에서만 기존 MapLibre 실제 지도를 지연 로드한다.
- 지도 데이터는 `getUpcomingLiveEventsForGroup()`이 반환한 이벤트 중 좌표가 검증된 공연장만 사용한다.
- 공연장 핀과 아래 일정 카드가 같은 이벤트·공연장 데이터를 공유하며 그룹과 무관한 핀은 표시하지 않는다.
- 영향 파일: 그룹 상세 페이지, 그룹 전용 라이브 지도 컴포넌트, 지도 관련 CSS, 기획·작업지시·인수인계.

### 실패·빈 상태·회귀 위험

- 예정 공연이 없으면 공식 일정 링크와 수집 중 상태를 제공한다.
- 공연은 있으나 좌표 있는 공연장이 없으면 지도를 숨기고 일정만 표시하며 위치 미확인을 명시한다.
- 실제 지도 초기화 실패 시 공연장 목록·주소·Google Maps 링크로 대체한다.
- 위험: 클라이언트 그룹 페이지 번들 증가, 탭 변경 시 지도 중복 초기화, 같은 공연장의 중복 핀, 다국어 라벨 누락.

### 완료 조건과 검증

- 라이브 탭을 누르기 전에는 실제 지도 UI가 보이지 않는다.
- 라이브 탭에서는 선택 그룹의 좌표 있는 공연장만 보이고 핀 선택과 일정 카드가 연결된다.
- 탭 키보드 상태, 빈 상태, 지도 실패 상태를 지원한다.
- 타입 검사, 데이터 검사, 프로덕션 빌드, `git diff --check`를 통과한다.

### 실행 결과

- 그룹 상세 기본 화면을 `프로필 · 멤버`로 유지하고 `라이브 · 공연장 지도` 탭을 추가했다. 지도 컴포넌트는 라이브 탭이 선택된 뒤에만 마운트된다.
- 선택 그룹의 예정 공연에서 중복 공연장을 제거하고, 검증 좌표가 있는 공연장만 GSI 실제 지도에 표시한다. 핀 선택 시 공연장명·주소·Google Maps·해당 장소의 그룹 공연을 연결한다.
- 예정 공연 없음, 좌표 있는 공연장 없음, 지도 초기화 실패 상태를 각각 분리했다. 지도 실패 시에도 기존 일정 카드와 공식 링크는 유지된다.
- TypeScript 검사: 통과.
- 데이터 검사: 통과 — 37그룹, 188멤버, 공연 38건, 후보 55건, 경고 39건.
- Next.js 프로덕션 빌드: 통과 — 678/678. 그룹 상세 7.54 kB / 첫 로드 194 kB.
- 로컬 프로덕션 서버 교체 및 `/ko/g/title-mitei` HTTP 확인: 200.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.
- 실제 화면 검증: 사용자가 전체 새 흐름에 만족한다고 승인했으며 추가 캡처 없이 다음 MVP 진행을 요청했다.

## 2026-08-30 지역↔그룹↔라이브 탐색 왕복 완결

### 변경 범위

- 그룹 상세 breadcrumb의 지역을 `/?mapRegion={region}&mapDistrict={district}` 링크로 바꾼다.
- `tab=live`를 초기 상태·클릭·`popstate`와 연결하고 프로필 선택 시 쿼리를 제거한다.
- 라이브 탭의 전체 일정 링크는 기존 그룹 필터를 유지한다.
- 영향 파일은 그룹 상세 페이지와 기획·작업지시·인수인계로 제한한다.

### 실패·회귀 위험과 완료 조건

- 유효하지 않은 `tab`은 프로필로 복귀한다.
- 뒤로 가기 시 탭과 URL이 어긋나지 않아야 한다.
- 지역 링크는 현재 locale을 유지하며 홈의 활동 지역 그룹 카드 단계로 복귀한다.
- 타입 검사, 데이터 검사, 프로덕션 빌드, 관련 HTTP 경로, `git diff --check`를 검증한다.

### 실행 결과

- 그룹 상세 지역 breadcrumb를 locale을 유지하는 홈 `mapRegion`·`mapDistrict` 링크로 연결했다.
- 라이브 탭은 `?tab=live`를 pushState로 기록하고, 프로필 탭은 쿼리를 제거한다. 새로고침과 `popstate`에서 URL 상태를 복원하며 다른 값은 프로필로 정규화한다.
- 탭과 패널에 `aria-controls`·`aria-labelledby` 연결을 추가했다.
- TypeScript 및 데이터 검사 통과 — 37그룹, 188멤버, 공연 38건, 경고 39건.
- Next.js 프로덕션 빌드 통과 — 678/678, 그룹 상세 7.82 kB / 첫 로드 195 kB.
- 새 서버 교체 후 라이브 직접 URL과 아키하바라 복귀 URL 모두 HTTP 200.
- `git diff --check`: 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-30 이미지 MVP 0~1 — 계약·감사·카드 폴백

### 변경 범위와 연결

- 그룹 스키마에 이미지 종류·권리 상태·출처 URL·확인일 계약을 추가한다. 멤버의 기존 `imageKind`도 공식/플레이스홀더 의미를 엄격히 적용한다.
- 데이터 검증기는 이미지가 있더라도 권리 메타데이터가 없으면 공식 이미지로 인정하지 않고, 공식 이미지 선언과 출처·권리 상태의 모순을 오류로 처리한다.
- 지역 그룹 카드, 일반 그룹 카드, 멤버 카드에서 공식 사진/로고와 생성 대체물을 시각·문구로 구분한다.
- 기존 사용자 데이터는 일괄 덮어쓰지 않는다. 메타데이터가 없는 레거시 로컬 SVG는 호환 기본값 `placeholder`로 해석한다.

### 실패·빈 상태·회귀 위험

- 이미지 로딩 실패는 이름·브랜드 색·담당색 대체 비주얼로 즉시 복귀한다.
- 공식 사진이 없어도 링크와 카드 탐색은 유지한다.
- 위험: 기존 11개 그룹 SVG가 공식 사진처럼 계속 보임, Next Image 레이아웃 변화, 스키마 기본값으로 권리 없는 원격 URL이 묵인됨.

### 완료 조건과 검증

- 공식 이미지는 출처·확인일과 임베드 가능 권리 상태 없이는 검증을 통과할 수 없다.
- 레거시 SVG와 미분류 이미지는 플레이스홀더 라벨로 표시된다.
- 그룹·멤버 카드의 이미지 실패와 이미지 없음 상태가 텍스트 탐색을 막지 않는다.
- 이미지 커버리지 보고, 데이터 검사, 타입 검사, 프로덕션 빌드, `git diff --check`를 실행한다.

### 실행 결과

- 그룹 스키마에 `official_photo / official_logo / placeholder` 종류와 권리 상태·출처·확인일을 추가했다. 멤버에도 동일한 권리·출처 메타데이터를 연결했다. 레거시 누락은 호환상 플레이스홀더로 해석한다.
- 검증기는 공식 이미지에 임베드 가능한 권리 상태, 이미지 경로, 공식 출처, 확인일을 모두 요구한다. `link_only`인데 `imageUrl`로 임베드하면 실패한다.
- 이미지 감사 결과: 그룹 공식 0 / 플레이스홀더 37, 멤버 공식 0 / 플레이스홀더 188. 기존 로컬 SVG를 공식 사진으로 오인하지 않는다.
- 지역 그룹 카드, 일반 그룹 카드, 검색 그룹 카드에 항상 대표 비주얼 영역을 제공하고 공식 사진·공식 로고·대체 이미지 라벨을 구분했다. 이미지 없음·로드 실패는 브랜드 색과 이름으로 복귀한다.
- 멤버 카드는 기존 담당색·글리프 폴백과 `사진 준비 중` 라벨을 새 계약의 기본 플레이스홀더 판정과 연결했다.
- TypeScript, 데이터 검사, 프로덕션 빌드 678/678, `git diff --check` 통과. 홈 8.16 kB / 첫 로드 144 kB, 검색 3.04 kB / 첫 로드 190 kB.
- 새 프로덕션 서버로 교체하고 `/ko` HTTP 200을 확인했다.
- 실제 공식 이미지·로고 추가는 권리 근거를 확보하는 이미지 MVP 2 소배치로 남겼다.

## 2026-08-30 이미지 MVP 2 — 공식 원문 후보 큐 1차 배치

### 조사 결과와 범위

- FRUITS ZIPPER·CANDY TUNE은 ASOBISYSTEM 공식 탤런트/그룹 페이지에서 대표 이미지를, iLiFE!는 일본 컬럼비아 공식 프로필에서 대표 이미지를 확인했다.
- 세 공식 페이지 모두 현재 조사 범위에서 제3자 카드 임베드·다운로드·재배포를 허용하는 명시적 라이선스나 공개 미디어킷을 확인하지 못했다.
- 따라서 이미지 URL을 추출·다운로드하지 않고 공식 프로필 URL만 `link_only`, `rights_review` 후보로 저장한다.

### 변경 범위와 연결

- 공개 그룹 데이터와 분리된 이미지 후보 데이터셋·스키마·검증·보고 명령을 추가한다.
- 후보는 대상 종류, 그룹/멤버 ID, 공식 원문, 자산 종류, 권리 상태, 검토 상태, 차단 사유, 확인일을 가진다.
- `approved` 후보만 공개 이미지 계약으로 수동 승격할 수 있으며 `link_only` 후보에는 임베드 이미지 URL을 저장할 수 없다.
- 일일 자동 업데이트에 공개 승격은 연결하지 않는다. 후보 보고 명령만 운영자가 실행한다.

### 실패·회귀 위험과 완료 조건

- 존재하지 않는 그룹/멤버 대상, 중복 후보 ID, 권리 없는 승인, `link_only` 이미지 URL 저장은 데이터 검사에서 실패한다.
- 후보 큐는 공개 UI와 `getGroups()`가 읽지 않아야 한다.
- 세 공식 원문 후보가 권리 확인 대기로 보고되고, 데이터·타입·빌드·diff 검사가 통과해야 한다.

### 실행 결과

- 공개 데이터와 분리된 `chika-image-candidates.json`, `ImageCandidateDataset`, `data:review:image-candidates` 보고 명령을 추가했다.
- 1차 공식 원문 후보 3건을 등록했다: FRUITS ZIPPER·CANDY TUNE ASOBISYSTEM 공식 프로필, iLiFE! 일본 컬럼비아 공식 프로필.
- 세 건 모두 공식 대표 이미지 존재는 확인했지만 제3자 카드 재사용 허용 근거를 확인하지 못해 `rights_review / link_only`, `assetUrl: null`로 유지했다.
- 검증기는 대상 ID, 중복 ID, `link_only` 자산 URL 저장, 권리 없는 승인, 차단 사유 누락을 검사한다. 후보 데이터는 공개 UI에서 읽지 않는다.
- 후보 보고: 전체 3 / 권리 검토 3 / 승인 0 / 거절 0.
- TypeScript·데이터 검사·후보 보고·Next.js 프로덕션 빌드 678/678·`git diff --check` 통과. 기존 LF→CRLF 안내만 출력됐다.
- 실제 카드 사진 승격은 0건이다. 사용 허가 또는 명시적 미디어킷 라이선스를 확보하기 전에는 플레이스홀더를 유지한다.

## 2026-08-30 이미지 MVP 2-B — 정책 판정·허가 요청 준비

### 변경 범위

- 이미지 후보에 권리 정책 URL과 허가 문의 URL을 추가한다.
- 일본 컬럼비아의 무단 이용 금지 원문에 따라 iLiFE! 현재 프로필 이미지 후보는 `rejected`로 닫는다.
- ASOBISYSTEM 두 후보는 소속 탤런트 권리 보호 공지와 업무 문의 폼을 연결하고 `rights_review`로 유지한다.
- 한국어·일본어 허가 요청 초안을 문서로 작성한다. 실제 폼 제출·메일 발송은 하지 않는다.

### 실패·회귀 위험과 완료 조건

- `rights_review` 후보는 문의 URL과 차단 사유가 없으면 실패한다. `rejected` 후보에는 거절 근거 정책 URL이 필요하다.
- 허가 요청 문서는 이미지 자체를 첨부하거나 다운로드하지 않는다.
- 후보 보고에서 권리 검토 2·거절 1·승인 0이 확인되고 타입·데이터·빌드·diff 검사를 통과해야 한다.

### 실행 결과

- 후보 계약에 `rightsPolicyUrl`과 `permissionContactUrl`을 추가했다. 권리 검토 후보는 문의 창구, 거절 후보는 정책 근거가 없으면 데이터 검사가 실패한다.
- ASOBISYSTEM의 소속 탤런트 초상·퍼블리시티권 보호 공지와 업무 문의 폼을 FRUITS ZIPPER·CANDY TUNE 후보에 연결했다. 두 건은 `rights_review`를 유지한다.
- 일본 컬럼비아가 홈페이지 아티스트 이미지의 무단 사용을 금지한다고 명시해 iLiFE! 현재 프로필 자산 후보를 `rejected`로 전환했다.
- `IMAGE_PERMISSION_REQUESTS.md`에 일본어 문의 초안, 한국어 내부 확인본, 사용 위치·크기·변형·저장 방식·출처·기간·철회 조건과 답변 기록 필드를 작성했다. 외부 전송은 하지 않았다.
- 후보 보고 결과: 전체 3 / 권리 검토 2 / 승인 0 / 거절 1.
- TypeScript·데이터·후보 보고·프로덕션 빌드 678/678·`git diff --check` 통과. 기존 LF→CRLF 안내만 출력됐다.

## 2026-08-30 사진 제한 그룹 로고·워드마크 폴백

### 변경 범위

- 그룹 이미지 종류에 `text_wordmark`를 추가하고 기존 로컬 생성 SVG 11개를 공식 사진/로고가 아닌 자체 워드마크로 분류한다.
- 카드 라벨을 `OFFICIAL LOGO`와 `TEXT WORDMARK`로 구분한다.
- 로컬 생성 SVG 내부의 오해 소지가 있는 `OFFICIAL IDOL GROUP` 문구를 `CHIKA DIRECTORY WORDMARK`로 교체한다. 미사용 에셋도 같은 문구를 정리해 향후 오인 재사용을 막는다.
- 사진 사용 제한 후보는 사진 승인으로 전환하지 않으며, 공식 로고 후보는 별도 권리 검토 대상으로 유지한다.

### 실패·회귀 위험과 완료 조건

- `text_wordmark`에는 권리 상태·공식 이미지 출처를 요구하지 않지만 로컬 경로만 허용한다.
- 원격 URL이나 공식 이미지 자산을 `text_wordmark`로 위장하면 검증에 실패해야 한다.
- 기존 11개 카드가 이미지 없이 깨지지 않고 워드마크로 표시되며, 공식 이미지 감사 수치는 0을 유지한다.
- 데이터·타입·빌드·SVG 문구 검색·diff 검사를 통과한다.

### 실행 결과

- 그룹 이미지 종류에 `text_wordmark`를 추가하고 현재 공개 데이터가 참조하는 11개 로컬 SVG를 공식 이미지가 아닌 자체 워드마크로 분류했다.
- 지역·일반·검색 그룹 카드에서 `TEXT WORDMARK`를 표시하고 `OFFICIAL PHOTO/LOGO`와 구분한다.
- 그룹 SVG 에셋 18개에서 `OFFICIAL IDOL GROUP` 문구를 제거하고 `CHIKA DIRECTORY TEXT WORDMARK`로 교체했다. 잔여 오해 문구 검색 결과는 0건이다.
- 검증기는 `text_wordmark`가 `/images/groups/*.svg` 로컬 경로가 아니면 실패한다. 원격 공식 자산을 자체 워드마크로 위장할 수 없다.
- 최신 감사: 그룹 공식 0 / 텍스트 워드마크 11 / 플레이스홀더 26, 멤버 공식 0 / 플레이스홀더 188.
- TypeScript·데이터 검사·프로덕션 빌드 678/678·`git diff --check` 통과. 새 서버 `/ko` HTTP 200.
