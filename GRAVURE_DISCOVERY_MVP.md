# Chika Idol Box 그라비아 아이돌 발견 MVP

기준일: 2026-08-27

## 1. 제품 역할

그라비아 아이돌은 홈의 장식 섹션이 아니라 공식 출판·발매 근거를 통해 인물과 작품을 발견하는 독립 핵심 가지다.

`인물 → 그룹/개인 신원 → 공식 출판사·소속사 기사/발매 → 제호·발매일·공식 판매처 → 권리 상태에 맞는 링크`

라이브아이돌과 동일 인물이 겹칠 수 있지만 공연 지역·인기 지표·사진 권리는 자동 상속하지 않는다.

## 2. 데이터 줄기

- 발견 출처: 출판사 공식 그라비아 목록, 소속사 공지, 공식 판매처.
- 후보: 인물 표기, 그룹 표기, 제목, 제호, 발매일, 기사·상품 URL, 확인일, 연결 후보 ID.
- 검토: 동명이인·개명·그룹 이동·미성년자·이미지 권리·판매 종료를 확인한다.
- 공개: 공식 인물 연결이 가능하고 출판/발매 원문과 확인일, 권리 상태가 있는 레코드만 공개한다.

## 3. 권리와 안전 정책

- 초기 공개 정책은 `link_only`다. 썸네일 URL을 발견해도 복제·캐시·프록시·임베드하지 않는다.
- `official_embed`는 출판사가 명시적으로 제공한 임베드 방식과 약관을 확인한 경우만 사용한다.
- `permission_confirmed`는 권리자와 허용 범위·기간을 기록한 경우만 사용한다.
- 미성년자는 공식 출판·소속사 원문에 공개된 활동 사실만 링크하며 선정적 설명·자동 요약·신체 평가를 만들지 않는다.
- 검색엔진·제3자 갤러리·무단 전재 사이트는 후보 발견에도 우선 사용하지 않는다.

## 4. 첫 수직 실행

- 출처 카탈로그의 集英社 週プレNEWS/グラジャパ와 秋田書店 ヤンチャンWeb를 읽는 dry-run 후보 어댑터 계약을 만든다.
- 자동 수집은 공개 `chika-gravure.json`을 수정하지 않고 별도 후보 파일에 저장한다.
- 후보 ID는 출처·원문 URL 기반으로 안정화하고 반복 수집에서 검토 상태를 보존한다.
- 기존 멤버 ID와 이름이 정확히 일치하지 않으면 자동 연결하지 않는다.
- 검토 보고 명령은 신규·연결 가능·동명이인 위험·권리 미확정·공개 가능 상태를 구분한다.

## 5. 실패·빈 상태와 회귀 위험

- HTTP·파싱 실패가 하나라도 있으면 write 모드에서 기존 후보 파일을 보존한다.
- 공식 목록이 비어 있으면 정상 빈 결과로 기록하되 기존 공개 레코드를 삭제하지 않는다.
- 페이지 마크업 변경으로 제목·날짜가 불확실하면 값을 추정하지 않는다.
- 후보 수집 성공을 이미지 사용 허가나 공개 승인으로 표현하지 않는다.

## 6. 완료 조건

- 출판사 공식 후보가 공개 데이터와 분리된 스키마로 검증된다.
- dry-run이 기본이고 명시적 `--write`에서만 후보 파일을 변경한다.
- 동일 입력 반복 실행이 중복을 만들지 않고 사람의 검토 상태를 보존한다.
- 권리 상태가 없는 후보는 공개 UI에서 읽히지 않는다.
- 데이터 검사·타입 검사·프로덕션 빌드·`git diff --check`가 통과한다.
