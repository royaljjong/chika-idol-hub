# Chika Idol Box MapLibre 기술 스파이크

> **문서 지위 — 완료된 기술 이력:** 이 문서의 스파이크 범위, 공급자 후보, 미검증 항목과 실행 순서는 현재 제품 요구사항이나 다음 작업 지시가 아니다. 현행 지도 계약은 `AUDIT_AND_REBUILD_PLAN.md`와 `CODEX_HANDOVER.md`를 따른다. 홈은 로컬 일본 SVG 기반 계층 탐색을 사용하고, MapLibre/GSI 실제 지도는 선택 그룹의 라이브 탭에서만 지연 로드하며 실패 시 일정·공식 링크를 유지한다. 아래 상충하는 시점별 판단은 당시 기록으로만 읽는다.

기준일: 2026-08-27

## 2026-08-29 프로덕션 경계 보완

- 이 절의 1차 스파이크 플래그 방침은 아래 `GSI 실지도 MVP 결정`으로 대체됐다. MapLibre 6.6.0과 GSI 담색 타일은 로컬 MVP의 기본 지도이며 `NEXT_PUBLIC_MAP_PROVIDER=fallback`에서만 외부 요청 없는 SVG/목록으로 강제 전환한다.
- WebGL·모듈·초기 로드 실패 시에도 SVG/목록으로 복귀한다. 기본값 변경은 공개 배포 승인이 아니며, 제한 없는 네트워크의 실제 화면·모바일 검증 전에는 배포하지 않는다.
- 공식 실시간 타일 이용 안내와 attribution 조건은 확인했지만 상용 SLA는 없다. 공급자 장애·정책 변경·개인정보 위험은 kill switch와 목록 폴백으로 운영한다.

## 2026-08-29 GSI 실지도 MVP 결정

- 국토지리원 공식 타일 목록은 웹사이트·앱에서 타일을 실시간 로드할 경우 출처 표시로 신청 없이 이용할 수 있다고 안내한다: `https://maps.gsi.go.jp/development/ichiran.html`.
- 적용 규약은 국토지리원 콘텐츠 이용규약이며, 제3자 권리 표시가 있는 타일과 기본측량성과의 복제·가공은 개별 조건을 다시 확인한다: `https://www.gsi.go.jp/kikakuchousei/kikakuchousei40182.html`.
- MVP는 기존 담색 raster URL을 실시간 표시하고 지도 안에 `地理院タイル` attribution을 유지한다. 타일을 대량 다운로드·재배포·오프라인 캐시하지 않는다.
- GSI는 상용 SLA가 아니며 요청 시 사용자 IP가 GSI 서버에 전달될 수 있다. 운영 중 장애·정책 변경 시 `NEXT_PUBLIC_MAP_PROVIDER=fallback`으로 외부 지도를 비활성화한다.
- 실제 시각·모바일·네트워크 검증이 완료되기 전 프로덕션 배포는 보류한다.

## 2026-08-29 도쿄 행정경계 공급 방식 결정

- 국토수치정보 N03 2025 행정구역 원본은 CC BY 4.0이지만 개별 페이지가 2차 이용 시 국토지리원 신청이 필요할 수 있다고 명시한다. 승인 여부를 확정하지 않은 채 원본을 잘라 저장소에 복제·재배포하지 않는다.
- 국토지리원 벡터 타일 제공 실험은 기본측량성과가 아닌 실시간 타일로 제공되며, 콘텐츠 이용규약과 출처 표시 조건으로 이용할 수 있다. MVP는 `experimental_bvmap`의 `boundary` source-layer에서 `ftCode=1212`인 시구정촌계만 네트워크로 겹친다.
- 시구정촌계는 제공 스키마상 vector zoom 11 이상에서 나타나므로 도쿄 선택 시 상세 카메라에서만 표시한다. 전국 또는 도쿄 전체 축척에서 23개 면을 임의 생성하거나 경계가 모두 보인다고 표현하지 않는다.
- attribution에 `国土地理院ベクトルタイル提供実験`을 추가하고 실험 URL·속성 변경 또는 종료 시 경계 레이어만 사라져도 raster 지도·공연장·목록 탐색은 유지한다.

## 1. 의도와 현재 위치

전국 지도 Gate 1은 활동 그룹 36개, 공간 근거 36/36, 도쿄·삿포로·후쿠오카·오사카 각 3개 이상으로 통과했다. 이번 단계는 완성 지도 출시가 아니라 실제 좌표계·WebGL·타일·실패 대체 UI가 제품 환경에서 안전하게 동작하는지 확인하는 기술 스파이크다.

## 2. 승인된 UX 방향

- 정보 구조를 우선하고 팬 친화적 색상·강조를 적용한다.
- 데스크톱은 지도와 결과 패널을 병렬로, 모바일은 45~55vh 지도와 bottom sheet 형태 결과를 사용한다.
- WebGL·네트워크·타일 실패와 저사양 환경에서도 동일한 지역 목록을 사용할 수 있어야 한다.
- 전국 Gate 1 경험을 먼저 만들고 도쿄 23구 경계·공연장 레이어는 Gate 2 이후 추가한다.

## 3. 스파이크 범위

- `maplibre-gl`을 클라이언트에서 지연 로드해 SSR과 초기 목록 렌더링을 방해하지 않는지 확인한다.
- 기존 검증 그룹의 도시 중심 좌표와 그룹 수를 GeoJSON point source로 전달하고 클릭·키보드 선택을 기존 목록 필터와 동기화한다.
- 공급자 계약 없이 테스트 가능한 스타일만 개발 환경 스파이크에 사용한다. 프로덕션 기본 타일 공급자는 아직 결정하지 않는다.
- 현행 SVG 지도는 즉시 삭제하지 않고 MapLibre 로드 실패 시 대체 UI로 유지한다.
- 번들 크기, 첫 로드, WebGL 실패, 모바일 높이, attribution 노출을 검증한다.

## 4. 타일·경계 후보와 권리 경계

- MapLibre GL JS는 WebGL 기반 TypeScript 지도 렌더러이며 GeoJSON·vector/raster source와 Style Specification을 사용한다.
- 일본 프로토타입 베이스맵은 국토지리원 담색 지도를 우선 비교 후보로 둔다. 실시간 웹 로드는 출처와 타일 목록 링크를 명시해야 한다.
- `tile.openstreetmap.org`는 용량·SLA가 보장되지 않고 대량 다운로드·프리패치가 금지되므로 프로덕션 기본 백엔드로 채택하지 않는다.
- 행정경계는 국토수치정보 N03의 최신 행정구역 데이터를 후보로 두되, 원본 전체를 초기 번들에 넣지 않고 필요한 레벨·지역만 단순화해 지연 로드한다.
- 상용 공급자 비교는 일본어 지명 품질, 월간 비용, 캐시·CDN 허용, 개인정보, SLA, attribution, 벤더 전환성을 표로 작성한 뒤 별도 결정한다.

## 5. 실패·빈 상태와 회귀 위험

- 지도 모듈 또는 타일이 실패하면 지역 목록과 그룹 카드가 계속 동작한다.
- 좌표가 없는 엔터티는 지역 중심점에 위장해 표시하지 않는다.
- 활동권역과 행정구역을 같은 경계 레이어로 합치지 않는다.
- 지도 선택은 공유 URL과 브라우저 뒤로가기를 깨뜨리지 않아야 한다.
- 기존 홈 검색·그룹 상세·공연 탐색 데이터 계약을 MapLibre 전용 구조로 오염시키지 않는다.

## 6. 스파이크 완료 조건

### 2026-08-27 브라우저 검증 메모

- WebGL과 MapLibre 초기화는 성공했다.
- 격리 Playwright 환경은 외부 GSI 타일을 네트워크 정책으로 차단했고, 10초 후 SVG 폴백이 정상 작동했다.
- 복구 가능한 개별 타일 오류는 지도 전체를 즉시 제거하지 않도록 수정했다.
- 제한 없는 네트워크에서 타일 렌더·줌·핀 클릭을 확인하는 항목은 출시 전 잔여 검증이다.

- 세 언어 빌드와 정적 생성이 통과한다.
- 실제 좌표 기반 전국 지도에서 5개 지역의 그룹 수를 선택할 수 있다.
- MapLibre 실패 시 기존 SVG/목록 대체 UI가 노출된다.
- 지도 attribution이 항상 보인다.
- 기술 결과와 번들·성능·공급자 미결정을 `WORK_ORDER.md`와 인수인계에 기록한다.
