# Japan map asset

`japan-prefectures.svg` is copied from [geolonia/japanese-prefectures](https://github.com/geolonia/japanese-prefectures), based on Wikipedia's `日本地図.svg` and distributed under the GNU Free Documentation License (GFDL). The UI includes visible attribution. The asset is stored locally so the discovery flow does not depend on a map tile network.
